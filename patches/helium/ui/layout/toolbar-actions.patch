--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -835,6 +835,16 @@ inline constexpr char kShowMenuButton[]
 // the toolbar.
 inline constexpr char kShowMediaButton[] = "helium.browser.show_media_button";
 
+// A boolean pref set to true if the Vertical Tabs collapse button should be
+// visible on the toolbar when the vertical Helium layout is active.
+inline constexpr char kShowVerticalTabsCollapseButton[] =
+    "helium.browser.show_vertical_tabs_collapse_button";
+
+// A boolean pref set to true if the New Tab button should be visible on the
+// toolbar when the dynamic Helium layout is active.
+inline constexpr char kShowDynamicNewTabButton[] =
+    "helium.browser.show_dynamic_new_tab_button";
+
 // A boolean pref set to true if Gemini integration be enabled. This is managed
 // by enterprise policy.
 inline constexpr char kGeminiSettings[] = "browser.gemini_settings";
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -152,6 +152,8 @@ void RegisterBrowserUserPrefs(user_prefs
   registry->RegisterBooleanPref(prefs::kShowExtensionsButton, true);
   registry->RegisterBooleanPref(prefs::kShowMenuButton, true);
   registry->RegisterBooleanPref(prefs::kShowMediaButton, true);
+  registry->RegisterBooleanPref(prefs::kShowVerticalTabsCollapseButton, true);
+  registry->RegisterBooleanPref(prefs::kShowDynamicNewTabButton, true);
 
   registry->RegisterInt64Pref(prefs::kDefaultBrowserInfobarLastDeclined, 0);
   registry->RegisterBooleanPref(prefs::kWebAppCreateOnDesktop, true);
--- a/chrome/browser/ui/browser_element_identifiers.cc
+++ b/chrome/browser/ui/browser_element_identifiers.cc
@@ -206,6 +206,7 @@ DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbar
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarChromeLabsButtonElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarDownloadBubbleElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarDownloadButtonElementId);
+DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarDynamicNewTabButtonElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kDownloadBubbleRowElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kDownloadBubbleOpenButtonId);
 DEFINE_CUSTOM_ELEMENT_EVENT_TYPE(kDownloadEndedCustomEventId);
@@ -223,6 +224,7 @@ DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbar
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarSidePanelButtonElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarSidePanelContainerElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarTabCounterButtonElementId);
+DEFINE_ELEMENT_IDENTIFIER_VALUE(kToolbarVerticalTabsCollapseButtonElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kTopContainerElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kTranslatePageActionElementId);
 DEFINE_ELEMENT_IDENTIFIER_VALUE(kUnfocusTabGroupButtonElementId);
--- a/chrome/browser/ui/browser_element_identifiers.h
+++ b/chrome/browser/ui/browser_element_identifiers.h
@@ -249,6 +249,7 @@ DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolba
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarChromeLabsButtonElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarDownloadBubbleElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarDownloadButtonElementId);
+DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarDynamicNewTabButtonElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kDownloadBubbleRowElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kDownloadBubbleOpenButtonId);
 DECLARE_CUSTOM_ELEMENT_EVENT_TYPE(kDownloadEndedCustomEventId);
@@ -267,6 +268,7 @@ DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolba
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarSidePanelButtonElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarSidePanelContainerElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarTabCounterButtonElementId);
+DECLARE_ELEMENT_IDENTIFIER_VALUE(kToolbarVerticalTabsCollapseButtonElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kTopContainerElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kTranslatePageActionElementId);
 DECLARE_ELEMENT_IDENTIFIER_VALUE(kUnfocusTabGroupButtonElementId);
--- a/chrome/browser/ui/toolbar/pinned_toolbar/pinned_toolbar_actions_model.cc
+++ b/chrome/browser/ui/toolbar/pinned_toolbar/pinned_toolbar_actions_model.cc
@@ -220,6 +220,8 @@ void PinnedToolbarActionsModel::ResetToD
   pref_service_->ClearPref(prefs::kShowExtensionsButton);
   pref_service_->ClearPref(prefs::kShowMenuButton);
   pref_service_->ClearPref(prefs::kShowMediaButton);
+  pref_service_->ClearPref(prefs::kShowVerticalTabsCollapseButton);
+  pref_service_->ClearPref(prefs::kShowDynamicNewTabButton);
 
   pref_service_->ClearPref(prefs::kShowHomeButton);
   pref_service_->ClearPref(prefs::kShowForwardButton);
@@ -244,6 +246,8 @@ bool PinnedToolbarActionsModel::IsDefaul
                             prefs::kShowExtensionsButton,
                             prefs::kShowMenuButton,
                             prefs::kShowMediaButton,
+                            prefs::kShowVerticalTabsCollapseButton,
+                            prefs::kShowDynamicNewTabButton,
                             prefs::kPinContextualTaskButton},
       is_default_pref_value);
 }
--- a/chrome/browser/ui/view_ids.h
+++ b/chrome/browser/ui/view_ids.h
@@ -120,6 +120,10 @@ enum ViewID {
 
   // Media Picker screen scroll view.
   VIEW_ID_MEDIA_PICKER_SCREEN_SCROLL_VIEW,
+
+  // Helium toolbar buttons.
+  VIEW_ID_DYNAMIC_NEW_TAB_BUTTON,
+  VIEW_ID_COLLAPSE_VERTICAL_TABS_BUTTON,
 };
 
 #endif  // CHROME_BROWSER_UI_VIEW_IDS_H_
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -130,6 +130,7 @@
 #include "components/send_tab_to_self/features.h"
 #include "components/signin/public/base/signin_buildflags.h"
 #include "components/strings/grit/components_strings.h"
+#include "components/vector_icons/vector_icons.h"
 #include "content/public/browser/render_view_host.h"
 #include "content/public/browser/web_contents.h"
 #include "third_party/skia/include/core/SkPath.h"
@@ -162,6 +163,7 @@
 #include "ui/views/layout/proposed_layout.h"
 #include "ui/views/mouse_watcher.h"
 #include "ui/views/mouse_watcher_view_host.h"
+#include "ui/views/vector_icons.h"
 #include "ui/views/view.h"
 #include "ui/views/view_class_properties.h"
 #include "ui/views/view_utils.h"
@@ -187,6 +189,18 @@ constexpr int kLocationBarFlexOrder = kT
 constexpr int kToolbarActionsFlexOrder = kToolbarFlexOrderOffset + 2;
 constexpr int kExtensionsFlexOrder = kToolbarFlexOrderOffset + 3;
 
+constexpr int kDynamicNewTabButtonIconSize = 16;
+
+class DynamicNewTabToolbarButton : public ToolbarButton {
+ public:
+  explicit DynamicNewTabToolbarButton(PressedCallback callback)
+      : ToolbarButton(std::move(callback)) {}
+  ~DynamicNewTabToolbarButton() override = default;
+
+ protected:
+  int GetIconSize() const override { return kDynamicNewTabButtonIconSize; }
+};
+
 // Gets the display mode for a given browser.
 ToolbarView::DisplayMode GetDisplayMode(Browser* browser) {
   // Checked in this order because even tabbed PWAs use the CUSTOM_TAB
@@ -209,7 +223,9 @@ auto& GetViewCommandMap() {
        {VIEW_ID_FORWARD_BUTTON, IDC_FORWARD},
        {VIEW_ID_HOME_BUTTON, IDC_HOME},
        {VIEW_ID_RELOAD_BUTTON, IDC_RELOAD},
-       {VIEW_ID_AVATAR_BUTTON, IDC_SHOW_AVATAR_MENU}});
+       {VIEW_ID_AVATAR_BUTTON, IDC_SHOW_AVATAR_MENU},
+       {VIEW_ID_DYNAMIC_NEW_TAB_BUTTON, IDC_NEW_TAB},
+       {VIEW_ID_COLLAPSE_VERTICAL_TABS_BUTTON, IDC_TOGGLE_VERTICAL_TABS}});
   return kViewCommandMap;
 }
 
@@ -434,6 +450,16 @@ void ToolbarView::Init() {
 #endif
 
   // Always add children in order from left to right, for accessibility.
+
+  vertical_tabs_collapse_button_ = AddChildView(std::make_unique<ToolbarButton>(
+      base::BindRepeating(&ToolbarView::VerticalTabsCollapseButtonPressed,
+                          base::Unretained(this))));
+  vertical_tabs_collapse_button_->SetID(VIEW_ID_COLLAPSE_VERTICAL_TABS_BUTTON);
+  vertical_tabs_collapse_button_->SetVisible(false);
+  vertical_tabs_collapse_button_->SetProperty(
+      views::kElementIdentifierKey,
+      kToolbarVerticalTabsCollapseButtonElementId);
+
   if (!features::IsWebUIBackForwardButtonEnabled()) {
     back_ = AddChildView(std::make_unique<BackForwardButton>(
         BackForwardButton::Direction::kBack,
@@ -496,6 +522,21 @@ void ToolbarView::Init() {
     location_bar_ = toolbar_webview_->GetLocationBar();
   }
 
+  if (auto* vertical_tab_strip_state_controller =
+          tabs::VerticalTabStripStateController::From(
+              browser_view_->browser())) {
+    vertical_tab_mode_subscription_ =
+        vertical_tab_strip_state_controller->RegisterOnModeChanged(
+            base::BindRepeating(&ToolbarView::OnVerticalTabStripModeChanged,
+                                base::Unretained(this)));
+    vertical_tab_collapse_subscription_ =
+        vertical_tab_strip_state_controller->RegisterOnCollapseChanged(
+            base::BindRepeating(&ToolbarView::OnVerticalTabStripCollapseChanged,
+                                base::Unretained(this)));
+    should_display_vertical_tabs_ =
+        vertical_tab_strip_state_controller->ShouldDisplayVerticalTabs();
+  }
+
   bool is_glic_left_of_profile =
       features::kGlicToolbarButtonLocationParam.Get() ==
           features::GlicToolbarButtonLocation::kLeftOfProfileChip ||
@@ -515,6 +556,25 @@ void ToolbarView::Init() {
             0, GetLayoutConstant(LayoutConstant::kToolbarDividerSpacing)));
   }
 
+  std::unique_ptr<ToolbarButton> dynamic_new_tab_button =
+      std::make_unique<DynamicNewTabToolbarButton>(base::BindRepeating(
+          &ToolbarView::NewTabButtonPressed, base::Unretained(this)));
+  const int dynamic_new_tab_button_size =
+      GetLayoutConstant(LayoutConstant::kToolbarButtonHeight);
+  dynamic_new_tab_button->SetPreferredSize(
+      gfx::Size(dynamic_new_tab_button_size, dynamic_new_tab_button_size));
+  dynamic_new_tab_button_ = AddChildView(std::move(dynamic_new_tab_button));
+  dynamic_new_tab_button_->SetID(VIEW_ID_DYNAMIC_NEW_TAB_BUTTON);
+  dynamic_new_tab_button_->SetTooltipText(
+      l10n_util::GetStringUTF16(IDS_TOOLTIP_NEW_TAB));
+  dynamic_new_tab_button_->GetViewAccessibility().SetName(
+      l10n_util::GetStringUTF16(IDS_ACCNAME_NEWTAB));
+  dynamic_new_tab_button_->SetVectorIcon(vector_icons::kAddIcon);
+  dynamic_new_tab_button_->SetHorizontalAlignment(gfx::ALIGN_CENTER);
+  dynamic_new_tab_button_->SetVisible(false);
+  dynamic_new_tab_button_->SetProperty(views::kElementIdentifierKey,
+                                       kToolbarDynamicNewTabButtonElementId);
+
   if (extensions_container) {
     extensions_container_ = AddChildView(std::move(extensions_container));
     extensions_toolbar_coordinator_ =
@@ -681,16 +741,6 @@ void ToolbarView::Init() {
   }
 
   if (glic::GlicEnabling::IsProfileEligible(browser_view_->GetProfile())) {
-    auto* vertical_tab_strip_state_controller =
-        tabs::VerticalTabStripStateController::From(browser_view_->browser());
-    if (vertical_tab_strip_state_controller) {
-      vertical_tab_subscription_ =
-          vertical_tab_strip_state_controller->RegisterOnModeChanged(
-              base::BindRepeating(&ToolbarView::OnVerticalTabStripModeChanged,
-                                  base::Unretained(this)));
-      should_display_vertical_tabs_ =
-          vertical_tab_strip_state_controller->ShouldDisplayVerticalTabs();
-    }
     UpdateGlicButtonVisibility();
   }
 
@@ -708,6 +758,16 @@ void ToolbarView::Init() {
 
   reload_->SetVisible(show_reload_button_.GetValue());
 
+  show_vertical_tabs_collapse_button_.Init(
+      prefs::kShowVerticalTabsCollapseButton, prefs,
+      base::BindRepeating(&ToolbarView::UpdateToolbarLayout,
+                          base::Unretained(this)));
+
+  show_dynamic_new_tab_button_.Init(
+      prefs::kShowDynamicNewTabButton, prefs,
+      base::BindRepeating(&ToolbarView::UpdateToolbarLayout,
+                          base::Unretained(this)));
+
   show_avatar_button_.Init(
       prefs::kShowAvatarButton, prefs,
       base::BindRepeating(&ToolbarView::OnShowAvatarButtonChanged,
@@ -756,8 +816,9 @@ void ToolbarView::Init() {
       views::FlexSpecification(views::MinimumFlexSizeRule::kPreferred,
                                views::MaximumFlexSizeRule::kPreferred);
 
-  for (auto* button : std::array<views::Button*, 5>{back_, forward_, reload_,
-                                                    home_, avatar_}) {
+  for (auto* button : std::array<views::Button*, 7>{
+           vertical_tabs_collapse_button_, back_, forward_, reload_, home_,
+           dynamic_new_tab_button_, avatar_}) {
     if (button) {
       button->set_tag(GetViewCommandMap().at(button->GetID()));
       button->SetProperty(views::kFlexBehaviorKey, flex_preferred);
@@ -784,6 +845,12 @@ void ToolbarView::OnVerticalTabStripMode
   should_display_vertical_tabs_ = controller->ShouldDisplayVerticalTabs();
   UpdateGlicButtonVisibility();
   UpdateGlicActorVisibility();
+  UpdateToolbarLayout();
+}
+
+void ToolbarView::OnVerticalTabStripCollapseChanged(
+    tabs::VerticalTabStripCollapseState) {
+  UpdateVerticalTabsCollapseButton();
 }
 
 std::unique_ptr<GlicAndActorButtonsContainer>
@@ -1628,6 +1695,16 @@ void ToolbarView::NewTabButtonPressed(co
                  NewTabTypes::kNewTabButtonInToolbarForTouch);
 }
 
+void ToolbarView::VerticalTabsCollapseButtonPressed(const ui::Event& event) {
+  auto* controller =
+      tabs::VerticalTabStripStateController::From(browser_view_->browser());
+  if (!controller) {
+    return;
+  }
+
+  controller->RequestCollapse(!controller->IsCollapsed());
+}
+
 bool ToolbarView::AcceleratorPressed(const ui::Accelerator& accelerator) {
   const views::View* focused_view = focus_manager()->GetFocusedView();
   if (focused_view && (focused_view->GetID() == VIEW_ID_OMNIBOX)) {
@@ -1790,13 +1867,35 @@ void ToolbarView::LayoutCommon() {
       browser_->window() &&
       (browser_->window()->IsMaximized() || browser_->window()->IsFullscreen());
   const int margin = extend_buttons_to_edge ? interior_margin.left() : 0;
-  if (features::IsWebUIBackForwardButtonEnabled()) {
-    toolbar_webview_->SetBackButtonLeadingMargin(margin);
-  } else {
-    back_->SetLeadingMargin(margin);
+  const bool collapse_button_on_trailing_edge =
+      vertical_tabs_collapse_button_ &&
+      vertical_tabs_collapse_button_->GetVisible() && IsVerticalLayout() &&
+      IsVerticalTabsRightAligned();
+
+  ToolbarButton* const leading_button =
+      vertical_tabs_collapse_button_ &&
+              vertical_tabs_collapse_button_->GetVisible() &&
+              !collapse_button_on_trailing_edge
+          ? vertical_tabs_collapse_button_.get()
+          : back_.get();
+
+  leading_button->SetLeadingMargin(margin);
+  if (leading_button != back_.get()) {
+    back_->SetLeadingMargin(0);
+  }
+  if (vertical_tabs_collapse_button_.get() != leading_button) {
+    vertical_tabs_collapse_button_->SetLeadingMargin(0);
   }
   app_menu_button_->SetTrailingMargin(
-      extend_buttons_to_edge ? interior_margin.right() : 0);
+      extend_buttons_to_edge && !collapse_button_on_trailing_edge
+         ? interior_margin.right()
+         : 0);
+  if (vertical_tabs_collapse_button_) {
+    vertical_tabs_collapse_button_->SetTrailingMargin(
+        extend_buttons_to_edge && collapse_button_on_trailing_edge
+            ? interior_margin.right()
+            : 0);
+  }
   // Cast button visibility is controlled externally.
 }
 
@@ -1851,6 +1950,19 @@ void ToolbarView::UpdateToolbarLayout()
                      .WithOrder(order);
   };
 
+  UpdateVerticalTabsCollapseButton();
+  UpdateDynamicNewTabButton();
+
+  if (vertical_tabs_collapse_button_) {
+    const size_t target_index =
+        IsVerticalLayout() && IsVerticalTabsRightAligned()
+            ? GetIndexOf(app_menu_button_).value() + 1
+            : 0;
+    if (GetIndexOf(vertical_tabs_collapse_button_).value() != target_index) {
+      ReorderChildView(vertical_tabs_collapse_button_, target_index);
+    }
+  }
+
   if (location_bar_view_) {
     location_bar_view_->SetProperty(views::kFlexBehaviorKey,
                                     location_bar_flex_rule);
@@ -1895,6 +2007,67 @@ bool ToolbarView::IsCompactLayout() cons
   return controller && controller->IsCompactLayout();
 }
 
+bool ToolbarView::IsDynamicLayout() const {
+  auto* controller =
+      HeliumLayoutStateController::From(browser_view_->browser());
+  return controller && controller->IsDynamicLayout();
+}
+
+bool ToolbarView::IsVerticalLayout() const {
+  auto* controller =
+      HeliumLayoutStateController::From(browser_view_->browser());
+  return controller && controller->IsVerticalLayout();
+}
+
+bool ToolbarView::IsVerticalTabsRightAligned() const {
+  auto* controller =
+      tabs::VerticalTabStripStateController::From(browser_view_->browser());
+  return controller && controller->IsTabStripRightAligned();
+}
+
+bool ToolbarView::ShouldShowToolbarNewTabButton() const {
+  // TODO: Show the toolbar new tab button in Compact layout
+  // once we improve it.
+  return IsDynamicLayout();
+}
+
+void ToolbarView::UpdateVerticalTabsCollapseButton() {
+  if (!vertical_tabs_collapse_button_) {
+    return;
+  }
+
+  auto* controller =
+      tabs::VerticalTabStripStateController::From(browser_view_->browser());
+  const bool should_show = controller && IsVerticalLayout() &&
+                           show_vertical_tabs_collapse_button_.GetValue();
+  vertical_tabs_collapse_button_->SetVisible(should_show);
+
+  if (!should_show) {
+    return;
+  }
+
+  const bool should_use_menu_open_icon =
+      controller->IsCollapsed() == controller->IsTabStripRightAligned();
+  const gfx::VectorIcon& icon =
+      should_use_menu_open_icon ? views::kMenuOpenIcon : views::kMenuCloseIcon;
+  const int text_id = controller->IsCollapsed() ? IDS_EXPAND_VERTICAL_TABS
+                                                : IDS_COLLAPSE_VERTICAL_TABS;
+  const std::u16string text = l10n_util::GetStringUTF16(text_id);
+
+  vertical_tabs_collapse_button_->SetVectorIcon(icon);
+  vertical_tabs_collapse_button_->SetTooltipText(text);
+  vertical_tabs_collapse_button_->GetViewAccessibility().SetName(text);
+}
+
+void ToolbarView::UpdateDynamicNewTabButton() {
+  if (!dynamic_new_tab_button_) {
+    return;
+  }
+
+  dynamic_new_tab_button_->SetVisible(ShouldShowToolbarNewTabButton() &&
+                                      show_dynamic_new_tab_button_.GetValue());
+}
+
 // AppMenuIconController::Delegate:
 void ToolbarView::UpdateTypeAndSeverity(
     AppMenuIconController::TypeAndSeverity type_and_severity) {
--- a/chrome/browser/ui/views/toolbar/toolbar_view.h
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.h
@@ -16,6 +16,7 @@
 #include "chrome/browser/glic/browser_ui/glic_nudge_delegate.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/page_action/page_action_icon_type.h"
+#include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
 #include "chrome/browser/ui/toolbar/app_menu_icon_controller.h"
 #include "chrome/browser/ui/toolbar/back_forward_menu_model.h"
 #include "chrome/browser/ui/views/frame/browser_root_view.h"
@@ -290,6 +291,12 @@ class ToolbarView : public views::Access
 
   // Returns whether the Helium layout is Compact.
   bool IsCompactLayout() const;
+  bool IsDynamicLayout() const;
+  bool IsVerticalLayout() const;
+  bool IsVerticalTabsRightAligned() const;
+  bool ShouldShowToolbarNewTabButton() const;
+  void UpdateVerticalTabsCollapseButton();
+  void UpdateDynamicNewTabButton();
 
   // AppMenuIconController::Delegate:
   void UpdateTypeAndSeverity(
@@ -361,11 +368,14 @@ class ToolbarView : public views::Access
   void OnTouchUiChanged();
 
   void NewTabButtonPressed(const ui::Event& event);
+  void VerticalTabsCollapseButtonPressed(const ui::Event& event);
 
   void InitGlicContainer();
 
   void OnVerticalTabStripModeChanged(
       tabs::VerticalTabStripStateController* controller);
+  void OnVerticalTabStripCollapseChanged(
+      tabs::VerticalTabStripCollapseState);
 
   void SetForwardButtonVisibility(bool visible);
 
@@ -403,6 +413,7 @@ class ToolbarView : public views::Access
   // Controls. Most of these can be null, e.g. in popup windows. Only
   // |location_bar_| is guaranteed to exist. These pointers are owned by the
   // view hierarchy.
+  raw_ptr<ToolbarButton> vertical_tabs_collapse_button_ = nullptr;
   raw_ptr<ToolbarButton> back_ = nullptr;
   raw_ptr<ToolbarButton> forward_ = nullptr;
   raw_ptr<ReloadButton> reload_ = nullptr;
@@ -427,6 +438,7 @@ class ToolbarView : public views::Access
   // `toolbar_webview_->GetPinnedActionsContainer()`.
   raw_ptr<PinnedToolbarActions> pinned_toolbar_actions_ = nullptr;
   raw_ptr<AvatarToolbarButton> avatar_ = nullptr;
+  raw_ptr<ToolbarButton> dynamic_new_tab_button_ = nullptr;
   raw_ptr<MediaToolbarButtonView> media_button_ = nullptr;
   raw_ptr<BrowserAppMenuButton> app_menu_button_ = nullptr;
   raw_ptr<PinnedActionToolbarButton> tab_search_button_ = nullptr;
@@ -475,6 +487,10 @@ class ToolbarView : public views::Access
 
   BooleanPrefMember show_reload_button_;
 
+  BooleanPrefMember show_vertical_tabs_collapse_button_;
+
+  BooleanPrefMember show_dynamic_new_tab_button_;
+
   BooleanPrefMember show_avatar_button_;
 
   BooleanPrefMember show_extensions_button_;
@@ -505,8 +521,8 @@ class ToolbarView : public views::Access
   // `toolbar_controller_`.
   raw_ptr<OverflowButton> overflow_button_ = nullptr;
 
-  // Subscription for when tab strip mode changes
-  base::CallbackListSubscription vertical_tab_subscription_;
+  base::CallbackListSubscription vertical_tab_mode_subscription_;
+  base::CallbackListSubscription vertical_tab_collapse_subscription_;
 
   bool should_display_vertical_tabs_ = false;
   bool should_show_glic_button_ = false;
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -298,6 +298,10 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kShowMediaButton] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kShowVerticalTabsCollapseButton] =
+      settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kShowDynamicNewTabButton] =
+      settings_api::PrefType::kBoolean;
 
   // Appearance settings.
   (*s_allowlist)[::prefs::kCurrentThemeID] = settings_api::PrefType::kString;
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar.mojom
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar.mojom
@@ -18,6 +18,8 @@ enum ActionId {
   kForward,
   kBack,
   kReload,
+  kVerticalTabsCollapse,
+  kDynamicNewTab,
   kAvatar,
   kExtensions,
   kMenu,
--- a/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar_handler.cc
+++ b/chrome/browser/ui/webui/side_panel/customize_chrome/customize_toolbar/customize_toolbar_handler.cc
@@ -35,6 +35,7 @@
 #include "ui/base/models/image_model.h"
 #include "ui/display/screen.h"
 #include "ui/gfx/vector_icon_types.h"
+#include "ui/views/vector_icons.h"
 
 namespace {
 std::optional<side_panel::customize_chrome::mojom::ActionId>
@@ -52,6 +53,11 @@ MojoActionForChromeAction(actions::Actio
       return side_panel::customize_chrome::mojom::ActionId::kBack;
     case kActionReload:
       return side_panel::customize_chrome::mojom::ActionId::kReload;
+    case kActionToggleCollapseVertical:
+      return side_panel::customize_chrome::mojom::ActionId::
+          kVerticalTabsCollapse;
+    case kActionNewTab:
+      return side_panel::customize_chrome::mojom::ActionId::kDynamicNewTab;
     case kActionAvatar:
       return side_panel::customize_chrome::mojom::ActionId::kAvatar;
     case kActionExtensions:
@@ -113,6 +119,10 @@ std::optional<actions::ActionId> ChromeA
       return kActionBack;
     case side_panel::customize_chrome::mojom::ActionId::kReload:
       return kActionReload;
+    case side_panel::customize_chrome::mojom::ActionId::kVerticalTabsCollapse:
+      return kActionToggleCollapseVertical;
+    case side_panel::customize_chrome::mojom::ActionId::kDynamicNewTab:
+      return kActionNewTab;
     case side_panel::customize_chrome::mojom::ActionId::kAvatar:
       return kActionAvatar;
     case side_panel::customize_chrome::mojom::ActionId::kExtensions:
@@ -203,6 +213,16 @@ CustomizeToolbarHandler::CustomizeToolba
                           base::Unretained(this), kActionReload,
                           prefs::kShowReloadButton));
   pref_change_registrar_.Add(
+      prefs::kShowVerticalTabsCollapseButton,
+      base::BindRepeating(&CustomizeToolbarHandler::OnActionPinnedChanged,
+                          base::Unretained(this), kActionToggleCollapseVertical,
+                          prefs::kShowVerticalTabsCollapseButton));
+  pref_change_registrar_.Add(
+      prefs::kShowDynamicNewTabButton,
+      base::BindRepeating(&CustomizeToolbarHandler::OnActionPinnedChanged,
+                          base::Unretained(this), kActionNewTab,
+                          prefs::kShowDynamicNewTabButton));
+  pref_change_registrar_.Add(
       prefs::kShowAvatarButton,
       base::BindRepeating(&CustomizeToolbarHandler::OnActionPinnedChanged,
                           base::Unretained(this), kActionAvatar,
@@ -286,6 +306,32 @@ void CustomizeToolbarHandler::ListAction
               .Rasterize(&provider),
           scale_factor)));
 
+  auto vertical_tabs_collapse_action =
+      side_panel::customize_chrome::mojom::Action::New(
+          MojoActionForChromeAction(kActionToggleCollapseVertical).value(),
+          base::UTF16ToUTF8(
+              l10n_util::GetStringUTF16(IDS_COLLAPSE_VERTICAL_TABS)),
+          prefs()->GetBoolean(prefs::kShowVerticalTabsCollapseButton), false,
+          side_panel::customize_chrome::mojom::CategoryId::kNavigation,
+          GURL(webui::EncodePNGAndMakeDataURI(
+              ui::ImageModel::FromVectorIcon(views::kMenuCloseIcon,
+                                             icon_color_id)
+                  .Rasterize(&provider),
+              scale_factor)));
+
+  auto dynamic_new_tab_action =
+      side_panel::customize_chrome::mojom::Action::New(
+          MojoActionForChromeAction(kActionNewTab).value(),
+          base::UTF16ToUTF8(BrowserActions::GetCleanTitleAndTooltipText(
+              l10n_util::GetStringUTF16(IDS_NEW_TAB))),
+          prefs()->GetBoolean(prefs::kShowDynamicNewTabButton), false,
+          side_panel::customize_chrome::mojom::CategoryId::kNavigation,
+          GURL(webui::EncodePNGAndMakeDataURI(
+              ui::ImageModel::FromVectorIcon(vector_icons::kAddIcon,
+                                             icon_color_id)
+                  .Rasterize(&provider),
+              scale_factor)));
+
   auto avatar_action = side_panel::customize_chrome::mojom::Action::New(
       MojoActionForChromeAction(kActionAvatar).value(),
       base::UTF16ToUTF8(l10n_util::GetStringUTF16(IDS_PROFILES_MENU_NAME)),
@@ -371,6 +417,9 @@ void CustomizeToolbarHandler::ListAction
     actions.push_back(std::move(contextual_task_action));
   }
 
+  actions.push_back(std::move(vertical_tabs_collapse_action));
+  actions.push_back(std::move(dynamic_new_tab_action));
+
   actions.push_back(std::move(extensions_action));
   actions.push_back(std::move(media_controls_action));
   actions.push_back(std::move(avatar_action));
@@ -516,6 +565,12 @@ void CustomizeToolbarHandler::PinAction(
     case kActionReload:
       prefs()->SetBoolean(prefs::kShowReloadButton, pin);
       break;
+    case kActionToggleCollapseVertical:
+      prefs()->SetBoolean(prefs::kShowVerticalTabsCollapseButton, pin);
+      break;
+    case kActionNewTab:
+      prefs()->SetBoolean(prefs::kShowDynamicNewTabButton, pin);
+      break;
     case kActionAvatar:
       prefs()->SetBoolean(prefs::kShowAvatarButton, pin);
       break;
