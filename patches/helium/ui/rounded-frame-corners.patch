--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -747,6 +747,10 @@ inline constexpr char kNewTabNextToActiv
 // when cycling through tabs with Ctrl+Tab.
 inline constexpr char kTabCyclingMRU[] = "helium.browser.mru_tab_cycling";
 
+// A boolean pref set to true if web contents should be shown inside a rounded
+// frame.
+inline constexpr char kHeliumRoundedFrame[] = "helium.browser.rounded_frame";
+
 // A boolean pref set to true if a Home button to open the Home pages should be
 // visible on the toolbar.
 inline constexpr char kShowHomeButton[] = "browser.show_home_button";
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -126,6 +126,8 @@ void RegisterBrowserUserPrefs(user_prefs

   registry->RegisterBooleanPref(prefs::kTabCyclingMRU, false);

+  registry->RegisterBooleanPref(prefs::kHeliumRoundedFrame, true);
+
   registry->RegisterBooleanPref(prefs::kShowHomeButton, false,
                                 pref_registration_flags);
   registry->RegisterBooleanPref(prefs::kSplitViewDragAndDropEnabled, true,
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -267,6 +267,9 @@
     <message name="IDS_SETTINGS_CYCLE_TABS_IN_MRU_ORDER" desc="Label for the checkbox which enables or disables cycling through tabs in MRU order when using Ctrl+Tab.">
       Cycle through tabs in most recently used order with Ctrl+Tab
     </message>
+    <message name="IDS_SETTINGS_ROUNDED_FRAME" desc="Label for the toggle which enables or disables a rounded frame around web contents.">
+      Show a rounded frame around web contents
+    </message>
     <message name="IDS_SETTINGS_SHOW_HOME_BUTTON" desc="Label for the checkbox which enables or disables showing the home button in the toolbar.">
       Show home button
     </message>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -517,6 +517,7 @@ void AddAppearanceStrings(content::WebUI
       {"systemMode", IDS_NTP_CUSTOMIZE_CHROME_COLOR_SCHEME_MODE_SYSTEM_LABEL},
       {"openNewTabNextToActive", IDS_SETTINGS_NEW_TAB_NEXT_TO_ACTIVE_BUTTON},
       {"cycleTabsInMRUOrder", IDS_SETTINGS_CYCLE_TABS_IN_MRU_ORDER},
+      {"roundedFrame", IDS_SETTINGS_ROUNDED_FRAME},
       {"showHomeButton", IDS_SETTINGS_SHOW_HOME_BUTTON},
       {"showBookmarksBar", IDS_SETTINGS_SHOW_BOOKMARKS_BAR},
       {"showTabSearchButton", IDS_SETTINGS_SHOW_TAB_SEARCH_BUTTON},
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -267,6 +267,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kTabCyclingMRU] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kHeliumRoundedFrame] =
+      settings_api::PrefType::kBoolean;
 
   (*s_allowlist)[::prefs::kShowHomeButton] = settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kShowForwardButton] =
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.html
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.html
@@ -97,7 +97,11 @@
             hidden="[[!showHr_(
                 pageVisibility.setTheme, pageVisibility.homeButton)]]">
         </div>
-        <settings-toggle-button elide-label
+        <settings-toggle-button
+            pref="{{prefs.helium.browser.rounded_frame}}"
+            label="$i18n{roundedFrame}">
+        </settings-toggle-button>
+        <settings-toggle-button class="hr" elide-label
             hidden="[[!pageVisibility.homeButton]]"
             pref="{{prefs.browser.show_home_button}}"
             label="$i18n{showHomeButton}"
--- a/chrome/browser/ui/views/frame/contents_container_view.h
+++ b/chrome/browser/ui/views/frame/contents_container_view.h
@@ -116,6 +116,7 @@ class ContentsContainerView : public vie
                               int font_size);
 
   void UpdateBorderAndOverlay(bool is_in_split,
+                              bool should_round_contents,
                               bool is_active,
                               bool is_highlighted,
                               bool round_bottom_left_window_corner,
@@ -161,6 +162,7 @@ class ContentsContainerView : public vie
       const views::SizeBounds& size_bounds) const override;
 
   bool is_in_split_ = false;
+  bool should_round_contents_ = false;
   bool round_bottom_left_window_corner_ = false;
   bool round_bottom_right_window_corner_ = false;
 
--- a/chrome/browser/ui/views/frame/contents_container_view.cc
+++ b/chrome/browser/ui/views/frame/contents_container_view.cc
@@ -192,20 +192,24 @@ std::vector<views::View*> ContentsContai
 }
 
 void ContentsContainerView::UpdateBorderAndOverlay(bool is_in_split,
+                                                   bool should_round_contents,
                                                    bool is_active,
                                                    bool is_highlighted,
                                                    bool round_bottom_left,
                                                    bool round_bottom_right) {
   const bool split_changed = is_in_split != is_in_split_;
+  const bool rounded_contents_changed =
+      should_round_contents != should_round_contents_;
   const bool bottom_corner_state_changed =
       round_bottom_left_window_corner_ != round_bottom_left ||
       round_bottom_right_window_corner_ != round_bottom_right;
   is_in_split_ = is_in_split;
+  should_round_contents_ = should_round_contents;
   round_bottom_left_window_corner_ = round_bottom_left;
   round_bottom_right_window_corner_ = round_bottom_right;
 
-  if (!is_in_split) {
-    if (split_changed) {
+  if (!should_round_contents) {
+    if (split_changed || rounded_contents_changed) {
       SetBorder(nullptr);
       ClearBorderRoundedCorners();
 
@@ -215,22 +219,33 @@ void ContentsContainerView::UpdateBorder
       }
     }
   } else {
-    if (split_changed) {
-      SetBorder(views::CreateEmptyBorder(gfx::Insets(
-          kSplitViewContentPadding + ContentsContainerOutline::kThickness)));
+    if (split_changed || rounded_contents_changed) {
+      if (is_in_split) {
+        SetBorder(views::CreateEmptyBorder(gfx::Insets(
+            kSplitViewContentPadding + ContentsContainerOutline::kThickness)));
+      } else {
+        SetBorder(nullptr);
+      }
     }
-    if (split_changed || bottom_corner_state_changed) {
+    if (split_changed || rounded_contents_changed ||
+        bottom_corner_state_changed) {
       UpdateBorderRoundedCorners();
     }
 
-    container_outline_->UpdateState(is_active, is_highlighted);
+    if (is_in_split) {
+      container_outline_->UpdateState(is_active, is_highlighted);
+    } else if (browser_view_->GetIsNormalType()) {
+      container_outline_->UpdateState(false, kColorToolbarContentAreaSeparator);
+    } else {
+      container_outline_->SetVisible(false);
+    }
     if (capture_contents_border_view_) {
       capture_contents_border_view_->SetIsInSplit(true);
     }
   }
 
 #if BUILDFLAG(IS_CHROMEOS)
-  if (split_changed) {
+  if ((split_changed || rounded_contents_changed) && GetWidget()) {
     // Ensures correct window rounded corners after updating contents rounded
     // corners in UpdateBorderRoundedCorners()/ClearBorderRoundedCorners().
     GetWidget()->non_client_view()->frame_view()->UpdateWindowRoundedCorners();
@@ -250,21 +265,18 @@ void ContentsContainerView::UpdateBorder
   const gfx::RoundedCornersF container_rounded_corners(
       generic_radius, generic_radius, lower_right_radius, lower_left_radius);
 
-  const float inner_generic_radius = generic_radius - kSplitViewContentPadding;
-  const float inner_lower_left_radius =
-      lower_left_radius - kSplitViewContentPadding;
-  const float inner_lower_right_radius =
-      lower_right_radius - kSplitViewContentPadding;
+  const int inner_padding = is_in_split_ ? kSplitViewContentPadding : 0;
+  const float inner_generic_radius = generic_radius - inner_padding;
+  const float inner_lower_left_radius = lower_left_radius - inner_padding;
+  const float inner_lower_right_radius = lower_right_radius - inner_padding;
 
   const gfx::RoundedCornersF inner_rounded_corners(
       inner_generic_radius, inner_generic_radius,
       inner_lower_right_radius, inner_lower_left_radius);
 
   container_outline_->SetCornerRadii(container_rounded_corners);
-  if (capture_contents_border_widget_) {
-    static_cast<ContentsCaptureBorderView*>(
-        capture_contents_border_widget_->GetContentsView())
-        ->SetCornerRadii(container_rounded_corners);
+  if (capture_contents_border_view_) {
+    capture_contents_border_view_->SetCornerRadii(container_rounded_corners);
   }
 
   // Update devtools rounded corners. Note, devtools exists behind the contents
@@ -372,7 +384,7 @@ void ContentsContainerView::ClearBorderR
 
 void ContentsContainerView::ChildVisibilityChanged(View* child) {
   if ((child == new_tab_footer_view_ || child == devtools_web_view_) &&
-      is_in_split_) {
+      should_round_contents_) {
     UpdateBorderRoundedCorners();
   }
 }
@@ -397,7 +409,7 @@ views::View::Views ContentsContainerView
 void ContentsContainerView::OnViewBoundsChanged(View* observed_view) {
   if (observed_view == contents_view_) {
     UpdateDevToolsDockedPlacement();
-    if (is_in_split_) {
+    if (should_round_contents_) {
       UpdateBorderRoundedCorners();
     }
   }
--- a/chrome/browser/ui/views/frame/multi_contents_view.h
+++ b/chrome/browser/ui/views/frame/multi_contents_view.h
@@ -171,6 +171,7 @@ class MultiContentsView
 
   bool IsDragAndDropEnabled() const;
   void OnDragAndDropPrefStateChange();
+  void OnRoundedFramePrefStateChange();
 
   void SetShouldShowTopSeparator(bool should_show);
   void SetSplitViewInsets(gfx::Insets start_contents_view_inset,
@@ -261,6 +262,8 @@ class MultiContentsView
   // Returns 0 if not in a split view.
   int GetMinViewWidth(gfx::Rect available_space) const;
 
+  bool ShouldUseRoundedFrame() const;
+
   void UpdateContentsBorderAndOverlay();
 
   double CalculateRatioWithSnapPoints(double end_width,
@@ -322,6 +325,7 @@ class MultiContentsView
   // Tracks and handles drag and drop settings change.
   PrefChangeRegistrar pref_change_registrar_;
   bool is_drag_drop_pref_enabled_ = false;
+  bool is_rounded_frame_pref_enabled_ = false;
 
   // Maps a container to its current focusable children. This is needed since
   // for split tabs, some of these webviews are part of the focus helper for the
--- a/chrome/browser/ui/views/frame/multi_contents_view.cc
+++ b/chrome/browser/ui/views/frame/multi_contents_view.cc
@@ -158,15 +158,23 @@ MultiContentsView::MultiContentsView(
     }
   }
 
+  auto* const profile_prefs = browser_view_->GetProfile()->GetPrefs();
   is_drag_drop_pref_enabled_ =
-      browser_view_->GetProfile()->GetPrefs()->GetBoolean(
-          prefs::kSplitViewDragAndDropEnabled);
+      profile_prefs->GetBoolean(prefs::kSplitViewDragAndDropEnabled);
+  is_rounded_frame_pref_enabled_ =
+      profile_prefs->GetBoolean(prefs::kHeliumRoundedFrame);
 
-  pref_change_registrar_.Init(browser_view_->GetProfile()->GetPrefs());
+  pref_change_registrar_.Init(profile_prefs);
   pref_change_registrar_.Add(
       prefs::kSplitViewDragAndDropEnabled,
       base::BindRepeating(&MultiContentsView::OnDragAndDropPrefStateChange,
                           base::Unretained(this)));
+  pref_change_registrar_.Add(
+      prefs::kHeliumRoundedFrame,
+      base::BindRepeating(&MultiContentsView::OnRoundedFramePrefStateChange,
+                          base::Unretained(this)));
+
+  UpdateContentsBorderAndOverlay();
 }
 
 MultiContentsView::~MultiContentsView() {
@@ -418,6 +426,7 @@ void MultiContentsView::OnThemeChanged()
 
 void MultiContentsView::OnFullscreenStateChanged() {
   UpdateContentsBorderAndOverlay();
+  InvalidateLayout();
 }
 
 int MultiContentsView::GetInactiveIndex() const {
@@ -486,8 +495,10 @@ views::ProposedLayout MultiContentsView:
 
   gfx::Rect available_space = gfx::Rect(width, height);
 
+  const bool should_use_rounded_frame = ShouldUseRoundedFrame();
   const bool show_background =
-      drop_target_view_->GetVisible() || IsInSplitView();
+      drop_target_view_->GetVisible() || IsInSplitView() ||
+      should_use_rounded_frame;
   layouts.child_layouts.emplace_back(background_view_.get(), show_background,
                                      available_space);
 
@@ -512,6 +523,15 @@ views::ProposedLayout MultiContentsView:
   if (IsInSplitView()) {
     start_rect.Inset(start_contents_view_inset_);
     end_rect.Inset(end_contents_view_inset_);
+  } else if (should_use_rounded_frame) {
+    gfx::Insets single_inset(kSplitViewContentInset);
+    single_inset.set_top(
+        contents_separators_.should_show_top ? 0 : kSplitViewContentInset);
+    single_inset.set_left(
+        contents_separators_.should_show_leading ? 0 : kSplitViewContentInset);
+    single_inset.set_right(
+        contents_separators_.should_show_trailing ? 0 : kSplitViewContentInset);
+    start_rect.Inset(single_inset);
   }
 
   layouts.child_layouts.emplace_back(contents_container_views_[0],
@@ -622,7 +642,7 @@ gfx::Rect MultiContentsView::CalculateDr
 gfx::Rect MultiContentsView::CalculateSeparatorLayouts(
     const gfx::Rect& available_space,
     std::vector<views::ChildLayout>& child_layouts) const {
-  if (IsInSplitView()) {
+  if (IsInSplitView() || ShouldUseRoundedFrame()) {
     child_layouts.emplace_back(contents_separators_.top_separator.get(), false,
                                gfx::Rect());
     child_layouts.emplace_back(contents_separators_.leading_separator.get(),
@@ -752,27 +772,43 @@ int MultiContentsView::GetMinViewWidth(g
                   std::max(kConstrainedMinWebContentsWidth, min_percentage));
 }
 
+bool MultiContentsView::ShouldUseRoundedFrame() const {
+  const views::Widget* widget = browser_view_->GetWidget();
+  const bool is_fullscreen = widget && widget->IsFullscreen();
+  return browser_view_->GetIsNormalType() && is_rounded_frame_pref_enabled_ &&
+         !is_fullscreen;
+}
+
 void MultiContentsView::UpdateContentsBorderAndOverlay() {
   const views::Widget* widget = browser_view_->GetWidget();
   const bool is_fullscreen = widget && widget->IsFullscreen();
   const bool is_in_split = IsInSplitView();
+  const bool should_round_single_contents =
+      !is_in_split && ShouldUseRoundedFrame();
 
   const bool use_system_bottom_left_radius =
-      is_in_split && !is_fullscreen &&
+      (is_in_split || should_round_single_contents) && !is_fullscreen &&
       !contents_separators_.should_show_leading;
   const bool use_system_bottom_right_radius =
-      is_in_split && !is_fullscreen &&
+      (is_in_split || should_round_single_contents) && !is_fullscreen &&
       !contents_separators_.should_show_trailing;
 
   for (auto* contents_container_view : contents_container_views_) {
     const bool is_active =
         contents_container_view->contents_view() == GetActiveContentsView();
     const bool is_first_view =
+        !is_in_split ||
         contents_container_view == contents_container_views_.front();
     const bool is_last_view =
+        !is_in_split ||
         contents_container_view == contents_container_views_.back();
+    const bool is_single_contents_view =
+        !is_in_split && contents_container_view->GetVisible();
+    const bool should_round_contents =
+        is_in_split ||
+        (is_single_contents_view && should_round_single_contents);
     contents_container_view->UpdateBorderAndOverlay(
-        is_in_split, is_active,
+        is_in_split, should_round_contents, is_active,
         is_active && active_contents_view_highlighted_,
         use_system_bottom_left_radius && is_first_view,
         use_system_bottom_right_radius && is_last_view);
@@ -813,12 +849,20 @@ void MultiContentsView::OnDragAndDropPre
   InvalidateLayout();
 }
 
+void MultiContentsView::OnRoundedFramePrefStateChange() {
+  is_rounded_frame_pref_enabled_ =
+      browser_view_->GetProfile()->GetPrefs()->GetBoolean(
+          prefs::kHeliumRoundedFrame);
+  UpdateContentsBorderAndOverlay();
+  InvalidateLayout();
+}
+
 void MultiContentsView::SetShouldShowTopSeparator(bool should_show) {
   if (contents_separators_.should_show_top == should_show) {
     return;
   }
   contents_separators_.should_show_top = should_show;
-  if (IsInSplitView()) {
+  if (IsInSplitView() || ShouldUseRoundedFrame()) {
     UpdateContentsBorderAndOverlay();
   }
   // This can be called during BrowserView layout, so protect against creating a
@@ -835,7 +879,7 @@ void MultiContentsView::SetSplitViewInse
   }
   start_contents_view_inset_ = std::move(start_contents_view_inset);
   end_contents_view_inset_ = std::move(end_contents_view_inset);
-  if (IsInSplitView()) {
+  if (IsInSplitView() || ShouldUseRoundedFrame()) {
     UpdateContentsBorderAndOverlay();
   }
   // This can be called during BrowserView layout, so protect against creating a
--- a/chrome/browser/ui/views/frame/contents_container_outline.h
+++ b/chrome/browser/ui/views/frame/contents_container_outline.h
@@ -39,6 +39,7 @@ class ContentsContainerOutline : public
 
   void SetCornerRadii(const gfx::RoundedCornersF& radii);
   void UpdateState(bool is_active, bool is_highlighted);
+  void UpdateState(bool is_highlighted, ui::ColorId stroke_color);
 
  private:
   // views::View
@@ -48,8 +49,8 @@ class ContentsContainerOutline : public
   void OnViewBoundsChanged(views::View* observed_view) override;
   void OnViewIsDeleting(views::View* observed_view) override;
 
-  bool is_active_ = false;
   bool is_highlighted_ = false;
+  ui::ColorId stroke_color_ = kColorMultiContentsViewInactiveContentOutline;
   gfx::RoundedCornersF corner_radii_{kCornerRadius};
   base::ScopedObservation<views::View, views::ViewObserver>
       view_bounds_observer_{this};
--- a/chrome/browser/ui/views/frame/contents_container_outline.cc
+++ b/chrome/browser/ui/views/frame/contents_container_outline.cc
@@ -101,8 +101,13 @@ void ContentsContainerOutline::SetCorner
 
 void ContentsContainerOutline::UpdateState(bool is_active,
                                            bool is_highlighted) {
-  is_active_ = is_active;
+  UpdateState(is_highlighted, GetColor(is_active, is_highlighted));
+}
+
+void ContentsContainerOutline::UpdateState(bool is_highlighted,
+                                           ui::ColorId stroke_color) {
   is_highlighted_ = is_highlighted;
+  stroke_color_ = stroke_color;
   SetVisible(true);
   SchedulePaint();
 }
@@ -111,8 +116,7 @@ void ContentsContainerOutline::OnPaint(g
   // Draw the bordering stroke.
   cc::PaintFlags flags;
   flags.setStrokeWidth(GetThickness(is_highlighted_));
-  flags.setColor(
-      GetColorProvider()->GetColor(GetColor(is_active_, is_highlighted_)));
+  flags.setColor(GetColorProvider()->GetColor(stroke_color_));
   flags.setStyle(cc::PaintFlags::kStroke_Style);
   flags.setAntiAlias(true);
 
