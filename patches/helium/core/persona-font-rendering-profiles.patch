--- a/chrome/browser/chrome_content_browser_client.cc	2026-06-15 00:03:30
+++ b/chrome/browser/chrome_content_browser_client.cc	2026-06-15 00:07:22
@@ -847,6 +847,18 @@
   target.font_pack_name = source.font_pack_name;
   target.font_families = source.font_families;
   target.font_aliases = source.font_aliases;
+  target.font_rendering_id = source.font_rendering_id;
+  target.font_rendering_name = source.font_rendering_name;
+  target.font_rendering_engine = source.font_rendering_engine;
+  target.font_rendering_hinting = source.font_rendering_hinting;
+  target.font_antialiasing = source.font_antialiasing;
+  target.font_subpixel_rendering = source.font_subpixel_rendering;
+  target.font_subpixel_positioning = source.font_subpixel_positioning;
+  target.font_lcd_text = source.font_lcd_text;
+  target.font_use_autohinter = source.font_use_autohinter;
+  target.font_use_bitmaps = source.font_use_bitmaps;
+  target.font_text_gamma = source.font_text_gamma;
+  target.font_text_contrast = source.font_text_contrast;
   target.client_hints_enabled = source.client_hints_enabled;
   target.reduce_system_info = source.reduce_system_info;
   target.canvas_noise = source.canvas_noise;
--- a/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts	2026-06-15 00:03:30
+++ b/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts	2026-06-15 00:06:19
@@ -18,6 +18,7 @@
   screen: Record<string, unknown>;
   hardware: Record<string, unknown>;
   fonts: Record<string, unknown>;
+  fontRendering: Record<string, unknown>;
   advanced: Record<string, unknown>;
   builtin?: boolean;
   modified?: boolean;
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html	2026-06-15 00:03:30
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html	2026-06-15 00:16:51
@@ -144,6 +144,11 @@
   </template>
 
   <div class="persona-section-title">$i18n{heliumPersonaSaved}</div>
+  <div class="actions-row">
+    <cr-button on-click="onNewPersonaClick_">
+      New custom
+    </cr-button>
+  </div>
   <template is="dom-repeat" items="[[personas_]]" as="persona">
     <div class="cr-row persona-card">
       <div class="persona-main cr-padded-text">
@@ -261,6 +266,38 @@
     <cr-input class="wide" label="Aliases"
         value="{{fontAliasesText_}}"
         on-change="onFontAliasesChanged_"></cr-input>
+    <cr-input label="Rendering profile id"
+        value="{{editablePersona_.fontRendering.id}}"></cr-input>
+    <cr-input label="Rendering profile"
+        value="{{editablePersona_.fontRendering.displayName}}"></cr-input>
+    <cr-input label="Rendering engine"
+        value="{{editablePersona_.fontRendering.engine}}"></cr-input>
+    <cr-input label="Hinting"
+        value="{{editablePersona_.fontRendering.hinting}}"></cr-input>
+    <cr-input label="Text gamma"
+        value="{{editablePersona_.fontRendering.textGamma}}"
+        type="number"></cr-input>
+    <cr-input label="Text contrast"
+        value="{{editablePersona_.fontRendering.textContrast}}"
+        type="number"></cr-input>
+    <cr-checkbox checked="{{editablePersona_.fontRendering.antialiasing}}">
+      Antialiasing
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.fontRendering.subpixelRendering}}">
+      Subpixel rendering
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.fontRendering.subpixelPositioning}}">
+      Subpixel positioning
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.fontRendering.lcdText}}">
+      LCD text
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.fontRendering.useAutohinter}}">
+      Autohinter
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.fontRendering.useBitmaps}}">
+      Bitmap strikes
+    </cr-checkbox>
   </div>
 
   <div class="persona-section-title">$i18n{heliumPersonaAdvanced}</div>
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts	2026-06-15 00:03:30
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts	2026-06-15 00:16:51
@@ -78,7 +78,9 @@
   }
 
   private clonePersona_(persona: PersonaRecord): PersonaRecord {
-    return JSON.parse(JSON.stringify(persona || {}));
+    const clone = JSON.parse(JSON.stringify(persona || {}));
+    this.ensureEditableShape_(clone);
+    return clone;
   }
 
   private syncTextFields_() {
@@ -210,6 +212,15 @@
     await this.loadState_();
   }
 
+  private async onNewPersonaClick_() {
+    const preset = this.presets_.find(item => !this.isPassthroughPersona_(item));
+    if (!preset) {
+      return;
+    }
+    await this.browserProxy_.clonePreset(preset.id);
+    await this.loadState_();
+  }
+
   private async onUsePersonaClick_(event: Event) {
     const model = (event as CustomEvent).model;
     const persona = model.persona as PersonaRecord;
@@ -231,6 +242,20 @@
     persona.hardware = persona.hardware || {};
     persona.screen = persona.screen || {};
     persona.fonts = persona.fonts || {};
+    persona.fontRendering = persona.fontRendering || {
+      id: 'generic-grayscale',
+      displayName: 'Generic grayscale',
+      engine: 'Skia',
+      hinting: 'medium',
+      antialiasing: true,
+      subpixelRendering: false,
+      subpixelPositioning: false,
+      lcdText: false,
+      useAutohinter: false,
+      useBitmaps: false,
+      textGamma: 1.4,
+      textContrast: 0,
+    };
     persona.advanced = persona.advanced || {};
   }
 
@@ -255,6 +280,21 @@
     persona.screen['colorDepth'] = toNumber(persona.screen['colorDepth']);
     persona.screen['pixelDepth'] = toNumber(persona.screen['pixelDepth']);
     persona.screen['refreshRate'] = toNumber(persona.screen['refreshRate']);
+    persona.fontRendering['textGamma'] =
+        toNumber(persona.fontRendering['textGamma']);
+    persona.fontRendering['textContrast'] =
+        toNumber(persona.fontRendering['textContrast']);
+    persona.fontRendering['antialiasing'] =
+        Boolean(persona.fontRendering['antialiasing']);
+    persona.fontRendering['subpixelRendering'] =
+        Boolean(persona.fontRendering['subpixelRendering']);
+    persona.fontRendering['subpixelPositioning'] =
+        Boolean(persona.fontRendering['subpixelPositioning']);
+    persona.fontRendering['lcdText'] = Boolean(persona.fontRendering['lcdText']);
+    persona.fontRendering['useAutohinter'] =
+        Boolean(persona.fontRendering['useAutohinter']);
+    persona.fontRendering['useBitmaps'] =
+        Boolean(persona.fontRendering['useBitmaps']);
   }
 
   private async onSaveClick_() {
--- a/components/helium_persona/persona_prefs.cc	2026-06-15 00:14:59
+++ b/components/helium_persona/persona_prefs.cc	2026-06-15 00:15:19
@@ -68,6 +68,29 @@
   return nullptr;
 }
 
+base::Value::Dict MakeDefaultFontRenderingProfile() {
+  base::Value::Dict rendering;
+  rendering.Set("id", "generic-grayscale");
+  rendering.Set("displayName", "Generic grayscale");
+  rendering.Set("engine", "Skia");
+  rendering.Set("hinting", "medium");
+  rendering.Set("antialiasing", true);
+  rendering.Set("subpixelRendering", false);
+  rendering.Set("subpixelPositioning", false);
+  rendering.Set("lcdText", false);
+  rendering.Set("useAutohinter", false);
+  rendering.Set("useBitmaps", false);
+  rendering.Set("textGamma", 1.4);
+  rendering.Set("textContrast", 0.0);
+  return rendering;
+}
+
+void EnsurePersonaCompatibilityFields(base::Value::Dict& persona) {
+  if (!persona.FindDict("fontRendering")) {
+    persona.Set("fontRendering", MakeDefaultFontRenderingProfile());
+  }
+}
+
 base::Value::Dict MakeEditablePersona(base::Value::Dict persona) {
   persona.Set("id", MakeCustomPersonaId());
   persona.Set("source", "custom");
@@ -110,6 +133,7 @@
 base::Value::Dict MakeSnapshotFromPersona(base::Value::Dict persona) {
   // Runtime consumers should read this single normalized snapshot rather than
   // reinterpreting presets and custom records independently.
+  EnsurePersonaCompatibilityFields(persona);
   persona.Set("snapshotVersion", 1);
   persona.Set("warnings", ValidatePersona(persona));
   return persona;
@@ -133,7 +157,9 @@
       pref_service.GetList(prefs::kHeliumPersonaRecords);
   if (const base::Value::Dict* persona =
           FindPersonaInList(user_personas, active_id)) {
-    return persona->Clone();
+    base::Value::Dict active = persona->Clone();
+    EnsurePersonaCompatibilityFields(active);
+    return active;
   }
 
   if (base::Value::Dict preset = FindBuiltinPersona(active_id); !preset.empty()) {
@@ -192,6 +218,7 @@
 }
 
 bool SavePersona(PrefService& pref_service, base::Value::Dict persona) {
+  EnsurePersonaCompatibilityFields(persona);
   if (!PersonaHasRequiredFields(persona) || IsPassthroughPersona(persona)) {
     return false;
   }
--- a/components/helium_persona/persona_prefs_unittest.cc	2026-06-15 00:03:30
+++ b/components/helium_persona/persona_prefs_unittest.cc	2026-06-15 00:11:32
@@ -43,7 +43,7 @@
 
 TEST(HeliumPersonaPrefsTest, BuiltinPresetsContainCompleteSystemImage) {
   base::Value::List presets = GetBuiltinPersonaPresets();
-  ASSERT_GE(presets.size(), 5u);
+  ASSERT_GE(presets.size(), 12u);
 
   const base::Value::Dict& windows = DictAt(presets, 1);
   EXPECT_TRUE(PersonaHasRequiredFields(windows));
@@ -65,6 +65,11 @@
   ASSERT_TRUE(families);
   EXPECT_FALSE(families->empty());
 
+  const base::Value::Dict* font_rendering = windows.FindDict("fontRendering");
+  ASSERT_TRUE(font_rendering);
+  EXPECT_EQ("windows-cleartype", *font_rendering->FindString("id"));
+  EXPECT_TRUE(font_rendering->FindBool("antialiasing").value_or(false));
+
   const base::Value::Dict& current = DictAt(presets, 0);
   EXPECT_TRUE(current.FindBool("passthrough").value_or(false));
   EXPECT_TRUE(PersonaHasRequiredFields(current));
@@ -145,12 +150,17 @@
   ASSERT_TRUE(hardware);
   hardware->Set("deviceMemory", 16);
   hardware->Set("maxTouchPoints", 2);
+  base::Value::Dict* font_rendering = persona.FindDict("fontRendering");
+  ASSERT_TRUE(font_rendering);
+  font_rendering->Set("id", "windows-cleartype");
+  font_rendering->Set("engine", "DirectWrite");
 
   base::Value::List warnings = ValidatePersona(persona);
   EXPECT_TRUE(WarningsContain(warnings, "Chinese language profiles"));
   EXPECT_TRUE(WarningsContain(warnings, "Accept-Language should begin"));
   EXPECT_TRUE(WarningsContain(warnings, "deviceMemory should use"));
   EXPECT_TRUE(WarningsContain(warnings, "maxTouchPoints should be 0"));
+  EXPECT_TRUE(WarningsContain(warnings, "CoreText font rendering"));
   EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
 }
 
@@ -163,6 +173,8 @@
   EXPECT_FALSE(snapshot.passthrough);
   EXPECT_EQ("Europe/Berlin", snapshot.timezone);
   EXPECT_FALSE(snapshot.font_families.empty());
+  EXPECT_EQ("linux-freetype", snapshot.font_rendering_id);
+  EXPECT_EQ("slight", snapshot.font_rendering_hinting);
 
   base::Value::Dict value = PersonaSnapshotToValue(snapshot);
   EXPECT_TRUE(PersonaHasRequiredFields(value));
@@ -171,6 +183,9 @@
   EXPECT_EQ("149.0.7827.114",
             *value.FindDict("uaCh")->FindString("fullVersion"));
   ASSERT_TRUE(value.FindDict("uaCh")->FindList("brands"));
+  ASSERT_TRUE(value.FindDict("fontRendering"));
+  EXPECT_EQ("FreeType",
+            *value.FindDict("fontRendering")->FindString("engine"));
   EXPECT_EQ("de-DE", *value.FindDict("region")->FindString("locale"));
   EXPECT_TRUE(value.FindDict("advanced")->FindBool("clientHintsEnabled")
                   .value_or(false));
--- a/components/helium_persona/persona_presets.cc	2026-06-15 00:03:30
+++ b/components/helium_persona/persona_presets.cc	2026-06-15 00:08:32
@@ -130,6 +130,34 @@
   return font_pack;
 }
 
+base::Value::Dict MakeFontRendering(std::string_view id,
+                                   std::string_view display_name,
+                                   std::string_view engine,
+                                   std::string_view hinting,
+                                   bool antialiasing,
+                                   bool subpixel_rendering,
+                                   bool subpixel_positioning,
+                                   bool lcd_text,
+                                   bool use_autohinter,
+                                   bool use_bitmaps,
+                                   double text_gamma,
+                                   double text_contrast) {
+  base::Value::Dict rendering;
+  rendering.Set("id", id);
+  rendering.Set("displayName", display_name);
+  rendering.Set("engine", engine);
+  rendering.Set("hinting", hinting);
+  rendering.Set("antialiasing", antialiasing);
+  rendering.Set("subpixelRendering", subpixel_rendering);
+  rendering.Set("subpixelPositioning", subpixel_positioning);
+  rendering.Set("lcdText", lcd_text);
+  rendering.Set("useAutohinter", use_autohinter);
+  rendering.Set("useBitmaps", use_bitmaps);
+  rendering.Set("textGamma", text_gamma);
+  rendering.Set("textContrast", text_contrast);
+  return rendering;
+}
+
 base::Value::Dict MakeAdvanced(bool client_hints_enabled,
                                bool reduce_system_info,
                                bool canvas_noise,
@@ -157,6 +185,7 @@
                              base::Value::Dict screen,
                              base::Value::Dict hardware,
                              base::Value::Dict fonts,
+                             base::Value::Dict font_rendering,
                              base::Value::Dict advanced) {
   base::Value::Dict preset;
   preset.Set("id", id);
@@ -172,12 +201,37 @@
   preset.Set("screen", std::move(screen));
   preset.Set("hardware", std::move(hardware));
   preset.Set("fonts", std::move(fonts));
+  preset.Set("fontRendering", std::move(font_rendering));
   preset.Set("advanced", std::move(advanced));
   preset.Set("builtin", true);
   preset.Set("modified", false);
   return preset;
 }
 
+base::Value::Dict MakeWindowsClearTypeRendering() {
+  return MakeFontRendering("windows-cleartype", "Windows ClearType",
+                           "DirectWrite", "full", true, true, false,
+                           true, false, false, 1.4, 0.5);
+}
+
+base::Value::Dict MakeMacCoreTextRendering() {
+  return MakeFontRendering("macos-coretext", "macOS CoreText",
+                           "CoreText", "none", true, false, true,
+                           false, false, false, 1.4, 0.0);
+}
+
+base::Value::Dict MakeLinuxFreeTypeRendering() {
+  return MakeFontRendering("linux-freetype", "Linux FreeType",
+                           "FreeType", "slight", true, true, false,
+                           false, true, false, 1.4, 0.0);
+}
+
+base::Value::Dict MakeGenericGrayRendering() {
+  return MakeFontRendering("generic-grayscale", "Generic grayscale",
+                           "Skia", "medium", true, false, false,
+                           false, false, false, 1.4, 0.0);
+}
+
 }  // namespace
 
 base::Value::Dict MakeCurrentSystemPreset() {
@@ -195,6 +249,7 @@
       MakeScreen(0, 0, 0, 0, 0, 0, 0),
       MakeHardware(0, 0, false, 0),
       MakeFontPack("current-system", "Current installed fonts", {}, {}),
+      MakeGenericGrayRendering(),
       MakeAdvanced(true, false, true, true, true));
   preset.Set("passthrough", true);
   return preset;
@@ -227,6 +282,7 @@
                     "Georgia", "Segoe UI", "Times New Roman", "Verdana"},
                    {"sans-serif=Arial", "serif=Times New Roman",
                     "monospace=Consolas", "system-ui=Segoe UI"}),
+      MakeWindowsClearTypeRendering(),
       MakeAdvanced(true, false, true, true, true)));
 
   presets.Append(MakePreset(
@@ -250,6 +306,7 @@
                     "New York", "SF Pro Display", "SF Pro Text", "Times"},
                    {"sans-serif=Helvetica", "serif=Times",
                     "monospace=Menlo", "system-ui=SF Pro Text"}),
+      MakeMacCoreTextRendering(),
       MakeAdvanced(true, false, true, true, true)));
 
   presets.Append(MakePreset(
@@ -276,6 +333,7 @@
                     "Times New Roman"},
                    {"sans-serif=Microsoft YaHei", "serif=SimSun",
                     "monospace=Consolas", "system-ui=Microsoft YaHei"}),
+      MakeWindowsClearTypeRendering(),
       MakeAdvanced(true, false, true, true, true)));
 
   presets.Append(MakePreset(
@@ -301,6 +359,187 @@
                     "Noto Serif", "Ubuntu"},
                    {"sans-serif=Noto Sans", "serif=Noto Serif",
                     "monospace=Liberation Mono", "system-ui=Noto Sans"}),
+      MakeLinuxFreeTypeRendering(),
+      MakeAdvanced(true, false, true, true, true)));
+
+
+  presets.Append(MakePreset(
+      "preset-windows-11-en-gb-london",
+      "Windows 11 / en-GB / Europe/London",
+      "builtin",
+      "Desktop PC",
+      "Windows 11",
+      "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
+      "(KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36",
+      "Win32",
+      MakeUaCh("Windows", "19.0.0", "x86", "64", "", false),
+      MakeRegion("Europe/London", "en-GB",
+                 "en-GB,en;q=0.9", {"en-GB", "en"}),
+      MakeGpu("Intel Inc.",
+              "ANGLE (Intel, Intel(R) Iris(R) Xe Graphics Direct3D11 vs_5_0 "
+              "ps_5_0, D3D11)",
+              "Intel Iris Xe Graphics", "D3D11"),
+      MakeScreen(1920, 1080, 1920, 1040, 1.0, 24, 60),
+      MakeHardware(8, 8, false, 0),
+      MakeFontPack("windows-11-en-gb", "Windows 11 English fonts",
+                   {"Arial", "Calibri", "Cambria", "Candara", "Consolas",
+                    "Georgia", "Segoe UI", "Times New Roman", "Verdana"},
+                   {"sans-serif=Arial", "serif=Times New Roman",
+                    "monospace=Consolas", "system-ui=Segoe UI"}),
+      MakeWindowsClearTypeRendering(),
+      MakeAdvanced(true, false, true, true, true)));
+
+  presets.Append(MakePreset(
+      "preset-windows-11-ja-jp-tokyo",
+      "Windows 11 / ja-JP / Asia/Tokyo",
+      "builtin",
+      "Desktop PC",
+      "Windows 11",
+      "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
+      "(KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36",
+      "Win32",
+      MakeUaCh("Windows", "19.0.0", "x86", "64", "", false),
+      MakeRegion("Asia/Tokyo", "ja-JP",
+                 "ja-JP,ja;q=0.9,en;q=0.8", {"ja-JP", "ja", "en"}),
+      MakeGpu("NVIDIA Corporation",
+              "ANGLE (NVIDIA, NVIDIA GeForce RTX 4060 Direct3D11 vs_5_0 "
+              "ps_5_0, D3D11)",
+              "NVIDIA GeForce RTX 4060", "D3D11"),
+      MakeScreen(2560, 1440, 2560, 1392, 1.25, 24, 144),
+      MakeHardware(12, 8, false, 0),
+      MakeFontPack("windows-ja-jp", "Windows Japanese fonts",
+                   {"Arial", "BIZ UD Gothic", "Consolas", "Meiryo",
+                    "MS Gothic", "MS Mincho", "Segoe UI", "Yu Gothic",
+                    "Yu Mincho"},
+                   {"sans-serif=Meiryo", "serif=Yu Mincho",
+                    "monospace=Consolas", "system-ui=Yu Gothic"}),
+      MakeWindowsClearTypeRendering(),
+      MakeAdvanced(true, false, true, true, true)));
+
+  presets.Append(MakePreset(
+      "preset-windows-11-pt-br-sao-paulo",
+      "Windows 11 / pt-BR / America/Sao_Paulo",
+      "builtin",
+      "Desktop PC",
+      "Windows 11",
+      "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
+      "(KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36",
+      "Win32",
+      MakeUaCh("Windows", "19.0.0", "x86", "64", "", false),
+      MakeRegion("America/Sao_Paulo", "pt-BR",
+                 "pt-BR,pt;q=0.9,en;q=0.8", {"pt-BR", "pt", "en"}),
+      MakeGpu("AMD",
+              "ANGLE (AMD, AMD Radeon RX 6600 Direct3D11 vs_5_0 ps_5_0, "
+              "D3D11)",
+              "AMD Radeon RX 6600", "D3D11"),
+      MakeScreen(1920, 1080, 1920, 1032, 1.0, 24, 75),
+      MakeHardware(8, 8, false, 0),
+      MakeFontPack("windows-pt-br", "Windows Portuguese fonts",
+                   {"Arial", "Calibri", "Cambria", "Candara", "Consolas",
+                    "Georgia", "Segoe UI", "Times New Roman", "Verdana"},
+                   {"sans-serif=Arial", "serif=Times New Roman",
+                    "monospace=Consolas", "system-ui=Segoe UI"}),
+      MakeWindowsClearTypeRendering(),
+      MakeAdvanced(true, false, true, true, true)));
+
+  presets.Append(MakePreset(
+      "preset-macos-en-us-los-angeles",
+      "macOS / en-US / America/Los_Angeles",
+      "builtin",
+      "MacBook Air",
+      "macOS 15",
+      "Mozilla/5.0 (Macintosh; Intel Mac OS X 15_6) AppleWebKit/537.36 "
+      "(KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36",
+      "MacIntel",
+      MakeUaCh("macOS", "15.6.0", "arm", "64", "", false),
+      MakeRegion("America/Los_Angeles", "en-US",
+                 "en-US,en;q=0.9", {"en-US", "en"}),
+      MakeGpu("Apple Inc.",
+              "ANGLE (Apple, Apple M2, Metal)", "Apple M2", "Metal"),
+      MakeScreen(1470, 956, 1470, 918, 2.0, 24, 60),
+      MakeHardware(8, 8, false, 0),
+      MakeFontPack("macos-en-us", "macOS English fonts",
+                   {"Arial", "Helvetica", "Helvetica Neue", "Menlo", "Monaco",
+                    "New York", "SF Pro Display", "SF Pro Text", "Times"},
+                   {"sans-serif=Helvetica", "serif=Times",
+                    "monospace=Menlo", "system-ui=SF Pro Text"}),
+      MakeMacCoreTextRendering(),
+      MakeAdvanced(true, false, true, true, true)));
+
+  presets.Append(MakePreset(
+      "preset-macos-zh-tw-taipei",
+      "macOS / zh-TW / Asia/Taipei",
+      "builtin",
+      "MacBook Pro",
+      "macOS 15",
+      "Mozilla/5.0 (Macintosh; Intel Mac OS X 15_6) AppleWebKit/537.36 "
+      "(KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36",
+      "MacIntel",
+      MakeUaCh("macOS", "15.6.0", "arm", "64", "", false),
+      MakeRegion("Asia/Taipei", "zh-TW",
+                 "zh-TW,zh;q=0.9,en;q=0.8", {"zh-TW", "zh", "en"}),
+      MakeGpu("Apple Inc.",
+              "ANGLE (Apple, Apple M3 Pro, Metal)", "Apple M3 Pro", "Metal"),
+      MakeScreen(1728, 1117, 1728, 1079, 2.0, 24, 120),
+      MakeHardware(12, 8, false, 0),
+      MakeFontPack("macos-zh-tw", "macOS Traditional Chinese fonts",
+                   {"Arial", "Helvetica", "Helvetica Neue", "Menlo",
+                    "PingFang TC", "SF Pro Display", "SF Pro Text", "Times"},
+                   {"sans-serif=PingFang TC", "serif=Times",
+                    "monospace=Menlo", "system-ui=PingFang TC"}),
+      MakeMacCoreTextRendering(),
+      MakeAdvanced(true, false, true, true, true)));
+
+  presets.Append(MakePreset(
+      "preset-linux-en-gb-london",
+      "Linux / en-GB / Europe/London",
+      "builtin",
+      "Linux workstation",
+      "Linux x86_64",
+      "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 "
+      "(KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36",
+      "Linux x86_64",
+      MakeUaCh("Linux", "", "x86", "64", "", false),
+      MakeRegion("Europe/London", "en-GB",
+                 "en-GB,en;q=0.9", {"en-GB", "en"}),
+      MakeGpu("Mesa",
+              "Mesa AMD Radeon Graphics (RADV RAPHAEL_MENDOCINO)",
+              "AMD Radeon Graphics", "Mesa"),
+      MakeScreen(1920, 1080, 1920, 1040, 1.0, 24, 60),
+      MakeHardware(8, 8, false, 0),
+      MakeFontPack("linux-en-gb", "Linux English fonts",
+                   {"Cantarell", "DejaVu Sans", "DejaVu Serif",
+                    "Liberation Mono", "Liberation Sans", "Noto Sans",
+                    "Noto Serif", "Ubuntu"},
+                   {"sans-serif=Noto Sans", "serif=Noto Serif",
+                    "monospace=Liberation Mono", "system-ui=Noto Sans"}),
+      MakeLinuxFreeTypeRendering(),
+      MakeAdvanced(true, false, true, true, true)));
+
+  presets.Append(MakePreset(
+      "preset-linux-fr-fr-paris",
+      "Linux / fr-FR / Europe/Paris",
+      "builtin",
+      "Linux workstation",
+      "Linux x86_64",
+      "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 "
+      "(KHTML, like Gecko) Chrome/149.0.0.0 Safari/537.36",
+      "Linux x86_64",
+      MakeUaCh("Linux", "", "x86", "64", "", false),
+      MakeRegion("Europe/Paris", "fr-FR",
+                 "fr-FR,fr;q=0.9,en;q=0.8", {"fr-FR", "fr", "en"}),
+      MakeGpu("Mesa",
+              "Mesa Intel(R) Xe Graphics (TGL GT2)", "Mesa Intel Xe Graphics",
+              "Mesa"),
+      MakeScreen(1920, 1080, 1920, 1040, 1.0, 24, 60),
+      MakeHardware(8, 8, false, 0),
+      MakeFontPack("linux-fr-fr", "Linux French fonts",
+                   {"Cantarell", "DejaVu Sans", "DejaVu Serif",
+                    "Liberation Mono", "Liberation Sans", "Noto Sans",
+                    "Noto Serif", "Ubuntu"},
+                   {"sans-serif=Noto Sans", "serif=Noto Serif",
+                    "monospace=Liberation Mono", "system-ui=Noto Sans"}),
+      MakeLinuxFreeTypeRendering(),
       MakeAdvanced(true, false, true, true, true)));
 
   return presets;
--- a/components/helium_persona/persona_snapshot.cc	2026-06-15 00:03:30
+++ b/components/helium_persona/persona_snapshot.cc	2026-06-15 00:06:19
@@ -183,6 +183,31 @@
   snapshot.font_families = GetStringList(persona, "fonts", "families");
   snapshot.font_aliases = GetStringList(persona, "fonts", "aliases");
 
+  snapshot.font_rendering_id =
+      GetNestedString(persona, "fontRendering", "id");
+  snapshot.font_rendering_name =
+      GetNestedString(persona, "fontRendering", "displayName");
+  snapshot.font_rendering_engine =
+      GetNestedString(persona, "fontRendering", "engine");
+  snapshot.font_rendering_hinting =
+      GetNestedString(persona, "fontRendering", "hinting");
+  snapshot.font_antialiasing =
+      GetNestedBool(persona, "fontRendering", "antialiasing", true);
+  snapshot.font_subpixel_rendering =
+      GetNestedBool(persona, "fontRendering", "subpixelRendering", false);
+  snapshot.font_subpixel_positioning =
+      GetNestedBool(persona, "fontRendering", "subpixelPositioning", false);
+  snapshot.font_lcd_text =
+      GetNestedBool(persona, "fontRendering", "lcdText", false);
+  snapshot.font_use_autohinter =
+      GetNestedBool(persona, "fontRendering", "useAutohinter", false);
+  snapshot.font_use_bitmaps =
+      GetNestedBool(persona, "fontRendering", "useBitmaps", false);
+  snapshot.font_text_gamma =
+      GetNestedDouble(persona, "fontRendering", "textGamma");
+  snapshot.font_text_contrast =
+      GetNestedDouble(persona, "fontRendering", "textContrast");
+
   snapshot.client_hints_enabled =
       GetNestedBool(persona, "advanced", "clientHintsEnabled", true);
   snapshot.reduce_system_info =
@@ -257,6 +282,21 @@
   fonts.Set("families", MakeStringList(snapshot.font_families));
   fonts.Set("aliases", MakeStringList(snapshot.font_aliases));
   value.Set("fonts", std::move(fonts));
+
+  base::Value::Dict font_rendering;
+  font_rendering.Set("id", snapshot.font_rendering_id);
+  font_rendering.Set("displayName", snapshot.font_rendering_name);
+  font_rendering.Set("engine", snapshot.font_rendering_engine);
+  font_rendering.Set("hinting", snapshot.font_rendering_hinting);
+  font_rendering.Set("antialiasing", snapshot.font_antialiasing);
+  font_rendering.Set("subpixelRendering", snapshot.font_subpixel_rendering);
+  font_rendering.Set("subpixelPositioning", snapshot.font_subpixel_positioning);
+  font_rendering.Set("lcdText", snapshot.font_lcd_text);
+  font_rendering.Set("useAutohinter", snapshot.font_use_autohinter);
+  font_rendering.Set("useBitmaps", snapshot.font_use_bitmaps);
+  font_rendering.Set("textGamma", snapshot.font_text_gamma);
+  font_rendering.Set("textContrast", snapshot.font_text_contrast);
+  value.Set("fontRendering", std::move(font_rendering));
 
   base::Value::Dict advanced;
   advanced.Set("clientHintsEnabled", snapshot.client_hints_enabled);
--- a/components/helium_persona/persona_snapshot.h	2026-06-15 00:03:30
+++ b/components/helium_persona/persona_snapshot.h	2026-06-15 00:06:19
@@ -69,6 +69,19 @@
   std::vector<std::string> font_families;
   std::vector<std::string> font_aliases;
 
+  std::string font_rendering_id;
+  std::string font_rendering_name;
+  std::string font_rendering_engine;
+  std::string font_rendering_hinting;
+  bool font_antialiasing = true;
+  bool font_subpixel_rendering = false;
+  bool font_subpixel_positioning = false;
+  bool font_lcd_text = false;
+  bool font_use_autohinter = false;
+  bool font_use_bitmaps = false;
+  double font_text_gamma = 0;
+  double font_text_contrast = 0;
+
   bool client_hints_enabled = true;
   bool reduce_system_info = false;
   bool canvas_noise = true;
--- a/components/helium_persona/persona_validation.cc	2026-06-15 00:03:30
+++ b/components/helium_persona/persona_validation.cc	2026-06-15 00:09:23
@@ -132,6 +132,13 @@
          base::ToLowerASCII(std::string(rhs));
 }
 
+bool IsKnownHintingValue(std::string_view value) {
+  return EqualsInsensitive(value, "none") ||
+         EqualsInsensitive(value, "slight") ||
+         EqualsInsensitive(value, "medium") ||
+         EqualsInsensitive(value, "full");
+}
+
 bool IsDeviceMemoryBucket(double value) {
   constexpr double kBuckets[] = {0.25, 0.5, 1, 2, 4, 8};
   for (double bucket : kBuckets) {
@@ -155,7 +162,7 @@
       persona.FindDict("uaCh") && persona.FindDict("region") &&
       persona.FindDict("gpu") && persona.FindDict("screen") &&
       persona.FindDict("hardware") && persona.FindDict("fonts") &&
-      persona.FindDict("advanced");
+      persona.FindDict("fontRendering") && persona.FindDict("advanced");
   if (!has_base_shape || IsPassthroughPersona(persona)) {
     return has_base_shape;
   }
@@ -184,7 +191,11 @@
          HasNestedString(persona, "fonts", "id") &&
          HasNestedString(persona, "fonts", "displayName") &&
          HasNestedStringList(persona, "fonts", "families") &&
-         HasNestedStringList(persona, "fonts", "aliases");
+         HasNestedStringList(persona, "fonts", "aliases") &&
+         HasNestedString(persona, "fontRendering", "id") &&
+         HasNestedString(persona, "fontRendering", "displayName") &&
+         HasNestedString(persona, "fontRendering", "engine") &&
+         HasNestedString(persona, "fontRendering", "hinting");
 }
 
 base::Value::List ValidatePersona(const base::Value::Dict& persona) {
@@ -240,6 +251,22 @@
       GetNestedBool(persona, "hardware", "touchEnabled");
   const int max_touch_points =
       GetNestedInt(persona, "hardware", "maxTouchPoints");
+  const std::string_view font_rendering_id =
+      GetNestedString(persona, "fontRendering", "id");
+  const std::string_view font_rendering_engine =
+      GetNestedString(persona, "fontRendering", "engine");
+  const std::string_view font_hinting =
+      GetNestedString(persona, "fontRendering", "hinting");
+  const bool font_antialiasing =
+      GetNestedBool(persona, "fontRendering", "antialiasing");
+  const bool font_subpixel_rendering =
+      GetNestedBool(persona, "fontRendering", "subpixelRendering");
+  const bool font_lcd_text =
+      GetNestedBool(persona, "fontRendering", "lcdText");
+  const double font_text_gamma =
+      GetNestedDouble(persona, "fontRendering", "textGamma");
+  const double font_text_contrast =
+      GetNestedDouble(persona, "fontRendering", "textContrast");
 
   if (Contains(os, "windows") &&
       (!Contains(ua, "windows nt") || !Contains(platform, "win"))) {
@@ -341,6 +368,20 @@
     AddWarning(warnings,
                "German profiles commonly pair with a Europe/* timezone.");
   }
+  if ((Contains(locale, "en-gb") || Contains(locale, "fr-fr")) &&
+      !Contains(timezone, "europe")) {
+    AddWarning(warnings,
+               "European language profiles commonly pair with a Europe/* timezone.");
+  }
+  if ((Contains(locale, "ja-jp") || Contains(locale, "zh-tw")) &&
+      !Contains(timezone, "asia")) {
+    AddWarning(warnings,
+               "East Asian language profiles commonly pair with an Asia/* timezone.");
+  }
+  if (Contains(locale, "pt-br") && !Contains(timezone, "america")) {
+    AddWarning(warnings,
+               "Brazilian Portuguese profiles commonly pair with an America/* timezone.");
+  }
   if (!accept_language.empty() &&
       !StartsWithInsensitive(accept_language, locale)) {
     AddWarning(warnings,
@@ -399,6 +440,38 @@
   if (ua_ch_mobile && width >= 1024 && height >= 720) {
     AddWarning(warnings,
                "Mobile UA-CH metadata does not match a desktop screen size.");
+  }
+
+  if (!IsKnownHintingValue(font_hinting)) {
+    AddWarning(warnings,
+               "Font rendering hinting should be none, slight, medium, or full.");
+  }
+  if (Contains(os, "windows") &&
+      !(Contains(font_rendering_id, "cleartype") ||
+        Contains(font_rendering_engine, "directwrite"))) {
+    AddWarning(warnings,
+               "Windows personas should normally use a ClearType/DirectWrite font rendering profile.");
+  }
+  if (Contains(os, "macos") &&
+      !(Contains(font_rendering_id, "coretext") ||
+        Contains(font_rendering_engine, "coretext"))) {
+    AddWarning(warnings,
+               "macOS personas should normally use a CoreText font rendering profile.");
+  }
+  if (Contains(os, "linux") &&
+      !(Contains(font_rendering_id, "freetype") ||
+        Contains(font_rendering_engine, "freetype"))) {
+    AddWarning(warnings,
+               "Linux personas should normally use a FreeType font rendering profile.");
+  }
+  if (!font_antialiasing && (font_subpixel_rendering || font_lcd_text)) {
+    AddWarning(warnings,
+               "Subpixel/LCD text should not be enabled when antialiasing is disabled.");
+  }
+  if (font_text_gamma < 0 || font_text_gamma > 3 || font_text_contrast < 0 ||
+      font_text_contrast > 2) {
+    AddWarning(warnings,
+               "Font rendering gamma/contrast values are outside common desktop ranges.");
   }
 
   return warnings;
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h	2026-06-15 00:03:30
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h	2026-06-15 00:06:19
@@ -69,6 +69,19 @@
   std::vector<std::string> font_families;
   std::vector<std::string> font_aliases;
 
+  std::string font_rendering_id;
+  std::string font_rendering_name;
+  std::string font_rendering_engine;
+  std::string font_rendering_hinting;
+  bool font_antialiasing = true;
+  bool font_subpixel_rendering = false;
+  bool font_subpixel_positioning = false;
+  bool font_lcd_text = false;
+  bool font_use_autohinter = false;
+  bool font_use_bitmaps = false;
+  double font_text_gamma = 0;
+  double font_text_contrast = 0;
+
   bool client_hints_enabled = true;
   bool reduce_system_info = false;
   bool canvas_noise = true;
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h	2026-06-15 00:03:30
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h	2026-06-15 00:06:19
@@ -186,6 +186,53 @@
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.font_aliases;
   }
+  static const std::string& font_rendering_id(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_rendering_id;
+  }
+  static const std::string& font_rendering_name(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_rendering_name;
+  }
+  static const std::string& font_rendering_engine(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_rendering_engine;
+  }
+  static const std::string& font_rendering_hinting(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_rendering_hinting;
+  }
+  static bool font_antialiasing(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_antialiasing;
+  }
+  static bool font_subpixel_rendering(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_subpixel_rendering;
+  }
+  static bool font_subpixel_positioning(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_subpixel_positioning;
+  }
+  static bool font_lcd_text(const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_lcd_text;
+  }
+  static bool font_use_autohinter(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_use_autohinter;
+  }
+  static bool font_use_bitmaps(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_use_bitmaps;
+  }
+  static double font_text_gamma(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_text_gamma;
+  }
+  static double font_text_contrast(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.font_text_contrast;
+  }
   static bool client_hints_enabled(
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.client_hints_enabled;
@@ -228,6 +275,14 @@
   out->device_memory = data.device_memory();
   out->touch_enabled = data.touch_enabled();
   out->max_touch_points = data.max_touch_points();
+  out->font_antialiasing = data.font_antialiasing();
+  out->font_subpixel_rendering = data.font_subpixel_rendering();
+  out->font_subpixel_positioning = data.font_subpixel_positioning();
+  out->font_lcd_text = data.font_lcd_text();
+  out->font_use_autohinter = data.font_use_autohinter();
+  out->font_use_bitmaps = data.font_use_bitmaps();
+  out->font_text_gamma = data.font_text_gamma();
+  out->font_text_contrast = data.font_text_contrast();
   out->client_hints_enabled = data.client_hints_enabled();
   out->reduce_system_info = data.reduce_system_info();
   out->canvas_noise = data.canvas_noise();
@@ -256,7 +311,11 @@
          data.ReadFontPackId(&out->font_pack_id) &&
          data.ReadFontPackName(&out->font_pack_name) &&
          data.ReadFontFamilies(&out->font_families) &&
-         data.ReadFontAliases(&out->font_aliases);
+         data.ReadFontAliases(&out->font_aliases) &&
+         data.ReadFontRenderingId(&out->font_rendering_id) &&
+         data.ReadFontRenderingName(&out->font_rendering_name) &&
+         data.ReadFontRenderingEngine(&out->font_rendering_engine) &&
+         data.ReadFontRenderingHinting(&out->font_rendering_hinting);
 }
 
 }  // namespace mojo
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom	2026-06-15 00:03:30
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom	2026-06-15 00:06:19
@@ -59,6 +59,19 @@
   array<string> font_families;
   array<string> font_aliases;
 
+  string font_rendering_id;
+  string font_rendering_name;
+  string font_rendering_engine;
+  string font_rendering_hinting;
+  bool font_antialiasing;
+  bool font_subpixel_rendering;
+  bool font_subpixel_positioning;
+  bool font_lcd_text;
+  bool font_use_autohinter;
+  bool font_use_bitmaps;
+  double font_text_gamma;
+  double font_text_contrast;
+
   bool client_hints_enabled;
   bool reduce_system_info;
   bool canvas_noise;
--- a/third_party/blink/renderer/core/exported/web_view_impl.cc	2026-06-15 00:03:30
+++ b/third_party/blink/renderer/core/exported/web_view_impl.cc	2026-06-15 00:13:24
@@ -31,6 +31,7 @@
 
 #include <algorithm>
 #include <memory>
+#include <optional>
 #include <utility>
 #include <vector>
 
@@ -435,6 +436,44 @@
 }
 #endif  // !BUILDFLAG(IS_MAC) && !BUILDFLAG(IS_WIN)
 
+void CopyHeliumPersonaFontPreferences(const RendererPreferences& source,
+                                      RendererPreferences& target) {
+  target.should_antialias_text = source.should_antialias_text;
+  target.text_contrast = source.text_contrast;
+  target.text_gamma = source.text_gamma;
+  target.hinting = source.hinting;
+  target.use_autohinter = source.use_autohinter;
+  target.use_bitmaps = source.use_bitmaps;
+  target.subpixel_rendering = source.subpixel_rendering;
+  target.use_subpixel_positioning = source.use_subpixel_positioning;
+}
+
+bool HeliumPersonaFontPreferencesEqual(const RendererPreferences& lhs,
+                                       const RendererPreferences& rhs) {
+  return lhs.should_antialias_text == rhs.should_antialias_text &&
+         lhs.text_contrast == rhs.text_contrast &&
+         lhs.text_gamma == rhs.text_gamma && lhs.hinting == rhs.hinting &&
+         lhs.use_autohinter == rhs.use_autohinter &&
+         lhs.use_bitmaps == rhs.use_bitmaps &&
+         lhs.subpixel_rendering == rhs.subpixel_rendering &&
+         lhs.use_subpixel_positioning == rhs.use_subpixel_positioning;
+}
+
+gfx::FontRenderParams::Hinting HeliumPersonaHintingToRendererHinting(
+    const std::string& hinting) {
+  String value = String::FromUTF8(hinting).LowerASCII();
+  if (value == "none") {
+    return gfx::FontRenderParams::HINTING_NONE;
+  }
+  if (value == "slight") {
+    return gfx::FontRenderParams::HINTING_SLIGHT;
+  }
+  if (value == "full") {
+    return gfx::FontRenderParams::HINTING_FULL;
+  }
+  return gfx::FontRenderParams::HINTING_MEDIUM;
+}
+
 void ForEachFrameWidgetControlledByView(
     WebViewImpl& web_view,
     base::FunctionRef<void(WebFrameWidgetImpl*)> callback) {
@@ -474,9 +513,11 @@
     timezone_override_.reset();
   }
 
-  void Apply(const HeliumPersonaSnapshot& snapshot) {
+  void Apply(const HeliumPersonaSnapshot& snapshot,
+             RendererPreferences& preferences) {
     ApplyTimezone(snapshot);
     ApplyLocale(snapshot);
+    ApplyFontRendering(snapshot, preferences);
   }
 
  private:
@@ -512,6 +553,34 @@
     }
   }
 
+  void ApplyFontRendering(const HeliumPersonaSnapshot& snapshot,
+                          RendererPreferences& preferences) {
+    if (!font_rendering_baseline_) {
+      font_rendering_baseline_ = preferences;
+    }
+    if (!snapshot.IsEnabled() || snapshot.font_rendering_id.empty()) {
+      CopyHeliumPersonaFontPreferences(*font_rendering_baseline_, preferences);
+      return;
+    }
+
+    preferences.should_antialias_text = snapshot.font_antialiasing;
+    preferences.hinting =
+        HeliumPersonaHintingToRendererHinting(snapshot.font_rendering_hinting);
+    preferences.subpixel_rendering =
+        snapshot.font_subpixel_rendering || snapshot.font_lcd_text
+            ? gfx::FontRenderParams::SUBPIXEL_RENDERING_RGB
+            : gfx::FontRenderParams::SUBPIXEL_RENDERING_NONE;
+    preferences.use_subpixel_positioning = snapshot.font_subpixel_positioning;
+    preferences.use_autohinter = snapshot.font_use_autohinter;
+    preferences.use_bitmaps = snapshot.font_use_bitmaps;
+    if (snapshot.font_text_gamma > 0) {
+      preferences.text_gamma = snapshot.font_text_gamma;
+    }
+    if (snapshot.font_text_contrast >= 0) {
+      preferences.text_contrast = snapshot.font_text_contrast;
+    }
+  }
+
   void ResetLocale() {
     if (!locale_override_active_) {
       return;
@@ -521,6 +590,7 @@
   }
 
   std::unique_ptr<TimeZoneController::TimeZoneOverride> timezone_override_;
+  std::optional<RendererPreferences> font_rendering_baseline_;
   bool locale_override_active_ = false;
 };
 
@@ -3509,7 +3579,12 @@
     helium_persona_runtime_override_ =
         std::make_unique<HeliumPersonaRuntimeOverride>();
   }
-  helium_persona_runtime_override_->Apply(snapshot);
+  RendererPreferences preferences = GetRendererPreferences();
+  RendererPreferences original_preferences = preferences;
+  helium_persona_runtime_override_->Apply(snapshot, preferences);
+  if (!HeliumPersonaFontPreferencesEqual(original_preferences, preferences)) {
+    SetRendererPreferences(preferences);
+  }
 }
 
 void WebViewImpl::SetBaseBackgroundColorOverrideTransparent(
