diff --git a/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts b/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
index 4927e40..ca97767 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
@@ -53,9 +53,11 @@ export interface HeliumPersonaBrowserProxy {
   deletePersonaPreset(presetId: string): Promise<boolean>;
   savePersona(persona: PersonaRecord): Promise<boolean>;
   deletePersona(personaId: string): Promise<boolean>;
-  saveFontPackProfile(profile: Record<string, unknown>): Promise<boolean>;
+  saveFontPackProfile(
+      profile: Record<string, unknown>): Promise<Record<string, unknown>>;
   deleteFontPackProfile(profileId: string): Promise<boolean>;
-  saveFontRenderingProfile(profile: Record<string, unknown>): Promise<boolean>;
+  saveFontRenderingProfile(
+      profile: Record<string, unknown>): Promise<Record<string, unknown>>;
   deleteFontRenderingProfile(profileId: string): Promise<boolean>;
 }
 
@@ -90,7 +92,8 @@ export class HeliumPersonaBrowserProxyImpl implements
   }
 
   saveFontPackProfile(profile: Record<string, unknown>) {
-    return sendWithPromise<boolean>('saveHeliumFontPackProfile', profile);
+    return sendWithPromise<Record<string, unknown>>(
+        'saveHeliumFontPackProfile', profile);
   }
 
   deleteFontPackProfile(profileId: string) {
@@ -98,7 +101,8 @@ export class HeliumPersonaBrowserProxyImpl implements
   }
 
   saveFontRenderingProfile(profile: Record<string, unknown>) {
-    return sendWithPromise<boolean>('saveHeliumFontRenderingProfile', profile);
+    return sendWithPromise<Record<string, unknown>>(
+        'saveHeliumFontRenderingProfile', profile);
   }
 
   deleteFontRenderingProfile(profileId: string) {
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index a91a517..98cb362 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -3089,11 +3089,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const editablePersona = this.clonePersona_(this.editablePersona_);
     editablePersona.fontRendering = JSON.parse(JSON.stringify(profile));
     this.clearStatus_();
-    if (!await this.browserProxy_.saveFontRenderingProfile(profile)) {
+    const saved = await this.browserProxy_.saveFontRenderingProfile(profile);
+    if (!saved || !saved['id']) {
       this.showStatus_('Could not save font rendering profile.');
       return;
     }
-    editablePersona.fontRendering = this.stripProfileListMetadata_(profile);
+    editablePersona.fontRendering = this.stripProfileListMetadata_(saved);
     await this.reloadStateKeepingEditor_(editablePersona, false);
   }
 
@@ -3105,11 +3106,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const editablePersona = this.clonePersona_(this.editablePersona_);
     editablePersona.fonts = JSON.parse(JSON.stringify(profile));
     this.clearStatus_();
-    if (!await this.browserProxy_.saveFontPackProfile(profile)) {
+    const saved = await this.browserProxy_.saveFontPackProfile(profile);
+    if (!saved || !saved['id']) {
       this.showStatus_('Could not save font pack profile.');
       return;
     }
-    editablePersona.fonts = this.stripProfileListMetadata_(profile);
+    editablePersona.fonts = this.stripProfileListMetadata_(saved);
     await this.reloadStateKeepingEditor_(editablePersona, false);
   }
 
diff --git a/chrome/browser/ui/webui/settings/persona_handler.cc b/chrome/browser/ui/webui/settings/persona_handler.cc
index 0211a12..65b9b81 100644
--- a/chrome/browser/ui/webui/settings/persona_handler.cc
+++ b/chrome/browser/ui/webui/settings/persona_handler.cc
@@ -194,17 +194,19 @@ void HeliumPersonaHandler::HandleSaveFontPackProfile(
   const base::Value::Dict* profile = args[1].GetIfDict();
   if (!profile) {
     AllowJavascript();
-    ResolveJavascriptCallback(base::Value(callback_id), base::Value(false));
+    ResolveJavascriptCallback(base::Value(callback_id),
+                              base::Value(base::Value::Dict()));
     return;
   }
 
-  const bool success =
+  base::Value::Dict saved =
       helium_persona::SaveFontPackProfile(*pref_service_, profile->Clone());
-  if (success) {
+  if (!saved.empty()) {
     BroadcastActivePersonaSnapshot();
   }
   AllowJavascript();
-  ResolveJavascriptCallback(base::Value(callback_id), base::Value(success));
+  ResolveJavascriptCallback(base::Value(callback_id),
+                            base::Value(std::move(saved)));
 }
 
 void HeliumPersonaHandler::HandleDeleteFontPackProfile(
@@ -229,17 +231,19 @@ void HeliumPersonaHandler::HandleSaveFontRenderingProfile(
   const base::Value::Dict* profile = args[1].GetIfDict();
   if (!profile) {
     AllowJavascript();
-    ResolveJavascriptCallback(base::Value(callback_id), base::Value(false));
+    ResolveJavascriptCallback(base::Value(callback_id),
+                              base::Value(base::Value::Dict()));
     return;
   }
 
-  const bool success = helium_persona::SaveFontRenderingProfile(
+  base::Value::Dict saved = helium_persona::SaveFontRenderingProfile(
       *pref_service_, profile->Clone());
-  if (success) {
+  if (!saved.empty()) {
     BroadcastActivePersonaSnapshot();
   }
   AllowJavascript();
-  ResolveJavascriptCallback(base::Value(callback_id), base::Value(success));
+  ResolveJavascriptCallback(base::Value(callback_id),
+                            base::Value(std::move(saved)));
 }
 
 void HeliumPersonaHandler::HandleDeleteFontRenderingProfile(
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index d19e25c..dac9548 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -1834,11 +1834,11 @@ bool SavePersona(PrefService& pref_service, base::Value::Dict persona) {
   return true;
 }
 
-bool SaveFontPackProfile(PrefService& pref_service,
-                         base::Value::Dict profile) {
+base::Value::Dict SaveFontPackProfile(PrefService& pref_service,
+                                      base::Value::Dict profile) {
   if (!HasProfileString(profile, "displayName") ||
       !HasProfileStringList(profile, "families")) {
-    return false;
+    return base::Value::Dict();
   }
 
   profile = NormalizeFontPackProfileForStorage(std::move(profile));
@@ -1877,7 +1877,7 @@ bool SaveFontPackProfile(PrefService& pref_service,
   pref_service.SetInteger(
       prefs::kHeliumPersonaRevision,
       pref_service.GetInteger(prefs::kHeliumPersonaRevision) + 1);
-  return true;
+  return updated_profile;
 }
 
 bool DeleteFontPackProfile(PrefService& pref_service,
@@ -1905,12 +1905,12 @@ bool DeleteFontPackProfile(PrefService& pref_service,
   return true;
 }
 
-bool SaveFontRenderingProfile(PrefService& pref_service,
-                              base::Value::Dict profile) {
+base::Value::Dict SaveFontRenderingProfile(PrefService& pref_service,
+                                           base::Value::Dict profile) {
   if (!HasProfileString(profile, "displayName") ||
       !HasProfileString(profile, "engine") ||
       !HasProfileString(profile, "hinting")) {
-    return false;
+    return base::Value::Dict();
   }
 
   profile = NormalizeFontRenderingProfileForStorage(std::move(profile));
@@ -1949,7 +1949,7 @@ bool SaveFontRenderingProfile(PrefService& pref_service,
   pref_service.SetInteger(
       prefs::kHeliumPersonaRevision,
       pref_service.GetInteger(prefs::kHeliumPersonaRevision) + 1);
-  return true;
+  return updated_profile;
 }
 
 bool DeleteFontRenderingProfile(PrefService& pref_service,
diff --git a/components/helium_persona/persona_prefs.h b/components/helium_persona/persona_prefs.h
index 0f069f0..4b75208 100644
--- a/components/helium_persona/persona_prefs.h
+++ b/components/helium_persona/persona_prefs.h
@@ -48,16 +48,16 @@ COMPONENT_EXPORT(HELIUM_PERSONA)
 bool DeletePersona(PrefService& pref_service, std::string_view persona_id);
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
-bool SaveFontPackProfile(PrefService& pref_service,
-                         base::Value::Dict profile);
+base::Value::Dict SaveFontPackProfile(PrefService& pref_service,
+                                      base::Value::Dict profile);
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
 bool DeleteFontPackProfile(PrefService& pref_service,
                            std::string_view profile_id);
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
-bool SaveFontRenderingProfile(PrefService& pref_service,
-                              base::Value::Dict profile);
+base::Value::Dict SaveFontRenderingProfile(PrefService& pref_service,
+                                           base::Value::Dict profile);
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
 bool DeleteFontRenderingProfile(PrefService& pref_service,
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 3920451..c75543e 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -879,7 +879,7 @@ TEST(HeliumPersonaPrefsTest, StateIncludesIndependentFontRenderingProfiles) {
   custom.Set("useBitmaps", false);
   custom.Set("textGamma", 1.5);
   custom.Set("textContrast", 0.25);
-  EXPECT_TRUE(SaveFontRenderingProfile(prefs, std::move(custom)));
+  EXPECT_FALSE(SaveFontRenderingProfile(prefs, std::move(custom)).empty());
 
   state = GetPersonaState(prefs);
   profiles = state.FindList("fontRenderingProfiles");
@@ -892,7 +892,7 @@ TEST(HeliumPersonaPrefsTest, StateIncludesIndependentFontRenderingProfiles) {
 
   base::Value::Dict copied_builtin = DictAt(*profiles, 0).Clone();
   copied_builtin.Set("displayName", "Copied ClearType");
-  EXPECT_TRUE(SaveFontRenderingProfile(prefs, std::move(copied_builtin)));
+  EXPECT_FALSE(SaveFontRenderingProfile(prefs, std::move(copied_builtin)).empty());
   state = GetPersonaState(prefs);
   profiles = state.FindList("fontRenderingProfiles");
   ASSERT_TRUE(profiles);
@@ -937,7 +937,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontRenderingPropagatesLinkedPersonas) {
   rendering.Set("useBitmaps", false);
   rendering.Set("textGamma", 1.4);
   rendering.Set("textContrast", 0.0);
-  EXPECT_TRUE(SaveFontRenderingProfile(prefs, rendering.Clone()));
+  EXPECT_FALSE(SaveFontRenderingProfile(prefs, rendering.Clone()).empty());
 
   base::Value::Dict state = GetPersonaState(prefs);
   const base::Value::Dict* saved_rendering = FindProfileById(
@@ -963,7 +963,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontRenderingPropagatesLinkedPersonas) {
   rendering.Set("displayName", "Follow rendering updated");
   rendering.Set("hinting", "full");
   rendering.Set("textGamma", 1.6);
-  EXPECT_TRUE(SaveFontRenderingProfile(prefs, std::move(rendering)));
+  EXPECT_FALSE(SaveFontRenderingProfile(prefs, std::move(rendering)).empty());
 
   state = GetPersonaState(prefs);
   const base::Value::List* personas = state.FindList("personas");
@@ -1006,7 +1006,7 @@ TEST(HeliumPersonaPrefsTest, SaveFontRenderingProfileCreatesIdWhenEmpty) {
   custom.Set("useBitmaps", false);
   custom.Set("textGamma", 1.4);
   custom.Set("textContrast", 0.0);
-  EXPECT_TRUE(SaveFontRenderingProfile(prefs, std::move(custom)));
+  EXPECT_FALSE(SaveFontRenderingProfile(prefs, std::move(custom)).empty());
 
   base::Value::Dict state = GetPersonaState(prefs);
   EXPECT_TRUE(ProfilesContainId(*state.FindList("fontRenderingProfiles"),
@@ -1027,7 +1027,7 @@ TEST(HeliumPersonaPrefsTest, SaveFontRenderingProfileNormalizesStorageShape) {
   custom.Set("textGamma", "bad");
   custom.Set("textContrast", 1);
   custom.Set("unexpected", "drop me");
-  EXPECT_TRUE(SaveFontRenderingProfile(prefs, std::move(custom)));
+  EXPECT_FALSE(SaveFontRenderingProfile(prefs, std::move(custom)).empty());
 
   base::Value::Dict state = GetPersonaState(prefs);
   const base::Value::Dict* profile = FindProfileById(
@@ -1058,7 +1058,15 @@ TEST(HeliumPersonaPrefsTest, SaveFontRenderingProfileSlugifiesCustomId) {
   custom.Set("engine", "Skia");
   custom.Set("hinting", "medium");
 
-  EXPECT_TRUE(SaveFontRenderingProfile(prefs, std::move(custom)));
+  base::Value::Dict saved_profile =
+      SaveFontRenderingProfile(prefs, std::move(custom));
+  ASSERT_FALSE(saved_profile.empty());
+  EXPECT_EQ("my-render-profile", *saved_profile.FindString("id"));
+  EXPECT_EQ(1, saved_profile.FindInt("profileVersion").value_or(0));
+  EXPECT_FALSE(saved_profile.FindBool("builtin").value_or(true));
+  EXPECT_TRUE(saved_profile.FindBool("modified").value_or(false));
+  EXPECT_TRUE(saved_profile.FindDouble("updated").has_value());
+
   base::Value::Dict state = GetPersonaState(prefs);
   const base::Value::List* profiles = state.FindList("fontRenderingProfiles");
   ASSERT_TRUE(profiles);
@@ -1108,7 +1116,7 @@ TEST(HeliumPersonaPrefsTest, StateIncludesIndependentFontPackProfiles) {
   aliases.Append("sans-serif=Noto Sans");
   aliases.Append("system-ui=Noto Sans CJK SC");
   custom.Set("aliases", std::move(aliases));
-  EXPECT_TRUE(SaveFontPackProfile(prefs, std::move(custom)));
+  EXPECT_FALSE(SaveFontPackProfile(prefs, std::move(custom)).empty());
 
   state = GetPersonaState(prefs);
   profiles = state.FindList("fontPackProfiles");
@@ -1121,7 +1129,7 @@ TEST(HeliumPersonaPrefsTest, StateIncludesIndependentFontPackProfiles) {
 
   base::Value::Dict copied_builtin = DictAt(*profiles, 0).Clone();
   copied_builtin.Set("displayName", "Copied Windows fonts");
-  EXPECT_TRUE(SaveFontPackProfile(prefs, std::move(copied_builtin)));
+  EXPECT_FALSE(SaveFontPackProfile(prefs, std::move(copied_builtin)).empty());
   state = GetPersonaState(prefs);
   profiles = state.FindList("fontPackProfiles");
   ASSERT_TRUE(profiles);
@@ -1169,7 +1177,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontPackProfilePropagatesLinkedPersonas) {
   base::Value::List aliases;
   aliases.Append("sans-serif=Arial");
   pack.Set("aliases", std::move(aliases));
-  EXPECT_TRUE(SaveFontPackProfile(prefs, pack.Clone()));
+  EXPECT_FALSE(SaveFontPackProfile(prefs, pack.Clone()).empty());
 
   base::Value::Dict state = GetPersonaState(prefs);
   const base::Value::Dict* saved_pack = FindProfileById(
@@ -1209,7 +1217,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontPackProfilePropagatesLinkedPersonas) {
   updated_families.Append("Arial");
   updated_families.Append("Noto Sans");
   pack.Set("families", std::move(updated_families));
-  EXPECT_TRUE(SaveFontPackProfile(prefs, std::move(pack)));
+  EXPECT_FALSE(SaveFontPackProfile(prefs, std::move(pack)).empty());
 
   state = GetPersonaState(prefs);
   const base::Value::List* personas = state.FindList("personas");
@@ -1256,7 +1264,7 @@ TEST(HeliumPersonaPrefsTest, SaveFontPackProfileCreatesIdWhenEmpty) {
   base::Value::List families;
   families.Append("Arial");
   custom.Set("families", std::move(families));
-  EXPECT_TRUE(SaveFontPackProfile(prefs, std::move(custom)));
+  EXPECT_FALSE(SaveFontPackProfile(prefs, std::move(custom)).empty());
 
   base::Value::Dict state = GetPersonaState(prefs);
   EXPECT_TRUE(ProfilesContainId(*state.FindList("fontPackProfiles"),
@@ -1283,7 +1291,7 @@ TEST(HeliumPersonaPrefsTest, SaveFontPackProfileNormalizesStorageShape) {
   aliases.Append("system-ui=Noto Sans");
   custom.Set("aliases", std::move(aliases));
   custom.Set("unexpected", "drop me");
-  EXPECT_TRUE(SaveFontPackProfile(prefs, std::move(custom)));
+  EXPECT_FALSE(SaveFontPackProfile(prefs, std::move(custom)).empty());
 
   base::Value::Dict state = GetPersonaState(prefs);
   const base::Value::Dict* profile =
@@ -1315,7 +1323,14 @@ TEST(HeliumPersonaPrefsTest, SaveFontPackProfileSlugifiesCustomId) {
   custom.Set("displayName", "Custom fonts");
   custom.Set("families", std::move(families));
 
-  EXPECT_TRUE(SaveFontPackProfile(prefs, std::move(custom)));
+  base::Value::Dict saved_profile =
+      SaveFontPackProfile(prefs, std::move(custom));
+  ASSERT_FALSE(saved_profile.empty());
+  EXPECT_EQ("my-font-pack", *saved_profile.FindString("id"));
+  EXPECT_FALSE(saved_profile.FindBool("builtin").value_or(true));
+  EXPECT_TRUE(saved_profile.FindBool("modified").value_or(false));
+  EXPECT_TRUE(saved_profile.FindDouble("updated").has_value());
+
   base::Value::Dict state = GetPersonaState(prefs);
   const base::Value::List* profiles = state.FindList("fontPackProfiles");
   ASSERT_TRUE(profiles);
@@ -1333,7 +1348,7 @@ TEST(HeliumPersonaPrefsTest, SaveFontPackProfileRejectsNonStringFamilies) {
   families.Append(false);
   families.Append("");
   custom.Set("families", std::move(families));
-  EXPECT_FALSE(SaveFontPackProfile(prefs, std::move(custom)));
+  EXPECT_TRUE(SaveFontPackProfile(prefs, std::move(custom)).empty());
 
   base::Value::Dict state = GetPersonaState(prefs);
   EXPECT_FALSE(ProfilesContainId(*state.FindList("fontPackProfiles"),
