diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index e430562c..2990ec7e 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -254,6 +254,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const allowWindowManagement =
         Boolean(advanced['allowWindowManagement']);
     const allowInstalledApps = Boolean(advanced['allowInstalledApps']);
+    const allowManagedDevice = Boolean(advanced['allowManagedDevice']);
     const allowPaymentHandler = Boolean(advanced['allowPaymentHandler']);
     const allowPlatformCredentials =
         Boolean(advanced['allowPlatformCredentials']);
@@ -267,6 +268,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const allowNotifications = Boolean(advanced['allowNotifications']);
     const allowPersistentStorage =
         Boolean(advanced['allowPersistentStorage']);
+    const allowBackgroundSync = Boolean(advanced['allowBackgroundSync']);
     const allowClipboard = Boolean(advanced['allowClipboard']);
     const allowIdleDetection = Boolean(advanced['allowIdleDetection']);
     const allowWakeLock = Boolean(advanced['allowWakeLock']);
@@ -659,6 +661,11 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       this.addDraftWarning_(
           warnings,
           'allowInstalledApps exposes installed app discovery service bindings.');
+    }
+    if (allowManagedDevice) {
+      this.addDraftWarning_(
+          warnings,
+          'allowManagedDevice exposes enterprise-managed device attributes.');
     }
     if (allowPaymentHandler) {
       this.addDraftWarning_(
@@ -704,6 +711,11 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'allowPersistentStorage exposes persistent storage permission behavior.');
     }
+    if (allowBackgroundSync) {
+      this.addDraftWarning_(
+          warnings,
+          'allowBackgroundSync exposes background sync, periodic sync, and background fetch service bindings.');
+    }
     if (allowClipboard) {
       this.addDraftWarning_(
           warnings,
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 2bc03d18..9f125159 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -852,6 +852,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsAboutInconsistentPersonas) {
   advanced->Set("allowWebNfc", true);
   advanced->Set("allowWindowManagement", true);
   advanced->Set("allowInstalledApps", true);
+  advanced->Set("allowManagedDevice", true);
   advanced->Set("allowPaymentHandler", true);
   advanced->Set("allowPlatformCredentials", true);
   advanced->Set("allowWebXr", true);
@@ -861,6 +862,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsAboutInconsistentPersonas) {
   advanced->Set("allowPersistentDeviceId", true);
   advanced->Set("allowNotifications", true);
   advanced->Set("allowPersistentStorage", true);
+  advanced->Set("allowBackgroundSync", true);
   advanced->Set("allowClipboard", true);
   advanced->Set("allowIdleDetection", true);
   advanced->Set("allowWakeLock", true);
@@ -945,6 +947,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsAboutInconsistentPersonas) {
   EXPECT_TRUE(WarningsContain(warnings, "allowWebNfc exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowWindowManagement exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowInstalledApps exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowManagedDevice exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowPaymentHandler exposes"));
   EXPECT_TRUE(WarningsContain(warnings,
                               "allowPlatformCredentials exposes real"));
@@ -955,6 +958,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsAboutInconsistentPersonas) {
   EXPECT_TRUE(WarningsContain(warnings, "allowPersistentDeviceId"));
   EXPECT_TRUE(WarningsContain(warnings, "allowNotifications exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowPersistentStorage exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowBackgroundSync exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowClipboard exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowIdleDetection exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowWakeLock exposes"));
@@ -1463,6 +1467,7 @@ TEST(HeliumPersonaPrefsTest, RuntimeSnapshotPreservesManualOverrides) {
   advanced->Set("allowGeolocation", true);
   advanced->Set("allowLocalFonts", true);
   advanced->Set("allowInstalledApps", true);
+  advanced->Set("allowManagedDevice", true);
   advanced->Set("allowPlatformCredentials", true);
   advanced->Set("virtualMediaDevices", false);
   advanced->Set("allowNotifications", true);
@@ -1647,6 +1652,7 @@ TEST(HeliumPersonaPrefsTest, RuntimeSnapshotPreservesManualOverrides) {
   EXPECT_TRUE(snapshot.allow_geolocation);
   EXPECT_TRUE(snapshot.allow_local_fonts);
   EXPECT_TRUE(snapshot.allow_installed_apps);
+  EXPECT_TRUE(snapshot.allow_managed_device);
   EXPECT_TRUE(snapshot.allow_platform_credentials);
   EXPECT_FALSE(snapshot.virtual_media_devices);
   EXPECT_TRUE(snapshot.allow_notifications);
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 67873ea5..5295c4c5 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -705,6 +705,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedBool(persona, "advanced", "allowWindowManagement");
   const bool allow_installed_apps =
       GetNestedBool(persona, "advanced", "allowInstalledApps");
+  const bool allow_managed_device =
+      GetNestedBool(persona, "advanced", "allowManagedDevice");
   const bool allow_payment_handler =
       GetNestedBool(persona, "advanced", "allowPaymentHandler");
   const bool allow_platform_credentials =
@@ -723,6 +725,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedBool(persona, "advanced", "allowNotifications");
   const bool allow_persistent_storage =
       GetNestedBool(persona, "advanced", "allowPersistentStorage");
+  const bool allow_background_sync =
+      GetNestedBool(persona, "advanced", "allowBackgroundSync");
   const bool allow_clipboard =
       GetNestedBool(persona, "advanced", "allowClipboard");
   const bool allow_idle_detection =
@@ -1178,6 +1182,10 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
   if (allow_installed_apps) {
     AddWarning(warnings,
                "allowInstalledApps exposes installed app discovery service bindings.");
+  }
+  if (allow_managed_device) {
+    AddWarning(warnings,
+               "allowManagedDevice exposes enterprise-managed device attributes.");
   }
   if (allow_payment_handler) {
     AddWarning(warnings,
@@ -1215,6 +1223,10 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "allowPersistentStorage exposes persistent storage permission behavior.");
   }
+  if (allow_background_sync) {
+    AddWarning(warnings,
+               "allowBackgroundSync exposes background sync, periodic sync, and background fetch service bindings.");
+  }
   if (allow_clipboard) {
     AddWarning(warnings,
                "allowClipboard exposes real clipboard read and write permission surfaces.");
