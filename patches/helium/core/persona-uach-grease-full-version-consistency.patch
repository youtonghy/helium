diff --git a/content/browser/client_hints/client_hints.cc b/content/browser/client_hints/client_hints.cc
index f8d2e131..3f936a0a 100644
--- a/content/browser/client_hints/client_hints.cc
+++ b/content/browser/client_hints/client_hints.cc
@@ -67,6 +67,12 @@ namespace content {
 
 namespace {
 using ::network::mojom::WebClientHintsType;
+
+bool IsHeliumPersonaGreaseBrand(const std::string& brand) {
+  std::string lower = base::ToLowerASCII(brand);
+  return lower.find("not") != std::string::npos &&
+         lower.find("brand") != std::string::npos;
+}
 
 blink::UserAgentBrandList ToUserAgentBrandList(
     const std::vector<blink::HeliumPersonaUaChBrand>& brands,
@@ -78,7 +84,7 @@ blink::UserAgentBrandList ToUserAgentBrandList(
       continue;
     }
     std::string version = brand.version;
-    if (use_full_version && brand.brand != "Not_A Brand" &&
+    if (use_full_version && !IsHeliumPersonaGreaseBrand(brand.brand) &&
         !full_version.empty()) {
       version = full_version;
     }
@@ -229,7 +235,7 @@ void ApplyPersonaUserAgentMetadata(
             : ua_metadata->brand_version_list;
     for (blink::UserAgentBrandVersion& brand_version :
          ua_metadata->brand_full_version_list) {
-      if (brand_version.brand != "Not_A Brand") {
+      if (!IsHeliumPersonaGreaseBrand(brand_version.brand)) {
         brand_version.version = persona_snapshot.ua_ch_full_version;
       }
     }
diff --git a/content/browser/service_worker/service_worker_version.cc b/content/browser/service_worker/service_worker_version.cc
index 8bf90c4f..462dd996 100644
--- a/content/browser/service_worker/service_worker_version.cc
+++ b/content/browser/service_worker/service_worker_version.cc
@@ -253,6 +253,12 @@ storage::mojom::CacheStorageControl* GetCacheStorageControl(
     return nullptr;
   }
   return control;
+}
+
+bool IsHeliumPersonaGreaseBrand(const std::string& brand) {
+  std::string lower = base::ToLowerASCII(brand);
+  return lower.find("not") != std::string::npos &&
+         lower.find("brand") != std::string::npos;
 }
 
 blink::UserAgentBrandList ToUserAgentBrandList(
@@ -265,7 +271,7 @@ blink::UserAgentBrandList ToUserAgentBrandList(
       continue;
     }
     std::string version = brand.version;
-    if (use_full_version && brand.brand != "Not_A Brand" &&
+    if (use_full_version && !IsHeliumPersonaGreaseBrand(brand.brand) &&
         !full_version.empty()) {
       version = full_version;
     }
@@ -312,7 +318,7 @@ void ApplyPersonaUserAgentMetadata(
             : ua_metadata->brand_version_list;
     for (blink::UserAgentBrandVersion& brand_version :
          ua_metadata->brand_full_version_list) {
-      if (brand_version.brand != "Not_A Brand") {
+      if (!IsHeliumPersonaGreaseBrand(brand_version.brand)) {
         brand_version.version = persona_snapshot.ua_ch_full_version;
       }
     }
diff --git a/content/browser/worker_host/shared_worker_host.cc b/content/browser/worker_host/shared_worker_host.cc
index 7f611b08..55b24c73 100644
--- a/content/browser/worker_host/shared_worker_host.cc
+++ b/content/browser/worker_host/shared_worker_host.cc
@@ -94,6 +94,12 @@ enum class SharedWorkerHostDestructionSource {
 void RecordDestructionSource(SharedWorkerHostDestructionSource source) {
   base::UmaHistogramEnumeration("Content.SharedWorker.Host.DestructionSource",
                                 source);
+}
+
+bool IsHeliumPersonaGreaseBrand(const std::string& brand) {
+  std::string lower = base::ToLowerASCII(brand);
+  return lower.find("not") != std::string::npos &&
+         lower.find("brand") != std::string::npos;
 }
 
 blink::UserAgentBrandList ToUserAgentBrandList(
@@ -106,7 +112,7 @@ blink::UserAgentBrandList ToUserAgentBrandList(
       continue;
     }
     std::string version = brand.version;
-    if (use_full_version && brand.brand != "Not_A Brand" &&
+    if (use_full_version && !IsHeliumPersonaGreaseBrand(brand.brand) &&
         !full_version.empty()) {
       version = full_version;
     }
@@ -153,7 +159,7 @@ void ApplyPersonaUserAgentMetadata(
             : ua_metadata->brand_version_list;
     for (blink::UserAgentBrandVersion& brand_version :
          ua_metadata->brand_full_version_list) {
-      if (brand_version.brand != "Not_A Brand") {
+      if (!IsHeliumPersonaGreaseBrand(brand_version.brand)) {
         brand_version.version = persona_snapshot.ua_ch_full_version;
       }
     }
