diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 3f6b5d6..d8ae942 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -262,6 +262,11 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     return Number.isFinite(number) && number >= 0 ? number : fallback;
   }
 
+  private signedNumberOr_(value: unknown, fallback: number): number {
+    const number = this.numberValueOr_(value, fallback);
+    return Number.isFinite(number) ? number : fallback;
+  }
+
   private majorVersionFromString_(value: unknown): number {
     const match = /^(\d+)/.exec(this.stringValue_(value));
     if (!match) {
@@ -3496,9 +3501,9 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
         persona.screen['width'], 1920);
     persona.screen['height'] = this.positiveNumberOr_(
         persona.screen['height'], 1080);
-    persona.screen['availLeft'] = this.nonNegativeNumberOr_(
+    persona.screen['availLeft'] = this.signedNumberOr_(
         persona.screen['availLeft'], 0);
-    persona.screen['availTop'] = this.nonNegativeNumberOr_(
+    persona.screen['availTop'] = this.signedNumberOr_(
         persona.screen['availTop'], 0);
     persona.screen['availWidth'] = this.positiveNumberOr_(
         persona.screen['availWidth'], Number(persona.screen['width']));
@@ -3509,9 +3514,9 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
         persona.screen['outerWidth'], Number(persona.screen['width']));
     persona.screen['outerHeight'] = this.positiveNumberOr_(
         persona.screen['outerHeight'], Number(persona.screen['height']));
-    persona.screen['windowScreenX'] = this.nonNegativeNumberOr_(
+    persona.screen['windowScreenX'] = this.signedNumberOr_(
         persona.screen['windowScreenX'], Number(persona.screen['availLeft']));
-    persona.screen['windowScreenY'] = this.nonNegativeNumberOr_(
+    persona.screen['windowScreenY'] = this.signedNumberOr_(
         persona.screen['windowScreenY'], Number(persona.screen['availTop']));
     persona.screen['deviceScaleFactor'] =
         this.positiveNumberOr_(persona.screen['deviceScaleFactor'], 1);
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index b75472a..0e87ee7 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -2151,8 +2151,8 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   ASSERT_TRUE(screen);
   screen->Set("width", 3000);
   screen->Set("height", 2000);
-  screen->Set("availLeft", 12);
-  screen->Set("availTop", 28);
+  screen->Set("availLeft", -1920);
+  screen->Set("availTop", -80);
   screen->Set("availWidth", 3000);
   screen->Set("availHeight", 1960);
   screen->Set("deviceScaleFactor", 1.5);
@@ -2163,8 +2163,8 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   screen->Set("orientationAngle", 90);
   screen->Set("outerWidth", 2800);
   screen->Set("outerHeight", 1800);
-  screen->Set("windowScreenX", 44);
-  screen->Set("windowScreenY", 55);
+  screen->Set("windowScreenX", -1888);
+  screen->Set("windowScreenY", -48);
   screen->Set("colorScheme", "dark");
   screen->Set("reducedMotion", "reduce");
   screen->Set("reducedTransparency", "reduce");
@@ -2317,18 +2317,20 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   ASSERT_TRUE(runtime.FindDict("gpu")->FindList("webgpuFeatures"));
   EXPECT_EQ(2u, runtime.FindDict("gpu")->FindList("webgpuFeatures")->size());
   EXPECT_EQ(3000, runtime.FindDict("screen")->FindInt("width").value_or(0));
-  EXPECT_EQ(12, runtime.FindDict("screen")->FindInt("availLeft").value_or(-1));
-  EXPECT_EQ(28, runtime.FindDict("screen")->FindInt("availTop").value_or(-1));
+  EXPECT_EQ(-1920,
+            runtime.FindDict("screen")->FindInt("availLeft").value_or(0));
+  EXPECT_EQ(-80,
+            runtime.FindDict("screen")->FindInt("availTop").value_or(0));
   EXPECT_EQ("portrait-primary",
             *runtime.FindDict("screen")->FindString("orientationType"));
   EXPECT_EQ(90,
             runtime.FindDict("screen")->FindInt("orientationAngle").value_or(-1));
   EXPECT_EQ(2800, runtime.FindDict("screen")->FindInt("outerWidth").value_or(0));
   EXPECT_EQ(1800, runtime.FindDict("screen")->FindInt("outerHeight").value_or(0));
-  EXPECT_EQ(44,
-            runtime.FindDict("screen")->FindInt("windowScreenX").value_or(-1));
-  EXPECT_EQ(55,
-            runtime.FindDict("screen")->FindInt("windowScreenY").value_or(-1));
+  EXPECT_EQ(-1888,
+            runtime.FindDict("screen")->FindInt("windowScreenX").value_or(0));
+  EXPECT_EQ(-48,
+            runtime.FindDict("screen")->FindInt("windowScreenY").value_or(0));
   EXPECT_EQ("dark", *runtime.FindDict("screen")->FindString("colorScheme"));
   EXPECT_EQ("reduce", *runtime.FindDict("screen")->FindString("reducedMotion"));
   EXPECT_EQ("reduce",
