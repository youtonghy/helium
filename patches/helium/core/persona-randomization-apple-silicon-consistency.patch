diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 24d380f..46d5074 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -1363,6 +1363,14 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     return this.randomFrom_(['15.0.0', '19.0.0', '19.0.0'], '19.0.0');
   }
 
+  private personaUsesAppleGpu_(persona: PersonaRecord): boolean {
+    const gpu = persona.gpu || {};
+    const vendor = String(gpu['vendor'] || '').toLowerCase();
+    const renderer = String(gpu['renderer'] || '').toLowerCase();
+    return vendor.includes('apple') || renderer.includes('apple m') ||
+        renderer.includes('apple gpu');
+  }
+
   private applyConsistentRandomUaChIdentity_(persona: PersonaRecord) {
     persona.uaCh = persona.uaCh || {};
     const osFamily = this.personaOsFamily_(persona);
@@ -1370,7 +1378,8 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       persona.uaCh['platform'] = 'macOS';
       persona.uaCh['platformVersion'] =
           this.randomChromePlatformVersion_(osFamily);
-      persona.uaCh['architecture'] = this.randomFrom_(['arm', 'x86'], 'arm');
+      persona.uaCh['architecture'] =
+          this.personaUsesAppleGpu_(persona) ? 'arm' : 'x86';
       persona.uaCh['bitness'] = '64';
       persona.uaCh['wow64'] = false;
     } else if (osFamily === 'linux') {
@@ -2121,8 +2130,9 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const compatiblePresentation = chance(6);
     const rareProtected = (protectedPercent: number, balancedPercent: number,
         compatiblePercent: number): [boolean, boolean, boolean] => {
+      void protectedPercent;
       return [
-        chance(protectedPercent),
+        false,
         chance(balancedPercent),
         chance(compatiblePercent),
       ];
