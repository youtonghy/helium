--- a/content/browser/client_hints/client_hints.cc
+++ b/content/browser/client_hints/client_hints.cc
@@ -55,6 +55,7 @@
 #include "third_party/blink/public/common/client_hints/enabled_client_hints.h"
 #include "third_party/blink/public/common/device_memory/approximated_device_memory.h"
 #include "third_party/blink/public/common/features.h"
+#include "third_party/blink/public/common/helium_persona/persona_snapshot.h"
 #include "third_party/blink/public/common/page/page_zoom.h"
 #include "third_party/blink/public/common/user_agent/user_agent_metadata.h"
 #include "ui/display/display.h"
@@ -235,12 +236,19 @@ void RemoveClientHintHeader(WebClientHin
   headers->RemoveHeader(network::GetClientHintToNameMap().at(client_hint_type));
 }
 
-void AddDeviceMemoryHeader(net::HttpRequestHeaders* headers,
-                           bool use_deprecated_version = false) {
+void AddDeviceMemoryHeader(
+    net::HttpRequestHeaders* headers,
+    std::optional<double> persona_device_memory,
+    bool use_deprecated_version = false) {
   DCHECK(headers);
-  blink::ApproximatedDeviceMemory::Initialize();
-  const float device_memory =
-      blink::ApproximatedDeviceMemory::GetApproximatedDeviceMemory();
+  float device_memory = 0.0;
+  if (persona_device_memory && *persona_device_memory > 0.0) {
+    device_memory = static_cast<float>(*persona_device_memory);
+  } else {
+    blink::ApproximatedDeviceMemory::Initialize();
+    device_memory =
+        blink::ApproximatedDeviceMemory::GetApproximatedDeviceMemory();
+  }
   DCHECK_LT(0.0, device_memory);
   SetHeaderToDouble(headers,
                     use_deprecated_version
@@ -252,15 +260,58 @@ void AddDeviceMemoryHeader(net::HttpRequ
 void AddDPRHeader(net::HttpRequestHeaders* headers,
                   BrowserContext* context,
                   const GURL& url,
+                  std::optional<double> persona_device_scale_factor,
                   bool use_deprecated_version = false) {
   DCHECK(headers);
   DCHECK(context);
-  double device_scale_factor = GetDeviceScaleFactor();
-  double zoom_factor = GetZoomFactor(context, url);
+  const double device_pixel_ratio =
+      persona_device_scale_factor && *persona_device_scale_factor > 0.0
+          ? *persona_device_scale_factor
+          : GetDeviceScaleFactor() * GetZoomFactor(context, url);
   SetHeaderToDouble(headers,
                     use_deprecated_version ? WebClientHintsType::kDpr_DEPRECATED
                                            : WebClientHintsType::kDpr,
-                    device_scale_factor * zoom_factor);
+                    device_pixel_ratio);
+}
+
+std::optional<blink::UserAgentMetadata> GetPersonaUserAgentMetadata(
+    const blink::HeliumPersonaSnapshot* snapshot) {
+  if (!snapshot || !snapshot->IsEnabled() ||
+      !snapshot->client_hints_enabled) {
+    return std::nullopt;
+  }
+
+  blink::UserAgentMetadata metadata =
+      GetContentClient()->browser()->GetUserAgentMetadata();
+  if (!snapshot->ua_ch_platform.empty())
+    metadata.platform = snapshot->ua_ch_platform;
+  if (!snapshot->ua_ch_platform_version.empty())
+    metadata.platform_version = snapshot->ua_ch_platform_version;
+  if (!snapshot->ua_ch_architecture.empty())
+    metadata.architecture = snapshot->ua_ch_architecture;
+  if (!snapshot->ua_ch_model.empty())
+    metadata.model = snapshot->ua_ch_model;
+  if (!snapshot->ua_ch_bitness.empty())
+    metadata.bitness = snapshot->ua_ch_bitness;
+  if (!snapshot->ua_ch_full_version.empty())
+    metadata.full_version = snapshot->ua_ch_full_version;
+
+  metadata.mobile = snapshot->ua_ch_mobile;
+  metadata.wow64 = snapshot->ua_ch_wow64;
+  if (!snapshot->ua_ch_form_factors.empty())
+    metadata.form_factors = snapshot->ua_ch_form_factors;
+
+  if (!snapshot->ua_ch_brands.empty()) {
+    metadata.brand_version_list.clear();
+    metadata.brand_full_version_list.clear();
+    for (const auto& brand : snapshot->ua_ch_brands) {
+      metadata.brand_version_list.emplace_back(brand.brand, brand.version);
+      metadata.brand_full_version_list.emplace_back(
+          brand.brand, blink::ResolveHeliumPersonaBrandFullVersion(
+                           brand.version, metadata.full_version));
+    }
+  }
+  return metadata;
 }
 
 void AddSaveDataHeader(net::HttpRequestHeaders* headers,
@@ -679,10 +730,14 @@ void UpdateNavigationRequestClientUaHead
     net::HttpRequestHeaders* headers,
     const network::ParsedPermissionsPolicy& container_policy,
     const ClientHintsExtendedData& data,
-    FrameTreeNode* ftn_for_devtools_override) {
+    FrameTreeNode* ftn_for_devtools_override,
+    std::optional<blink::UserAgentMetadata> persona_ua_metadata) {
   std::optional<blink::UserAgentMetadata> ua_metadata;
+  const bool has_persona_ua_metadata = persona_ua_metadata.has_value();
   bool disable_due_to_custom_ua = false;
-  if (is_ua_override_on) {
+  if (has_persona_ua_metadata) {
+    ua_metadata = std::move(persona_ua_metadata);
+  } else if (is_ua_override_on) {
     NavigatorDelegate* nav_delegate =
         ftn_for_web_contents_override
             ? ftn_for_web_contents_override->navigator().GetDelegate()
@@ -698,7 +753,7 @@ void UpdateNavigationRequestClientUaHead
     disable_due_to_custom_ua = !ua_metadata.has_value();
   }
 
-  if (ftn_for_devtools_override &&
+  if (!has_persona_ua_metadata && ftn_for_devtools_override &&
       devtools_instrumentation::ApplyUserAgentMetadataOverrides(
           ftn_for_devtools_override, &ua_metadata)) {
     // Likewise, if devtools says to override client hints but provides no
@@ -821,7 +876,8 @@ void UpdateNavigationRequestClientUaHead
     bool override_ua,
     FrameTreeNode* frame_tree_node,
     net::HttpRequestHeaders* headers,
-    const std::optional<GURL>& request_url) {
+    const std::optional<GURL>& request_url,
+    const blink::HeliumPersonaSnapshot* persona_snapshot) {
   DCHECK(frame_tree_node);
   if (!ShouldAddClientHints(origin, frame_tree_node, delegate, request_url)) {
     return;
@@ -833,7 +889,8 @@ void UpdateNavigationRequestClientUaHead
   UpdateNavigationRequestClientUaHeadersImpl(
       delegate, override_ua, frame_tree_node,
       ClientUaHeaderCallType::kAfterCreated, headers, {}, data,
-      /*ftn_for_devtools_override=*/frame_tree_node);
+      /*ftn_for_devtools_override=*/frame_tree_node,
+      GetPersonaUserAgentMetadata(persona_snapshot));
 }
 namespace {
 
@@ -853,25 +910,37 @@ void AddRequestClientHintsHeaders(
     bool is_ua_override_on,
     FrameTreeNode* frame_tree_node,
     const network::ParsedPermissionsPolicy& container_policy,
-    FrameTreeNode* ftn_for_devtools_override) {
+    FrameTreeNode* ftn_for_devtools_override,
+    const blink::HeliumPersonaSnapshot* persona_snapshot) {
   ClientHintsExtendedData data(origin, frame_tree_node, delegate);
   UpdateIFramePermissionsPolicyWithDelegationSupportForClientHints(
       data, container_policy);
 
   GURL url = origin.GetURL();
+  const bool persona_enabled = persona_snapshot && persona_snapshot->IsEnabled();
+  const std::optional<double> persona_device_memory =
+      persona_enabled && persona_snapshot->device_memory > 0
+          ? std::optional<double>(persona_snapshot->device_memory)
+          : std::nullopt;
+  const std::optional<double> persona_device_scale_factor =
+      persona_enabled && persona_snapshot->device_scale_factor > 0.0
+          ? std::optional<double>(persona_snapshot->device_scale_factor)
+          : std::nullopt;
 
   // Add Headers
   if (ShouldAddClientHint(data, WebClientHintsType::kDeviceMemory_DEPRECATED)) {
-    AddDeviceMemoryHeader(headers, /*use_deprecated_version*/ true);
+    AddDeviceMemoryHeader(headers, persona_device_memory,
+                          /*use_deprecated_version=*/true);
   }
   if (ShouldAddClientHint(data, WebClientHintsType::kDeviceMemory)) {
-    AddDeviceMemoryHeader(headers);
+    AddDeviceMemoryHeader(headers, persona_device_memory);
   }
   if (ShouldAddClientHint(data, WebClientHintsType::kDpr_DEPRECATED)) {
-    AddDPRHeader(headers, context, url, /*use_deprecated_version*/ true);
+    AddDPRHeader(headers, context, url, persona_device_scale_factor,
+                 /*use_deprecated_version=*/true);
   }
   if (ShouldAddClientHint(data, WebClientHintsType::kDpr)) {
-    AddDPRHeader(headers, context, url);
+    AddDPRHeader(headers, context, url, persona_device_scale_factor);
   }
   if (ShouldAddClientHint(data,
                           WebClientHintsType::kViewportWidth_DEPRECATED)) {
@@ -900,7 +969,7 @@ void AddRequestClientHintsHeaders(
   UpdateNavigationRequestClientUaHeadersImpl(
       delegate, is_ua_override_on, frame_tree_node,
       ClientUaHeaderCallType::kDuringCreation, headers, container_policy, data,
-      ftn_for_devtools_override);
+      ftn_for_devtools_override, GetPersonaUserAgentMetadata(persona_snapshot));
 
   if (ShouldAddClientHint(data, WebClientHintsType::kPrefersColorScheme)) {
     AddPrefersColorSchemeHeader(headers, frame_tree_node);
@@ -952,9 +1021,11 @@ void AddPrefetchNavigationRequestClientH
   // FrameTreeNode is nullptr because it is possible that no navigation has been
   // committed yet. Nevertheless, `ftn_for_devtools_override` serves as a
   // workaround for DevTools-initiated UA overrides.
-  AddRequestClientHintsHeaders(origin, headers, context, delegate,
-                               is_ua_override_on, nullptr, {},
-                               ftn_for_devtools_override);
+  const blink::HeliumPersonaSnapshot persona_snapshot =
+      GetContentClient()->browser()->GetHeliumPersonaSnapshot(context);
+  AddRequestClientHintsHeaders(
+      origin, headers, context, delegate, is_ua_override_on, nullptr, {},
+      ftn_for_devtools_override, &persona_snapshot);
 }
 
 void AddNavigationRequestClientHintsHeaders(
@@ -965,7 +1036,8 @@ void AddNavigationRequestClientHintsHead
     bool is_ua_override_on,
     FrameTreeNode* frame_tree_node,
     const network::ParsedPermissionsPolicy& container_policy,
-    const std::optional<GURL>& request_url) {
+    const std::optional<GURL>& request_url,
+    const blink::HeliumPersonaSnapshot* persona_snapshot) {
   DCHECK(frame_tree_node);
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
   DCHECK(context);
@@ -977,7 +1049,8 @@ void AddNavigationRequestClientHintsHead
   // the DevTools UA override logic.
   AddRequestClientHintsHeaders(
       origin, headers, context, delegate, is_ua_override_on, frame_tree_node,
-      container_policy, /*ftn_for_devtools_override=*/frame_tree_node);
+      container_policy, /*ftn_for_devtools_override=*/frame_tree_node,
+      persona_snapshot);
 }
 
 std::optional<std::vector<WebClientHintsType>>
--- a/content/browser/client_hints/client_hints.h
+++ b/content/browser/client_hints/client_hints.h
@@ -6,6 +6,7 @@
 #define CONTENT_BROWSER_CLIENT_HINTS_CLIENT_HINTS_H_
 
 #include <memory>
+#include <optional>
 #include <string>
 
 #include "content/common/content_export.h"
@@ -14,6 +15,7 @@
 #include "services/network/public/cpp/permissions_policy/permissions_policy_declaration.h"
 #include "services/network/public/cpp/resource_request.h"
 #include "services/network/public/mojom/parsed_headers.mojom-forward.h"
+#include "third_party/blink/public/common/helium_persona/persona_snapshot.h"
 #include "url/gurl.h"
 
 namespace net {
@@ -105,7 +107,8 @@ CONTENT_EXPORT void UpdateNavigationRequ
     bool override_ua,
     FrameTreeNode* frame_tree_node,
     net::HttpRequestHeaders* headers,
-    const std::optional<GURL>& request_url = std::nullopt);
+    const std::optional<GURL>& request_url = std::nullopt,
+    const blink::HeliumPersonaSnapshot* persona_snapshot = nullptr);
 
 CONTENT_EXPORT void AddNavigationRequestClientHintsHeaders(
     const url::Origin& origin,
@@ -115,7 +118,8 @@ CONTENT_EXPORT void AddNavigationRequest
     bool is_ua_override_on,
     FrameTreeNode* frame_tree_node,
     const network::ParsedPermissionsPolicy& container_policy,
-    const std::optional<GURL>& request_url = std::nullopt);
+    const std::optional<GURL>& request_url = std::nullopt,
+    const blink::HeliumPersonaSnapshot* persona_snapshot = nullptr);
 
 // Adds client hints headers for a prefetch navigation that is not associated
 // with a frame. It must be a main frame navigation. `ftn_for_devtools_override`
--- a/content/browser/renderer_host/navigation_request.cc
+++ b/content/browser/renderer_host/navigation_request.cc
@@ -369,6 +369,14 @@ std::string ComputeUserAgentValue(const
   return GetContentClient()->browser()->GetUserAgent();
 }
 
+std::string GetPersonaAwareUserAgentValue(
+    const blink::HeliumPersonaSnapshot& persona_snapshot,
+    const std::string& user_agent_override) {
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.user_agent.empty())
+    return ComputeUserAgentValue(persona_snapshot.user_agent);
+  return ComputeUserAgentValue(user_agent_override);
+}
+
 void AddAdditionalRequestHeaders(
     net::HttpRequestHeaders* headers,
     const GURL& url,
@@ -401,7 +409,10 @@ void AddAdditionalRequestHeaders(
 
   headers->SetHeaderIfMissing(
       net::HttpRequestHeaders::kUserAgent,
-      ComputeUserAgentValue(user_agent_override));
+      GetPersonaAwareUserAgentValue(
+          GetContentClient()->browser()->GetHeliumPersonaSnapshot(
+              browser_context),
+          user_agent_override));
 
   if (!render_prefs.enable_referrers) {
     *referrer =
@@ -2088,7 +2099,7 @@ NavigationRequest::NavigationRequest(
           GetTentativeOriginAtRequestTime(), &client_hints_headers,
           browser_context, client_hints_delegate, is_overriding_user_agent(),
           frame_tree_node_, commit_params_->frame_policy.container_policy,
-          common_params_->url);
+          common_params_->url, &commit_params_->persona_snapshot);
       headers.MergeFrom(client_hints_headers);
     }
 
@@ -6095,14 +6106,15 @@ void NavigationRequest::OnRedirectChecks
         GetTentativeOriginAtRequestTime(), &client_hints_extra_headers,
         browser_context, client_hints_delegate, is_overriding_user_agent(),
         frame_tree_node_, commit_params_->frame_policy.container_policy,
-        common_params_->url);
+        common_params_->url, &commit_params_->persona_snapshot);
     modified_headers.MergeFrom(client_hints_extra_headers);
     // On a redirect, unless devtools has overridden the User-Agent header, we
     // should send the User-Agent string based on policy.
     if (!devtools_user_agent_override_) {
       modified_headers.SetHeader(
           net::HttpRequestHeaders::kUserAgent,
-          ComputeUserAgentValue(GetUserAgentOverride()));
+          GetPersonaAwareUserAgentValue(commit_params_->persona_snapshot,
+                                        GetUserAgentOverride()));
     }
   }
 
@@ -10065,10 +10077,12 @@ void NavigationRequest::SetIsOverridingU
     UpdateNavigationRequestClientUaHeaders(
         GetTentativeOriginAtRequestTime(), client_hints_delegate,
         is_overriding_user_agent(), frame_tree_node_, &headers,
-        common_params_->url);
+        common_params_->url, &commit_params_->persona_snapshot);
   }
   headers.SetHeader(net::HttpRequestHeaders::kUserAgent,
-                    ComputeUserAgentValue(GetUserAgentOverride()));
+                    GetPersonaAwareUserAgentValue(
+                        commit_params_->persona_snapshot,
+                        GetUserAgentOverride()));
   begin_params_->headers = headers.ToString();
   // |request_headers_| comes from |begin_params_|. Clear |request_headers_| now
   // so that if |request_headers_| are needed, they will be updated.
--- a/content/browser/client_hints/critical_client_hints_throttle.cc
+++ b/content/browser/client_hints/critical_client_hints_throttle.cc
@@ -12,9 +12,7 @@
 #include "content/common/features.h"
 #include "content/public/browser/browser_context.h"
 #include "content/public/browser/client_hints_controller_delegate.h"
-#include "content/public/browser/content_browser_client.h"
 #include "content/public/browser/web_contents.h"
-#include "content/public/common/content_client.h"
 #include "net/http/http_request_headers.h"
 #include "net/http/http_util.h"
 #include "services/metrics/public/cpp/ukm_builders.h"
@@ -166,7 +164,11 @@ void CriticalClientHintsThrottle::MaybeR
         frame_tree_node,
         frame_tree_node->navigation_request()
             ->commit_params()
-            .frame_policy.container_policy);
+            .frame_policy.container_policy,
+        std::nullopt,
+        &frame_tree_node->navigation_request()
+             ->commit_params()
+             .persona_snapshot);
   } else {
     // TODO(crbug.com/427866914): Set correct `ftn_for_devtools_override` to
     // apply DevTools-initiated UA overrides.
--- a/content/browser/client_hints/client_hints_unittest.cc
+++ b/content/browser/client_hints/client_hints_unittest.cc
@@ -513,6 +513,36 @@ TEST_F(ClientHintsTest, AddNavigationReq
   EXPECT_FALSE(headers.HasHeader("sec-ch-ua-bitness"));
 }
 
+TEST_F(ClientHintsTest, AddNavigationRequestUsesPersonaMemoryAndDpr) {
+  GURL main_url(kOriginUrl);
+  contents()->NavigateAndCommit(main_url);
+  url::Origin origin = url::Origin::Create(main_url);
+  FrameTreeNode* main_frame_node = contents()->GetPrimaryFrameTree().root();
+
+  blink::UserAgentMetadata ua_metadata;
+  MockClientHintsControllerDelegate delegate(ua_metadata);
+  delegate.SetAdditionalClientHints(
+      {WebClientHintsType::kDeviceMemory_DEPRECATED,
+       WebClientHintsType::kDeviceMemory, WebClientHintsType::kDpr_DEPRECATED,
+       WebClientHintsType::kDpr});
+
+  blink::HeliumPersonaSnapshot snapshot;
+  snapshot.enabled = true;
+  snapshot.device_memory = 16;
+  snapshot.device_scale_factor = 1.25;
+
+  net::HttpRequestHeaders headers;
+  AddNavigationRequestClientHintsHeaders(
+      origin, &headers, browser_context(), &delegate,
+      /*is_ua_override_on=*/false, main_frame_node,
+      /*container_policy=*/{}, std::nullopt, &snapshot);
+
+  EXPECT_EQ("16", headers.GetHeader("device-memory").value_or(""));
+  EXPECT_EQ("16", headers.GetHeader("sec-ch-device-memory").value_or(""));
+  EXPECT_EQ("1.25", headers.GetHeader("dpr").value_or(""));
+  EXPECT_EQ("1.25", headers.GetHeader("sec-ch-dpr").value_or(""));
+}
+
 TEST_F(ClientHintsTest, AddNavigationRequestClientHintsHeaders_Subframe) {
   GURL url = GURL(ClientHintsTest::kOriginUrl);
   contents()->NavigateAndCommit(url);
--- a/content/browser/loader/navigation_url_loader_impl.cc
+++ b/content/browser/loader/navigation_url_loader_impl.cc
@@ -1832,7 +1832,11 @@ void NavigationURLLoaderImpl::OnAcceptCH
       frame_tree_node,
       frame_tree_node->navigation_request()
           ->commit_params()
-          .frame_policy.container_policy);
+          .frame_policy.container_policy,
+      std::nullopt,
+      &frame_tree_node->navigation_request()
+           ->commit_params()
+           .persona_snapshot);
   client_hint_delegate->ClearAdditionalClientHints();
 
   LogAcceptCHFrameStatus(AcceptCHFrameRestart::kNavigationRestarted);
--- a/content/renderer/render_frame_impl.cc
+++ b/content/renderer/render_frame_impl.cc
@@ -368,11 +368,11 @@ blink::UserAgentMetadata ApplyHeliumPers
   if (!snapshot.ua_ch_brands.empty()) {
     metadata.brand_version_list.clear();
     metadata.brand_full_version_list.clear();
-    const std::string& full_version = snapshot.ua_ch_full_version;
     for (const auto& brand : snapshot.ua_ch_brands) {
       metadata.brand_version_list.emplace_back(brand.brand, brand.version);
       metadata.brand_full_version_list.emplace_back(
-          brand.brand, !full_version.empty() ? full_version : brand.version);
+          brand.brand, blink::ResolveHeliumPersonaBrandFullVersion(
+                           brand.version, metadata.full_version));
     }
   }
 
--- a/third_party/blink/renderer/core/loader/frame_fetch_context.cc
+++ b/third_party/blink/renderer/core/loader/frame_fetch_context.cc
@@ -659,23 +659,22 @@ void FrameFetchContext::AddClientHintsIf
   const ClientHintsPreferences& hints_preferences = GetClientHintsPreferences();
 
   using network::mojom::blink::WebClientHintsType;
+  const float device_memory =
+      document_->domWindow()->navigator()->deviceMemory();
 
   if (ShouldSendClientHint(*policy, resource_origin, is_1p_origin,
                            WebClientHintsType::kDeviceMemory_DEPRECATED,
                            hints_preferences)) {
     request.SetHttpHeaderField(
         http_names::kDeviceMemory_DEPRECATED,
-        AtomicString(String::Number(
-            ApproximatedDeviceMemory::GetApproximatedDeviceMemory())));
+        AtomicString(String::Number(device_memory)));
   }
 
   if (ShouldSendClientHint(*policy, resource_origin, is_1p_origin,
                            WebClientHintsType::kDeviceMemory,
                            hints_preferences)) {
     request.SetHttpHeaderField(
-        http_names::kDeviceMemory,
-        AtomicString(String::Number(
-            ApproximatedDeviceMemory::GetApproximatedDeviceMemory())));
+        http_names::kDeviceMemory, AtomicString(String::Number(device_memory)));
   }
 
   if (ShouldSendClientHint(*policy, resource_origin, is_1p_origin,
@@ -1362,7 +1361,7 @@ float FrameFetchContext::GetDevicePixelR
   if (GetResourceFetcherProperties().IsDetached()) {
     return frozen_state_->device_pixel_ratio;
   }
-  return document_->DevicePixelRatio();
+  return document_->domWindow()->devicePixelRatio();
 }
 
 FetchContext* FrameFetchContext::Detach() {
