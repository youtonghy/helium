diff --git a/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts b/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
index ca97767..ec04879 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
@@ -51,7 +51,7 @@ export interface HeliumPersonaBrowserProxy {
   clonePreset(presetId: string): Promise<PersonaRecord>;
   savePersonaPreset(preset: PersonaRecord): Promise<PersonaRecord>;
   deletePersonaPreset(presetId: string): Promise<boolean>;
-  savePersona(persona: PersonaRecord): Promise<boolean>;
+  savePersona(persona: PersonaRecord): Promise<PersonaRecord>;
   deletePersona(personaId: string): Promise<boolean>;
   saveFontPackProfile(
       profile: Record<string, unknown>): Promise<Record<string, unknown>>;
@@ -84,7 +84,7 @@ export class HeliumPersonaBrowserProxyImpl implements
   }
 
   savePersona(persona: PersonaRecord) {
-    return sendWithPromise<boolean>('saveHeliumPersona', persona);
+    return sendWithPromise<PersonaRecord>('saveHeliumPersona', persona);
   }
 
   deletePersona(personaId: string) {
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 98cb362..715ab16 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -2865,11 +2865,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     persona.passthrough = false;
     this.normalizeNumbers_(persona);
     this.clearStatus_();
-    if (!await this.browserProxy_.savePersona(persona)) {
+    const saved = await this.browserProxy_.savePersona(persona);
+    if (!saved || !saved.id) {
       this.showStatus_('Could not create persona.');
       return;
     }
-    await this.loadState_();
+    await this.reloadStateKeepingEditor_(saved);
   }
 
   private onApplyFontPackClick_(event: Event) {
@@ -3857,11 +3858,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       persona.name = 'Custom persona';
     }
     this.clearStatus_();
-    if (!await this.browserProxy_.savePersona(persona)) {
+    const saved = await this.browserProxy_.savePersona(persona);
+    if (!saved || !saved.id) {
       this.showStatus_('Could not save persona.');
       return;
     }
-    await this.loadState_();
+    await this.reloadStateKeepingEditor_(saved);
   }
 }
 
diff --git a/chrome/browser/ui/webui/settings/persona_handler.cc b/chrome/browser/ui/webui/settings/persona_handler.cc
index 65b9b81..0c69b5c 100644
--- a/chrome/browser/ui/webui/settings/persona_handler.cc
+++ b/chrome/browser/ui/webui/settings/persona_handler.cc
@@ -158,18 +158,20 @@ void HeliumPersonaHandler::HandleSavePersona(const base::ListValue& args) {
   const base::Value::Dict* persona = args[1].GetIfDict();
   if (!persona) {
     AllowJavascript();
-    ResolveJavascriptCallback(base::Value(callback_id), base::Value(false));
+    ResolveJavascriptCallback(base::Value(callback_id),
+                              base::Value(base::Value::Dict()));
     return;
   }
 
-  const bool success =
+  base::Value::Dict saved =
       helium_persona::SavePersona(*pref_service_, persona->Clone());
-  if (success) {
+  if (!saved.empty()) {
     BroadcastActivePersonaSnapshot();
   }
 
   AllowJavascript();
-  ResolveJavascriptCallback(base::Value(callback_id), base::Value(success));
+  ResolveJavascriptCallback(base::Value(callback_id),
+                            base::Value(std::move(saved)));
 }
 
 void HeliumPersonaHandler::HandleDeletePersona(const base::ListValue& args) {
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index dac9548..74c5571 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -1774,7 +1774,8 @@ bool SetActivePersona(PrefService& pref_service, std::string_view persona_id) {
   return true;
 }
 
-bool SavePersona(PrefService& pref_service, base::Value::Dict persona) {
+base::Value::Dict SavePersona(PrefService& pref_service,
+                              base::Value::Dict persona) {
   EnsurePersonaCompatibilityFields(persona);
   StripEmbeddedProfileMetadata(persona);
   std::string id = GetPersonaId(persona);
@@ -1791,7 +1792,7 @@ bool SavePersona(PrefService& pref_service, base::Value::Dict persona) {
   }
 
   if (!PersonaHasRequiredFields(persona) || IsPassthroughPersona(persona)) {
-    return false;
+    return base::Value::Dict();
   }
 
   if (!FindPersonaPreset(pref_service, id).empty()) {
@@ -1819,6 +1820,7 @@ bool SavePersona(PrefService& pref_service, base::Value::Dict persona) {
   persona.Set("updated", base::Time::Now().InMillisecondsFSinceUnixEpoch());
   persona.Set("warnings", ValidatePersona(persona));
 
+  base::Value::Dict saved_persona = persona.Clone();
   base::Value::List personas = CloneUserPersonas(pref_service);
   if (base::Value::Dict* existing = FindPersonaInList(personas, id)) {
     *existing = std::move(persona);
@@ -1831,7 +1833,7 @@ bool SavePersona(PrefService& pref_service, base::Value::Dict persona) {
   pref_service.SetInteger(
       prefs::kHeliumPersonaRevision,
       pref_service.GetInteger(prefs::kHeliumPersonaRevision) + 1);
-  return true;
+  return saved_persona;
 }
 
 base::Value::Dict SaveFontPackProfile(PrefService& pref_service,
diff --git a/components/helium_persona/persona_prefs.h b/components/helium_persona/persona_prefs.h
index 4b75208..4800228 100644
--- a/components/helium_persona/persona_prefs.h
+++ b/components/helium_persona/persona_prefs.h
@@ -42,7 +42,8 @@ COMPONENT_EXPORT(HELIUM_PERSONA)
 bool SetActivePersona(PrefService& pref_service, std::string_view persona_id);
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
-bool SavePersona(PrefService& pref_service, base::Value::Dict persona);
+base::Value::Dict SavePersona(PrefService& pref_service,
+                              base::Value::Dict persona);
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
 bool DeletePersona(PrefService& pref_service, std::string_view persona_id);
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index c75543e..c468abe 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -608,7 +608,7 @@ TEST(HeliumPersonaPrefsTest, SavingCustomPresetAsPersonaCreatesInstanceId) {
   const std::string* preset_id = saved_preset.FindString("id");
   ASSERT_TRUE(preset_id);
 
-  EXPECT_TRUE(SavePersona(prefs, saved_preset.Clone()));
+  EXPECT_FALSE(SavePersona(prefs, saved_preset.Clone()).empty());
   base::Value::Dict active = GetActivePersona(prefs);
   const std::string* persona_id = active.FindString("id");
   ASSERT_TRUE(persona_id);
@@ -641,7 +641,7 @@ TEST(HeliumPersonaPrefsTest, PassthroughPresetCannotBecomeCustomPersona) {
 
   EXPECT_TRUE(ClonePresetIntoProfile(prefs, kDefaultPresetId).empty());
   EXPECT_TRUE(prefs.GetList(prefs::kHeliumPersonaRecords).empty());
-  EXPECT_FALSE(SavePersona(prefs, MakeCurrentSystemPreset()));
+  EXPECT_TRUE(SavePersona(prefs, MakeCurrentSystemPreset()).empty());
 }
 
 TEST(HeliumPersonaPrefsTest, SaveNewCustomPersonaReplacesEmptyNoiseSeed) {
@@ -655,7 +655,18 @@ TEST(HeliumPersonaPrefsTest, SaveNewCustomPersonaReplacesEmptyNoiseSeed) {
   persona.Set("builtin", false);
   persona.Set("modified", true);
 
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  base::Value::Dict saved = SavePersona(prefs, std::move(persona));
+  ASSERT_FALSE(saved.empty());
+  const std::string* saved_id = saved.FindString("id");
+  const std::string* saved_noise_seed = saved.FindString("noiseSeed");
+  ASSERT_TRUE(saved_id);
+  ASSERT_TRUE(saved_noise_seed);
+  EXPECT_EQ(0u, saved_id->rfind("persona-", 0));
+  EXPECT_EQ(0u, saved_noise_seed->rfind("seed-", 0));
+  EXPECT_NE(*saved_id, *saved_noise_seed);
+  EXPECT_EQ("custom", *saved.FindString("source"));
+  EXPECT_FALSE(saved.FindBool("builtin").value_or(true));
+  EXPECT_TRUE(saved.FindBool("modified").value_or(false));
   base::Value::Dict active = GetActivePersona(prefs);
   const std::string* id = active.FindString("id");
   const std::string* noise_seed = active.FindString("noiseSeed");
@@ -717,7 +728,7 @@ TEST(HeliumPersonaPrefsTest, SavePersonaMigratesLegacyManualShape) {
   advanced->Remove("allowHandwritingRecognition");
   advanced->Remove("speechVoiceUri");
 
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(persona)).empty());
   base::Value::Dict active = GetActivePersona(prefs);
   EXPECT_TRUE(PersonaHasRequiredFields(active));
   ASSERT_TRUE(active.FindDict("uaCh"));
@@ -762,7 +773,7 @@ TEST(HeliumPersonaPrefsTest, SavePersonaRepairsPartialGpuProfiles) {
   webgpu_profile->Remove("maxBufferSize");
   webgpu_profile->Remove("maxComputeWorkgroupsPerDimension");
 
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(persona)).empty());
   base::Value::Dict active = GetActivePersona(prefs);
   ASSERT_TRUE(active.FindDict("gpu"));
   webgl_profile = active.FindDict("gpu")->FindDict("webglProfile");
@@ -825,7 +836,7 @@ TEST(HeliumPersonaPrefsTest, SaveBuiltinIdCreatesCustomPersona) {
   base::Value::Dict persona = DictAt(presets, 1).Clone();
   const std::string original_id = *persona.FindString("id");
 
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(persona)).empty());
   base::Value::Dict active = GetActivePersona(prefs);
   ASSERT_TRUE(active.FindString("id"));
   EXPECT_NE(original_id, *active.FindString("id"));
@@ -844,7 +855,7 @@ TEST(HeliumPersonaPrefsTest, SaveBuiltinIdPreservesManualNoiseSeed) {
   const std::string original_id = *persona.FindString("id");
   persona.Set("noiseSeed", "manual-seed");
 
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(persona)).empty());
   base::Value::Dict active = GetActivePersona(prefs);
   EXPECT_NE(original_id, *active.FindString("id"));
   EXPECT_EQ("manual-seed", *active.FindString("noiseSeed"));
@@ -949,7 +960,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontRenderingPropagatesLinkedPersonas) {
   ASSERT_FALSE(linked.empty());
   const std::string linked_id = *linked.FindString("id");
   linked.Set("fontRendering", saved_rendering->Clone());
-  EXPECT_TRUE(SavePersona(prefs, std::move(linked)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(linked)).empty());
 
   base::Value::Dict detached = ClonePresetIntoProfile(
       prefs, "preset-windows-11-en-us-new-york");
@@ -958,7 +969,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontRenderingPropagatesLinkedPersonas) {
   base::Value::Dict detached_rendering = saved_rendering->Clone();
   detached_rendering.Set("textGamma", 1.9);
   detached.Set("fontRendering", std::move(detached_rendering));
-  EXPECT_TRUE(SavePersona(prefs, std::move(detached)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(detached)).empty());
 
   rendering.Set("displayName", "Follow rendering updated");
   rendering.Set("hinting", "full");
@@ -1189,7 +1200,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontPackProfilePropagatesLinkedPersonas) {
   ASSERT_FALSE(linked.empty());
   const std::string linked_id = *linked.FindString("id");
   linked.Set("fonts", saved_pack->Clone());
-  EXPECT_TRUE(SavePersona(prefs, std::move(linked)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(linked)).empty());
 
   base::Value::Dict linked_preset = ClonePresetIntoProfile(
       prefs, "preset-windows-11-en-us-new-york");
@@ -1210,7 +1221,7 @@ TEST(HeliumPersonaPrefsTest, UpdatingFontPackProfilePropagatesLinkedPersonas) {
   detached_families.Append("Manual Sans");
   detached_fonts.Set("families", std::move(detached_families));
   detached.Set("fonts", std::move(detached_fonts));
-  EXPECT_TRUE(SavePersona(prefs, std::move(detached)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(detached)).empty());
 
   pack.Set("displayName", "Follow fonts updated");
   base::Value::List updated_families;
@@ -1561,7 +1572,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "Network RTT and downlink"));
   EXPECT_TRUE(WarningsContain(warnings, "Network type none"));
   EXPECT_TRUE(WarningsContain(warnings, "effectiveType 4g"));
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(persona)).empty());
 }
 
 TEST(HeliumPersonaPrefsTest,
@@ -1583,7 +1594,7 @@ TEST(HeliumPersonaPrefsTest,
 
   base::Value::List retina_warnings = ValidatePersona(retina_persona);
   EXPECT_TRUE(WarningsContain(retina_warnings, "Retina macOS personas"));
-  EXPECT_TRUE(SavePersona(prefs, std::move(retina_persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(retina_persona)).empty());
 
   base::Value::Dict non_retina_persona = ClonePresetIntoProfile(
       prefs, "preset-macos-en-au-melbourne");
@@ -1599,7 +1610,7 @@ TEST(HeliumPersonaPrefsTest,
   base::Value::List non_retina_warnings = ValidatePersona(non_retina_persona);
   EXPECT_TRUE(WarningsContain(non_retina_warnings,
                               "Non-Retina macOS personas"));
-  EXPECT_TRUE(SavePersona(prefs, std::move(non_retina_persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(non_retina_persona)).empty());
 }
 
 TEST(HeliumPersonaPrefsTest, ValidationRequiresManualTouchFields) {
@@ -1839,7 +1850,7 @@ TEST(HeliumPersonaPrefsTest, SavePersonaSanitizesStringLists) {
   aliases.Append(false);
   fonts->Set("aliases", std::move(aliases));
 
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(persona)).empty());
   base::Value::Dict active = GetActivePersona(prefs);
 
   const base::Value::List* saved_brands =
@@ -1902,7 +1913,7 @@ TEST(HeliumPersonaPrefsTest, SavePersonaStripsEmbeddedProfileListMetadata) {
   rendering->Set("modified", true);
   rendering->Set("updated", 34.0);
 
-  EXPECT_TRUE(SavePersona(prefs, persona.Clone()));
+  EXPECT_FALSE(SavePersona(prefs, persona.Clone()).empty());
   base::Value::Dict active = GetActivePersona(prefs);
   const base::Value::Dict* saved_fonts = active.FindDict("fonts");
   ASSERT_TRUE(saved_fonts);
@@ -2173,7 +2184,7 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   advanced->Set("allowSpeechSynthesis", true);
   advanced->Set("allowHandwritingRecognition", true);
 
-  EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
+  EXPECT_FALSE(SavePersona(prefs, std::move(persona)).empty());
 
   base::Value::Dict runtime = MakeRuntimeSnapshot(prefs);
   EXPECT_EQ("manual-runtime-seed", *runtime.FindString("noiseSeed"));
