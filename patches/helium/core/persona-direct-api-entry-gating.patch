diff --git a/third_party/blink/renderer/core/dom/element.cc b/third_party/blink/renderer/core/dom/element.cc
index b7af670..b119fc2 100644
--- a/third_party/blink/renderer/core/dom/element.cc
+++ b/third_party/blink/renderer/core/dom/element.cc
@@ -178,6 +178,7 @@
 #include "third_party/blink/renderer/core/frame/settings.h"
 #include "third_party/blink/renderer/core/frame/visual_viewport.h"
 #include "third_party/blink/renderer/core/fullscreen/fullscreen.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/geometry/dom_rect.h"
 #include "third_party/blink/renderer/core/geometry/dom_rect_list.h"
 #include "third_party/blink/renderer/core/html/canvas/html_canvas_element.h"
@@ -11452,6 +11453,13 @@ ScriptPromise<IDLUndefined> Element::requestPointerLock(
                           "is no frame or that frame has no page."));
   }
 
+  if (PersonaSpoofingHelper::ShouldBlockPointerLock(&GetDocument())) {
+    return ScriptPromise<IDLUndefined>::RejectWithDOMException(
+        script_state, MakeGarbageCollected<DOMException>(
+                          DOMExceptionCode::kNotAllowedError,
+                          "PointerLock is not allowed."));
+  }
+
   auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLUndefined>>(
       script_state, exception_state.GetContext());
   GetDocument().GetPage()->GetPointerLockController().RequestPointerLock(
diff --git a/third_party/blink/renderer/core/frame/local_dom_window.cc b/third_party/blink/renderer/core/frame/local_dom_window.cc
index 4a74348..b94b1bc 100644
--- a/third_party/blink/renderer/core/frame/local_dom_window.cc
+++ b/third_party/blink/renderer/core/frame/local_dom_window.cc
@@ -1413,6 +1413,10 @@ Element* LocalDOMWindow::frameElement() const {
 }
 
 void LocalDOMWindow::print(ScriptState* script_state) {
+  if (PersonaSpoofingHelper::ShouldBlockWebPrinting(this)) {
+    return;
+  }
+
   // Don't try to print if there's no frame attached anymore.
   if (!GetFrame())
     return;
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
index c2afe77..3aa4974 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
@@ -305,6 +305,18 @@ bool PersonaSpoofingHelper::ShouldBlockManagedDevice(
   return snapshot && !snapshot->allow_managed_device;
 }
 
+bool PersonaSpoofingHelper::ShouldBlockPointerLock(
+    const ExecutionContext* context) {
+  return ShouldBlockIfNotAllowed(context,
+                                 &HeliumPersonaSnapshot::allow_pointer_lock);
+}
+
+bool PersonaSpoofingHelper::ShouldBlockWebPrinting(
+    const ExecutionContext* context) {
+  return ShouldBlockIfNotAllowed(context,
+                                 &HeliumPersonaSnapshot::allow_web_printing);
+}
+
 bool PersonaSpoofingHelper::ShouldBlockPermission(
     const ExecutionContext* context,
     mojom::blink::PermissionName permission_name) {
@@ -375,8 +387,7 @@ bool PersonaSpoofingHelper::ShouldBlockPermission(
       return ShouldBlockIfNotAllowed(
           context, &HeliumPersonaSnapshot::allow_storage_access);
     case mojom::blink::PermissionName::POINTER_LOCK:
-      return ShouldBlockIfNotAllowed(
-          context, &HeliumPersonaSnapshot::allow_pointer_lock);
+      return ShouldBlockPointerLock(context);
     case mojom::blink::PermissionName::FULLSCREEN:
       return ShouldBlockIfNotAllowed(
           context, &HeliumPersonaSnapshot::allow_fullscreen);
@@ -384,8 +395,7 @@ bool PersonaSpoofingHelper::ShouldBlockPermission(
       return ShouldBlockIfNotAllowed(
           context, &HeliumPersonaSnapshot::allow_web_app_installation);
     case mojom::blink::PermissionName::WEB_PRINTING:
-      return ShouldBlockIfNotAllowed(
-          context, &HeliumPersonaSnapshot::allow_web_printing);
+      return ShouldBlockWebPrinting(context);
     default:
       return false;
   }
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
index 0f53f7c..a702cd8 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
@@ -53,6 +53,8 @@ class CORE_EXPORT PersonaSpoofingHelper {
   static bool ShouldBlockPersistentDeviceId(const ExecutionContext* context);
   static bool ShouldBlockInstalledApps(const ExecutionContext* context);
   static bool ShouldBlockManagedDevice(const ExecutionContext* context);
+  static bool ShouldBlockPointerLock(const ExecutionContext* context);
+  static bool ShouldBlockWebPrinting(const ExecutionContext* context);
   static bool ShouldBlockPermission(
       const ExecutionContext* context,
       mojom::blink::PermissionName permission_name);
