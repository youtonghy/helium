diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index acb393c6..be3d9aed 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -903,6 +903,8 @@ blink::HeliumPersonaSnapshot ToBlinkPersonaSnapshot(
   target.webgpu_features = source.webgpu_features;
   target.screen_width = source.screen_width;
   target.screen_height = source.screen_height;
+  target.avail_left = source.avail_left;
+  target.avail_top = source.avail_top;
   target.avail_width = source.avail_width;
   target.avail_height = source.avail_height;
   target.device_scale_factor = source.device_scale_factor;
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.html b/chrome/browser/resources/settings/privacy_page/persona_page.html
index e18d0eaa..ab050e51 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html
@@ -398,6 +398,10 @@
         value="{{editablePersona_.screen.width}}" type="number"></cr-input>
     <cr-input label="Height"
         value="{{editablePersona_.screen.height}}" type="number"></cr-input>
+    <cr-input label="Available left"
+        value="{{editablePersona_.screen.availLeft}}" type="number"></cr-input>
+    <cr-input label="Available top"
+        value="{{editablePersona_.screen.availTop}}" type="number"></cr-input>
     <cr-input label="Available width"
         value="{{editablePersona_.screen.availWidth}}" type="number"></cr-input>
     <cr-input label="Available height"
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index a1fc2d80..21908d4a 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -566,6 +566,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     ];
     persona.hardware = persona.hardware || {};
     persona.screen = persona.screen || {};
+    if (persona.screen['availLeft'] === undefined) {
+      persona.screen['availLeft'] = 0;
+    }
+    if (persona.screen['availTop'] === undefined) {
+      persona.screen['availTop'] = 0;
+    }
     persona.fonts = persona.fonts || {};
     persona.fontRendering = persona.fontRendering || {
       id: 'generic-grayscale',
@@ -699,6 +705,8 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     persona.navigatorVendorSub = String(persona.navigatorVendorSub || '');
     persona.screen['width'] = toNumber(persona.screen['width']);
     persona.screen['height'] = toNumber(persona.screen['height']);
+    persona.screen['availLeft'] = toNumber(persona.screen['availLeft']);
+    persona.screen['availTop'] = toNumber(persona.screen['availTop']);
     persona.screen['availWidth'] = toNumber(persona.screen['availWidth']);
     persona.screen['availHeight'] = toNumber(persona.screen['availHeight']);
     persona.screen['deviceScaleFactor'] =
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index eeb7325c..9752a27e 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -523,6 +523,8 @@ TEST(HeliumPersonaPrefsTest, SnapshotRoundTripsCompleteFields) {
   EXPECT_EQ(8192, snapshot.webgpu_max_texture_dimension_2d);
   EXPECT_EQ(65535, snapshot.webgpu_max_compute_workgroups_per_dimension);
   EXPECT_EQ(268435456, snapshot.webgpu_max_buffer_size);
+  EXPECT_EQ(0, snapshot.avail_left);
+  EXPECT_EQ(0, snapshot.avail_top);
   ASSERT_EQ(1u, snapshot.webgpu_features.size());
   EXPECT_EQ("texture-compression-bc", snapshot.webgpu_features[0]);
 
@@ -663,6 +665,8 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   ASSERT_TRUE(screen);
   screen->Set("width", 3000);
   screen->Set("height", 2000);
+  screen->Set("availLeft", 12);
+  screen->Set("availTop", 28);
   screen->Set("availWidth", 3000);
   screen->Set("availHeight", 1960);
   screen->Set("deviceScaleFactor", 1.5);
@@ -775,6 +779,8 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   ASSERT_TRUE(runtime.FindDict("gpu")->FindList("webgpuFeatures"));
   EXPECT_EQ(2u, runtime.FindDict("gpu")->FindList("webgpuFeatures")->size());
   EXPECT_EQ(3000, runtime.FindDict("screen")->FindInt("width").value_or(0));
+  EXPECT_EQ(12, runtime.FindDict("screen")->FindInt("availLeft").value_or(-1));
+  EXPECT_EQ(28, runtime.FindDict("screen")->FindInt("availTop").value_or(-1));
   EXPECT_EQ(6, runtime.FindDict("hardware")
                    ->FindInt("hardwareConcurrency")
                    .value_or(0));
diff --git a/components/helium_persona/persona_presets.cc b/components/helium_persona/persona_presets.cc
index 2bf68912..10b2f722 100644
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -181,6 +181,8 @@ base::Value::Dict MakeScreen(int width,
   base::Value::Dict screen;
   screen.Set("width", width);
   screen.Set("height", height);
+  screen.Set("availLeft", 0);
+  screen.Set("availTop", 0);
   screen.Set("availWidth", avail_width);
   screen.Set("availHeight", avail_height);
   screen.Set("deviceScaleFactor", dpr);
diff --git a/components/helium_persona/persona_snapshot.cc b/components/helium_persona/persona_snapshot.cc
index 8ae1afbd..c2e29d47 100644
--- a/components/helium_persona/persona_snapshot.cc
+++ b/components/helium_persona/persona_snapshot.cc
@@ -291,6 +291,8 @@ PersonaSnapshot MakePersonaSnapshot(const base::Value::Dict& persona) {
 
   snapshot.screen_width = GetNestedInt(persona, "screen", "width");
   snapshot.screen_height = GetNestedInt(persona, "screen", "height");
+  snapshot.avail_left = GetNestedInt(persona, "screen", "availLeft");
+  snapshot.avail_top = GetNestedInt(persona, "screen", "availTop");
   snapshot.avail_width = GetNestedInt(persona, "screen", "availWidth");
   snapshot.avail_height = GetNestedInt(persona, "screen", "availHeight");
   snapshot.device_scale_factor =
@@ -564,6 +566,8 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot) {
   base::Value::Dict screen;
   screen.Set("width", snapshot.screen_width);
   screen.Set("height", snapshot.screen_height);
+  screen.Set("availLeft", snapshot.avail_left);
+  screen.Set("availTop", snapshot.avail_top);
   screen.Set("availWidth", snapshot.avail_width);
   screen.Set("availHeight", snapshot.avail_height);
   screen.Set("deviceScaleFactor", snapshot.device_scale_factor);
diff --git a/components/helium_persona/persona_snapshot.h b/components/helium_persona/persona_snapshot.h
index f1b1b0f6..2ee5af73 100644
--- a/components/helium_persona/persona_snapshot.h
+++ b/components/helium_persona/persona_snapshot.h
@@ -103,6 +103,8 @@ struct COMPONENT_EXPORT(HELIUM_PERSONA) PersonaSnapshot {
 
   int screen_width = 0;
   int screen_height = 0;
+  int avail_left = 0;
+  int avail_top = 0;
   int avail_width = 0;
   int avail_height = 0;
   double device_scale_factor = 0;
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index e76d4a09..0218d033 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -325,6 +325,8 @@ bool PersonaHasRequiredFields(const base::Value::Dict& persona) {
          HasNestedStringList(persona, "gpu", "webgpuFeatures") &&
          HasNestedPositiveInt(persona, "screen", "width") &&
          HasNestedPositiveInt(persona, "screen", "height") &&
+         HasNestedInt(persona, "screen", "availLeft") &&
+         HasNestedInt(persona, "screen", "availTop") &&
          HasNestedPositiveInt(persona, "screen", "availWidth") &&
          HasNestedPositiveInt(persona, "screen", "availHeight") &&
          HasNestedPositiveDouble(persona, "screen", "deviceScaleFactor") &&
diff --git a/content/browser/helium_noise/noise_token_data.cc b/content/browser/helium_noise/noise_token_data.cc
index 485001f8..7fc61cb5 100644
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -182,6 +182,8 @@ std::string PersonaNoiseSalt(
 
   AppendSaltPart(salt, persona_snapshot.screen_width);
   AppendSaltPart(salt, persona_snapshot.screen_height);
+  AppendSaltPart(salt, persona_snapshot.avail_left);
+  AppendSaltPart(salt, persona_snapshot.avail_top);
   AppendSaltPart(salt, persona_snapshot.avail_width);
   AppendSaltPart(salt, persona_snapshot.avail_height);
   AppendSaltPart(salt, persona_snapshot.device_scale_factor);
diff --git a/content/browser/helium_noise/noise_token_data_unittest.cc b/content/browser/helium_noise/noise_token_data_unittest.cc
index 3b37809d..a70b4187 100644
--- a/content/browser/helium_noise/noise_token_data_unittest.cc
+++ b/content/browser/helium_noise/noise_token_data_unittest.cc
@@ -61,6 +61,8 @@ blink::HeliumPersonaSnapshot MakeSeededPersona(std::string noise_seed) {
 
   snapshot.screen_width = 1920;
   snapshot.screen_height = 1080;
+  snapshot.avail_left = 0;
+  snapshot.avail_top = 0;
   snapshot.avail_width = 1920;
   snapshot.avail_height = 1040;
   snapshot.device_scale_factor = 1.0;
@@ -145,6 +147,8 @@ TEST_F(HeliumNoiseTokenDataTest,
   different_network.network_downlink = 3.5;
   different_network.network_downlink_max = 12.0;
   different_network.network_save_data = true;
+  different_network.avail_left = 12;
+  different_network.avail_top = 28;
   blink::HeliumPersonaSnapshot different_seed =
       MakeSeededPersona("manual-seed-2");
 
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot.h b/third_party/blink/public/common/helium_persona/persona_snapshot.h
index f13a5a8c..707b7db5 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -103,6 +103,8 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
 
   int screen_width = 0;
   int screen_height = 0;
+  int avail_left = 0;
+  int avail_top = 0;
   int avail_width = 0;
   int avail_height = 0;
   double device_scale_factor = 0;
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
index efb7b919..f6d7ba33 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
@@ -337,6 +337,12 @@ struct BLINK_COMMON_EXPORT
   static int screen_height(const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.screen_height;
   }
+  static int avail_left(const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.avail_left;
+  }
+  static int avail_top(const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.avail_top;
+  }
   static int avail_width(const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.avail_width;
   }
@@ -634,6 +640,8 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
   out->ua_ch_wow64 = data.ua_ch_wow64();
   out->screen_width = data.screen_width();
   out->screen_height = data.screen_height();
+  out->avail_left = data.avail_left();
+  out->avail_top = data.avail_top();
   out->avail_width = data.avail_width();
   out->avail_height = data.avail_height();
   out->device_scale_factor = data.device_scale_factor();
diff --git a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
index 1b016cf3..a51e5e74 100644
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
@@ -93,6 +93,8 @@ struct HeliumPersonaSnapshot {
 
   int32 screen_width;
   int32 screen_height;
+  int32 avail_left;
+  int32 avail_top;
   int32 avail_width;
   int32 avail_height;
   double device_scale_factor;
diff --git a/third_party/blink/renderer/core/frame/screen.cc b/third_party/blink/renderer/core/frame/screen.cc
index cf7af56f..654a3ea3 100644
--- a/third_party/blink/renderer/core/frame/screen.cc
+++ b/third_party/blink/renderer/core/frame/screen.cc
@@ -127,12 +127,18 @@ unsigned Screen::pixelDepth() const {
 int Screen::availLeft() const {
   if (!DomWindow())
     return 0;
+  if (auto value =
+          PersonaSpoofingHelper::ScreenAvailLeft(GetExecutionContext()))
+    return *value;
   return GetRect(/*available=*/true).x();
 }
 
 int Screen::availTop() const {
   if (!DomWindow())
     return 0;
+  if (auto value =
+          PersonaSpoofingHelper::ScreenAvailTop(GetExecutionContext()))
+    return *value;
   return GetRect(/*available=*/true).y();
 }
 
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
index 59cad200..51da82ee 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
@@ -442,6 +442,18 @@ std::optional<int> PersonaSpoofingHelper::ScreenHeight(
   return snapshot ? PositiveInt(snapshot->screen_height) : std::nullopt;
 }
 
+std::optional<int> PersonaSpoofingHelper::ScreenAvailLeft(
+    const ExecutionContext* context) {
+  const HeliumPersonaSnapshot* snapshot = Get(context);
+  return snapshot ? snapshot->avail_left : std::nullopt;
+}
+
+std::optional<int> PersonaSpoofingHelper::ScreenAvailTop(
+    const ExecutionContext* context) {
+  const HeliumPersonaSnapshot* snapshot = Get(context);
+  return snapshot ? snapshot->avail_top : std::nullopt;
+}
+
 std::optional<int> PersonaSpoofingHelper::ScreenAvailWidth(
     const ExecutionContext* context) {
   const HeliumPersonaSnapshot* snapshot = Get(context);
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
index 5bc373d7..9bdf2412 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
@@ -66,6 +66,8 @@ class CORE_EXPORT PersonaSpoofingHelper {
 
   static std::optional<int> ScreenWidth(const ExecutionContext* context);
   static std::optional<int> ScreenHeight(const ExecutionContext* context);
+  static std::optional<int> ScreenAvailLeft(const ExecutionContext* context);
+  static std::optional<int> ScreenAvailTop(const ExecutionContext* context);
   static std::optional<int> ScreenAvailWidth(const ExecutionContext* context);
   static std::optional<int> ScreenAvailHeight(const ExecutionContext* context);
   static std::optional<unsigned> ScreenColorDepth(
diff --git a/third_party/blink/renderer/modules/screen_details/screen_details.cc b/third_party/blink/renderer/modules/screen_details/screen_details.cc
index de73eccf..7730107e 100644
--- a/third_party/blink/renderer/modules/screen_details/screen_details.cc
+++ b/third_party/blink/renderer/modules/screen_details/screen_details.cc
@@ -33,6 +33,10 @@ display::ScreenInfos PersonaScreenInfosFor(
       std::max(1, info.rect.width()));
   const int height = PersonaSpoofingHelper::ScreenHeight(window).value_or(
       std::max(1, info.rect.height()));
+  const int avail_left =
+      PersonaSpoofingHelper::ScreenAvailLeft(window).value_or(0);
+  const int avail_top =
+      PersonaSpoofingHelper::ScreenAvailTop(window).value_or(0);
   const int avail_width =
       PersonaSpoofingHelper::ScreenAvailWidth(window).value_or(width);
   const int avail_height =
@@ -46,7 +50,8 @@ display::ScreenInfos PersonaScreenInfosFor(
 
   info.display_id = display::kDefaultDisplayId;
   info.rect = gfx::Rect(0, 0, width, height);
-  info.available_rect = gfx::Rect(0, 0, avail_width, avail_height);
+  info.available_rect =
+      gfx::Rect(avail_left, avail_top, avail_width, avail_height);
   info.device_scale_factor = static_cast<float>(device_scale_factor);
   info.depth = static_cast<int>(color_depth);
   info.depth_per_component = 8;
