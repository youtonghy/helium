--- a/third_party/blink/renderer/modules/webusb/usb_device.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/webusb/usb_device.cc	2026-06-15 07:06:31
@@ -220,7 +220,7 @@
       script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
 
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     resolver->RejectWithSecurityError(kAccessDeniedError, kAccessDeniedError);
     return promise;
   }
@@ -234,7 +234,7 @@
 
 ScriptPromise<IDLUndefined> USBDevice::close(ScriptState* script_state,
                                              ExceptionState& exception_state) {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     OnDeviceOpenedOrClosed(false);
     device_.reset();
     return ToResolvedUndefinedPromise(script_state);
@@ -748,7 +748,7 @@
     return;
   }
 
-  if (opened_ && PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (opened_ && PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     exception_state.ThrowSecurityError(kAccessDeniedError, kAccessDeniedError);
     return;
   }
--- a/third_party/blink/renderer/modules/serial/serial_port.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/serial/serial_port.cc	2026-06-15 07:06:31
@@ -197,7 +197,7 @@
       script_state, exception_state.GetContext());
   auto promise = open_resolver_->Promise();
 
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     open_resolver_->RejectWithDOMException(DOMExceptionCode::kNetworkError,
                                            kOpenError);
     open_resolver_ = nullptr;
@@ -215,7 +215,7 @@
 
 ReadableStream* SerialPort::readable(ScriptState* script_state,
                                      ExceptionState& exception_state) {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     if (port_.is_bound())
       OnConnectionError();
     return nullptr;
@@ -246,7 +246,7 @@
 
 WritableStream* SerialPort::writable(ScriptState* script_state,
                                      ExceptionState& exception_state) {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     if (port_.is_bound())
       OnConnectionError();
     return nullptr;
@@ -292,7 +292,7 @@
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<SerialInputSignals>>(
           script_state, exception_state.GetContext());
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     OnConnectionError();
     resolver->RejectWithDOMException(DOMExceptionCode::kNetworkError,
                                      kDeviceLostError);
@@ -330,7 +330,7 @@
 
   auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLUndefined>>(
       script_state, exception_state.GetContext());
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     OnConnectionError();
     resolver->RejectWithDOMException(DOMExceptionCode::kNetworkError,
                                      kDeviceLostError);
@@ -428,7 +428,7 @@
 
   auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLUndefined>>(
       script_state, exception_state.GetContext());
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     resolver->Resolve();
     return resolver->Promise();
   }
--- a/third_party/blink/renderer/modules/hid/hid_device.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/hid/hid_device.cc	2026-06-15 07:06:31
@@ -230,7 +230,7 @@
 
 void HIDDevice::OnInputReport(uint8_t report_id,
                               const Vector<uint8_t>& buffer) {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext()))
     return;
 
   DispatchEvent(*MakeGarbageCollected<HIDInputReportEvent>(
@@ -239,7 +239,7 @@
 
 bool HIDDevice::opened() const {
   return connection_.is_bound() &&
-         !PersonaSpoofingHelper::IsEnabled(GetExecutionContext());
+         !PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext());
 }
 
 uint16_t HIDDevice::vendorId() const {
@@ -280,7 +280,7 @@
     return promise;
   }
 
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     resolver->RejectWithDOMException(DOMExceptionCode::kNotAllowedError,
                                      kOpenFailed);
     return promise;
@@ -303,7 +303,7 @@
   auto* resolver =
       MakeGarbageCollected<ScriptPromiseResolver<IDLUndefined>>(script_state);
   auto promise = resolver->Promise();
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     connection_.reset();
     receiver_.reset();
     resolver->Resolve();
--- a/third_party/blink/renderer/modules/bluetooth/bluetooth_device.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/bluetooth/bluetooth_device.cc	2026-06-15 07:06:31
@@ -158,7 +158,7 @@
   // 2. If this.[[watchAdvertisementsState]] is 'not-watching':
   DCHECK(!client_receiver_.is_bound());
 
-  if (PersonaSpoofingHelper::IsEnabled(context)) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(context)) {
     auto* resolver =
         MakeGarbageCollected<ScriptPromiseResolver<IDLUndefined>>(
             script_state, exception_state.GetContext());
@@ -224,7 +224,7 @@
   auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLUndefined>>(
       script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext())) {
     resolver->Resolve();
     return promise;
   }
@@ -241,7 +241,7 @@
 
 void BluetoothDevice::AdvertisingEvent(
     mojom::blink::WebBluetoothAdvertisingEventPtr advertising_event) {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(GetExecutionContext()))
     return;
 
   auto* event = MakeGarbageCollected<BluetoothAdvertisingEvent>(
--- a/third_party/blink/renderer/modules/bluetooth/bluetooth_remote_gatt_server.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/bluetooth/bluetooth_remote_gatt_server.cc	2026-06-15 07:06:31
@@ -116,7 +116,7 @@
           script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
 
-  if (PersonaSpoofingHelper::IsEnabled(device_->GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockDeviceApis(device_->GetExecutionContext())) {
     resolver->Reject(BluetoothError::CreateDOMException(
         mojom::blink::WebBluetoothResult::DEVICE_NO_LONGER_IN_RANGE));
     return promise;
--- a/third_party/blink/renderer/modules/gamepad/navigator_gamepad.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/gamepad/navigator_gamepad.cc	2026-06-15 07:06:31
@@ -112,7 +112,7 @@
     return HeapVector<Member<Gamepad>>();
   }
 
-  if (PersonaSpoofingHelper::IsEnabled(context)) {
+  if (PersonaSpoofingHelper::ShouldBlockGamepads(context)) {
     HeapVector<Member<Gamepad>> gamepads;
     gamepads.resize(device::Gamepads::kItemsLengthCap);
     return gamepads;
@@ -155,7 +155,7 @@
 }
 
 void NavigatorGamepad::SampleGamepads() {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockGamepads(GetExecutionContext())) {
     if (gamepads_back_.size() == 0)
       gamepads_back_.resize(device::Gamepads::kItemsLengthCap);
     for (uint32_t i = 0; i < device::Gamepads::kItemsLengthCap; ++i)
@@ -323,7 +323,7 @@
   }
 
   if (has_connection_event_listener_ || has_input_changed_event_listener_) {
-    if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    if (PersonaSpoofingHelper::ShouldBlockGamepads(GetExecutionContext()))
       return;
 
     bool first_event_listener = !has_event_listener_;
@@ -364,7 +364,7 @@
 }
 
 void NavigatorGamepad::SampleAndCompareGamepadState() {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockGamepads(GetExecutionContext())) {
     if (gamepads_.size() == 0)
       gamepads_.resize(device::Gamepads::kItemsLengthCap);
     for (uint32_t i = 0; i < device::Gamepads::kItemsLengthCap; ++i) {
@@ -509,7 +509,7 @@
 }
 
 void NavigatorGamepad::PageVisibilityChanged() {
-  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+  if (PersonaSpoofingHelper::ShouldBlockGamepads(GetExecutionContext())) {
     StopUpdating();
     return;
   }
--- a/third_party/blink/renderer/modules/device_orientation/device_orientation_absolute_controller.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/device_orientation/device_orientation_absolute_controller.cc	2026-06-15 07:06:31
@@ -41,7 +41,7 @@
   if (event_type != EventTypeName())
     return;
 
-  if (PersonaSpoofingHelper::IsEnabled(&GetWindow()))
+  if (PersonaSpoofingHelper::ShouldBlockSensorApis(&GetWindow()))
     return;
 
   // The window could be detached, e.g. if it is the `contentWindow` of an
--- a/third_party/blink/renderer/modules/device_orientation/device_sensor_event_pump.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/device_orientation/device_sensor_event_pump.cc	2026-06-15 07:06:31
@@ -15,7 +15,7 @@
 
   if (state_ != PumpState::kStopped)
     return;
-  if (PersonaSpoofingHelper::IsEnabled(frame.DomWindow()))
+  if (PersonaSpoofingHelper::ShouldBlockSensorApis(frame.DomWindow()))
     return;
 
   DCHECK(!timer_.IsActive());
--- a/third_party/blink/renderer/modules/webmidi/midi_access_initializer.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/webmidi/midi_access_initializer.cc	2026-06-15 07:06:31
@@ -38,7 +38,7 @@
       permission_service_(ExecutionContext::From(script_state)) {}
 
 ScriptPromise<MIDIAccess> MIDIAccessInitializer::Start(LocalDOMWindow* window) {
-  if (PersonaSpoofingHelper::IsEnabled(window)) {
+  if (PersonaSpoofingHelper::ShouldBlockMidi(window)) {
     resolver_->Resolve(MakeGarbageCollected<MIDIAccess>(
         nullptr, /*sysex_enabled=*/false, port_descriptors_,
         resolver_->GetExecutionContext()));
--- a/third_party/blink/renderer/modules/mediastream/user_media_processor.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/mediastream/user_media_processor.cc	2026-06-15 07:09:03
@@ -844,8 +844,8 @@
                            "(Requesting device capabilities)",
                            current_request_info_->request_id()));
     current_request_info_->StartTrace("GetAudioInputCapabilities");
-    if (PersonaSpoofingHelper::IsEnabled(frame_ ? frame_->DomWindow()
-                                                : nullptr)) {
+    if (PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
+                                                      : nullptr)) {
       SelectAudioDeviceSettings(request, MakePersonaAudioInputCapabilities());
       return;
     }
@@ -1079,8 +1079,8 @@
 
   if (blink::IsDeviceMediaType(video_controls.stream_type)) {
     current_request_info_->StartTrace("GetVideoInputCapabilities");
-    if (PersonaSpoofingHelper::IsEnabled(frame_ ? frame_->DomWindow()
-                                                : nullptr)) {
+    if (PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
+                                                      : nullptr)) {
       SelectVideoDeviceSettings(request, MakePersonaVideoInputCapabilities());
       return;
     }
@@ -1282,8 +1282,8 @@
       base::JoinString(
           current_request_info_->stream_controls()->video.device_ids, ",")
           .c_str()));
-  if (PersonaSpoofingHelper::IsEnabled(frame_ ? frame_->DomWindow()
-                                              : nullptr)) {
+  if (PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
+                                                      : nullptr)) {
     NormalizePersonaDeviceIdsForCapture(
         current_request_info_->stream_controls()->audio, true);
     NormalizePersonaDeviceIdsForCapture(
@@ -1416,8 +1416,8 @@
           return settings.device_id() == selected_id;
         });
     if (selected_audio_settings == eligible_audio_settings.end() &&
-        !PersonaSpoofingHelper::IsEnabled(frame_ ? frame_->DomWindow()
-                                                : nullptr)) {
+        !PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
+                                                      : nullptr)) {
       CHECK_NE(selected_audio_settings, eligible_audio_settings.end());
     }
     const auto& visible_audio_settings =
@@ -1448,8 +1448,8 @@
           return settings.device_id() == selected_id;
         });
     if (selected_video_settings == eligible_video_settings.end() &&
-        !PersonaSpoofingHelper::IsEnabled(frame_ ? frame_->DomWindow()
-                                                : nullptr)) {
+        !PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
+                                                      : nullptr)) {
       CHECK_NE(selected_video_settings, eligible_video_settings.end());
     }
     const auto& visible_video_settings =
--- a/third_party/blink/renderer/modules/mediastream/media_stream_track_impl.cc	2026-06-15 07:05:19
+++ b/third_party/blink/renderer/modules/mediastream/media_stream_track_impl.cc	2026-06-15 07:06:31
@@ -90,7 +90,7 @@
 bool IsPersonaDeviceMediaStreamTrackEnabled(
     ExecutionContext* context,
     const std::optional<const MediaStreamDevice>& source_device) {
-  return PersonaSpoofingHelper::IsEnabled(context) && source_device &&
+  return PersonaSpoofingHelper::UseVirtualMediaDevices(context) && source_device &&
          IsDeviceMediaType(source_device->type);
 }
 
