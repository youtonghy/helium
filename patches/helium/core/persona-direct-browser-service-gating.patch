diff --git a/content/browser/renderer_host/render_process_host_impl.cc b/content/browser/renderer_host/render_process_host_impl.cc
index d8c38408..586b565c 100644
--- a/content/browser/renderer_host/render_process_host_impl.cc
+++ b/content/browser/renderer_host/render_process_host_impl.cc
@@ -203,6 +203,7 @@
 #include "storage/browser/quota/quota_manager.h"
 #include "third_party/blink/public/common/associated_interfaces/associated_interface_registry.h"
 #include "third_party/blink/public/common/features.h"
+#include "third_party/blink/public/common/helium_persona/persona_snapshot.h"
 #include "third_party/blink/public/common/page/launching_process_state.h"
 #include "third_party/blink/public/common/storage_key/storage_key.h"
 #include "third_party/blink/public/common/switches.h"
@@ -2465,6 +2466,13 @@ void RenderProcessHostImpl::CreatePermissionService(
 void RenderProcessHostImpl::CreatePaymentManagerForOrigin(
     const url::Origin& origin,
     mojo::PendingReceiver<payments::mojom::PaymentManager> receiver) {
+  const blink::HeliumPersonaSnapshot persona_snapshot =
+      GetContentClient()->browser()->GetHeliumPersonaSnapshot(
+          GetBrowserContext());
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_payment_handler) {
+    return;
+  }
+
   storage_partition_impl_->GetPaymentAppContext()
       ->CreatePaymentManagerForOrigin(origin, std::move(receiver));
 }
@@ -2475,6 +2483,13 @@ void RenderProcessHostImpl::CreateNotificationService(
     const blink::StorageKey& storage_key,
     mojo::PendingReceiver<blink::mojom::NotificationService> receiver) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
+  const blink::HeliumPersonaSnapshot persona_snapshot =
+      GetContentClient()->browser()->GetHeliumPersonaSnapshot(
+          GetBrowserContext());
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_notifications) {
+    return;
+  }
+
   RenderFrameHost* rfh = RenderFrameHost::FromID(rfh_id);
   WeakDocumentPtr weak_document_ptr =
       rfh ? rfh->GetWeakDocumentPtr() : WeakDocumentPtr();
diff --git a/content/browser/renderer_host/render_frame_host_impl.cc b/content/browser/renderer_host/render_frame_host_impl.cc
index 4f1898f1..174b7c74 100644
--- a/content/browser/renderer_host/render_frame_host_impl.cc
+++ b/content/browser/renderer_host/render_frame_host_impl.cc
@@ -14701,6 +14701,12 @@ IdleManagerImpl* RenderFrameHostImpl::GetIdleManager() {
 
 void RenderFrameHostImpl::BindIdleManager(
     mojo::PendingReceiver<blink::mojom::IdleManager> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_idle_detection) {
+    return;
+  }
+
   if (!IsFeatureEnabled(
           network::mojom::PermissionsPolicyFeature::kIdleDetection)) {
     mojo::ReportBadMessage(
diff --git a/content/browser/web_contents/web_contents_impl.cc b/content/browser/web_contents/web_contents_impl.cc
index c2ac2e2a..151f39f9 100644
--- a/content/browser/web_contents/web_contents_impl.cc
+++ b/content/browser/web_contents/web_contents_impl.cc
@@ -6324,6 +6324,12 @@ device::mojom::WakeLockContext* WebContentsImpl::GetWakeLockContext() {
   if (!enable_wake_locks_) {
     return nullptr;
   }
+  const blink::HeliumPersonaSnapshot persona_snapshot =
+      GetContentClient()->browser()->GetHeliumPersonaSnapshot(
+          GetBrowserContext());
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_wake_lock) {
+    return nullptr;
+  }
   if (!wake_lock_context_host_) {
     wake_lock_context_host_ = std::make_unique<WakeLockContextHost>(this);
   }
