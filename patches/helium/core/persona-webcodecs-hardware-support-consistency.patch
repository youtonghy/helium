diff --git a/third_party/blink/renderer/modules/webcodecs/DEPS b/third_party/blink/renderer/modules/webcodecs/DEPS
index 9d027cb783..521b56589e 100644
--- a/third_party/blink/renderer/modules/webcodecs/DEPS
+++ b/third_party/blink/renderer/modules/webcodecs/DEPS
@@ -25,6 +25,7 @@ include_rules = [
     "+media/renderers",
     "+media/video",
 
+    "+third_party/blink/renderer/core/helium_persona",
     "+third_party/libaom",
     "+third_party/libvpx",
     "+third_party/libyuv",
diff --git a/third_party/blink/renderer/modules/webcodecs/audio_decoder.cc b/third_party/blink/renderer/modules/webcodecs/audio_decoder.cc
index b73b18c143..3edcc8447b 100644
--- a/third_party/blink/renderer/modules/webcodecs/audio_decoder.cc
+++ b/third_party/blink/renderer/modules/webcodecs/audio_decoder.cc
@@ -25,6 +25,8 @@
 #include "third_party/blink/renderer/bindings/modules/v8/v8_audio_decoder_init.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_audio_decoder_support.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_encoded_audio_chunk.h"
+#include "third_party/blink/renderer/core/execution_context/execution_context.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/modules/webaudio/base_audio_context.h"
 #include "third_party/blink/renderer/modules/webcodecs/array_buffer_util.h"
 #include "third_party/blink/renderer/modules/webcodecs/audio_data.h"
@@ -36,6 +38,35 @@
 
 namespace blink {
 
+bool IsPersonaStableAudioSampleRate(uint32_t sample_rate) {
+  return sample_rate == 8000 || sample_rate == 16000 ||
+         sample_rate == 22050 || sample_rate == 24000 ||
+         sample_rate == 32000 || sample_rate == 44100 ||
+         sample_rate == 48000;
+}
+
+bool IsPersonaStableAudioDecoderType(const AudioDecoderConfig& config,
+                                     const media::AudioType& audio_type) {
+  if (config.numberOfChannels() > 2 ||
+      !IsPersonaStableAudioSampleRate(config.sampleRate())) {
+    return false;
+  }
+
+  switch (audio_type.codec) {
+    case media::AudioCodec::kAAC:
+    case media::AudioCodec::kMP3:
+    case media::AudioCodec::kPCM:
+    case media::AudioCodec::kVorbis:
+    case media::AudioCodec::kFLAC:
+    case media::AudioCodec::kPCM_MULAW:
+    case media::AudioCodec::kOpus:
+    case media::AudioCodec::kPCM_ALAW:
+      return true;
+    default:
+      return false;
+  }
+}
+
 bool VerifyDescription(const AudioDecoderConfig& config,
                        String* js_error_message) {
   // https://www.w3.org/TR/webcodecs-flac-codec-registration
@@ -203,7 +234,13 @@ ScriptPromise<AudioDecoderSupport> AudioDecoder::isConfigSupported(
   }
 
   AudioDecoderSupport* support = AudioDecoderSupport::Create();
-  support->setSupported(media::IsDecoderSupportedAudioType(*audio_type));
+  bool supported = media::IsDecoderSupportedAudioType(*audio_type);
+  if (PersonaSpoofingHelper::ReduceSystemInfo(
+          ExecutionContext::From(script_state)) &&
+      !IsPersonaStableAudioDecoderType(*config, *audio_type)) {
+    supported = false;
+  }
+  support->setSupported(supported);
   support->setConfig(CopyConfig(*config));
   return ToResolvedPromise<AudioDecoderSupport>(script_state, support);
 }
@@ -356,6 +393,15 @@ std::optional<media::AudioDecoderConfig> AudioDecoder::MakeMediaConfig(
     const ConfigType& config,
     String* js_error_message) {
   DCHECK(js_error_message);
+  if (PersonaSpoofingHelper::ReduceSystemInfo(GetExecutionContext())) {
+    std::optional<media::AudioType> audio_type =
+        IsValidAudioDecoderConfig(config, js_error_message);
+    if (!audio_type ||
+        !IsPersonaStableAudioDecoderType(config, *audio_type)) {
+      *js_error_message = "Unsupported config.";
+      return std::nullopt;
+    }
+  }
   return MakeMediaAudioDecoderConfig(config, js_error_message /* out */);
 }
 
diff --git a/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc b/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc
index c863cd794d..c8e00641dc 100644
--- a/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc
+++ b/third_party/blink/renderer/modules/webcodecs/audio_encoder.cc
@@ -33,6 +33,8 @@
 #include "third_party/blink/renderer/bindings/modules/v8/v8_opus_application.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_opus_encoder_config.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_opus_signal.h"
+#include "third_party/blink/renderer/core/execution_context/execution_context.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/modules/webcodecs/array_buffer_util.h"
 #include "third_party/blink/renderer/modules/webcodecs/encoded_audio_chunk.h"
 #include "third_party/blink/renderer/platform/bindings/exception_state.h"
@@ -52,6 +54,15 @@ constexpr uint32_t kDefaultOpusComplexity = 5;
 constexpr uint32_t kDefaultOpusComplexity = 9;
 #endif
 
+bool IsPersonaStableAudioEncoderConfig(
+    const AudioEncoderTraits::ParsedConfig& config) {
+  return config.options.codec == media::AudioCodec::kOpus &&
+         config.options.channels >= 1 && config.options.channels <= 2 &&
+         (config.options.sample_rate == 16000 ||
+          config.options.sample_rate == 24000 ||
+          config.options.sample_rate == 48000);
+}
+
 template <typename T>
 bool VerifyParameterValues(const T& value,
                            String error_message_base_base,
@@ -623,6 +634,11 @@ bool AudioEncoder::CanReconfigure(ParsedConfig& original_config,
 
 bool AudioEncoder::VerifyCodecSupport(ParsedConfig* config,
                                       String* js_error_message) {
+  if (PersonaSpoofingHelper::ReduceSystemInfo(GetExecutionContext()) &&
+      !IsPersonaStableAudioEncoderConfig(*config)) {
+    *js_error_message = "Unsupported codec type.";
+    return false;
+  }
   return VerifyCodecSupportStatic(config, js_error_message);
 }
 
@@ -694,8 +710,14 @@ ScriptPromise<AudioEncoderSupport> AudioEncoder::isConfigSupported(
 
   String unused_js_error_message;
   auto* support = AudioEncoderSupport::Create();
-  support->setSupported(
-      VerifyCodecSupportStatic(parsed_config, &unused_js_error_message));
+  bool supported =
+      VerifyCodecSupportStatic(parsed_config, &unused_js_error_message);
+  if (PersonaSpoofingHelper::ReduceSystemInfo(
+          ExecutionContext::From(script_state)) &&
+      !IsPersonaStableAudioEncoderConfig(*parsed_config)) {
+    supported = false;
+  }
+  support->setSupported(supported);
   support->setConfig(CopyConfig(*config));
   return ToResolvedPromise<AudioEncoderSupport>(script_state, support);
 }
diff --git a/third_party/blink/renderer/modules/webcodecs/video_decoder.cc b/third_party/blink/renderer/modules/webcodecs/video_decoder.cc
index 810bf89aa6..d6ad51a3c1 100644
--- a/third_party/blink/renderer/modules/webcodecs/video_decoder.cc
+++ b/third_party/blink/renderer/modules/webcodecs/video_decoder.cc
@@ -31,6 +31,8 @@
 #include "third_party/blink/renderer/bindings/modules/v8/v8_video_decoder_config.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_video_decoder_support.h"
 #include "third_party/blink/renderer/core/dom/dom_exception.h"
+#include "third_party/blink/renderer/core/execution_context/execution_context.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/typed_arrays/dom_array_buffer.h"
 #include "third_party/blink/renderer/modules/webcodecs/array_buffer_util.h"
 #include "third_party/blink/renderer/modules/webcodecs/decrypt_config_util.h"
@@ -91,6 +93,39 @@ void DecoderSupport_OnKnown(
   resolver->Resolve(support);
 }
 
+bool IsPersonaStableVideoCodecProfile(const media::VideoType& video_type) {
+  switch (video_type.codec) {
+    case media::VideoCodec::kH264:
+      return video_type.profile == media::H264PROFILE_BASELINE ||
+             video_type.profile == media::H264PROFILE_MAIN ||
+             video_type.profile == media::H264PROFILE_HIGH;
+    case media::VideoCodec::kVP8:
+      return true;
+    case media::VideoCodec::kVP9:
+      return video_type.profile == media::VP9PROFILE_PROFILE0 ||
+             video_type.profile == media::VIDEO_CODEC_PROFILE_UNKNOWN;
+    case media::VideoCodec::kAV1:
+      return video_type.profile == media::AV1PROFILE_PROFILE_MAIN ||
+             video_type.profile == media::VIDEO_CODEC_PROFILE_UNKNOWN;
+    default:
+      return false;
+  }
+}
+
+bool IsPersonaStableVideoDecoderConfig(const VideoDecoderConfig& config,
+                                       const media::VideoType& video_type) {
+  if (!IsPersonaStableVideoCodecProfile(video_type)) {
+    return false;
+  }
+
+  if (config.hasCodedWidth() && config.hasCodedHeight() &&
+      (config.codedWidth() > 3840 || config.codedHeight() > 2160)) {
+    return false;
+  }
+
+  return true;
+}
+
 bool ParseCodecString(const String& codec_string,
                       media::VideoType& out_video_type,
                       String& js_error_message) {
@@ -300,6 +335,14 @@ ScriptPromise<VideoDecoderSupport> VideoDecoder::isConfigSupported(
   VideoDecoderSupport* support = VideoDecoderSupport::Create();
   support->setConfig(config_copy);
 
+  if (PersonaSpoofingHelper::ReduceSystemInfo(
+          ExecutionContext::From(script_state)) &&
+      (hw_pref == HardwarePreference::kPreferHardware ||
+       !IsPersonaStableVideoDecoderConfig(*config_copy, *video_type))) {
+    support->setSupported(false);
+    return ToResolvedPromise<VideoDecoderSupport>(script_state, support);
+  }
+
   if ((hw_pref == HardwarePreference::kPreferSoftware &&
        !media::IsDecoderBuiltInVideoCodec(video_type->codec)) ||
       !media::IsDecoderSupportedVideoType(*video_type)) {
@@ -337,6 +380,10 @@ ScriptPromise<VideoDecoderSupport> VideoDecoder::isConfigSupported(
 HardwarePreference VideoDecoder::GetHardwarePreference(
     const ConfigType& config) {
   auto config_pref = GetHardwareAccelerationPreference(config);
+  if (PersonaSpoofingHelper::ReduceSystemInfo(GetExecutionContext())) {
+    return HardwarePreference::kPreferSoftware;
+  }
+
   if (decoder_specific_data_.decoder_helper &&
       decoder_specific_data_.decoder_helper->RequiresSoftwareDecoder()) {
     // This should have already been verified by isConfigSupported().
@@ -589,6 +636,16 @@ std::optional<media::VideoDecoderConfig> VideoDecoder::MakeMediaConfig(
     const ConfigType& config,
     String* js_error_message) {
   DCHECK(js_error_message);
+  if (PersonaSpoofingHelper::ReduceSystemInfo(GetExecutionContext())) {
+    std::optional<media::VideoType> video_type =
+        IsValidVideoDecoderConfig(config, js_error_message);
+    if (!video_type ||
+        !IsPersonaStableVideoDecoderConfig(config, *video_type)) {
+      *js_error_message = "Unsupported config.";
+      pending_codec_ = media::VideoCodec::kUnknown;
+      return std::nullopt;
+    }
+  }
   auto media_config = MakeMediaVideoDecoderConfigInternal(
       config, decoder_specific_data_ /* out */, js_error_message /* out */);
   pending_codec_ =
diff --git a/third_party/blink/renderer/modules/webcodecs/video_encoder.cc b/third_party/blink/renderer/modules/webcodecs/video_encoder.cc
index 640007b025..90d9b911f3 100644
--- a/third_party/blink/renderer/modules/webcodecs/video_encoder.cc
+++ b/third_party/blink/renderer/modules/webcodecs/video_encoder.cc
@@ -67,6 +67,8 @@
 #include "third_party/blink/renderer/bindings/modules/v8/v8_video_encoder_support.h"
 #include "third_party/blink/renderer/bindings/modules/v8/v8_video_pixel_format.h"
 #include "third_party/blink/renderer/core/dom/dom_exception.h"
+#include "third_party/blink/renderer/core/execution_context/execution_context.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
 #include "third_party/blink/renderer/core/streams/readable_stream.h"
@@ -117,6 +119,32 @@ constexpr const char kCategory[] = "media";
 // instead of media::VideoFrame.
 BASE_FEATURE(kUseBlinkTimestampForEncoding, base::FEATURE_ENABLED_BY_DEFAULT);
 
+bool IsPersonaStableVideoEncoderConfig(
+    const VideoEncoderTraits::ParsedConfig& config) {
+  if (config.options.frame_size.width() > 1920 ||
+      config.options.frame_size.height() > 1080 ||
+      config.options.framerate.value_or(30.0) > 60) {
+    return false;
+  }
+
+  switch (config.codec) {
+    case media::VideoCodec::kH264:
+      return config.profile == media::H264PROFILE_BASELINE ||
+             config.profile == media::H264PROFILE_MAIN ||
+             config.profile == media::H264PROFILE_HIGH;
+    case media::VideoCodec::kVP8:
+      return true;
+    case media::VideoCodec::kVP9:
+      return config.profile == media::VP9PROFILE_PROFILE0 ||
+             config.profile == media::VIDEO_CODEC_PROFILE_UNKNOWN;
+    case media::VideoCodec::kAV1:
+      return config.profile == media::AV1PROFILE_PROFILE_MAIN ||
+             config.profile == media::VIDEO_CODEC_PROFILE_UNKNOWN;
+    default:
+      return false;
+  }
+}
+
 // TODO(crbug.com/40215121): This is very similar to the method in
 // video_frame.cc. It should probably be a function in video_types.cc.
 media::VideoPixelFormat ToOpaqueMediaPixelFormat(media::VideoPixelFormat fmt) {
@@ -634,11 +662,21 @@ VideoEncoder::ParsedConfig* VideoEncoder::OnNewConfigure(
     const VideoEncoderConfig* config,
     ExceptionState& exception_state) {
   first_input_transformation_.reset();
-  return ParseConfigStatic(config, exception_state);
+  ParsedConfig* parsed_config = ParseConfigStatic(config, exception_state);
+  if (parsed_config &&
+      PersonaSpoofingHelper::ReduceSystemInfo(GetExecutionContext())) {
+    parsed_config->hw_pref = HardwarePreference::kPreferSoftware;
+  }
+  return parsed_config;
 }
 
 bool VideoEncoder::VerifyCodecSupport(ParsedConfig* config,
                                       String* js_error_message) {
+  if (PersonaSpoofingHelper::ReduceSystemInfo(GetExecutionContext()) &&
+      !IsPersonaStableVideoEncoderConfig(*config)) {
+    *js_error_message = "Unsupported codec type.";
+    return false;
+  }
   return VerifyCodecSupportStatic(config, js_error_message);
 }
 
@@ -1740,11 +1778,24 @@ ScriptPromise<VideoEncoderSupport> VideoEncoder::isConfigSupported(
     return ToResolvedPromise<VideoEncoderSupport>(script_state, support);
   }
 
+  const bool use_persona_software_only =
+      PersonaSpoofingHelper::ReduceSystemInfo(
+          ExecutionContext::From(script_state));
+  if (use_persona_software_only &&
+      (parsed_config->hw_pref == HardwarePreference::kPreferHardware ||
+       !IsPersonaStableVideoEncoderConfig(*parsed_config))) {
+    auto* support = VideoEncoderSupport::Create();
+    support->setConfig(config_copy);
+    support->setSupported(false);
+    return ToResolvedPromise<VideoEncoderSupport>(script_state, support);
+  }
+
   // Schedule tasks for determining hardware and software encoding support and
   // register them with HeapBarrierCallback.
   wtf_size_t num_callbacks = 0;
-  if (parsed_config->hw_pref != HardwarePreference::kPreferSoftware ||
-      media::MayHaveAndAllowSelectOSSoftwareEncoder(parsed_config->codec)) {
+  if (!use_persona_software_only &&
+      (parsed_config->hw_pref != HardwarePreference::kPreferSoftware ||
+       media::MayHaveAndAllowSelectOSSoftwareEncoder(parsed_config->codec))) {
     ++num_callbacks;
   }
   if (parsed_config->hw_pref != HardwarePreference::kPreferHardware) {
@@ -1758,8 +1809,9 @@ ScriptPromise<VideoEncoderSupport> VideoEncoder::isConfigSupported(
       num_callbacks,
       BindOnce(&FindAnySupported, MakeUnwrappingCrossThreadHandle(resolver)));
 
-  if (parsed_config->hw_pref != HardwarePreference::kPreferSoftware ||
-      media::MayHaveAndAllowSelectOSSoftwareEncoder(parsed_config->codec)) {
+  if (!use_persona_software_only &&
+      (parsed_config->hw_pref != HardwarePreference::kPreferSoftware ||
+       media::MayHaveAndAllowSelectOSSoftwareEncoder(parsed_config->codec))) {
     // Hardware support not denied, detect support by hardware encoders.
     auto* support = VideoEncoderSupport::Create();
     support->setConfig(config_copy);
