diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.html b/chrome/browser/resources/settings/privacy_page/persona_page.html
index e18d0eaa..a8fb76cb 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html
@@ -83,4 +631,4 @@
     <cr-checkbox checked="{{editablePersona_.advanced.allowKeyboardLayout}}">
-      Allow keyboard layout map
+      Allow keyboard layout and lock
     </cr-checkbox>
     <cr-checkbox checked="{{editablePersona_.advanced.allowRealBatteryStatus}}">
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 2990ec7e..577b4b40 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -647,7 +647,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     if (allowLocalNetworkApis) {
       this.addDraftWarning_(
           warnings,
-          'allowLocalNetworkApis exposes local network probing and address-adjacent APIs.');
+          'allowLocalNetworkApis exposes Direct Sockets, WebRTC P2P, and local address-adjacent APIs; WebSocket and WebTransport are not controlled by this switch.');
     }
     if (allowWebNfc) {
       this.addDraftWarning_(
@@ -685,7 +685,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     if (allowKeyboardLayout) {
       this.addDraftWarning_(
           warnings,
-          'allowKeyboardLayout exposes real keyboard layout information.');
+          'allowKeyboardLayout exposes real keyboard layout information and keyboard lock behavior.');
     }
     if (allowRealBatteryStatus) {
       this.addDraftWarning_(
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 5295c4c5..6beaa67a 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -1172,7 +1172,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
   }
   if (allow_local_network_apis) {
     AddWarning(warnings,
-               "allowLocalNetworkApis exposes local network probing and address-adjacent APIs.");
+               "allowLocalNetworkApis exposes Direct Sockets, WebRTC P2P, "
+               "and local address-adjacent APIs; WebSocket and WebTransport are not controlled by this switch.");
   }
   if (allow_web_nfc) {
     AddWarning(warnings, "allowWebNfc exposes NFC hardware capability surfaces.");
@@ -1204,7 +1204,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
   }
   if (allow_keyboard_layout) {
     AddWarning(warnings,
-               "allowKeyboardLayout exposes real keyboard layout information.");
+               "allowKeyboardLayout exposes real keyboard layout information "
+               "and keyboard lock behavior.");
   }
   if (allow_real_battery_status) {
     AddWarning(warnings,
diff --git a/content/browser/browser_interface_binders.cc b/content/browser/browser_interface_binders.cc
index 8f2e2f4a..0a47b6f1 100644
--- a/content/browser/browser_interface_binders.cc
+++ b/content/browser/browser_interface_binders.cc
@@ -763,6 +763,15 @@ blink::HeliumPersonaSnapshot GetHeliumPersonaSnapshot(
   return GetContentClient()->browser()->GetHeliumPersonaSnapshot(
       browser_context);
 }
 
+blink::HeliumPersonaSnapshot GetHeliumPersonaSnapshot(ServiceWorkerHost* host) {
+  if (!host) {
+    return blink::HeliumPersonaSnapshot();
+  }
+  BrowserContext* browser_context =
+      host->version()->context()->wrapper()->browser_context();
+  return GetHeliumPersonaSnapshot(browser_context);
+}
+
 bool HeliumPersonaAllowsContacts(RenderFrameHost* host) {
   blink::HeliumPersonaSnapshot snapshot =
@@ -787,6 +796,34 @@ bool HeliumPersonaAllowsBackgroundFetch(ServiceWorkerHost* host) {
   return !snapshot.IsEnabled() || snapshot.allow_background_sync;
 }
 
+bool HeliumPersonaAllowsContentIndex(RenderFrameHost* host) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshot(host ? host->GetBrowserContext() : nullptr);
+  return !snapshot.IsEnabled() || snapshot.allow_installed_apps;
+}
+
+bool HeliumPersonaAllowsContentIndex(ServiceWorkerHost* host) {
+  blink::HeliumPersonaSnapshot snapshot = GetHeliumPersonaSnapshot(host);
+  return !snapshot.IsEnabled() || snapshot.allow_installed_apps;
+}
+
+bool HeliumPersonaAllowsCookieStore(RenderFrameHost* host) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshot(host ? host->GetBrowserContext() : nullptr);
+  return !snapshot.IsEnabled() || snapshot.cookie_enabled;
+}
+
+bool HeliumPersonaAllowsCookieStore(ServiceWorkerHost* host) {
+  blink::HeliumPersonaSnapshot snapshot = GetHeliumPersonaSnapshot(host);
+  return !snapshot.IsEnabled() || snapshot.cookie_enabled;
+}
+
+bool HeliumPersonaAllowsKeyboardLock(RenderFrameHost* host) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshot(host ? host->GetBrowserContext() : nullptr);
+  return !snapshot.IsEnabled() || snapshot.allow_keyboard_layout;
+}
+
 }  // namespace
 
 // Documents/frames
@@ -1263,12 +1302,18 @@ void PopulateBinderMapWithContext(
   }
   map->Add<device::mojom::BatteryMonitor>(&BindBatteryMonitor);
   map->Add<blink::mojom::ColorChooserFactory>(&BindColorChooserFactoryForFrame);
   map->Add<blink::mojom::EyeDropperChooser>(&EyeDropperChooserImpl::Create);
-  map->Add<blink::mojom::CookieStore>(
-      &CookieStoreManager::BindReceiverForFrame);
-  map->Add<blink::mojom::ContentIndexService>(
-      &ContentIndexServiceImpl::CreateForFrame);
-  map->Add<blink::mojom::KeyboardLockService>(
-      &KeyboardLockServiceImpl::CreateMojoService);
+  if (HeliumPersonaAllowsCookieStore(host)) {
+    map->Add<blink::mojom::CookieStore>(
+        &CookieStoreManager::BindReceiverForFrame);
+  }
+  if (HeliumPersonaAllowsContentIndex(host)) {
+    map->Add<blink::mojom::ContentIndexService>(
+        &ContentIndexServiceImpl::CreateForFrame);
+  }
+  if (HeliumPersonaAllowsKeyboardLock(host)) {
+    map->Add<blink::mojom::KeyboardLockService>(
+        &KeyboardLockServiceImpl::CreateMojoService);
+  }
   if (base::FeatureList::IsEnabled(network::features::kInterestGroupStorage)) {
     map->Add<blink::mojom::AdAuctionService>(
@@ -1852,11 +1896,15 @@ void PopulateBinderMapWithContext(
   if (HeliumPersonaAllowsBackgroundFetch(host)) {
     map->Add<blink::mojom::BackgroundFetchService>(
         base::BindRepeating(&BackgroundFetchServiceImpl::CreateForWorker,
                             host->GetNetworkIsolationKey()));
   }
-  map->Add<blink::mojom::ContentIndexService>(
-      &ContentIndexServiceImpl::CreateForWorker);
-  map->Add<blink::mojom::CookieStore>(
-      &CookieStoreManager::BindReceiverForWorker);
+  if (HeliumPersonaAllowsContentIndex(host)) {
+    map->Add<blink::mojom::ContentIndexService>(
+        &ContentIndexServiceImpl::CreateForWorker);
+  }
+  if (HeliumPersonaAllowsCookieStore(host)) {
+    map->Add<blink::mojom::CookieStore>(
+        &CookieStoreManager::BindReceiverForWorker);
+  }
 
   // RenderProcessHost binders taking an origin
