diff --git a/chrome/browser/chrome_browser_interface_binders.cc b/chrome/browser/chrome_browser_interface_binders.cc
index 81e202b6..b580bccf 100644
--- a/chrome/browser/chrome_browser_interface_binders.cc
+++ b/chrome/browser/chrome_browser_interface_binders.cc
@@ -301,6 +301,13 @@ bool HeliumPersonaAllowsPayment(content::RenderFrameHost* render_frame_host) {
   return !snapshot.IsEnabled() || snapshot.allow_payment_handler;
 }
 
+bool HeliumPersonaAllowsPlatformCredentials(
+    content::RenderFrameHost* render_frame_host) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshot(render_frame_host);
+  return !snapshot.IsEnabled() || snapshot.allow_platform_credentials;
+}
+
 bool HeliumPersonaAllowsProtectedMediaIdentifier(
     content::RenderFrameHost* render_frame_host) {
   blink::HeliumPersonaSnapshot snapshot =
@@ -515,7 +522,9 @@ void PopulateChromeFrameBinders(
 
   map->Add<optimization_guide::mojom::ModelBroker>(&BindModelBroker);
 
-  map->Add<blink::mojom::CredentialManager>(&BindCredentialManager);
+  if (HeliumPersonaAllowsPlatformCredentials(render_frame_host)) {
+    map->Add<blink::mojom::CredentialManager>(&BindCredentialManager);
+  }
 
   map->Add<chrome::mojom::OpenSearchDescriptionDocumentHandler>(
       base::BindRepeating(
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index c2da5f58..7b4bd213 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -243,6 +243,8 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const allowProtectedMediaIdentifier =
         Boolean(advanced['allowProtectedMediaIdentifier']);
     const allowDeviceApis = Boolean(advanced['allowDeviceApis']);
+    const allowPlatformCredentials =
+        Boolean(advanced['allowPlatformCredentials']);
     const allowPresentationApis = Boolean(advanced['allowPresentationApis']);
     const allowRemotePlayback = Boolean(advanced['allowRemotePlayback']);
     const speechVoiceName = String(advanced['speechVoiceName'] || '');
@@ -578,6 +580,11 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'allowDeviceApis exposes real Bluetooth, WebUSB, Web Serial, WebHID, and Smart Card service bindings.');
     }
+    if (allowPlatformCredentials) {
+      this.addDraftWarning_(
+          warnings,
+          'allowPlatformCredentials exposes real Credential Management and platform passkey service bindings.');
+    }
     if (!allowPresentationApis && allowRemotePlayback) {
       this.addDraftWarning_(
           warnings,
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index e23dee80..10b147ed 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -843,6 +843,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   advanced->Set("allowMediaCapture", true);
   advanced->Set("allowProtectedMediaIdentifier", true);
   advanced->Set("allowDeviceApis", true);
+  advanced->Set("allowPlatformCredentials", true);
   advanced->Set("allowRemotePlayback", true);
   advanced->Set("speechVoiceName", "Samantha");
   advanced->Set("speechVoiceLang", "en-US");
@@ -904,6 +905,8 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "allowMediaCapture exposes real camera"));
   EXPECT_TRUE(WarningsContain(warnings, "allowProtectedMediaIdentifier exposes real"));
   EXPECT_TRUE(WarningsContain(warnings, "allowDeviceApis exposes real Bluetooth"));
+  EXPECT_TRUE(WarningsContain(warnings,
+                              "allowPlatformCredentials exposes real"));
   EXPECT_TRUE(WarningsContain(warnings, "Remote Playback reuses"));
   EXPECT_TRUE(WarningsContain(warnings, "maxTouchPoints should be 0"));
   EXPECT_TRUE(WarningsContain(warnings, "CoreText font rendering"));
@@ -1397,6 +1400,7 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   advanced->Set("speechVoiceDefault", false);
   advanced->Set("allowGeolocation", true);
   advanced->Set("allowLocalFonts", true);
+  advanced->Set("allowPlatformCredentials", true);
   advanced->Set("virtualMediaDevices", false);
   advanced->Set("allowNotifications", true);
   advanced->Set("allowProtectedMediaIdentifier", true);
@@ -1579,6 +1583,7 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_FALSE(snapshot.speech_voice_default);
   EXPECT_TRUE(snapshot.allow_geolocation);
   EXPECT_TRUE(snapshot.allow_local_fonts);
+  EXPECT_TRUE(snapshot.allow_platform_credentials);
   EXPECT_FALSE(snapshot.virtual_media_devices);
   EXPECT_TRUE(snapshot.allow_notifications);
   EXPECT_TRUE(snapshot.allow_protected_media_identifier);
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index b171f562..8c2efd24 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -687,6 +687,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedBool(persona, "advanced", "allowProtectedMediaIdentifier");
   const bool allow_device_apis =
       GetNestedBool(persona, "advanced", "allowDeviceApis");
+  const bool allow_platform_credentials =
+      GetNestedBool(persona, "advanced", "allowPlatformCredentials");
   const bool allow_presentation_apis =
       GetNestedBool(persona, "advanced", "allowPresentationApis");
   const bool allow_remote_playback =
@@ -1081,6 +1083,10 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "allowDeviceApis exposes real Bluetooth, WebUSB, Web Serial, WebHID, and Smart Card service bindings.");
   }
+  if (allow_platform_credentials) {
+    AddWarning(warnings,
+               "allowPlatformCredentials exposes real Credential Management and platform passkey service bindings.");
+  }
   if (!allow_presentation_apis && allow_remote_playback) {
     AddWarning(warnings,
                "Remote Playback reuses PresentationService; disabling Presentation API does not fully isolate that browser service while Remote Playback is allowed.");
