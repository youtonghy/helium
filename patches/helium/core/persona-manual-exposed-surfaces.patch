diff -ruN /private/tmp/helium-final-files-before/chrome/browser/chrome_content_browser_client.cc /private/tmp/helium-final-files/chrome/browser/chrome_content_browser_client.cc
--- a/chrome/browser/chrome_content_browser_client.cc	2026-06-15 14:33:50
+++ b/chrome/browser/chrome_content_browser_client.cc	2026-06-15 14:39:53
@@ -930,6 +930,13 @@
   target.canvas_noise = source.canvas_noise;
   target.audio_noise = source.audio_noise;
   target.font_metric_noise = source.font_metric_noise;
+  target.pdf_viewer_enabled = source.pdf_viewer_enabled;
+  target.storage_quota_gb = source.storage_quota_gb;
+  target.speech_voice_name = source.speech_voice_name;
+  target.speech_voice_lang = source.speech_voice_lang;
+  target.speech_voice_uri = source.speech_voice_uri;
+  target.speech_voice_local_service = source.speech_voice_local_service;
+  target.speech_voice_default = source.speech_voice_default;
   target.allow_geolocation = source.allow_geolocation;
   target.allow_media_capture = source.allow_media_capture;
   target.virtual_media_devices = source.virtual_media_devices;
diff -ruN /private/tmp/helium-final-files-before/chrome/browser/resources/settings/privacy_page/persona_page.html /private/tmp/helium-final-files/chrome/browser/resources/settings/privacy_page/persona_page.html
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html	2026-06-15 14:33:50
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html	2026-06-15 14:39:53
@@ -488,6 +488,24 @@
     <cr-checkbox checked="{{editablePersona_.advanced.fontMetricNoise}}">
       Font metric noise
     </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.pdfViewerEnabled}}">
+      PDF viewer plugin surface
+    </cr-checkbox>
+    <cr-input label="Storage quota (GB, 0 = auto)"
+        value="{{editablePersona_.advanced.storageQuotaGb}}"
+        type="number"></cr-input>
+    <cr-input label="Speech voice name"
+        value="{{editablePersona_.advanced.speechVoiceName}}"></cr-input>
+    <cr-input label="Speech voice language"
+        value="{{editablePersona_.advanced.speechVoiceLang}}"></cr-input>
+    <cr-input class="wide" label="Speech voice URI"
+        value="{{editablePersona_.advanced.speechVoiceUri}}"></cr-input>
+    <cr-checkbox checked="{{editablePersona_.advanced.speechVoiceLocalService}}">
+      Speech voice local service
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.speechVoiceDefault}}">
+      Speech voice default
+    </cr-checkbox>
     <cr-checkbox checked="{{editablePersona_.advanced.allowGeolocation}}">
       Allow geolocation
     </cr-checkbox>
diff -ruN /private/tmp/helium-final-files-before/chrome/browser/resources/settings/privacy_page/persona_page.ts /private/tmp/helium-final-files/chrome/browser/resources/settings/privacy_page/persona_page.ts
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts	2026-06-15 14:33:50
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts	2026-06-15 14:41:14
@@ -474,6 +474,9 @@
       canvasNoise: true,
       audioNoise: true,
       fontMetricNoise: true,
+      pdfViewerEnabled: true,
+      speechVoiceLocalService: true,
+      speechVoiceDefault: true,
       allowGeolocation: false,
       allowMediaCapture: false,
       virtualMediaDevices: true,
@@ -501,6 +504,14 @@
         persona.advanced[key] = value;
       }
     }
+    if (persona.advanced['storageQuotaGb'] === undefined) {
+      persona.advanced['storageQuotaGb'] = 0;
+    }
+    for (const key of ['speechVoiceName', 'speechVoiceLang', 'speechVoiceUri']) {
+      if (persona.advanced[key] === undefined) {
+        persona.advanced[key] = '';
+      }
+    }
   }
 
   private normalizeNumbers_(persona: PersonaRecord) {
@@ -574,7 +585,24 @@
         Boolean(persona.fontRendering['useAutohinter']);
     persona.fontRendering['useBitmaps'] =
         Boolean(persona.fontRendering['useBitmaps']);
-    for (const key of Object.keys(persona.advanced)) {
+    persona.advanced['storageQuotaGb'] =
+        toNumber(persona.advanced['storageQuotaGb']);
+    for (const key of ['speechVoiceName', 'speechVoiceLang', 'speechVoiceUri']) {
+      persona.advanced[key] = String(persona.advanced[key] || '');
+    }
+    const booleanAdvancedKeys = [
+      'clientHintsEnabled', 'reduceSystemInfo', 'canvasNoise',
+      'audioNoise', 'fontMetricNoise', 'pdfViewerEnabled',
+      'speechVoiceLocalService', 'speechVoiceDefault', 'allowGeolocation',
+      'allowMediaCapture', 'virtualMediaDevices', 'allowDisplayCapture',
+      'allowDeviceApis', 'allowGamepads', 'allowSensorApis', 'allowMidi',
+      'allowLocalNetworkApis', 'allowWebNfc', 'allowWindowManagement',
+      'allowPresentationApis', 'allowRemotePlayback', 'allowWebXr',
+      'allowPlatformCredentials', 'allowKeyboardLayout',
+      'allowRealBatteryStatus', 'allowLocalFonts',
+      'allowPersistentDeviceId', 'allowInstalledApps', 'allowManagedDevice',
+    ];
+    for (const key of booleanAdvancedKeys) {
       persona.advanced[key] = Boolean(persona.advanced[key]);
     }
   }
diff -ruN /private/tmp/helium-final-files-before/components/helium_persona/persona_prefs_unittest.cc /private/tmp/helium-final-files/components/helium_persona/persona_prefs_unittest.cc
--- a/components/helium_persona/persona_prefs_unittest.cc	2026-06-15 14:33:50
+++ b/components/helium_persona/persona_prefs_unittest.cc	2026-06-15 14:41:50
@@ -511,6 +511,13 @@
   advanced->Set("canvasNoise", false);
   advanced->Set("audioNoise", false);
   advanced->Set("fontMetricNoise", false);
+  advanced->Set("pdfViewerEnabled", false);
+  advanced->Set("storageQuotaGb", 42.0);
+  advanced->Set("speechVoiceName", "Manual Voice");
+  advanced->Set("speechVoiceLang", "fr-FR");
+  advanced->Set("speechVoiceUri", "urn:manual:voice");
+  advanced->Set("speechVoiceLocalService", false);
+  advanced->Set("speechVoiceDefault", false);
   advanced->Set("allowGeolocation", true);
   advanced->Set("allowLocalFonts", true);
   advanced->Set("virtualMediaDevices", false);
@@ -551,6 +558,24 @@
   EXPECT_TRUE(runtime.FindDict("advanced")
                   ->FindBool("allowGeolocation")
                   .value_or(false));
+  EXPECT_FALSE(runtime.FindDict("advanced")
+                   ->FindBool("pdfViewerEnabled")
+                   .value_or(true));
+  EXPECT_EQ(42.0, runtime.FindDict("advanced")
+                      ->FindDouble("storageQuotaGb")
+                      .value_or(0));
+  EXPECT_EQ("Manual Voice", *runtime.FindDict("advanced")
+                                ->FindString("speechVoiceName"));
+  EXPECT_EQ("fr-FR", *runtime.FindDict("advanced")
+                         ->FindString("speechVoiceLang"));
+  EXPECT_EQ("urn:manual:voice", *runtime.FindDict("advanced")
+                                      ->FindString("speechVoiceUri"));
+  EXPECT_FALSE(runtime.FindDict("advanced")
+                   ->FindBool("speechVoiceLocalService")
+                   .value_or(true));
+  EXPECT_FALSE(runtime.FindDict("advanced")
+                   ->FindBool("speechVoiceDefault")
+                   .value_or(true));
 
   PersonaSnapshot snapshot = MakePersonaSnapshot(runtime);
   EXPECT_EQ("manual-runtime-seed", snapshot.noise_seed);
@@ -572,6 +597,13 @@
   EXPECT_EQ("manual-font-rendering", snapshot.font_rendering_id);
   EXPECT_FALSE(snapshot.client_hints_enabled);
   EXPECT_TRUE(snapshot.reduce_system_info);
+  EXPECT_FALSE(snapshot.pdf_viewer_enabled);
+  EXPECT_EQ(42.0, snapshot.storage_quota_gb);
+  EXPECT_EQ("Manual Voice", snapshot.speech_voice_name);
+  EXPECT_EQ("fr-FR", snapshot.speech_voice_lang);
+  EXPECT_EQ("urn:manual:voice", snapshot.speech_voice_uri);
+  EXPECT_FALSE(snapshot.speech_voice_local_service);
+  EXPECT_FALSE(snapshot.speech_voice_default);
   EXPECT_TRUE(snapshot.allow_geolocation);
   EXPECT_TRUE(snapshot.allow_local_fonts);
   EXPECT_FALSE(snapshot.virtual_media_devices);
diff -ruN /private/tmp/helium-final-files-before/components/helium_persona/persona_presets.cc /private/tmp/helium-final-files/components/helium_persona/persona_presets.cc
--- a/components/helium_persona/persona_presets.cc	2026-06-15 14:33:50
+++ b/components/helium_persona/persona_presets.cc	2026-06-15 14:39:53
@@ -240,6 +240,13 @@
   advanced.Set("canvasNoise", canvas_noise);
   advanced.Set("audioNoise", audio_noise);
   advanced.Set("fontMetricNoise", font_metric_noise);
+  advanced.Set("pdfViewerEnabled", true);
+  advanced.Set("storageQuotaGb", 0);
+  advanced.Set("speechVoiceName", "");
+  advanced.Set("speechVoiceLang", "");
+  advanced.Set("speechVoiceUri", "");
+  advanced.Set("speechVoiceLocalService", true);
+  advanced.Set("speechVoiceDefault", true);
   advanced.Set("allowGeolocation", false);
   advanced.Set("allowMediaCapture", false);
   advanced.Set("virtualMediaDevices", true);
diff -ruN /private/tmp/helium-final-files-before/components/helium_persona/persona_snapshot.cc /private/tmp/helium-final-files/components/helium_persona/persona_snapshot.cc
--- a/components/helium_persona/persona_snapshot.cc	2026-06-15 14:33:50
+++ b/components/helium_persona/persona_snapshot.cc	2026-06-15 14:39:53
@@ -333,6 +333,20 @@
       GetNestedBool(persona, "advanced", "audioNoise", true);
   snapshot.font_metric_noise =
       GetNestedBool(persona, "advanced", "fontMetricNoise", true);
+  snapshot.pdf_viewer_enabled =
+      GetNestedBool(persona, "advanced", "pdfViewerEnabled", true);
+  snapshot.storage_quota_gb =
+      GetNestedDouble(persona, "advanced", "storageQuotaGb");
+  snapshot.speech_voice_name =
+      GetNestedString(persona, "advanced", "speechVoiceName");
+  snapshot.speech_voice_lang =
+      GetNestedString(persona, "advanced", "speechVoiceLang");
+  snapshot.speech_voice_uri =
+      GetNestedString(persona, "advanced", "speechVoiceUri");
+  snapshot.speech_voice_local_service =
+      GetNestedBool(persona, "advanced", "speechVoiceLocalService", true);
+  snapshot.speech_voice_default =
+      GetNestedBool(persona, "advanced", "speechVoiceDefault", true);
   snapshot.allow_geolocation =
       GetNestedBool(persona, "advanced", "allowGeolocation", false);
   snapshot.allow_media_capture =
@@ -545,6 +559,13 @@
   advanced.Set("canvasNoise", snapshot.canvas_noise);
   advanced.Set("audioNoise", snapshot.audio_noise);
   advanced.Set("fontMetricNoise", snapshot.font_metric_noise);
+  advanced.Set("pdfViewerEnabled", snapshot.pdf_viewer_enabled);
+  advanced.Set("storageQuotaGb", snapshot.storage_quota_gb);
+  advanced.Set("speechVoiceName", snapshot.speech_voice_name);
+  advanced.Set("speechVoiceLang", snapshot.speech_voice_lang);
+  advanced.Set("speechVoiceUri", snapshot.speech_voice_uri);
+  advanced.Set("speechVoiceLocalService", snapshot.speech_voice_local_service);
+  advanced.Set("speechVoiceDefault", snapshot.speech_voice_default);
   advanced.Set("allowGeolocation", snapshot.allow_geolocation);
   advanced.Set("allowMediaCapture", snapshot.allow_media_capture);
   advanced.Set("virtualMediaDevices", snapshot.virtual_media_devices);
diff -ruN /private/tmp/helium-final-files-before/components/helium_persona/persona_snapshot.h /private/tmp/helium-final-files/components/helium_persona/persona_snapshot.h
--- a/components/helium_persona/persona_snapshot.h	2026-06-15 14:33:50
+++ b/components/helium_persona/persona_snapshot.h	2026-06-15 14:39:53
@@ -134,6 +134,13 @@
   bool canvas_noise = true;
   bool audio_noise = true;
   bool font_metric_noise = true;
+  bool pdf_viewer_enabled = true;
+  double storage_quota_gb = 0;
+  std::string speech_voice_name;
+  std::string speech_voice_lang;
+  std::string speech_voice_uri;
+  bool speech_voice_local_service = true;
+  bool speech_voice_default = true;
   bool allow_geolocation = false;
   bool allow_media_capture = false;
   bool virtual_media_devices = true;
diff -ruN /private/tmp/helium-final-files-before/content/browser/helium_noise/noise_token_data.cc /private/tmp/helium-final-files/content/browser/helium_noise/noise_token_data.cc
--- a/content/browser/helium_noise/noise_token_data.cc	2026-06-15 14:34:54
+++ b/content/browser/helium_noise/noise_token_data.cc	2026-06-15 14:39:53
@@ -212,6 +212,13 @@
   AppendSaltPart(salt, persona_snapshot.canvas_noise);
   AppendSaltPart(salt, persona_snapshot.audio_noise);
   AppendSaltPart(salt, persona_snapshot.font_metric_noise);
+  AppendSaltPart(salt, persona_snapshot.pdf_viewer_enabled);
+  AppendSaltPart(salt, persona_snapshot.storage_quota_gb);
+  AppendSaltPart(salt, persona_snapshot.speech_voice_name);
+  AppendSaltPart(salt, persona_snapshot.speech_voice_lang);
+  AppendSaltPart(salt, persona_snapshot.speech_voice_uri);
+  AppendSaltPart(salt, persona_snapshot.speech_voice_local_service);
+  AppendSaltPart(salt, persona_snapshot.speech_voice_default);
   AppendSaltPart(salt, persona_snapshot.allow_geolocation);
   AppendSaltPart(salt, persona_snapshot.allow_media_capture);
   AppendSaltPart(salt, persona_snapshot.virtual_media_devices);
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/public/common/helium_persona/persona_snapshot.h /private/tmp/helium-final-files/third_party/blink/public/common/helium_persona/persona_snapshot.h
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h	2026-06-15 14:33:50
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h	2026-06-15 14:39:53
@@ -134,6 +134,13 @@
   bool canvas_noise = true;
   bool audio_noise = true;
   bool font_metric_noise = true;
+  bool pdf_viewer_enabled = true;
+  double storage_quota_gb = 0;
+  std::string speech_voice_name;
+  std::string speech_voice_lang;
+  std::string speech_voice_uri;
+  bool speech_voice_local_service = true;
+  bool speech_voice_default = true;
   bool allow_geolocation = false;
   bool allow_media_capture = false;
   bool virtual_media_devices = true;
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h /private/tmp/helium-final-files/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h	2026-06-15 14:33:50
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h	2026-06-15 14:39:53
@@ -433,6 +433,34 @@
   static bool font_metric_noise(const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.font_metric_noise;
   }
+  static bool pdf_viewer_enabled(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.pdf_viewer_enabled;
+  }
+  static double storage_quota_gb(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.storage_quota_gb;
+  }
+  static const std::string& speech_voice_name(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.speech_voice_name;
+  }
+  static const std::string& speech_voice_lang(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.speech_voice_lang;
+  }
+  static const std::string& speech_voice_uri(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.speech_voice_uri;
+  }
+  static bool speech_voice_local_service(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.speech_voice_local_service;
+  }
+  static bool speech_voice_default(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.speech_voice_default;
+  }
   static bool allow_geolocation(
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.allow_geolocation;
@@ -617,6 +645,10 @@
   out->canvas_noise = data.canvas_noise();
   out->audio_noise = data.audio_noise();
   out->font_metric_noise = data.font_metric_noise();
+  out->pdf_viewer_enabled = data.pdf_viewer_enabled();
+  out->storage_quota_gb = data.storage_quota_gb();
+  out->speech_voice_local_service = data.speech_voice_local_service();
+  out->speech_voice_default = data.speech_voice_default();
   out->allow_geolocation = data.allow_geolocation();
   out->allow_media_capture = data.allow_media_capture();
   out->virtual_media_devices = data.virtual_media_devices();
@@ -668,7 +700,10 @@
          data.ReadFontRenderingId(&out->font_rendering_id) &&
          data.ReadFontRenderingName(&out->font_rendering_name) &&
          data.ReadFontRenderingEngine(&out->font_rendering_engine) &&
-         data.ReadFontRenderingHinting(&out->font_rendering_hinting);
+         data.ReadFontRenderingHinting(&out->font_rendering_hinting) &&
+         data.ReadSpeechVoiceName(&out->speech_voice_name) &&
+         data.ReadSpeechVoiceLang(&out->speech_voice_lang) &&
+         data.ReadSpeechVoiceUri(&out->speech_voice_uri);
 }
 
 }  // namespace mojo
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom /private/tmp/helium-final-files/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom	2026-06-15 14:33:50
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom	2026-06-15 14:39:53
@@ -124,6 +124,13 @@
   bool canvas_noise;
   bool audio_noise;
   bool font_metric_noise;
+  bool pdf_viewer_enabled;
+  double storage_quota_gb;
+  string speech_voice_name;
+  string speech_voice_lang;
+  string speech_voice_uri;
+  bool speech_voice_local_service;
+  bool speech_voice_default;
   bool allow_geolocation;
   bool allow_media_capture;
   bool virtual_media_devices;
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/renderer/modules/plugins/dom_plugin_array.cc /private/tmp/helium-final-files/third_party/blink/renderer/modules/plugins/dom_plugin_array.cc
--- a/third_party/blink/renderer/modules/plugins/dom_plugin_array.cc	2026-06-15 14:33:50
+++ b/third_party/blink/renderer/modules/plugins/dom_plugin_array.cc	2026-06-15 14:39:53
@@ -54,7 +54,8 @@
 }  // namespace
 
 DOMPluginArray::DOMPluginArray(LocalDOMWindow* window) : window_(window) {
-  if (PersonaSpoofingHelper::IsEnabled(window) || IsPdfViewerAvailable()) {
+  const HeliumPersonaSnapshot* snapshot = PersonaSpoofingHelper::Get(window);
+  if ((snapshot && snapshot->pdf_viewer_enabled) || IsPdfViewerAvailable()) {
     // See crbug.com/1164635 and https://github.com/whatwg/html/pull/6738.
     // To reduce fingerprinting and make plugins/mimetypes more
     // interoperable, this is the spec'd, hard-coded list of plugins:
@@ -136,8 +137,10 @@
 }
 
 bool DOMPluginArray::IsPdfViewerAvailable() {
-  if (PersonaSpoofingHelper::IsEnabled(window_))
-    return true;
+  if (const HeliumPersonaSnapshot* snapshot =
+          PersonaSpoofingHelper::Get(window_)) {
+    return snapshot->pdf_viewer_enabled;
+  }
   auto* data = GetPluginData();
   if (!data)
     return false;
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/renderer/modules/plugins/navigator_plugins.cc /private/tmp/helium-final-files/third_party/blink/renderer/modules/plugins/navigator_plugins.cc
--- a/third_party/blink/renderer/modules/plugins/navigator_plugins.cc	2026-06-15 14:33:50
+++ b/third_party/blink/renderer/modules/plugins/navigator_plugins.cc	2026-06-15 14:39:53
@@ -74,8 +74,10 @@
 }
 
 bool NavigatorPlugins::pdfViewerEnabled(LocalDOMWindow* window) const {
-  if (PersonaSpoofingHelper::IsEnabled(window))
-    return true;
+  if (const HeliumPersonaSnapshot* snapshot =
+          PersonaSpoofingHelper::Get(window)) {
+    return snapshot->pdf_viewer_enabled;
+  }
   return plugins(window)->IsPdfViewerAvailable();
 }
 
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/renderer/modules/quota/deprecated_storage_quota.cc /private/tmp/helium-final-files/third_party/blink/renderer/modules/quota/deprecated_storage_quota.cc
--- a/third_party/blink/renderer/modules/quota/deprecated_storage_quota.cc	2026-06-15 14:33:50
+++ b/third_party/blink/renderer/modules/quota/deprecated_storage_quota.cc	2026-06-15 14:39:53
@@ -72,6 +72,8 @@
   if (!snapshot)
     return fallback_quota;
 
+  if (snapshot->storage_quota_gb > 0)
+    return static_cast<int64_t>(snapshot->storage_quota_gb * kPersonaGiB);
   if (snapshot->device_memory >= 8)
     return 120 * kPersonaGiB;
   if (snapshot->device_memory >= 4)
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/renderer/modules/quota/storage_manager.cc /private/tmp/helium-final-files/third_party/blink/renderer/modules/quota/storage_manager.cc
--- a/third_party/blink/renderer/modules/quota/storage_manager.cc	2026-06-15 14:33:50
+++ b/third_party/blink/renderer/modules/quota/storage_manager.cc	2026-06-15 14:39:53
@@ -56,6 +56,8 @@
   if (!snapshot)
     return fallback_quota;
 
+  if (snapshot->storage_quota_gb > 0)
+    return static_cast<int64_t>(snapshot->storage_quota_gb * kPersonaGiB);
   if (snapshot->device_memory >= 8)
     return 120 * kPersonaGiB;
   if (snapshot->device_memory >= 4)
diff -ruN /private/tmp/helium-final-files-before/third_party/blink/renderer/modules/speech/speech_synthesis.cc /private/tmp/helium-final-files/third_party/blink/renderer/modules/speech/speech_synthesis.cc
--- a/third_party/blink/renderer/modules/speech/speech_synthesis.cc	2026-06-15 14:33:50
+++ b/third_party/blink/renderer/modules/speech/speech_synthesis.cc	2026-06-15 14:41:03
@@ -57,6 +57,8 @@
 String PersonaSpeechLocale(ExecutionContext* context) {
   if (const HeliumPersonaSnapshot* snapshot =
           PersonaSpoofingHelper::Get(context)) {
+    if (!snapshot->speech_voice_lang.empty())
+      return String::FromUTF8(snapshot->speech_voice_lang);
     if (!snapshot->locale.empty())
       return String::FromUTF8(snapshot->locale);
     if (!snapshot->navigator_languages.empty())
@@ -75,6 +77,11 @@
 
 String PersonaSpeechVoiceName(ExecutionContext* context,
                               const String& locale) {
+  if (const HeliumPersonaSnapshot* snapshot =
+          PersonaSpoofingHelper::Get(context)) {
+    if (!snapshot->speech_voice_name.empty())
+      return String::FromUTF8(snapshot->speech_voice_name);
+  }
   const String lower_locale = locale.DeprecatedLower();
   const bool is_windows = PersonaOsContains(context, "windows");
   const bool is_macos =
@@ -119,18 +126,35 @@
     ExecutionContext* context) {
   String locale = PersonaSpeechLocale(context);
   auto voice = mojom::blink::SpeechSynthesisVoice::New();
-  voice->voice_uri =
-      String(kPersonaSpeechVoiceUriPrefix) + locale.DeprecatedLower();
+  if (const HeliumPersonaSnapshot* snapshot =
+          PersonaSpoofingHelper::Get(context)) {
+    if (!snapshot->speech_voice_uri.empty()) {
+      voice->voice_uri = String::FromUTF8(snapshot->speech_voice_uri);
+    }
+    voice->is_local_service = snapshot->speech_voice_local_service;
+    voice->is_default = snapshot->speech_voice_default;
+  }
+  if (voice->voice_uri.empty()) {
+    voice->voice_uri =
+        String(kPersonaSpeechVoiceUriPrefix) + locale.DeprecatedLower();
+  }
   voice->name = PersonaSpeechVoiceName(context, locale);
   voice->lang = locale;
-  voice->is_local_service = true;
-  voice->is_default = true;
   return voice;
 }
 
 bool HasPersonaSpeechVoiceList(
     const HeapVector<Member<SpeechSynthesisVoice>>& voices,
     ExecutionContext* context) {
+  if (!voices.empty()) {
+    if (const HeliumPersonaSnapshot* snapshot =
+            PersonaSpoofingHelper::Get(context)) {
+      if (!snapshot->speech_voice_uri.empty()) {
+        return voices.front()->voiceURI() ==
+               String::FromUTF8(snapshot->speech_voice_uri);
+      }
+    }
+  }
   const String expected_voice_uri =
       String(kPersonaSpeechVoiceUriPrefix) +
       PersonaSpeechLocale(context).DeprecatedLower();
@@ -140,8 +164,9 @@
 
 bool HasAnyPersonaSpeechVoiceList(
     const HeapVector<Member<SpeechSynthesisVoice>>& voices) {
-  return !voices.empty() &&
-         voices.front()->voiceURI().starts_with(kPersonaSpeechVoiceUriPrefix);
+  return voices.size() == 1 &&
+         (voices.front()->voiceURI().starts_with(kPersonaSpeechVoiceUriPrefix) ||
+          voices.front()->localService());
 }
 
 void SetPersonaSpeechVoiceList(
