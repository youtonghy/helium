diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index e9ec5da..db2c22d 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -2923,6 +2923,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const profile = this.makeCustomFontRenderingProfile_({
       id: 'rendering',
       displayName: 'Custom rendering profile',
+      profileVersion: 1,
       engine: 'Skia',
       hinting: 'medium',
       antialiasing: true,
@@ -2940,10 +2941,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
   private makeCustomProfileId_(
       sourceId: string, prefix: string,
       profiles: Array<Record<string, unknown>>): string {
-    const slug = (sourceId || prefix)
-        .toLowerCase()
-        .replace(/[^a-z0-9]+/g, '-')
-        .replace(/^-+|-+$/g, '') || prefix;
+    const slug = this.slugProfileId_(sourceId || prefix) || prefix;
     const usedIds = new Set(
         (profiles || []).map(profile => String(profile['id'] || '')));
     let candidate = `custom-${slug}`;
@@ -2954,6 +2952,13 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     return candidate;
   }
 
+  private slugProfileId_(sourceId: unknown): string {
+    return String(sourceId || '')
+        .toLowerCase()
+        .replace(/[^a-z0-9]+/g, '-')
+        .replace(/^-+|-+$/g, '');
+  }
+
   private makeCustomFontPackId_(sourceId: string): string {
     return this.makeCustomProfileId_(
         sourceId, 'font-pack', this.fontPackProfiles_ || []);
@@ -2995,13 +3000,15 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const displayName = String(profile['displayName'] || sourceId || 'Rendering');
     profile['displayName'] = displayName.endsWith(' copy') ?
         displayName : `${displayName} copy`;
+    profile['profileVersion'] =
+        this.positiveNumberOr_(profile['profileVersion'], 1);
     profile['builtin'] = false;
     profile['modified'] = true;
     return profile;
   }
 
   private normalizeFontPackProfile_(profile: Record<string, unknown>) {
-    profile['id'] = this.stringValue_(profile['id']);
+    profile['id'] = this.slugProfileId_(profile['id']);
     profile['displayName'] = this.stringValue_(profile['displayName']);
     profile['families'] = this.stringListValue_(profile['families']);
     if ((profile['families'] as string[]).length === 0) {
@@ -3024,8 +3031,10 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
   }
 
   private normalizeFontRenderingProfile_(profile: Record<string, unknown>) {
-    profile['id'] = this.stringValue_(profile['id']);
+    profile['id'] = this.slugProfileId_(profile['id']);
     profile['displayName'] = this.stringValue_(profile['displayName']);
+    profile['profileVersion'] =
+        this.positiveNumberOr_(profile['profileVersion'], 1);
     profile['engine'] = this.stringValue_(profile['engine']) || 'Skia';
     profile['hinting'] = this.stringValue_(profile['hinting']) || 'medium';
     profile['textGamma'] = this.numberValueOr_(profile['textGamma'], 1.4);
@@ -3731,6 +3740,8 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     for (const key of ['id', 'displayName', 'engine', 'hinting']) {
       persona.fontRendering[key] = toString(persona.fontRendering[key]);
     }
+    persona.fontRendering['profileVersion'] =
+        this.positiveNumberOr_(persona.fontRendering['profileVersion'], 1);
     delete persona.fontRendering['builtin'];
     delete persona.fontRendering['modified'];
     delete persona.fontRendering['updated'];
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index ca40cf3..d19e25c 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -129,6 +129,7 @@ base::Value::Dict MakeDefaultFontRenderingProfile() {
   base::Value::Dict rendering;
   rendering.Set("id", "generic-grayscale");
   rendering.Set("displayName", "Generic grayscale");
+  rendering.Set("profileVersion", 1);
   rendering.Set("engine", "Skia");
   rendering.Set("hinting", "medium");
   rendering.Set("antialiasing", true);
@@ -752,6 +753,9 @@ void EnsureFontRenderingCompatibilityFields(base::Value::Dict& persona) {
   base::Value::Dict* font_rendering = persona.FindDict("fontRendering");
   EnsureString(*font_rendering, "id", "generic-grayscale");
   EnsureString(*font_rendering, "displayName", "Generic grayscale");
+  if (!font_rendering->FindInt("profileVersion")) {
+    font_rendering->Set("profileVersion", 1);
+  }
   EnsureString(*font_rendering, "engine", "Skia");
   EnsureString(*font_rendering, "hinting", "medium");
   EnsureBool(*font_rendering, "antialiasing", true);
@@ -1252,6 +1256,18 @@ double GetProfileNumber(const base::Value::Dict& profile,
   return default_value;
 }
 
+int GetPositiveProfileInt(const base::Value::Dict& profile,
+                          const char* key,
+                          int default_value) {
+  std::optional<int> value = profile.FindInt(key);
+  if (value && *value > 0) {
+    return *value;
+  }
+  std::optional<double> double_value = profile.FindDouble(key);
+  return double_value && *double_value > 0 ? static_cast<int>(*double_value)
+                                           : default_value;
+}
+
 base::Value::List CopyStringListItems(const base::Value::Dict& profile,
                                       const char* key) {
   base::Value::List clean;
@@ -1294,6 +1310,8 @@ base::Value::Dict NormalizeFontRenderingProfileForStorage(
   }
   normalized.Set("displayName",
                  TrimProfileString(*profile.FindString("displayName")));
+  normalized.Set("profileVersion",
+                 GetPositiveProfileInt(profile, "profileVersion", 1));
   normalized.Set("engine", TrimProfileString(*profile.FindString("engine")));
   normalized.Set("hinting", TrimProfileString(*profile.FindString("hinting")));
   EnsureBool(normalized, "antialiasing",
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index b429afb..91fbe6b 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -65,6 +65,7 @@ const base::Value::Dict* FindProfileById(const base::Value::List& profiles,
 }
 
 void ExpectFontRenderingProfileFields(const base::Value::Dict& font_rendering) {
+  EXPECT_TRUE(font_rendering.FindInt("profileVersion").has_value());
   EXPECT_TRUE(font_rendering.FindBool("antialiasing").has_value());
   EXPECT_TRUE(font_rendering.FindBool("subpixelRendering").has_value());
   EXPECT_TRUE(font_rendering.FindBool("subpixelPositioning").has_value());
@@ -1029,6 +1030,7 @@ TEST(HeliumPersonaPrefsTest, SaveFontRenderingProfileNormalizesStorageShape) {
       *state.FindList("fontRenderingProfiles"), "custom-minimal-rendering");
   ASSERT_TRUE(profile);
   EXPECT_EQ("Minimal rendering", *profile->FindString("displayName"));
+  EXPECT_EQ(1, profile->FindInt("profileVersion").value_or(0));
   EXPECT_EQ("Skia", *profile->FindString("engine"));
   EXPECT_EQ("slight", *profile->FindString("hinting"));
   EXPECT_FALSE(profile->FindBool("antialiasing").value_or(true));
diff --git a/components/helium_persona/persona_presets.cc b/components/helium_persona/persona_presets.cc
index 31e0bba..1ef4643 100644
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -277,6 +277,7 @@ base::Value::Dict MakeFontRendering(std::string_view id,
   base::Value::Dict rendering;
   rendering.Set("id", id);
   rendering.Set("displayName", display_name);
+  rendering.Set("profileVersion", 1);
   rendering.Set("engine", engine);
   rendering.Set("hinting", hinting);
   rendering.Set("antialiasing", antialiasing);
