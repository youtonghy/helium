diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index 75aef5ab..fc296a28 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -897,6 +897,7 @@ blink::HeliumPersonaSnapshot ToBlinkPersonaSnapshot(
       source.webgpu_max_compute_workgroup_size_z;
   target.webgpu_max_compute_workgroups_per_dimension =
       source.webgpu_max_compute_workgroups_per_dimension;
+  target.webgpu_features = source.webgpu_features;
   target.screen_width = source.screen_width;
   target.screen_height = source.screen_height;
   target.avail_width = source.avail_width;
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.html b/chrome/browser/resources/settings/privacy_page/persona_page.html
index cc828853..ccbdbd9a 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html
@@ -252,6 +252,9 @@
         value="{{editablePersona_.gpu.webgpuAdapter}}"></cr-input>
     <cr-input label="GPU driver"
         value="{{editablePersona_.gpu.driver}}"></cr-input>
+    <cr-input class="wide" label="WebGPU features"
+        value="{{webgpuFeaturesText_}}"
+        on-change="onWebGPUFeaturesChanged_"></cr-input>
     <cr-input label="WebGL max texture size"
         value="{{editablePersona_.gpu.webglProfile.maxTextureSize}}"
         type="number"></cr-input>
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 3b6499e9..32e30569 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -38,6 +38,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       uaChFormFactorsText_: String,
       fontFamiliesText_: String,
       fontAliasesText_: String,
+      webgpuFeaturesText_: String,
     };
   }
 
@@ -55,6 +56,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
   declare private uaChFormFactorsText_: string;
   declare private fontFamiliesText_: string;
   declare private fontAliasesText_: string;
+  declare private webgpuFeaturesText_: string;
 
   private browserProxy_ = HeliumPersonaBrowserProxyImpl.getInstance();
 
@@ -102,6 +104,10 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
         .join(', ');
     this.fontAliasesText_ = ((fonts['aliases'] as string[]|undefined) || [])
         .join(', ');
+
+    const gpu = this.editablePersona_.gpu || {};
+    this.webgpuFeaturesText_ =
+        ((gpu['webgpuFeatures'] as string[]|undefined) || []).join(', ');
   }
 
   private stringListFromInput_(value: string): string[] {
@@ -165,12 +171,19 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
         this.stringListFromInput_(this.fontAliasesText_));
   }
 
+  private onWebGPUFeaturesChanged_() {
+    this.set(
+        'editablePersona_.gpu.webgpuFeatures',
+        this.stringListFromInput_(this.webgpuFeaturesText_));
+  }
+
   private flushTextFieldsToEditablePersona_() {
     this.onNavigatorLanguagesChanged_();
     this.onUaChBrandsChanged_();
     this.onUaChFormFactorsChanged_();
     this.onFontFamiliesChanged_();
     this.onFontAliasesChanged_();
+    this.onWebGPUFeaturesChanged_();
   }
 
   private formatRegion_(persona: PersonaRecord): string {
@@ -453,6 +466,9 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       maxComputeWorkgroupSizeZ: 64,
       maxComputeWorkgroupsPerDimension: 65535,
     };
+    persona.gpu['webgpuFeatures'] = persona.gpu['webgpuFeatures'] || [
+      'texture-compression-bc',
+    ];
     persona.hardware = persona.hardware || {};
     persona.screen = persona.screen || {};
     persona.fonts = persona.fonts || {};
@@ -569,6 +585,10 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       'maxComputeWorkgroupSizeY', 'maxComputeWorkgroupSizeZ',
       'maxComputeWorkgroupsPerDimension',
     ]);
+    persona.gpu['webgpuFeatures'] =
+        (persona.gpu['webgpuFeatures'] as string[] || [])
+            .map(feature => String(feature).trim())
+            .filter(feature => feature.length > 0);
     persona.hardware['hardwareConcurrency'] =
         toNumber(persona.hardware['hardwareConcurrency']);
     persona.hardware['deviceMemory'] =
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index 58e8bdb4..daa228b2 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -192,6 +192,12 @@ base::Value::Dict MakeDefaultWebGPUProfile() {
   return profile;
 }
 
+base::Value::List MakeDefaultWebGPUFeatures() {
+  base::Value::List features;
+  features.Append("texture-compression-bc");
+  return features;
+}
+
 void EnsurePersonaCompatibilityFields(base::Value::Dict& persona) {
   const std::string* noise_seed = persona.FindString("noiseSeed");
   if (!noise_seed || noise_seed->empty()) {
@@ -215,6 +221,9 @@ void EnsurePersonaCompatibilityFields(base::Value::Dict& persona) {
   if (!gpu->FindDict("webgpuProfile")) {
     gpu->Set("webgpuProfile", MakeDefaultWebGPUProfile());
   }
+  if (!gpu->FindList("webgpuFeatures")) {
+    gpu->Set("webgpuFeatures", MakeDefaultWebGPUFeatures());
+  }
 }
 
 base::Value::Dict MakeEditablePersona(base::Value::Dict persona) {
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index ca9e41dd..81d7b4c1 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -87,6 +87,10 @@ TEST(HeliumPersonaPrefsTest, BuiltinPresetsContainCompleteSystemImage) {
   ASSERT_TRUE(gpu->FindDict("webglProfile"));
   ASSERT_TRUE(gpu->FindDict("webgl2Profile"));
   ASSERT_TRUE(gpu->FindDict("webgpuProfile"));
+  const base::Value::List* webgpu_features = gpu->FindList("webgpuFeatures");
+  ASSERT_TRUE(webgpu_features);
+  ASSERT_FALSE(webgpu_features->empty());
+  EXPECT_EQ("texture-compression-bc", (*webgpu_features)[0].GetString());
   EXPECT_EQ(16384,
             gpu->FindDict("webglProfile")->FindInt("maxTextureSize")
                 .value_or(0));
@@ -370,6 +374,9 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   webgpu_profile->Set("maxTextureDimension2D", 512);
   webgpu_profile->Set("maxBufferSize", 65536);
   webgpu_profile->Set("maxBindGroups", 2);
+  base::Value::List invalid_webgpu_features;
+  invalid_webgpu_features.Append("real-hardware-only-feature");
+  gpu->Set("webgpuFeatures", std::move(invalid_webgpu_features));
   base::Value::Dict* network = persona.FindDict("network");
   ASSERT_TRUE(network);
   network->Set("type", "none");
@@ -390,6 +397,8 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "sample count"));
   EXPECT_TRUE(WarningsContain(warnings, "anisotropy"));
   EXPECT_TRUE(WarningsContain(warnings, "WebGPU maxBufferSize"));
+  EXPECT_TRUE(WarningsContain(warnings, "WebGPU maxBindGroups"));
+  EXPECT_TRUE(WarningsContain(warnings, "unknown feature name"));
   EXPECT_TRUE(WarningsContain(warnings, "Network RTT and downlink"));
   EXPECT_TRUE(WarningsContain(warnings, "Network type none"));
   EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
@@ -431,6 +440,8 @@ TEST(HeliumPersonaPrefsTest, SnapshotRoundTripsCompleteFields) {
   EXPECT_EQ(8192, snapshot.webgpu_max_texture_dimension_2d);
   EXPECT_EQ(65535, snapshot.webgpu_max_compute_workgroups_per_dimension);
   EXPECT_EQ(268435456, snapshot.webgpu_max_buffer_size);
+  ASSERT_EQ(1u, snapshot.webgpu_features.size());
+  EXPECT_EQ("texture-compression-bc", snapshot.webgpu_features[0]);
 
   base::Value::Dict value = PersonaSnapshotToValue(snapshot);
   EXPECT_TRUE(PersonaHasRequiredFields(value));
@@ -443,6 +454,7 @@ TEST(HeliumPersonaPrefsTest, SnapshotRoundTripsCompleteFields) {
   ASSERT_TRUE(value.FindDict("fontRendering"));
   ASSERT_TRUE(value.FindDict("gpu")->FindDict("webglProfile"));
   ASSERT_TRUE(value.FindDict("gpu")->FindDict("webgpuProfile"));
+  ASSERT_TRUE(value.FindDict("gpu")->FindList("webgpuFeatures"));
   EXPECT_EQ(16384,
             value.FindDict("gpu")
                 ->FindDict("webglProfile")
@@ -556,6 +568,10 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   webgpu_profile->Set("maxComputeWorkgroupSizeY", 128);
   webgpu_profile->Set("maxComputeWorkgroupSizeZ", 32);
   webgpu_profile->Set("maxComputeWorkgroupsPerDimension", 32768);
+  base::Value::List manual_webgpu_features;
+  manual_webgpu_features.Append("timestamp-query");
+  manual_webgpu_features.Append("texture-compression-bc");
+  gpu->Set("webgpuFeatures", std::move(manual_webgpu_features));
 
   base::Value::Dict* screen = persona.FindDict("screen");
   ASSERT_TRUE(screen);
@@ -666,6 +682,8 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
                            ->FindDict("webgpuProfile")
                            ->FindInt("maxBufferSize")
                            .value_or(0));
+  ASSERT_TRUE(runtime.FindDict("gpu")->FindList("webgpuFeatures"));
+  EXPECT_EQ(2u, runtime.FindDict("gpu")->FindList("webgpuFeatures")->size());
   EXPECT_EQ(3000, runtime.FindDict("screen")->FindInt("width").value_or(0));
   EXPECT_EQ(6, runtime.FindDict("hardware")
                    ->FindInt("hardwareConcurrency")
@@ -729,6 +747,9 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_EQ(4096, snapshot.webgl_max_texture_size);
   EXPECT_EQ(512, snapshot.webgl2_max_array_texture_layers);
   EXPECT_EQ(134217728, snapshot.webgpu_max_buffer_size);
+  ASSERT_EQ(2u, snapshot.webgpu_features.size());
+  EXPECT_EQ("timestamp-query", snapshot.webgpu_features[0]);
+  EXPECT_EQ("texture-compression-bc", snapshot.webgpu_features[1]);
   EXPECT_EQ("manual-font-pack", snapshot.font_pack_id);
   EXPECT_EQ("manual-font-rendering", snapshot.font_rendering_id);
   EXPECT_FALSE(snapshot.client_hints_enabled);
@@ -759,4 +780,4 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_TRUE(snapshot.allow_web_printing);
 }
 
-}  // namespace helium_persona
\ No newline at end of file
+}  // namespace helium_persona
diff --git a/components/helium_persona/persona_presets.cc b/components/helium_persona/persona_presets.cc
index 562601ff..949e30b9 100644
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -143,6 +143,12 @@ base::Value::Dict MakeWebGPUProfile(int max_texture_dimension_2d,
   return profile;
 }
 
+base::Value::List MakeDesktopWebGPUFeatures() {
+  base::Value::List features;
+  features.Append("texture-compression-bc");
+  return features;
+}
+
 base::Value::Dict MakeGpu(std::string_view vendor,
                           std::string_view renderer,
                           std::string_view webgpu_adapter,
@@ -161,6 +167,7 @@ base::Value::Dict MakeGpu(std::string_view vendor,
   gpu.Set("webgl2Profile", MakeWebGL2Profile(2048, 2048, 8, max_samples));
   gpu.Set("webgpuProfile", MakeWebGPUProfile(8192, 2048, max_buffer_size,
                                              256));
+  gpu.Set("webgpuFeatures", MakeDesktopWebGPUFeatures());
   return gpu;
 }
 
diff --git a/components/helium_persona/persona_snapshot.cc b/components/helium_persona/persona_snapshot.cc
index e5bc2e5c..8331831d 100644
--- a/components/helium_persona/persona_snapshot.cc
+++ b/components/helium_persona/persona_snapshot.cc
@@ -274,6 +274,7 @@ PersonaSnapshot MakePersonaSnapshot(const base::Value::Dict& persona) {
       persona, "gpu", "webgpuProfile", "maxComputeWorkgroupSizeZ");
   snapshot.webgpu_max_compute_workgroups_per_dimension = GetNestedProfileInt(
       persona, "gpu", "webgpuProfile", "maxComputeWorkgroupsPerDimension");
+  snapshot.webgpu_features = GetStringList(persona, "gpu", "webgpuFeatures");
 
   snapshot.screen_width = GetNestedInt(persona, "screen", "width");
   snapshot.screen_height = GetNestedInt(persona, "screen", "height");
@@ -539,6 +540,7 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot) {
   webgpu_profile.Set("maxComputeWorkgroupsPerDimension",
                      snapshot.webgpu_max_compute_workgroups_per_dimension);
   gpu.Set("webgpuProfile", std::move(webgpu_profile));
+  gpu.Set("webgpuFeatures", MakeStringList(snapshot.webgpu_features));
   value.Set("gpu", std::move(gpu));
 
   base::Value::Dict screen;
diff --git a/components/helium_persona/persona_snapshot.h b/components/helium_persona/persona_snapshot.h
index 849939b8..daa58cee 100644
--- a/components/helium_persona/persona_snapshot.h
+++ b/components/helium_persona/persona_snapshot.h
@@ -96,6 +96,7 @@ struct COMPONENT_EXPORT(HELIUM_PERSONA) PersonaSnapshot {
   int webgpu_max_compute_workgroup_size_y = 0;
   int webgpu_max_compute_workgroup_size_z = 0;
   int webgpu_max_compute_workgroups_per_dimension = 0;
+  std::vector<std::string> webgpu_features;
 
   int screen_width = 0;
   int screen_height = 0;
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 75a9d6a8..e76d4a09 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -261,6 +261,16 @@ bool IsDeviceMemoryBucket(double value) {
   return false;
 }
 
+bool IsKnownWebGPUFeature(std::string_view feature) {
+  return EqualsInsensitive(feature, "depth32float-stencil8") ||
+         EqualsInsensitive(feature, "texture-compression-bc") ||
+         EqualsInsensitive(feature, "texture-compression-etc2") ||
+         EqualsInsensitive(feature, "texture-compression-astc") ||
+         EqualsInsensitive(feature, "timestamp-query") ||
+         EqualsInsensitive(feature, "texture-formats-tier1") ||
+         EqualsInsensitive(feature, "dual-source-blending");
+}
+
 void AddWarning(base::Value::List& warnings, std::string_view warning) {
   warnings.Append(std::string(warning));
 }
@@ -312,6 +322,7 @@ bool PersonaHasRequiredFields(const base::Value::Dict& persona) {
                                      "maxTextureDimension2D") &&
          HasNestedProfilePositiveInt(persona, "gpu", "webgpuProfile",
                                      "maxBufferSize") &&
+         HasNestedStringList(persona, "gpu", "webgpuFeatures") &&
          HasNestedPositiveInt(persona, "screen", "width") &&
          HasNestedPositiveInt(persona, "screen", "height") &&
          HasNestedPositiveInt(persona, "screen", "availWidth") &&
@@ -398,6 +409,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedProfileInt(persona, "gpu", "webgpuProfile", "maxBufferSize");
   const int webgpu_max_bind_groups =
       GetNestedProfileInt(persona, "gpu", "webgpuProfile", "maxBindGroups");
+  const std::vector<std::string> webgpu_features =
+      GetNestedStringList(persona, "gpu", "webgpuFeatures");
   const int width = GetNestedInt(persona, "screen", "width");
   const int height = GetNestedInt(persona, "screen", "height");
   const int avail_width = GetNestedInt(persona, "screen", "availWidth");
@@ -593,6 +606,13 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "WebGPU maxBindGroups should usually be at least 4.");
   }
+  for (const std::string& feature : webgpu_features) {
+    if (!IsKnownWebGPUFeature(feature)) {
+      AddWarning(warnings,
+                 "WebGPU feature list contains an unknown feature name.");
+      break;
+    }
+  }
 
   if (Contains(locale, "zh") && !Contains(timezone, "asia")) {
     AddWarning(warnings,
diff --git a/content/browser/helium_noise/noise_token_data.cc b/content/browser/helium_noise/noise_token_data.cc
index 20b05f53..14ed858c 100644
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -175,6 +175,7 @@ std::string PersonaNoiseSalt(
   AppendSaltPart(salt, persona_snapshot.webgpu_max_compute_workgroup_size_z);
   AppendSaltPart(salt,
                  persona_snapshot.webgpu_max_compute_workgroups_per_dimension);
+  AppendSaltList(salt, persona_snapshot.webgpu_features);
 
   AppendSaltPart(salt, persona_snapshot.screen_width);
   AppendSaltPart(salt, persona_snapshot.screen_height);
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot.h b/third_party/blink/public/common/helium_persona/persona_snapshot.h
index 641a750a..18916611 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -96,6 +96,7 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
   int webgpu_max_compute_workgroup_size_y = 0;
   int webgpu_max_compute_workgroup_size_z = 0;
   int webgpu_max_compute_workgroups_per_dimension = 0;
+  std::vector<std::string> webgpu_features;
 
   int screen_width = 0;
   int screen_height = 0;
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
index f4001d11..c1acc50f 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
@@ -315,6 +315,10 @@ struct BLINK_COMMON_EXPORT
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.webgpu_max_compute_workgroups_per_dimension;
   }
+  static const std::vector<std::string>& webgpu_features(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.webgpu_features;
+  }
   static int screen_width(const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.screen_width;
   }
@@ -687,6 +691,9 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
       data.webgpu_max_compute_workgroup_size_z();
   out->webgpu_max_compute_workgroups_per_dimension =
       data.webgpu_max_compute_workgroups_per_dimension();
+  if (!data.ReadWebgpuFeatures(&out->webgpu_features)) {
+    return false;
+  }
   out->font_antialiasing = data.font_antialiasing();
   out->font_subpixel_rendering = data.font_subpixel_rendering();
   out->font_subpixel_positioning = data.font_subpixel_positioning();
diff --git a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
index 2349b9a7..26fc5d09 100644
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
@@ -86,6 +86,7 @@ struct HeliumPersonaSnapshot {
   int32 webgpu_max_compute_workgroup_size_y;
   int32 webgpu_max_compute_workgroup_size_z;
   int32 webgpu_max_compute_workgroups_per_dimension;
+  array<string> webgpu_features;
 
   int32 screen_width;
   int32 screen_height;
diff --git a/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc b/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc
index f9b91bbb..cd454bf5 100644
--- a/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc
+++ b/third_party/blink/renderer/modules/webgpu/gpu_adapter.cc
@@ -4,6 +4,8 @@
 
 #include "third_party/blink/renderer/modules/webgpu/gpu_adapter.h"
 
+#include <optional>
+
 #include "services/metrics/public/cpp/ukm_builders.h"
 #include "third_party/blink/renderer/bindings/core/v8/script_promise_resolver.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_object_builder.h"
@@ -32,6 +34,50 @@ namespace blink {
 
 namespace {
 
+std::optional<V8GPUFeatureName::Enum> PersonaWebGPUFeatureEnum(
+    const String& feature) {
+  if (DeprecatedEqualIgnoringCase(feature, "depth32float-stencil8")) {
+    return V8GPUFeatureName::Enum::kDepth32FloatStencil8;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-compression-bc")) {
+    return V8GPUFeatureName::Enum::kTextureCompressionBc;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-compression-etc2")) {
+    return V8GPUFeatureName::Enum::kTextureCompressionEtc2;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-compression-astc")) {
+    return V8GPUFeatureName::Enum::kTextureCompressionAstc;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "timestamp-query")) {
+    return V8GPUFeatureName::Enum::kTimestampQuery;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-formats-tier1")) {
+    return V8GPUFeatureName::Enum::kTextureFormatsTier1;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "dual-source-blending")) {
+    return V8GPUFeatureName::Enum::kDualSourceBlending;
+  }
+  return std::nullopt;
+}
+
+GPUSupportedFeatures* MakePersonaFeatureNameSet(
+    const ExecutionContext* execution_context) {
+  GPUSupportedFeatures* features = MakeGarbageCollected<GPUSupportedFeatures>();
+  const HeliumPersonaSnapshot* snapshot =
+      PersonaSpoofingHelper::Get(execution_context);
+  if (!snapshot) {
+    return features;
+  }
+  for (const std::string& feature_name : snapshot->webgpu_features) {
+    auto feature_name_enum =
+        PersonaWebGPUFeatureEnum(String::FromUtf8(feature_name));
+    if (feature_name_enum) {
+      features->AddFeatureName(V8GPUFeatureName(feature_name_enum.value()));
+    }
+  }
+  return features;
+}
+
 GPUSupportedFeatures* MakeFeatureNameSet(wgpu::Adapter adapter) {
   GPUSupportedFeatures* features = MakeGarbageCollected<GPUSupportedFeatures>();
   DCHECK(features->FeatureNameSet().empty());
@@ -131,7 +177,7 @@ GPUAdapter::GPUAdapter(
   const bool use_persona_profile =
       PersonaSpoofingHelper::IsEnabled(execution_context);
   if (use_persona_profile) {
-    features_ = MakeGarbageCollected<GPUSupportedFeatures>();
+    features_ = MakePersonaFeatureNameSet(execution_context);
   } else {
     features_ = MakeFeatureNameSet(GetHandle());
   }
diff --git a/third_party/blink/renderer/modules/webgpu/gpu_device.cc b/third_party/blink/renderer/modules/webgpu/gpu_device.cc
index 5bf3048d..a8c7bfe3 100644
--- a/third_party/blink/renderer/modules/webgpu/gpu_device.cc
+++ b/third_party/blink/renderer/modules/webgpu/gpu_device.cc
@@ -55,6 +55,50 @@ namespace blink {
 
 namespace {
 
+std::optional<V8GPUFeatureName::Enum> PersonaWebGPUFeatureEnum(
+    const String& feature) {
+  if (DeprecatedEqualIgnoringCase(feature, "depth32float-stencil8")) {
+    return V8GPUFeatureName::Enum::kDepth32FloatStencil8;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-compression-bc")) {
+    return V8GPUFeatureName::Enum::kTextureCompressionBc;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-compression-etc2")) {
+    return V8GPUFeatureName::Enum::kTextureCompressionEtc2;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-compression-astc")) {
+    return V8GPUFeatureName::Enum::kTextureCompressionAstc;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "timestamp-query")) {
+    return V8GPUFeatureName::Enum::kTimestampQuery;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "texture-formats-tier1")) {
+    return V8GPUFeatureName::Enum::kTextureFormatsTier1;
+  }
+  if (DeprecatedEqualIgnoringCase(feature, "dual-source-blending")) {
+    return V8GPUFeatureName::Enum::kDualSourceBlending;
+  }
+  return std::nullopt;
+}
+
+GPUSupportedFeatures* MakePersonaFeatureNameSet(
+    const ExecutionContext* execution_context) {
+  GPUSupportedFeatures* features = MakeGarbageCollected<GPUSupportedFeatures>();
+  const HeliumPersonaSnapshot* snapshot =
+      PersonaSpoofingHelper::Get(execution_context);
+  if (!snapshot) {
+    return features;
+  }
+  for (const std::string& feature_name : snapshot->webgpu_features) {
+    auto feature_name_enum =
+        PersonaWebGPUFeatureEnum(String::FromUtf8(feature_name));
+    if (feature_name_enum) {
+      features->AddFeatureName(V8GPUFeatureName(feature_name_enum.value()));
+    }
+  }
+  return features;
+}
+
 std::optional<V8GPUFeatureName::Enum> RequiredFeatureForTextureFormat(
     V8GPUTextureFormat::Enum format) {
   switch (format) {
@@ -190,7 +234,7 @@ void GPUDevice::Initialize(wgpu::Device handle,
   const bool use_persona_profile =
       PersonaSpoofingHelper::IsEnabled(GetExecutionContext());
   if (use_persona_profile) {
-    features_ = MakeGarbageCollected<GPUSupportedFeatures>();
+    features_ = MakePersonaFeatureNameSet(GetExecutionContext());
   } else {
     wgpu::SupportedFeatures features;
     GetHandle().GetFeatures(&features);
