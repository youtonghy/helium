diff --git a/third_party/blink/renderer/modules/direct_sockets/socket.cc b/third_party/blink/renderer/modules/direct_sockets/socket.cc
index 52a9fbe5..65fb69cf 100644
--- a/third_party/blink/renderer/modules/direct_sockets/socket.cc
+++ b/third_party/blink/renderer/modules/direct_sockets/socket.cc
@@ -16,6 +16,7 @@
 #include "third_party/blink/renderer/bindings/core/v8/script_promise_resolver.h"
 #include "third_party/blink/renderer/core/dom/dom_exception.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/platform/bindings/exception_code.h"
 #include "third_party/blink/renderer/platform/bindings/exception_state.h"
 #include "third_party/blink/renderer/platform/scheduler/public/scheduling_policy.h"
@@ -91,6 +92,13 @@ bool Socket::CheckContextAndPermissions(ScriptState* script_state,
   }
 
   ExecutionContext* execution_context = ExecutionContext::From(script_state);
+  if (PersonaSpoofingHelper::IsEnabled(execution_context)) {
+    exception_state.ThrowDOMException(
+        DOMExceptionCode::kNotAllowedError,
+        "Direct Sockets are unavailable for the active browser persona.");
+    return false;
+  }
+
   if (execution_context->IsIsolatedContext()) {
     if (!execution_context->CrossOriginIsolatedCapability()) {
       exception_state.ThrowDOMException(
diff --git a/third_party/blink/renderer/modules/nfc/DEPS b/third_party/blink/renderer/modules/nfc/DEPS
index ccf3037a..c557062e 100644
--- a/third_party/blink/renderer/modules/nfc/DEPS
+++ b/third_party/blink/renderer/modules/nfc/DEPS
@@ -1,6 +1,7 @@
 include_rules = [
     "+services/device/public/mojom/nfc.mojom-blink.h",
     "+services/device/public/mojom/nfc.mojom-blink-forward.h",
+    "+third_party/blink/renderer/core/helium_persona",
 ]
 
 specific_include_rules = {
diff --git a/third_party/blink/renderer/modules/nfc/ndef_reader.cc b/third_party/blink/renderer/modules/nfc/ndef_reader.cc
index 7911eb1b..aaaa068a 100644
--- a/third_party/blink/renderer/modules/nfc/ndef_reader.cc
+++ b/third_party/blink/renderer/modules/nfc/ndef_reader.cc
@@ -19,6 +19,7 @@
 #include "third_party/blink/renderer/core/dom/scoped_abort_state.h"
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/inspector/console_message.h"
 #include "third_party/blink/renderer/modules/event_target_modules.h"
 #include "third_party/blink/renderer/modules/nfc/ndef_message.h"
@@ -77,6 +78,9 @@ constexpr char kNotSupportedOrPermissionDenied[] =
 constexpr char kChildFrameErrorMessage[] =
     "Web NFC can only be accessed in a top-level browsing context.";
 
+constexpr char kPersonaNfcUnavailable[] =
+    "Web NFC is unavailable for the active browser persona.";
+
 }  // namespace
 
 class NDEFReader::ReadAbortAlgorithm final : public AbortSignal::Algorithm {
@@ -173,6 +177,11 @@ ScriptPromise<IDLUndefined> NDEFReader::scan(ScriptState* script_state,
                                       kChildFrameErrorMessage);
     return EmptyPromise();
   }
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+    exception_state.ThrowDOMException(DOMExceptionCode::kNotAllowedError,
+                                      kPersonaNfcUnavailable);
+    return EmptyPromise();
+  }
 
   if (scan_signal_ && scan_abort_handle_) {
     scan_signal_->RemoveAlgorithm(scan_abort_handle_);
@@ -314,6 +323,11 @@ ScriptPromise<IDLUndefined> NDEFReader::write(
                                       kChildFrameErrorMessage);
     return EmptyPromise();
   }
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+    exception_state.ThrowDOMException(DOMExceptionCode::kNotAllowedError,
+                                      kPersonaNfcUnavailable);
+    return EmptyPromise();
+  }
 
   std::unique_ptr<ScopedAbortState> scoped_abort_state = nullptr;
   if (auto* signal = options->getSignalOr(nullptr)) {
@@ -438,6 +452,11 @@ ScriptPromise<IDLUndefined> NDEFReader::makeReadOnly(
                                       kChildFrameErrorMessage);
     return EmptyPromise();
   }
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+    exception_state.ThrowDOMException(DOMExceptionCode::kNotAllowedError,
+                                      kPersonaNfcUnavailable);
+    return EmptyPromise();
+  }
 
   std::unique_ptr<ScopedAbortState> scoped_abort_state = nullptr;
   if (auto* signal = options->getSignalOr(nullptr)) {
diff --git a/third_party/blink/renderer/modules/permissions/DEPS b/third_party/blink/renderer/modules/permissions/DEPS
index dadedbdc..fbc10a85 100644
--- a/third_party/blink/renderer/modules/permissions/DEPS
+++ b/third_party/blink/renderer/modules/permissions/DEPS
@@ -1,4 +1,5 @@
 include_rules = [
   "+mojo/public/cpp/bindings",
   "+services/service_manager/public/cpp",
+  "+third_party/blink/renderer/core/helium_persona",
 ]
diff --git a/third_party/blink/renderer/modules/permissions/permission_status_listener.cc b/third_party/blink/renderer/modules/permissions/permission_status_listener.cc
index 76d4cfc2..6eb625e5 100644
--- a/third_party/blink/renderer/modules/permissions/permission_status_listener.cc
+++ b/third_party/blink/renderer/modules/permissions/permission_status_listener.cc
@@ -8,6 +8,7 @@
 #include "mojo/public/cpp/bindings/remote.h"
 #include "third_party/blink/public/mojom/permissions/permission_status.mojom-blink.h"
 #include "third_party/blink/renderer/bindings/core/v8/v8_permission_state.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context_lifecycle_observer.h"
 #include "third_party/blink/renderer/modules/permissions/permissions.h"
 
@@ -34,6 +35,9 @@ PermissionStatusListener::PermissionStatusListener(
 PermissionStatusListener::~PermissionStatusListener() = default;
 
 void PermissionStatusListener::StartListening() {
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    return;
+
   DCHECK(!receiver_.is_bound());
   mojo::PendingRemote<mojom::blink::PermissionObserver> observer;
   scoped_refptr<base::SingleThreadTaskRunner> task_runner =
@@ -54,6 +58,9 @@ void PermissionStatusListener::StopListening() {
 void PermissionStatusListener::NotifyEventListener(
     const AtomicString& event_type,
     bool is_added) {
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    return;
+
   scoped_refptr<base::SingleThreadTaskRunner> task_runner =
       GetExecutionContext()->GetTaskRunner(TaskType::kPermission);
 
diff --git a/third_party/blink/renderer/modules/permissions/permissions.cc b/third_party/blink/renderer/modules/permissions/permissions.cc
index 7dfb5807..98611659 100644
--- a/third_party/blink/renderer/modules/permissions/permissions.cc
+++ b/third_party/blink/renderer/modules/permissions/permissions.cc
@@ -22,6 +22,7 @@
 #include "third_party/blink/renderer/core/frame/frame.h"
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/page/page.h"
 #include "third_party/blink/renderer/modules/permissions/permission_status.h"
 #include "third_party/blink/renderer/modules/permissions/permission_utils.h"
@@ -51,6 +52,40 @@ bool ArePermissionDescriptorsEquivalent(
   }
   return left.extension->Equals(*right.extension);
 }
+
+bool ShouldDenyForPersona(const mojom::blink::PermissionDescriptor& descriptor) {
+  switch (descriptor.name) {
+    case mojom::blink::PermissionName::GEOLOCATION:
+    case mojom::blink::PermissionName::GEOLOCATION_APPROXIMATE:
+    case mojom::blink::PermissionName::MIDI:
+    case mojom::blink::PermissionName::AUDIO_CAPTURE:
+    case mojom::blink::PermissionName::VIDEO_CAPTURE:
+    case mojom::blink::PermissionName::SENSORS:
+    case mojom::blink::PermissionName::NFC:
+    case mojom::blink::PermissionName::WINDOW_MANAGEMENT:
+    case mojom::blink::PermissionName::LOCAL_FONTS:
+    case mojom::blink::PermissionName::DISPLAY_CAPTURE:
+    case mojom::blink::PermissionName::CAPTURED_SURFACE_CONTROL:
+    case mojom::blink::PermissionName::SPEAKER_SELECTION:
+    case mojom::blink::PermissionName::KEYBOARD_LOCK:
+    case mojom::blink::PermissionName::POINTER_LOCK:
+    case mojom::blink::PermissionName::LOCAL_NETWORK_ACCESS:
+    case mojom::blink::PermissionName::LOCAL_NETWORK:
+    case mojom::blink::PermissionName::LOOPBACK_NETWORK:
+    case mojom::blink::PermissionName::VR:
+    case mojom::blink::PermissionName::AR:
+    case mojom::blink::PermissionName::HAND_TRACKING:
+    case mojom::blink::PermissionName::SMART_CARD:
+      return true;
+    default:
+      return false;
+  }
+}
+
+mojom::blink::PermissionStatusWithDetailsPtr PersonaDeniedStatus() {
+  return mojom::blink::PermissionStatusWithDetails::New(
+      mojom::blink::PermissionStatus::DENIED, nullptr);
+}
 }  // namespace
 
 bool ArePermissionDescriptorsEquivalentForTesting(
@@ -119,6 +154,12 @@ ScriptPromise<PermissionStatus> Permissions::query(
           script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
 
+  if (PersonaSpoofingHelper::IsEnabled(context) &&
+      ShouldDenyForPersona(*descriptor)) {
+    TaskComplete(resolver, std::move(descriptor), PersonaDeniedStatus());
+    return promise;
+  }
+
   // If the current origin is a file scheme, it will unlikely return a
   // meaningful value because most APIs are broken on file scheme and no
   // permission prompt will be shown even if the returned permission will most
@@ -149,8 +190,13 @@ ScriptPromise<PermissionStatus> Permissions::request(
           script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
 
-  PermissionDescriptorPtr descriptor_copy = descriptor->Clone();
+  if (PersonaSpoofingHelper::IsEnabled(context) &&
+      ShouldDenyForPersona(*descriptor)) {
+    TaskComplete(resolver, std::move(descriptor), PersonaDeniedStatus());
+    return promise;
+  }
 
+  PermissionDescriptorPtr descriptor_copy = descriptor->Clone();
   GetService(context)->RequestPermission(
       std::move(descriptor),
       BindOnce(&Permissions::VerifyPermissionAndReturnStatus,
@@ -173,6 +219,11 @@ ScriptPromise<PermissionStatus> Permissions::revoke(
           script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
 
+  if (PersonaSpoofingHelper::IsEnabled(ExecutionContext::From(script_state)) &&
+      ShouldDenyForPersona(*descriptor)) {
+    TaskComplete(resolver, std::move(descriptor), PersonaDeniedStatus());
+    return promise;
+  }
   PermissionDescriptorPtr descriptor_copy = descriptor->Clone();
   GetService(ExecutionContext::From(script_state))
       ->RevokePermission(
@@ -227,6 +278,26 @@ ScriptPromise<IDLSequence<PermissionStatus>> Permissions::requestAll(
   for (const auto& descriptor : internal_permissions)
     internal_permissions_copy.push_back(descriptor->Clone());
 
+  bool all_persona_denied = true;
+  for (const auto& descriptor : internal_permissions) {
+    if (!ShouldDenyForPersona(*descriptor)) {
+      all_persona_denied = false;
+      break;
+    }
+  }
+  if (PersonaSpoofingHelper::IsEnabled(context) && all_persona_denied) {
+    HeapVector<Member<PermissionStatus>> result;
+    result.ReserveInitialCapacity(caller_index_to_internal_index.size());
+    for (int internal_index : caller_index_to_internal_index) {
+      PermissionStatusListener* listener = GetOrCreatePermissionStatusListener(
+          PersonaDeniedStatus(), internal_permissions[internal_index]->Clone());
+      if (listener)
+        result.push_back(PermissionStatus::Take(listener, resolver));
+    }
+    resolver->Resolve(result);
+    return promise;
+  }
+
   GetService(context)->RequestPermissions(
       std::move(internal_permissions),
       BindOnce(
