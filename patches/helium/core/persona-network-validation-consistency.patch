diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 54e1775..19c5552 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -520,6 +520,8 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "high dynamic range"));
   EXPECT_TRUE(WarningsContain(warnings, "Network RTT and downlink"));
   EXPECT_TRUE(WarningsContain(warnings, "Network type none"));
+  EXPECT_TRUE(WarningsContain(warnings, "navigator.onLine"));
+  EXPECT_TRUE(WarningsContain(warnings, "effectiveType 4g"));
   EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
 }
 
@@ -539,6 +541,22 @@ TEST(HeliumPersonaPrefsTest, ValidationRequiresManualTouchFields) {
   EXPECT_TRUE(WarningsContain(warnings, "missing one or more required fields"));
 }
 
+TEST(HeliumPersonaPrefsTest, ValidationRequiresNetworkBooleanFields) {
+  sync_preferences::TestingPrefServiceSyncable prefs;
+  RegisterTestPrefs(prefs);
+
+  base::Value::Dict persona =
+      ClonePresetIntoProfile(prefs, "preset-windows-11-en-us-new-york");
+  base::Value::Dict* network = persona.FindDict("network");
+  ASSERT_TRUE(network);
+  network->Remove("saveData");
+  network->Remove("online");
+
+  EXPECT_FALSE(PersonaHasRequiredFields(persona));
+  base::Value::List warnings = ValidatePersona(persona);
+  EXPECT_TRUE(WarningsContain(warnings, "missing one or more required fields"));
+}
+
 TEST(HeliumPersonaPrefsTest, SnapshotRoundTripsCompleteFields) {
   sync_preferences::TestingPrefServiceSyncable prefs;
   RegisterTestPrefs(prefs);
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 0010274..7b31898 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -348,6 +348,8 @@ bool PersonaHasRequiredFields(const base::Value::Dict& persona) {
          HasNestedInt(persona, "network", "rtt") &&
          HasNestedDoubleValue(persona, "network", "downlink") &&
          HasNestedDoubleValue(persona, "network", "downlinkMax") &&
+         HasNestedBool(persona, "network", "saveData") &&
+         HasNestedBool(persona, "network", "online") &&
          HasNestedString(persona, "gpu", "vendor") &&
          HasNestedString(persona, "gpu", "renderer") &&
          HasNestedString(persona, "gpu", "driver") &&
@@ -550,6 +551,22 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "Network type none should usually pair with a very slow effectiveType.");
   }
+  if (EqualsInsensitive(network_effective_type, "4g") &&
+      (network_rtt > 300 || network_downlink < 1.0)) {
+    AddWarning(warnings,
+               "Network effectiveType 4g should usually pair with lower RTT and at least 1 Mbps downlink.");
+  }
+  if ((EqualsInsensitive(network_effective_type, "2g") ||
+       EqualsInsensitive(network_effective_type, "slow-2g")) &&
+      network_downlink > 1.5) {
+    AddWarning(warnings,
+               "Network effectiveType 2g should usually pair with low downlink.");
+  }
+  if (EqualsInsensitive(network_effective_type, "3g") &&
+      (network_rtt > 600 || network_downlink > 8.0)) {
+    AddWarning(warnings,
+               "Network effectiveType 3g should usually pair with moderate RTT and downlink.");
+  }
 
   if (Contains(os, "windows") &&
       (!Contains(ua, "windows nt") || !Contains(platform, "win"))) {
