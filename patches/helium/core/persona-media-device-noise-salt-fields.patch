diff --git a/content/browser/helium_noise/noise_token_data.cc b/content/browser/helium_noise/noise_token_data.cc
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -217,6 +217,28 @@ std::string PersonaNoiseSalt(
   AppendSaltPart(salt, persona_snapshot.touch_enabled);
   AppendSaltPart(salt, persona_snapshot.max_touch_points);
 
+  AppendSaltPart(salt, persona_snapshot.media_audio_input_device_id);
+  AppendSaltPart(salt, persona_snapshot.media_video_input_device_id);
+  AppendSaltPart(salt, persona_snapshot.media_audio_output_device_id);
+  AppendSaltPart(salt, persona_snapshot.media_input_group_id);
+  AppendSaltPart(salt, persona_snapshot.media_output_group_id);
+  AppendSaltPart(salt, persona_snapshot.media_audio_input_label);
+  AppendSaltPart(salt, persona_snapshot.media_video_input_label);
+  AppendSaltPart(salt, persona_snapshot.media_audio_output_label);
+  AppendSaltPart(salt, persona_snapshot.media_video_width);
+  AppendSaltPart(salt, persona_snapshot.media_video_height);
+  AppendSaltPart(salt, persona_snapshot.media_video_frame_rate);
+  AppendSaltPart(salt, persona_snapshot.media_audio_sample_rate);
+  AppendSaltPart(salt, persona_snapshot.media_audio_sample_size);
+  AppendSaltPart(salt, persona_snapshot.media_audio_channel_count);
+  AppendSaltPart(salt, persona_snapshot.media_audio_latency);
+  AppendSaltPart(salt, persona_snapshot.media_audio_base_latency);
+  AppendSaltPart(salt, persona_snapshot.media_audio_output_latency);
+  AppendSaltPart(salt, persona_snapshot.media_echo_cancellation);
+  AppendSaltPart(salt, persona_snapshot.media_auto_gain_control);
+  AppendSaltPart(salt, persona_snapshot.media_noise_suppression);
+  AppendSaltPart(salt, persona_snapshot.media_voice_isolation);
+
   AppendSaltPart(salt, persona_snapshot.font_pack_id);
   AppendSaltPart(salt, persona_snapshot.font_pack_name);
   AppendSaltList(salt, persona_snapshot.font_families);
