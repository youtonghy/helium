diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -1765,13 +1765,20 @@
         memory >= 8 ? [64, 96, 120, 160, 256] : [20, 40, 64, 80];
     const voiceProfiles = this.speechVoiceProfiles_(persona);
     const voiceProfile = this.randomFrom_(voiceProfiles, voiceProfiles[0]);
-    Object.assign(persona.advanced, {
+    const chance = (percent: number): boolean => {
+      return this.randomInt_(1, 100) <= percent;
+    };
+    const memoryRich = memory >= 8;
+    const protectedPresentation = chance(1);
+    const balancedPresentation = chance(3);
+    const compatiblePresentation = chance(6);
+    const protectedProfile: Record<string, boolean> = {
       clientHintsEnabled: true,
-      reduceSystemInfo: false,
+      reduceSystemInfo: chance(8),
       canvasNoise: true,
       audioNoise: true,
       fontMetricNoise: true,
-      pdfViewerEnabled: true,
+      pdfViewerEnabled: chance(98),
       cookieEnabled: true,
       speechVoiceLocalService: true,
       speechVoiceDefault: true,
@@ -1780,14 +1787,14 @@
       virtualMediaDevices: true,
       allowDisplayCapture: false,
       allowDeviceApis: false,
-      allowGamepads: false,
+      allowGamepads: chance(1),
       allowSensorApis: false,
       allowMidi: false,
       allowLocalNetworkApis: false,
       allowWebNfc: false,
       allowWindowManagement: false,
-      allowPresentationApis: false,
-      allowRemotePlayback: false,
+      allowPresentationApis: protectedPresentation,
+      allowRemotePlayback: protectedPresentation && chance(40),
       allowWebXr: false,
       allowPlatformCredentials: false,
       allowKeyboardLayout: false,
@@ -1796,33 +1803,99 @@
       allowPersistentDeviceId: false,
       allowInstalledApps: false,
       allowManagedDevice: false,
-      allowNotifications: false,
+      allowNotifications: chance(1),
       allowProtectedMediaIdentifier: false,
-      allowPersistentStorage: false,
-      allowBackgroundSync: false,
-      allowClipboard: false,
+      allowPersistentStorage: memoryRich && chance(2),
+      allowBackgroundSync: chance(3),
+      allowClipboard: chance(1),
       allowPaymentHandler: false,
       allowIdleDetection: false,
-      allowWakeLock: false,
-      allowStorageAccess: false,
-      allowPointerLock: false,
-      allowFullscreen: false,
-      allowWebAppInstallation: false,
-      allowWebPrinting: false,
+      allowWakeLock: chance(1),
+      allowStorageAccess: chance(3),
+      allowPointerLock: chance(1),
+      allowFullscreen: chance(10),
+      allowWebAppInstallation: chance(1),
+      allowWebPrinting: chance(2),
       allowFileSystemAccess: false,
-      allowWebLocks: false,
+      allowWebLocks: chance(2),
       allowContacts: false,
-      allowBadging: false,
-      allowWebShare: false,
+      allowBadging: chance(2),
+      allowWebShare: chance(1),
       allowEyeDropper: false,
-      allowMediaSession: false,
-      allowPictureInPicture: false,
+      allowMediaSession: chance(20),
+      allowPictureInPicture: chance(12),
       allowWebOtp: false,
       allowShapeDetection: false,
       allowWebSockets: true,
-      allowWebTransport: true,
+      allowWebTransport: chance(80),
+    };
+    const balancedProfile: Record<string, boolean> = {
+      ...protectedProfile,
+      reduceSystemInfo: chance(15),
+      pdfViewerEnabled: chance(97),
+      allowGamepads: chance(3),
+      allowPresentationApis: balancedPresentation,
+      allowRemotePlayback: balancedPresentation && chance(55),
+      allowNotifications: chance(4),
+      allowPersistentStorage: memoryRich && chance(7),
+      allowBackgroundSync: chance(8),
+      allowClipboard: chance(4),
+      allowIdleDetection: chance(1),
+      allowWakeLock: chance(3),
+      allowStorageAccess: chance(8),
+      allowPointerLock: chance(3),
+      allowFullscreen: chance(20),
+      allowWebAppInstallation: chance(3),
+      allowWebPrinting: chance(4),
+      allowWebLocks: chance(5),
+      allowBadging: chance(6),
+      allowWebShare: chance(5),
+      allowEyeDropper: chance(1),
+      allowMediaSession: chance(65),
+      allowPictureInPicture: chance(45),
+      allowWebOtp: chance(1),
+      allowShapeDetection: chance(2),
+      allowWebSockets: chance(98),
+      allowWebTransport: chance(90),
+    };
+    const compatibleProfile: Record<string, boolean> = {
+      ...balancedProfile,
+      reduceSystemInfo: false,
+      pdfViewerEnabled: true,
+      allowGamepads: chance(8),
+      allowWindowManagement: chance(3),
+      allowPresentationApis: compatiblePresentation,
+      allowRemotePlayback: compatiblePresentation && chance(60),
+      allowKeyboardLayout: chance(3),
+      allowNotifications: chance(10),
+      allowPersistentStorage: memoryRich && chance(14),
+      allowBackgroundSync: chance(18),
+      allowClipboard: chance(8),
+      allowPaymentHandler: chance(2),
+      allowIdleDetection: chance(2),
+      allowWakeLock: chance(6),
+      allowStorageAccess: chance(14),
+      allowPointerLock: chance(7),
+      allowFullscreen: chance(35),
+      allowWebAppInstallation: chance(5),
+      allowWebPrinting: chance(8),
+      allowWebLocks: chance(8),
+      allowBadging: chance(14),
+      allowWebShare: chance(12),
+      allowEyeDropper: chance(3),
+      allowMediaSession: true,
+      allowPictureInPicture: chance(70),
+      allowWebOtp: chance(2),
+      allowShapeDetection: chance(4),
+      allowWebSockets: true,
+      allowWebTransport: chance(96),
+    };
+    const roll = this.randomInt_(1, 100);
+    const profile = roll <= 55 ? protectedProfile :
+        roll <= 88 ? balancedProfile : compatibleProfile;
+    Object.assign(persona.advanced, profile, {
       storageQuotaGb: this.randomFrom_(quotaProfiles, quotaProfiles[0]),
-      doNotTrackEnabled: this.randomInt_(1, 100) <= 4,
+      doNotTrackEnabled: chance(4),
     }, voiceProfile);
   }

