diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 75f9b2c1..aaccc610 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -239,6 +239,9 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const touchEnabled = Boolean(hardware['touchEnabled']);
     const maxTouchPoints = this.numberValue_(hardware['maxTouchPoints']);
     const storageQuota = this.numberValue_(advanced['storageQuotaGb']);
+    const allowDeviceApis = Boolean(advanced['allowDeviceApis']);
+    const allowPresentationApis = Boolean(advanced['allowPresentationApis']);
+    const allowRemotePlayback = Boolean(advanced['allowRemotePlayback']);
     const speechVoiceName = String(advanced['speechVoiceName'] || '');
     const speechVoiceLang = String(advanced['speechVoiceLang'] || '');
     const speechVoiceUri = String(advanced['speechVoiceUri'] || '');
@@ -555,6 +559,16 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       this.addDraftWarning_(
           warnings,
           'Storage quota above 2 TB is unusual for desktop browser profiles.');
+    }
+    if (allowDeviceApis) {
+      this.addDraftWarning_(
+          warnings,
+          'allowDeviceApis exposes real Bluetooth, WebUSB, Web Serial, WebHID, and Smart Card service bindings.');
+    }
+    if (!allowPresentationApis && allowRemotePlayback) {
+      this.addDraftWarning_(
+          warnings,
+          'Remote Playback reuses PresentationService; disabling Presentation API does not fully isolate that browser service while Remote Playback is allowed.');
     }
     if (!touchEnabled && maxTouchPoints > 0) {
       this.addDraftWarning_(
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 7343a272..6287a72f 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -828,6 +828,8 @@ TEST(HeliumPersonaPrefsTest, ValidationFlagsInconsistentPersona) {
   base::Value::Dict* advanced = persona.FindDict("advanced");
   ASSERT_TRUE(advanced);
   advanced->Set("storageQuotaGb", 1.0);
+  advanced->Set("allowDeviceApis", true);
+  advanced->Set("allowRemotePlayback", true);
   advanced->Set("speechVoiceName", "Samantha");
   advanced->Set("speechVoiceLang", "en-US");
   advanced->Set("speechVoiceUri", "");
@@ -886,6 +887,8 @@ TEST(HeliumPersonaPrefsTest, ValidationFlagsInconsistentPersona) {
   EXPECT_TRUE(WarningsContain(warnings, "voice name and URI"));
   EXPECT_TRUE(WarningsContain(warnings, "deviceMemory should use"));
   EXPECT_TRUE(WarningsContain(warnings, "Storage quota is too small"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowDeviceApis exposes real Bluetooth"));
+  EXPECT_TRUE(WarningsContain(warnings, "Remote Playback reuses"));
   EXPECT_TRUE(WarningsContain(warnings, "maxTouchPoints should be 0"));
   EXPECT_TRUE(WarningsContain(warnings, "CoreText font rendering"));
   EXPECT_TRUE(WarningsContain(warnings, "UA-CH fullVersion major"));
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 0f3a3f75..259a4484 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -681,6 +681,12 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedInt(persona, "hardware", "maxTouchPoints");
   const double storage_quota_gb =
       GetNestedDouble(persona, "advanced", "storageQuotaGb");
+  const bool allow_device_apis =
+      GetNestedBool(persona, "advanced", "allowDeviceApis");
+  const bool allow_presentation_apis =
+      GetNestedBool(persona, "advanced", "allowPresentationApis");
+  const bool allow_remote_playback =
+      GetNestedBool(persona, "advanced", "allowRemotePlayback");
   const std::string_view speech_voice_name =
       GetNestedString(persona, "advanced", "speechVoiceName");
   const std::string_view speech_voice_lang =
@@ -1058,6 +1064,14 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
   if (storage_quota_gb > 2048) {
     AddWarning(warnings,
                "Storage quota above 2 TB is unusual for desktop browser profiles.");
+  }
+  if (allow_device_apis) {
+    AddWarning(warnings,
+               "allowDeviceApis exposes real Bluetooth, WebUSB, Web Serial, WebHID, and Smart Card service bindings.");
+  }
+  if (!allow_presentation_apis && allow_remote_playback) {
+    AddWarning(warnings,
+               "Remote Playback reuses PresentationService; disabling Presentation API does not fully isolate that browser service while Remote Playback is allowed.");
   }
 
   if (!touch_enabled && max_touch_points > 0) {
diff --git a/content/browser/renderer_host/render_frame_host_impl.cc b/content/browser/renderer_host/render_frame_host_impl.cc
index 854d9c06..8cc30c56 100644
--- a/content/browser/renderer_host/render_frame_host_impl.cc
+++ b/content/browser/renderer_host/render_frame_host_impl.cc
@@ -14224,6 +14224,12 @@ void RenderFrameHostImpl::CreateSecurePaymentConfirmationService(
 
 void RenderFrameHostImpl::CreateWebUsbService(
     mojo::PendingReceiver<blink::mojom::WebUsbService> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_device_apis) {
+    return;
+  }
+
   if (!base::FeatureList::IsEnabled(features::kWebUsb)) {
     return;
   }
@@ -14651,6 +14657,12 @@ void RenderFrameHostImpl::BindNFCReceiver(
 
 void RenderFrameHostImpl::BindSerialService(
     mojo::PendingReceiver<blink::mojom::SerialService> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_device_apis) {
+    return;
+  }
+
   if (!IsFeatureEnabled(network::mojom::PermissionsPolicyFeature::kSerial)) {
     mojo::ReportBadMessage("Permissions policy blocks access to Serial.");
     return;
@@ -14685,6 +14697,12 @@ void RenderFrameHostImpl::BindModelContextHost(
 #if !BUILDFLAG(IS_ANDROID)
 void RenderFrameHostImpl::GetHidService(
     mojo::PendingReceiver<blink::mojom::HidService> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_device_apis) {
+    return;
+  }
+
   HidService::Create(this, std::move(receiver));
 }
 #endif
@@ -14692,6 +14710,12 @@ void RenderFrameHostImpl::GetHidService(
 #if BUILDFLAG(IS_CHROMEOS)
 void RenderFrameHostImpl::GetSmartCardService(
     mojo::PendingReceiver<blink::mojom::SmartCardService> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_device_apis) {
+    return;
+  }
+
   SmartCardService::Create(this, std::move(receiver));
 }
 #endif
@@ -14722,6 +14746,14 @@ void RenderFrameHostImpl::BindIdleManager(
 
 void RenderFrameHostImpl::GetPresentationService(
     mojo::PendingReceiver<blink::mojom::PresentationService> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() &&
+      !persona_snapshot.allow_presentation_apis &&
+      !persona_snapshot.allow_remote_playback) {
+    return;
+  }
+
   if (!presentation_service_) {
     presentation_service_ = PresentationServiceImpl::Create(this);
   }
@@ -14745,6 +14777,12 @@ void RenderFrameHostImpl::GetSpeechSynthesis(
 
 void RenderFrameHostImpl::GetSensorProvider(
     mojo::PendingReceiver<blink::mojom::WebSensorProvider> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_sensor_apis) {
+    return;
+  }
+
   FrameSensorProviderProxy::GetOrCreateForCurrentDocument(this)->Bind(
       std::move(receiver));
 }
