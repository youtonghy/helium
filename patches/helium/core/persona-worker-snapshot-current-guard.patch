diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot.h b/third_party/blink/public/common/helium_persona/persona_snapshot.h
index fa8d3e2..7df7cd1 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -16,6 +16,8 @@ struct BLINK_COMMON_EXPORT HeliumPersonaUaChBrand {
 struct BLINK_COMMON_EXPORT HeliumPersonaUaChBrand {
   std::string brand;
   std::string version;
+  bool operator==(const HeliumPersonaUaChBrand&) const = default;
+
 };
 
 struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
@@ -241,6 +243,8 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
   bool allow_speech_synthesis = false;
   bool allow_handwriting_recognition = false;
 
+  bool operator==(const HeliumPersonaSnapshot&) const = default;
+
   bool IsEnabled() const { return enabled && !passthrough; }
 };
 
diff --git a/third_party/blink/renderer/core/exported/web_view_impl.cc b/third_party/blink/renderer/core/exported/web_view_impl.cc
index ec79c7c..4e701e2 100644
--- a/third_party/blink/renderer/core/exported/web_view_impl.cc
+++ b/third_party/blink/renderer/core/exported/web_view_impl.cc
@@ -642,7 +642,8 @@ void UpdateHeliumPersonaWorkerNoiseTokensIfCurrent(
 
 void UpdateHeliumPersonaWorkerSnapshot(WorkerThread* worker_thread,
                                        std::string origin,
-                                       HeliumPersonaSnapshot snapshot) {
+                                       HeliumPersonaSnapshot old_snapshot,
+                                       HeliumPersonaSnapshot new_snapshot) {
   DCHECK(worker_thread->IsCurrentThread());
   WorkerOrWorkletGlobalScope* global_scope = worker_thread->GlobalScope();
   if (!global_scope) {
@@ -653,8 +654,10 @@ void UpdateHeliumPersonaWorkerSnapshot(WorkerThread* worker_thread,
     return;
   }
 
-  global_scope->SetHeliumPersonaSnapshot(snapshot);
-  PersonaRuntimeOverride::ApplyGlobal(global_scope, snapshot);
+  if (global_scope->GetHeliumPersonaSnapshot() == old_snapshot) {
+    global_scope->SetHeliumPersonaSnapshot(new_snapshot);
+    PersonaRuntimeOverride::ApplyGlobal(global_scope, new_snapshot);
+  }
 }
 
 void MaybePreloadSystemFonts(Page* page) {
@@ -3734,6 +3737,7 @@ void WebViewImpl::UpdateHeliumPersonaNoiseTokens(
 
 void WebViewImpl::UpdateHeliumPersonaSnapshot(
     const HeliumPersonaSnapshot& snapshot) {
+  const HeliumPersonaSnapshot old_snapshot = page_->GetHeliumPersonaSnapshot();
   page_->SetHeliumPersonaSnapshot(snapshot);
   UpdateHeliumPersonaExecutionContexts(page_, nullptr, &snapshot);
   std::string origin;
@@ -3745,7 +3749,7 @@ void WebViewImpl::UpdateHeliumPersonaSnapshot(
   }
   WorkerThread::CallOnAllWorkerThreads(&UpdateHeliumPersonaWorkerSnapshot,
                                        TaskType::kInternalDefault, origin,
-                                       snapshot);
+                                       old_snapshot, snapshot);
   ApplyHeliumPersonaRuntimeOverride(snapshot);
   ApplyHeliumPersonaInputDeviceSettings(page_.Get(), snapshot);
   ApplyHeliumPersonaMediaPreferenceSettings(this, snapshot,
