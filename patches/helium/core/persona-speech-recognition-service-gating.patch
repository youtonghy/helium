diff --git a/chrome/browser/chrome_browser_interface_binders.cc b/chrome/browser/chrome_browser_interface_binders.cc
index 4b96a44c..15d701e0 100644
--- a/chrome/browser/chrome_browser_interface_binders.cc
+++ b/chrome/browser/chrome_browser_interface_binders.cc
@@ -301,6 +301,13 @@ bool HeliumPersonaAllowsPayment(content::RenderFrameHost* render_frame_host) {
   return !snapshot.IsEnabled() || snapshot.allow_payment_handler;
 }
 
+bool HeliumPersonaAllowsMediaCapture(
+    content::RenderFrameHost* render_frame_host) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshot(render_frame_host);
+  return !snapshot.IsEnabled() || snapshot.allow_media_capture;
+}
+
 bool HeliumPersonaAllowsWebAppInstallation(
     content::RenderFrameHost* render_frame_host) {
   blink::HeliumPersonaSnapshot snapshot =
@@ -330,6 +337,9 @@ void BindNetworkHintsHandler(
 void BindSpeechRecognitionContextHandler(
     content::RenderFrameHost* frame_host,
     mojo::PendingReceiver<media::mojom::SpeechRecognitionContext> receiver) {
+  if (!HeliumPersonaAllowsMediaCapture(frame_host)) {
+    return;
+  }
   if (!captions::IsLiveCaptionFeatureSupported()) {
     return;
   }
@@ -351,6 +361,9 @@ void BindSpeechRecognitionClientBrowserInterfaceHandler(
     content::RenderFrameHost* frame_host,
     mojo::PendingReceiver<media::mojom::SpeechRecognitionClientBrowserInterface>
         receiver) {
+  if (!HeliumPersonaAllowsMediaCapture(frame_host)) {
+    return;
+  }
   if (captions::IsLiveCaptionFeatureSupported()) {
     // Bind in this process.
     Profile* profile = Profile::FromBrowserContext(
@@ -364,6 +377,9 @@ void BindSpeechRecognitionRecognizerClientHandler(
     content::RenderFrameHost* frame_host,
     mojo::PendingReceiver<media::mojom::SpeechRecognitionRecognizerClient>
         client_receiver) {
+  if (!HeliumPersonaAllowsMediaCapture(frame_host)) {
+    return;
+  }
   Profile* profile = Profile::FromBrowserContext(
       frame_host->GetProcess()->GetBrowserContext());
   PrefService* profile_prefs = profile->GetPrefs();
@@ -380,6 +396,9 @@ void BindMediaFoundationRendererNotifierHandler(
     content::RenderFrameHost* frame_host,
     mojo::PendingReceiver<media::mojom::MediaFoundationRendererNotifier>
         receiver) {
+  if (!HeliumPersonaAllowsMediaCapture(frame_host)) {
+    return;
+  }
   if (captions::IsLiveCaptionFeatureSupported()) {
     captions::LiveCaptionUnavailabilityNotifier::Create(frame_host,
                                                         std::move(receiver));
@@ -391,6 +410,9 @@ void BindMediaFoundationRendererNotifierHandler(
 void BindOnDeviceSpeechRecognitionHandler(
     content::RenderFrameHost* frame_host,
     mojo::PendingReceiver<media::mojom::OnDeviceSpeechRecognition> receiver) {
+  if (!HeliumPersonaAllowsMediaCapture(frame_host)) {
+    return;
+  }
   speech::OnDeviceSpeechRecognitionImpl::GetOrCreateForCurrentDocument(
       frame_host)
       ->Bind(std::move(receiver));
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 8aa933c4..9a2b7f54 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -239,6 +239,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const touchEnabled = Boolean(hardware['touchEnabled']);
     const maxTouchPoints = this.numberValue_(hardware['maxTouchPoints']);
     const storageQuota = this.numberValue_(advanced['storageQuotaGb']);
+    const allowMediaCapture = Boolean(advanced['allowMediaCapture']);
     const allowDeviceApis = Boolean(advanced['allowDeviceApis']);
     const allowPresentationApis = Boolean(advanced['allowPresentationApis']);
     const allowRemotePlayback = Boolean(advanced['allowRemotePlayback']);
@@ -560,6 +561,11 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'Storage quota above 2 TB is unusual for desktop browser profiles.');
     }
+    if (allowMediaCapture) {
+      this.addDraftWarning_(
+          warnings,
+          'allowMediaCapture exposes real camera, microphone, screen speech recognition, and on-device speech recognition service bindings.');
+    }
     if (allowDeviceApis) {
       this.addDraftWarning_(
           warnings,
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 968af4c6..15cc6109 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -828,6 +828,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   base::Value::Dict* advanced = persona.FindDict("advanced");
   ASSERT_TRUE(advanced);
   advanced->Set("storageQuotaGb", 1.0);
+  advanced->Set("allowMediaCapture", true);
   advanced->Set("allowDeviceApis", true);
   advanced->Set("allowRemotePlayback", true);
   advanced->Set("speechVoiceName", "Samantha");
@@ -887,6 +888,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "voice name and URI"));
   EXPECT_TRUE(WarningsContain(warnings, "deviceMemory should use"));
   EXPECT_TRUE(WarningsContain(warnings, "Storage quota is too small"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowMediaCapture exposes real camera"));
   EXPECT_TRUE(WarningsContain(warnings, "allowDeviceApis exposes real Bluetooth"));
   EXPECT_TRUE(WarningsContain(warnings, "Remote Playback reuses"));
   EXPECT_TRUE(WarningsContain(warnings, "maxTouchPoints should be 0"));
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 69f1801c..96d50392 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -681,6 +681,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedInt(persona, "hardware", "maxTouchPoints");
   const double storage_quota_gb =
       GetNestedDouble(persona, "advanced", "storageQuotaGb");
+  const bool allow_media_capture =
+      GetNestedBool(persona, "advanced", "allowMediaCapture");
   const bool allow_device_apis =
       GetNestedBool(persona, "advanced", "allowDeviceApis");
   const bool allow_presentation_apis =
@@ -1065,6 +1067,10 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "Storage quota above 2 TB is unusual for desktop browser profiles.");
   }
+  if (allow_media_capture) {
+    AddWarning(warnings,
+               "allowMediaCapture exposes real camera, microphone, screen speech recognition, and on-device speech recognition service bindings.");
+  }
   if (allow_device_apis) {
     AddWarning(warnings,
                "allowDeviceApis exposes real Bluetooth, WebUSB, Web Serial, WebHID, and Smart Card service bindings.");
