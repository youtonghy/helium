diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index 19f48e4..7c9577b 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -949,2 +949,24 @@ blink::HeliumPersonaSnapshot ToBlinkPersonaSnapshot(
   target.font_text_contrast = source.font_text_contrast;
+
+  target.media_audio_input_device_id = source.media_audio_input_device_id;
+  target.media_video_input_device_id = source.media_video_input_device_id;
+  target.media_audio_output_device_id = source.media_audio_output_device_id;
+  target.media_input_group_id = source.media_input_group_id;
+  target.media_output_group_id = source.media_output_group_id;
+  target.media_audio_input_label = source.media_audio_input_label;
+  target.media_video_input_label = source.media_video_input_label;
+  target.media_audio_output_label = source.media_audio_output_label;
+  target.media_video_width = source.media_video_width;
+  target.media_video_height = source.media_video_height;
+  target.media_video_frame_rate = source.media_video_frame_rate;
+  target.media_audio_sample_rate = source.media_audio_sample_rate;
+  target.media_audio_sample_size = source.media_audio_sample_size;
+  target.media_audio_channel_count = source.media_audio_channel_count;
+  target.media_audio_latency = source.media_audio_latency;
+  target.media_audio_base_latency = source.media_audio_base_latency;
+  target.media_audio_output_latency = source.media_audio_output_latency;
+  target.media_echo_cancellation = source.media_echo_cancellation;
+  target.media_auto_gain_control = source.media_auto_gain_control;
+  target.media_noise_suppression = source.media_noise_suppression;
+  target.media_voice_isolation = source.media_voice_isolation;
   target.client_hints_enabled = source.client_hints_enabled;
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index 74c5571..1ff1d81 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -540,2 +540,28 @@ base::Value::Dict MakeDefaultGpuProfile() {
 
+base::Value::Dict MakeDefaultMediaDevicesProfile() {
+  base::Value::Dict media;
+  media.Set("audioInputDeviceId", "persona-audio-input-default");
+  media.Set("videoInputDeviceId", "persona-video-input-default");
+  media.Set("audioOutputDeviceId", "persona-audio-output-default");
+  media.Set("inputGroupId", "persona-media-group-input");
+  media.Set("outputGroupId", "persona-media-group-output");
+  media.Set("audioInputLabel", "Default Microphone");
+  media.Set("videoInputLabel", "Integrated Camera");
+  media.Set("audioOutputLabel", "Default Speakers");
+  media.Set("videoWidth", 1280);
+  media.Set("videoHeight", 720);
+  media.Set("videoFrameRate", 30.0);
+  media.Set("audioSampleRate", 48000);
+  media.Set("audioSampleSize", 16);
+  media.Set("audioChannelCount", 2);
+  media.Set("audioLatency", 0.01);
+  media.Set("audioBaseLatency", 0.01);
+  media.Set("audioOutputLatency", 0.02);
+  media.Set("echoCancellation", true);
+  media.Set("autoGainControl", true);
+  media.Set("noiseSuppression", true);
+  media.Set("voiceIsolation", false);
+  return media;
+}
+
 base::Value::Dict MakeDefaultAdvancedProfile() {
@@ -786,2 +812,42 @@ void EnsureFontPackCompatibilityFields(base::Value::Dict& persona) {
 
+void EnsureMediaDevicesCompatibilityFields(base::Value::Dict& persona) {
+  if (!persona.FindDict("mediaDevices")) {
+    persona.Set("mediaDevices", MakeDefaultMediaDevicesProfile());
+    return;
+  }
+
+  base::Value::Dict* media = persona.FindDict("mediaDevices");
+  EnsureString(*media, "audioInputDeviceId", "persona-audio-input-default");
+  EnsureString(*media, "videoInputDeviceId", "persona-video-input-default");
+  EnsureString(*media, "audioOutputDeviceId", "persona-audio-output-default");
+  EnsureString(*media, "inputGroupId", "persona-media-group-input");
+  EnsureString(*media, "outputGroupId", "persona-media-group-output");
+  EnsureString(*media, "audioInputLabel", "Default Microphone");
+  EnsureString(*media, "videoInputLabel", "Integrated Camera");
+  EnsureString(*media, "audioOutputLabel", "Default Speakers");
+  if (!media->FindInt("videoWidth")) {
+    media->Set("videoWidth", 1280);
+  }
+  if (!media->FindInt("videoHeight")) {
+    media->Set("videoHeight", 720);
+  }
+  EnsureNumeric(*media, "videoFrameRate", 30.0);
+  if (!media->FindInt("audioSampleRate")) {
+    media->Set("audioSampleRate", 48000);
+  }
+  if (!media->FindInt("audioSampleSize")) {
+    media->Set("audioSampleSize", 16);
+  }
+  if (!media->FindInt("audioChannelCount")) {
+    media->Set("audioChannelCount", 2);
+  }
+  EnsureNumeric(*media, "audioLatency", 0.01);
+  EnsureNumeric(*media, "audioBaseLatency", 0.01);
+  EnsureNumeric(*media, "audioOutputLatency", 0.02);
+  EnsureBool(*media, "echoCancellation", true);
+  EnsureBool(*media, "autoGainControl", true);
+  EnsureBool(*media, "noiseSuppression", true);
+  EnsureBool(*media, "voiceIsolation", false);
+}
+
 void EnsureAdvancedCompatibilityFields(base::Value::Dict& persona) {
@@ -999,2 +1065,3 @@ void EnsurePersonaCompatibilityFields(base::Value::Dict& persona) {
   EnsureFontRenderingCompatibilityFields(persona);
+  EnsureMediaDevicesCompatibilityFields(persona);
   EnsureAdvancedCompatibilityFields(persona);
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 3af850e..aa68a50 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -1812,2 +1812,22 @@ TEST(HeliumPersonaPrefsTest, ValidationRequiresIdentityAndRenderingFields) {
 
+TEST(HeliumPersonaPrefsTest, ValidationRequiresMediaDeviceFields) {
+  sync_preferences::TestingPrefServiceSyncable prefs;
+  RegisterTestPrefs(prefs);
+
+  base::Value::Dict persona =
+      ClonePresetIntoProfile(prefs, "preset-windows-11-en-us-new-york");
+  base::Value::Dict* media_devices = persona.FindDict("mediaDevices");
+  ASSERT_TRUE(media_devices);
+  media_devices->Remove("videoInputDeviceId");
+  media_devices->Remove("audioOutputLabel");
+  media_devices->Remove("videoWidth");
+  media_devices->Remove("audioSampleRate");
+  media_devices->Remove("audioLatency");
+  media_devices->Remove("echoCancellation");
+
+  EXPECT_FALSE(PersonaHasRequiredFields(persona));
+  base::Value::List warnings = ValidatePersona(persona);
+  EXPECT_TRUE(WarningsContain(warnings, "missing one or more required fields"));
+}
+
 TEST(HeliumPersonaPrefsTest, ValidationRequiresAdvancedExposedApiFields) {
@@ -1871,2 +1891,26 @@ TEST(HeliumPersonaPrefsTest, SnapshotRoundTripsCompleteFields) {
   EXPECT_EQ(0, snapshot.monochrome_depth);
+  EXPECT_EQ("persona-audio-input-default",
+            snapshot.media_audio_input_device_id);
+  EXPECT_EQ("persona-video-input-default",
+            snapshot.media_video_input_device_id);
+  EXPECT_EQ("persona-audio-output-default",
+            snapshot.media_audio_output_device_id);
+  EXPECT_EQ("persona-media-group-input", snapshot.media_input_group_id);
+  EXPECT_EQ("persona-media-group-output", snapshot.media_output_group_id);
+  EXPECT_EQ("Default Microphone", snapshot.media_audio_input_label);
+  EXPECT_EQ("Integrated Camera", snapshot.media_video_input_label);
+  EXPECT_EQ("Default Speakers", snapshot.media_audio_output_label);
+  EXPECT_EQ(1280, snapshot.media_video_width);
+  EXPECT_EQ(720, snapshot.media_video_height);
+  EXPECT_EQ(30.0, snapshot.media_video_frame_rate);
+  EXPECT_EQ(48000, snapshot.media_audio_sample_rate);
+  EXPECT_EQ(16, snapshot.media_audio_sample_size);
+  EXPECT_EQ(2, snapshot.media_audio_channel_count);
+  EXPECT_EQ(0.01, snapshot.media_audio_latency);
+  EXPECT_EQ(0.01, snapshot.media_audio_base_latency);
+  EXPECT_EQ(0.02, snapshot.media_audio_output_latency);
+  EXPECT_TRUE(snapshot.media_echo_cancellation);
+  EXPECT_TRUE(snapshot.media_auto_gain_control);
+  EXPECT_TRUE(snapshot.media_noise_suppression);
+  EXPECT_FALSE(snapshot.media_voice_isolation);
   ASSERT_EQ(1u, snapshot.webgpu_features.size());
@@ -1896,2 +1940,13 @@ TEST(HeliumPersonaPrefsTest, SnapshotRoundTripsCompleteFields) {
   EXPECT_EQ("standard", *value.FindDict("screen")->FindString("dynamicRange"));
+  ASSERT_TRUE(value.FindDict("mediaDevices"));
+  EXPECT_EQ("persona-video-input-default",
+            *value.FindDict("mediaDevices")->FindString("videoInputDeviceId"));
+  EXPECT_EQ(1280,
+            value.FindDict("mediaDevices")->FindInt("videoWidth").value_or(0));
+  EXPECT_EQ(48000, value.FindDict("mediaDevices")
+                       ->FindInt("audioSampleRate")
+                       .value_or(0));
+  EXPECT_TRUE(value.FindDict("mediaDevices")
+                  ->FindBool("echoCancellation")
+                  .value_or(false));
   EXPECT_TRUE(value.FindDict("advanced")->FindBool("clientHintsEnabled")
diff --git a/components/helium_persona/persona_presets.cc b/components/helium_persona/persona_presets.cc
index b9b7de3..75dd88f 100644
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -293,2 +293,39 @@ base::Value::Dict MakeFontRendering(std::string_view id,
 
+base::Value::Dict MakeMediaDevices(std::string_view audio_input_label,
+                                   std::string_view video_input_label,
+                                   std::string_view audio_output_label,
+                                   int video_width,
+                                   int video_height,
+                                   double video_frame_rate,
+                                   int audio_sample_rate) {
+  base::Value::Dict media;
+  media.Set("audioInputDeviceId", "persona-audio-input-default");
+  media.Set("videoInputDeviceId", "persona-video-input-default");
+  media.Set("audioOutputDeviceId", "persona-audio-output-default");
+  media.Set("inputGroupId", "persona-media-group-input");
+  media.Set("outputGroupId", "persona-media-group-output");
+  media.Set("audioInputLabel", audio_input_label);
+  media.Set("videoInputLabel", video_input_label);
+  media.Set("audioOutputLabel", audio_output_label);
+  media.Set("videoWidth", video_width);
+  media.Set("videoHeight", video_height);
+  media.Set("videoFrameRate", video_frame_rate);
+  media.Set("audioSampleRate", audio_sample_rate);
+  media.Set("audioSampleSize", 16);
+  media.Set("audioChannelCount", 2);
+  media.Set("audioLatency", 0.01);
+  media.Set("audioBaseLatency", 0.01);
+  media.Set("audioOutputLatency", 0.02);
+  media.Set("echoCancellation", true);
+  media.Set("autoGainControl", true);
+  media.Set("noiseSuppression", true);
+  media.Set("voiceIsolation", false);
+  return media;
+}
+
+base::Value::Dict MakeDesktopMediaDevices() {
+  return MakeMediaDevices("Default Microphone", "Integrated Camera",
+                          "Default Speakers", 1280, 720, 30.0, 48000);
+}
+
 base::Value::Dict MakeAdvanced(bool client_hints_enabled,
@@ -572,2 +609,3 @@ base::Value::Dict MakePreset(std::string_view id,
   preset.Set("fontRendering", std::move(font_rendering));
+  preset.Set("mediaDevices", MakeDesktopMediaDevices());
   preset.Set("advanced", std::move(advanced));
diff --git a/components/helium_persona/persona_snapshot.cc b/components/helium_persona/persona_snapshot.cc
index 1869ac7..d297e38 100644
--- a/components/helium_persona/persona_snapshot.cc
+++ b/components/helium_persona/persona_snapshot.cc
@@ -416,2 +416,45 @@ PersonaSnapshot MakePersonaSnapshot(const base::Value::Dict& persona) {
 
+  snapshot.media_audio_input_device_id =
+      GetNestedString(persona, "mediaDevices", "audioInputDeviceId");
+  snapshot.media_video_input_device_id =
+      GetNestedString(persona, "mediaDevices", "videoInputDeviceId");
+  snapshot.media_audio_output_device_id =
+      GetNestedString(persona, "mediaDevices", "audioOutputDeviceId");
+  snapshot.media_input_group_id =
+      GetNestedString(persona, "mediaDevices", "inputGroupId");
+  snapshot.media_output_group_id =
+      GetNestedString(persona, "mediaDevices", "outputGroupId");
+  snapshot.media_audio_input_label =
+      GetNestedString(persona, "mediaDevices", "audioInputLabel");
+  snapshot.media_video_input_label =
+      GetNestedString(persona, "mediaDevices", "videoInputLabel");
+  snapshot.media_audio_output_label =
+      GetNestedString(persona, "mediaDevices", "audioOutputLabel");
+  snapshot.media_video_width =
+      GetNestedInt(persona, "mediaDevices", "videoWidth");
+  snapshot.media_video_height =
+      GetNestedInt(persona, "mediaDevices", "videoHeight");
+  snapshot.media_video_frame_rate =
+      GetNestedDouble(persona, "mediaDevices", "videoFrameRate");
+  snapshot.media_audio_sample_rate =
+      GetNestedInt(persona, "mediaDevices", "audioSampleRate");
+  snapshot.media_audio_sample_size =
+      GetNestedInt(persona, "mediaDevices", "audioSampleSize");
+  snapshot.media_audio_channel_count =
+      GetNestedInt(persona, "mediaDevices", "audioChannelCount");
+  snapshot.media_audio_latency =
+      GetNestedDouble(persona, "mediaDevices", "audioLatency");
+  snapshot.media_audio_base_latency =
+      GetNestedDouble(persona, "mediaDevices", "audioBaseLatency");
+  snapshot.media_audio_output_latency =
+      GetNestedDouble(persona, "mediaDevices", "audioOutputLatency");
+  snapshot.media_echo_cancellation =
+      GetNestedBool(persona, "mediaDevices", "echoCancellation", true);
+  snapshot.media_auto_gain_control =
+      GetNestedBool(persona, "mediaDevices", "autoGainControl", true);
+  snapshot.media_noise_suppression =
+      GetNestedBool(persona, "mediaDevices", "noiseSuppression", true);
+  snapshot.media_voice_isolation =
+      GetNestedBool(persona, "mediaDevices", "voiceIsolation", false);
+
   snapshot.client_hints_enabled =
@@ -737,2 +780,30 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot) {
 
+  base::Value::Dict media_devices;
+  media_devices.Set("audioInputDeviceId",
+                    snapshot.media_audio_input_device_id);
+  media_devices.Set("videoInputDeviceId",
+                    snapshot.media_video_input_device_id);
+  media_devices.Set("audioOutputDeviceId",
+                    snapshot.media_audio_output_device_id);
+  media_devices.Set("inputGroupId", snapshot.media_input_group_id);
+  media_devices.Set("outputGroupId", snapshot.media_output_group_id);
+  media_devices.Set("audioInputLabel", snapshot.media_audio_input_label);
+  media_devices.Set("videoInputLabel", snapshot.media_video_input_label);
+  media_devices.Set("audioOutputLabel", snapshot.media_audio_output_label);
+  media_devices.Set("videoWidth", snapshot.media_video_width);
+  media_devices.Set("videoHeight", snapshot.media_video_height);
+  media_devices.Set("videoFrameRate", snapshot.media_video_frame_rate);
+  media_devices.Set("audioSampleRate", snapshot.media_audio_sample_rate);
+  media_devices.Set("audioSampleSize", snapshot.media_audio_sample_size);
+  media_devices.Set("audioChannelCount", snapshot.media_audio_channel_count);
+  media_devices.Set("audioLatency", snapshot.media_audio_latency);
+  media_devices.Set("audioBaseLatency", snapshot.media_audio_base_latency);
+  media_devices.Set("audioOutputLatency",
+                    snapshot.media_audio_output_latency);
+  media_devices.Set("echoCancellation", snapshot.media_echo_cancellation);
+  media_devices.Set("autoGainControl", snapshot.media_auto_gain_control);
+  media_devices.Set("noiseSuppression", snapshot.media_noise_suppression);
+  media_devices.Set("voiceIsolation", snapshot.media_voice_isolation);
+  value.Set("mediaDevices", std::move(media_devices));
+
   base::Value::Dict advanced;
diff --git a/components/helium_persona/persona_snapshot.h b/components/helium_persona/persona_snapshot.h
index bcd193c..ab26d92 100644
--- a/components/helium_persona/persona_snapshot.h
+++ b/components/helium_persona/persona_snapshot.h
@@ -153,2 +153,24 @@ struct COMPONENT_EXPORT(HELIUM_PERSONA) PersonaSnapshot {
 
+  std::string media_audio_input_device_id;
+  std::string media_video_input_device_id;
+  std::string media_audio_output_device_id;
+  std::string media_input_group_id;
+  std::string media_output_group_id;
+  std::string media_audio_input_label;
+  std::string media_video_input_label;
+  std::string media_audio_output_label;
+  int media_video_width = 0;
+  int media_video_height = 0;
+  double media_video_frame_rate = 0;
+  int media_audio_sample_rate = 0;
+  int media_audio_sample_size = 0;
+  int media_audio_channel_count = 0;
+  double media_audio_latency = 0;
+  double media_audio_base_latency = 0;
+  double media_audio_output_latency = 0;
+  bool media_echo_cancellation = true;
+  bool media_auto_gain_control = true;
+  bool media_noise_suppression = true;
+  bool media_voice_isolation = false;
+
   bool client_hints_enabled = true;
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 734c2ea..dbdd231 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -404,12 +404,17 @@ void AddWarning(base::Value::List& warnings, std::string_view warning) {
 bool PersonaHasRequiredFields(const base::Value::Dict& persona) {
-  const bool has_base_shape =
+  const bool has_identity_shape =
       HasString(persona, "id") && HasString(persona, "name") &&
-      HasString(persona, "device") && HasString(persona, "os") &&
+      HasString(persona, "device") && HasString(persona, "os");
+  if (!has_identity_shape || IsPassthroughPersona(persona)) {
+    return has_identity_shape;
+  }
+
+  const bool has_profile_shape =
       persona.FindDict("uaCh") && persona.FindDict("region") &&
-      persona.FindDict("network") &&
-      persona.FindDict("gpu") && persona.FindDict("screen") &&
-      persona.FindDict("hardware") && persona.FindDict("fonts") &&
-      persona.FindDict("fontRendering") && persona.FindDict("advanced");
-  if (!has_base_shape || IsPassthroughPersona(persona)) {
-    return has_base_shape;
+      persona.FindDict("network") && persona.FindDict("gpu") &&
+      persona.FindDict("screen") && persona.FindDict("hardware") &&
+      persona.FindDict("fonts") && persona.FindDict("fontRendering") &&
+      persona.FindDict("mediaDevices") && persona.FindDict("advanced");
+  if (!has_profile_shape) {
+    return false;
   }
@@ -568,2 +573,24 @@ bool PersonaHasRequiredFields(const base::Value::Dict& persona) {
          HasNestedDoubleValue(persona, "fontRendering", "textContrast") &&
+
+         HasNestedString(persona, "mediaDevices", "audioInputDeviceId") &&
+         HasNestedString(persona, "mediaDevices", "videoInputDeviceId") &&
+         HasNestedString(persona, "mediaDevices", "audioOutputDeviceId") &&
+         HasNestedString(persona, "mediaDevices", "inputGroupId") &&
+         HasNestedString(persona, "mediaDevices", "outputGroupId") &&
+         HasNestedString(persona, "mediaDevices", "audioInputLabel") &&
+         HasNestedString(persona, "mediaDevices", "videoInputLabel") &&
+         HasNestedString(persona, "mediaDevices", "audioOutputLabel") &&
+         HasNestedPositiveInt(persona, "mediaDevices", "videoWidth") &&
+         HasNestedPositiveInt(persona, "mediaDevices", "videoHeight") &&
+         HasNestedPositiveDouble(persona, "mediaDevices", "videoFrameRate") &&
+         HasNestedPositiveInt(persona, "mediaDevices", "audioSampleRate") &&
+         HasNestedPositiveInt(persona, "mediaDevices", "audioSampleSize") &&
+         HasNestedPositiveInt(persona, "mediaDevices", "audioChannelCount") &&
+         HasNestedPositiveDouble(persona, "mediaDevices", "audioLatency") &&
+         HasNestedPositiveDouble(persona, "mediaDevices", "audioBaseLatency") &&
+         HasNestedPositiveDouble(persona, "mediaDevices", "audioOutputLatency") &&
+         HasNestedBool(persona, "mediaDevices", "echoCancellation") &&
+         HasNestedBool(persona, "mediaDevices", "autoGainControl") &&
+         HasNestedBool(persona, "mediaDevices", "noiseSuppression") &&
+         HasNestedBool(persona, "mediaDevices", "voiceIsolation") &&
          HasNestedBool(persona, "advanced", "clientHintsEnabled") &&
@@ -745,2 +772,21 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedDouble(persona, "hardware", "deviceMemory");
+
+  const int media_video_width =
+      GetNestedInt(persona, "mediaDevices", "videoWidth");
+  const int media_video_height =
+      GetNestedInt(persona, "mediaDevices", "videoHeight");
+  const double media_video_frame_rate =
+      GetNestedDouble(persona, "mediaDevices", "videoFrameRate");
+  const int media_audio_sample_rate =
+      GetNestedInt(persona, "mediaDevices", "audioSampleRate");
+  const int media_audio_sample_size =
+      GetNestedInt(persona, "mediaDevices", "audioSampleSize");
+  const int media_audio_channel_count =
+      GetNestedInt(persona, "mediaDevices", "audioChannelCount");
+  const double media_audio_latency =
+      GetNestedDouble(persona, "mediaDevices", "audioLatency");
+  const double media_audio_base_latency =
+      GetNestedDouble(persona, "mediaDevices", "audioBaseLatency");
+  const double media_audio_output_latency =
+      GetNestedDouble(persona, "mediaDevices", "audioOutputLatency");
   const bool touch_enabled =
@@ -1264,2 +1310,51 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
   }
+
+  if (media_video_width > 0 && media_video_height > 0 &&
+      (media_video_width < 640 || media_video_height < 480)) {
+    AddWarning(warnings,
+               "Virtual camera resolution should usually be at least 640x480.");
+  }
+  if (media_video_width > 0 && width > 0 && media_video_width > width) {
+    AddWarning(warnings,
+               "Virtual camera width should usually not exceed the selected screen width.");
+  }
+  if (media_video_height > 0 && height > 0 && media_video_height > height) {
+    AddWarning(warnings,
+               "Virtual camera height should usually not exceed the selected screen height.");
+  }
+  if (media_video_frame_rate > 0 &&
+      (media_video_frame_rate < 15.0 || media_video_frame_rate > 60.0)) {
+    AddWarning(warnings,
+               "Virtual camera frame rate should usually stay between 15 and 60 fps.");
+  }
+  if (media_audio_sample_rate > 0 &&
+      media_audio_sample_rate != 44100 && media_audio_sample_rate != 48000) {
+    AddWarning(warnings,
+               "Virtual microphone sample rate should usually be 44100 or 48000 Hz.");
+  }
+  if (media_audio_sample_size > 0 && media_audio_sample_size != 16 &&
+      media_audio_sample_size != 24 && media_audio_sample_size != 32) {
+    AddWarning(warnings,
+               "Virtual microphone sample size should usually be 16, 24, or 32 bits.");
+  }
+  if (media_audio_channel_count > 0 &&
+      (media_audio_channel_count < 1 || media_audio_channel_count > 2)) {
+    AddWarning(warnings,
+               "Virtual microphone channel count should usually be mono or stereo.");
+  }
+  if (media_audio_latency > 0 &&
+      (media_audio_latency < 0.001 || media_audio_latency > 0.2)) {
+    AddWarning(warnings,
+               "Virtual microphone latency should usually stay between 0.001 and 0.2 seconds.");
+  }
+  if (media_audio_base_latency > 0 &&
+      (media_audio_base_latency < 0.001 || media_audio_base_latency > 0.2)) {
+    AddWarning(warnings,
+               "AudioContext baseLatency should usually stay between 0.001 and 0.2 seconds.");
+  }
+  if (media_audio_output_latency > 0 &&
+      (media_audio_output_latency < 0.001 || media_audio_output_latency > 0.5)) {
+    AddWarning(warnings,
+               "AudioContext outputLatency should usually stay between 0.001 and 0.5 seconds.");
+  }
   if (storage_quota_gb < 0) {
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot.h b/third_party/blink/public/common/helium_persona/persona_snapshot.h
index 8ca687d..87301b0 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -153,2 +153,24 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
 
+  std::string media_audio_input_device_id;
+  std::string media_video_input_device_id;
+  std::string media_audio_output_device_id;
+  std::string media_input_group_id;
+  std::string media_output_group_id;
+  std::string media_audio_input_label;
+  std::string media_video_input_label;
+  std::string media_audio_output_label;
+  int media_video_width = 0;
+  int media_video_height = 0;
+  double media_video_frame_rate = 0;
+  int media_audio_sample_rate = 0;
+  int media_audio_sample_size = 0;
+  int media_audio_channel_count = 0;
+  double media_audio_latency = 0;
+  double media_audio_base_latency = 0;
+  double media_audio_output_latency = 0;
+  bool media_echo_cancellation = true;
+  bool media_auto_gain_control = true;
+  bool media_noise_suppression = true;
+  bool media_voice_isolation = false;
+
   bool client_hints_enabled = true;
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
index 5992bad..7a51d01 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
@@ -498,2 +498,85 @@ struct BLINK_COMMON_EXPORT
   }
+
+  static const std::string& media_audio_input_device_id(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_input_device_id;
+  }
+  static const std::string& media_video_input_device_id(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_video_input_device_id;
+  }
+  static const std::string& media_audio_output_device_id(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_output_device_id;
+  }
+  static const std::string& media_input_group_id(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_input_group_id;
+  }
+  static const std::string& media_output_group_id(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_output_group_id;
+  }
+  static const std::string& media_audio_input_label(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_input_label;
+  }
+  static const std::string& media_video_input_label(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_video_input_label;
+  }
+  static const std::string& media_audio_output_label(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_output_label;
+  }
+  static int media_video_width(const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_video_width;
+  }
+  static int media_video_height(const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_video_height;
+  }
+  static double media_video_frame_rate(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_video_frame_rate;
+  }
+  static int media_audio_sample_rate(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_sample_rate;
+  }
+  static int media_audio_sample_size(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_sample_size;
+  }
+  static int media_audio_channel_count(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_channel_count;
+  }
+  static double media_audio_latency(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_latency;
+  }
+  static double media_audio_base_latency(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_base_latency;
+  }
+  static double media_audio_output_latency(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_audio_output_latency;
+  }
+  static bool media_echo_cancellation(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_echo_cancellation;
+  }
+  static bool media_auto_gain_control(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_auto_gain_control;
+  }
+  static bool media_noise_suppression(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_noise_suppression;
+  }
+  static bool media_voice_isolation(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.media_voice_isolation;
+  }
   static bool client_hints_enabled(
@@ -867,2 +950,16 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
   out->font_text_contrast = data.font_text_contrast();
+
+  out->media_video_width = data.media_video_width();
+  out->media_video_height = data.media_video_height();
+  out->media_video_frame_rate = data.media_video_frame_rate();
+  out->media_audio_sample_rate = data.media_audio_sample_rate();
+  out->media_audio_sample_size = data.media_audio_sample_size();
+  out->media_audio_channel_count = data.media_audio_channel_count();
+  out->media_audio_latency = data.media_audio_latency();
+  out->media_audio_base_latency = data.media_audio_base_latency();
+  out->media_audio_output_latency = data.media_audio_output_latency();
+  out->media_echo_cancellation = data.media_echo_cancellation();
+  out->media_auto_gain_control = data.media_auto_gain_control();
+  out->media_noise_suppression = data.media_noise_suppression();
+  out->media_voice_isolation = data.media_voice_isolation();
   out->client_hints_enabled = data.client_hints_enabled();
@@ -976,2 +1073,13 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
          data.ReadFontRenderingHinting(&out->font_rendering_hinting) &&
+         data.ReadMediaAudioInputDeviceId(
+             &out->media_audio_input_device_id) &&
+         data.ReadMediaVideoInputDeviceId(
+             &out->media_video_input_device_id) &&
+         data.ReadMediaAudioOutputDeviceId(
+             &out->media_audio_output_device_id) &&
+         data.ReadMediaInputGroupId(&out->media_input_group_id) &&
+         data.ReadMediaOutputGroupId(&out->media_output_group_id) &&
+         data.ReadMediaAudioInputLabel(&out->media_audio_input_label) &&
+         data.ReadMediaVideoInputLabel(&out->media_video_input_label) &&
+         data.ReadMediaAudioOutputLabel(&out->media_audio_output_label) &&
          data.ReadSpeechVoiceName(&out->speech_voice_name) &&
diff --git a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
index 6ad4435..ba6039b 100644
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
@@ -143,2 +143,25 @@ struct HeliumPersonaSnapshot {
 
+
+  string media_audio_input_device_id;
+  string media_video_input_device_id;
+  string media_audio_output_device_id;
+  string media_input_group_id;
+  string media_output_group_id;
+  string media_audio_input_label;
+  string media_video_input_label;
+  string media_audio_output_label;
+  int32 media_video_width;
+  int32 media_video_height;
+  double media_video_frame_rate;
+  int32 media_audio_sample_rate;
+  int32 media_audio_sample_size;
+  int32 media_audio_channel_count;
+  double media_audio_latency;
+  double media_audio_base_latency;
+  double media_audio_output_latency;
+  bool media_echo_cancellation;
+  bool media_auto_gain_control;
+  bool media_noise_suppression;
+  bool media_voice_isolation;
+
   bool client_hints_enabled;
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
index 58fa696..b598548 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
@@ -19,2 +19,23 @@ namespace {
 
+constexpr char kDefaultPersonaAudioInputDeviceId[] =
+    "persona-audio-input-default";
+constexpr char kDefaultPersonaVideoInputDeviceId[] =
+    "persona-video-input-default";
+constexpr char kDefaultPersonaAudioOutputDeviceId[] =
+    "persona-audio-output-default";
+constexpr char kDefaultPersonaInputGroupId[] = "persona-media-group-input";
+constexpr char kDefaultPersonaOutputGroupId[] = "persona-media-group-output";
+constexpr char kDefaultPersonaAudioInputLabel[] = "Default Microphone";
+constexpr char kDefaultPersonaVideoInputLabel[] = "Integrated Camera";
+constexpr char kDefaultPersonaAudioOutputLabel[] = "Default Speakers";
+constexpr int kDefaultPersonaVideoWidth = 1280;
+constexpr int kDefaultPersonaVideoHeight = 720;
+constexpr double kDefaultPersonaVideoFrameRate = 30.0;
+constexpr int kDefaultPersonaAudioSampleRate = 48000;
+constexpr int kDefaultPersonaAudioSampleSize = 16;
+constexpr int kDefaultPersonaAudioChannelCount = 2;
+constexpr double kDefaultPersonaAudioLatency = 0.01;
+constexpr double kDefaultPersonaAudioBaseLatency = 0.01;
+constexpr double kDefaultPersonaAudioOutputLatency = 0.02;
+
 String ToBlinkString(const std::string& value) {
@@ -23,2 +44,42 @@ String ToBlinkString(const std::string& value) {
 
+String PersonaStringOrDefault(const ExecutionContext* context,
+                              std::string HeliumPersonaSnapshot::*member,
+                              const char* fallback) {
+  const HeliumPersonaSnapshot* snapshot = PersonaSpoofingHelper::Get(context);
+  if (!snapshot || (snapshot->*member).empty()) {
+    return String(fallback);
+  }
+  return ToBlinkString(snapshot->*member);
+}
+
+int PersonaPositiveIntOrDefault(const ExecutionContext* context,
+                                int HeliumPersonaSnapshot::*member,
+                                int fallback) {
+  const HeliumPersonaSnapshot* snapshot = PersonaSpoofingHelper::Get(context);
+  if (!snapshot || (snapshot->*member) <= 0) {
+    return fallback;
+  }
+  return snapshot->*member;
+}
+
+double PersonaPositiveDoubleOrDefault(const ExecutionContext* context,
+                                      double HeliumPersonaSnapshot::*member,
+                                      double fallback) {
+  const HeliumPersonaSnapshot* snapshot = PersonaSpoofingHelper::Get(context);
+  if (!snapshot || (snapshot->*member) <= 0) {
+    return fallback;
+  }
+  return snapshot->*member;
+}
+
+bool PersonaBoolOrDefault(const ExecutionContext* context,
+                          bool HeliumPersonaSnapshot::*member,
+                          bool fallback) {
+  const HeliumPersonaSnapshot* snapshot = PersonaSpoofingHelper::Get(context);
+  if (!snapshot) {
+    return fallback;
+  }
+  return snapshot->*member;
+}
+
 std::optional<int> PositiveInt(int value) {
@@ -200,2 +261,147 @@ bool PersonaSpoofingHelper::UseVirtualMediaDevices(
 
+String PersonaSpoofingHelper::MediaAudioInputDeviceId(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_input_device_id,
+      kDefaultPersonaAudioInputDeviceId);
+}
+
+String PersonaSpoofingHelper::MediaVideoInputDeviceId(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_video_input_device_id,
+      kDefaultPersonaVideoInputDeviceId);
+}
+
+String PersonaSpoofingHelper::MediaAudioOutputDeviceId(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_output_device_id,
+      kDefaultPersonaAudioOutputDeviceId);
+}
+
+String PersonaSpoofingHelper::MediaInputGroupId(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_input_group_id,
+      kDefaultPersonaInputGroupId);
+}
+
+String PersonaSpoofingHelper::MediaOutputGroupId(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_output_group_id,
+      kDefaultPersonaOutputGroupId);
+}
+
+String PersonaSpoofingHelper::MediaAudioInputLabel(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_input_label,
+      kDefaultPersonaAudioInputLabel);
+}
+
+String PersonaSpoofingHelper::MediaVideoInputLabel(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_video_input_label,
+      kDefaultPersonaVideoInputLabel);
+}
+
+String PersonaSpoofingHelper::MediaAudioOutputLabel(
+    const ExecutionContext* context) {
+  return PersonaStringOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_output_label,
+      kDefaultPersonaAudioOutputLabel);
+}
+
+int PersonaSpoofingHelper::MediaVideoWidth(const ExecutionContext* context) {
+  return PersonaPositiveIntOrDefault(
+      context, &HeliumPersonaSnapshot::media_video_width,
+      kDefaultPersonaVideoWidth);
+}
+
+int PersonaSpoofingHelper::MediaVideoHeight(const ExecutionContext* context) {
+  return PersonaPositiveIntOrDefault(
+      context, &HeliumPersonaSnapshot::media_video_height,
+      kDefaultPersonaVideoHeight);
+}
+
+double PersonaSpoofingHelper::MediaVideoFrameRate(
+    const ExecutionContext* context) {
+  return PersonaPositiveDoubleOrDefault(
+      context, &HeliumPersonaSnapshot::media_video_frame_rate,
+      kDefaultPersonaVideoFrameRate);
+}
+
+int PersonaSpoofingHelper::MediaAudioSampleRate(
+    const ExecutionContext* context) {
+  return PersonaPositiveIntOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_sample_rate,
+      kDefaultPersonaAudioSampleRate);
+}
+
+int PersonaSpoofingHelper::MediaAudioSampleSize(
+    const ExecutionContext* context) {
+  return PersonaPositiveIntOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_sample_size,
+      kDefaultPersonaAudioSampleSize);
+}
+
+int PersonaSpoofingHelper::MediaAudioChannelCount(
+    const ExecutionContext* context) {
+  return PersonaPositiveIntOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_channel_count,
+      kDefaultPersonaAudioChannelCount);
+}
+
+double PersonaSpoofingHelper::MediaAudioLatency(
+    const ExecutionContext* context) {
+  return PersonaPositiveDoubleOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_latency,
+      kDefaultPersonaAudioLatency);
+}
+
+double PersonaSpoofingHelper::MediaAudioBaseLatency(
+    const ExecutionContext* context) {
+  return PersonaPositiveDoubleOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_base_latency,
+      kDefaultPersonaAudioBaseLatency);
+}
+
+double PersonaSpoofingHelper::MediaAudioOutputLatency(
+    const ExecutionContext* context) {
+  return PersonaPositiveDoubleOrDefault(
+      context, &HeliumPersonaSnapshot::media_audio_output_latency,
+      kDefaultPersonaAudioOutputLatency);
+}
+
+bool PersonaSpoofingHelper::MediaEchoCancellation(
+    const ExecutionContext* context) {
+  return PersonaBoolOrDefault(context,
+                              &HeliumPersonaSnapshot::media_echo_cancellation,
+                              true);
+}
+
+bool PersonaSpoofingHelper::MediaAutoGainControl(
+    const ExecutionContext* context) {
+  return PersonaBoolOrDefault(context,
+                              &HeliumPersonaSnapshot::media_auto_gain_control,
+                              true);
+}
+
+bool PersonaSpoofingHelper::MediaNoiseSuppression(
+    const ExecutionContext* context) {
+  return PersonaBoolOrDefault(context,
+                              &HeliumPersonaSnapshot::media_noise_suppression,
+                              true);
+}
+
+bool PersonaSpoofingHelper::MediaVoiceIsolation(
+    const ExecutionContext* context) {
+  return PersonaBoolOrDefault(context,
+                              &HeliumPersonaSnapshot::media_voice_isolation,
+                              false);
+}
+
 bool PersonaSpoofingHelper::ShouldBlockDisplayCapture(
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
index 086aeec..c1b7ea5 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
@@ -37,2 +37,25 @@ class CORE_EXPORT PersonaSpoofingHelper {
   static bool UseVirtualMediaDevices(const ExecutionContext* context);
+
+
+  static String MediaAudioInputDeviceId(const ExecutionContext* context);
+  static String MediaVideoInputDeviceId(const ExecutionContext* context);
+  static String MediaAudioOutputDeviceId(const ExecutionContext* context);
+  static String MediaInputGroupId(const ExecutionContext* context);
+  static String MediaOutputGroupId(const ExecutionContext* context);
+  static String MediaAudioInputLabel(const ExecutionContext* context);
+  static String MediaVideoInputLabel(const ExecutionContext* context);
+  static String MediaAudioOutputLabel(const ExecutionContext* context);
+  static int MediaVideoWidth(const ExecutionContext* context);
+  static int MediaVideoHeight(const ExecutionContext* context);
+  static double MediaVideoFrameRate(const ExecutionContext* context);
+  static int MediaAudioSampleRate(const ExecutionContext* context);
+  static int MediaAudioSampleSize(const ExecutionContext* context);
+  static int MediaAudioChannelCount(const ExecutionContext* context);
+  static double MediaAudioLatency(const ExecutionContext* context);
+  static double MediaAudioBaseLatency(const ExecutionContext* context);
+  static double MediaAudioOutputLatency(const ExecutionContext* context);
+  static bool MediaEchoCancellation(const ExecutionContext* context);
+  static bool MediaAutoGainControl(const ExecutionContext* context);
+  static bool MediaNoiseSuppression(const ExecutionContext* context);
+  static bool MediaVoiceIsolation(const ExecutionContext* context);
   static bool ShouldBlockDisplayCapture(const ExecutionContext* context);
diff --git a/third_party/blink/renderer/modules/mediastream/media_devices.cc b/third_party/blink/renderer/modules/mediastream/media_devices.cc
index 948ef5e..ddfb5d5 100644
--- a/third_party/blink/renderer/modules/mediastream/media_devices.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_devices.cc
@@ -89,11 +89,2 @@ namespace {
 constexpr base::TimeDelta kEnumerateDevicesTimeout = base::Seconds(4);
-constexpr char kPersonaAudioInputDeviceId[] = "persona-audio-input-default";
-constexpr char kPersonaVideoInputDeviceId[] = "persona-video-input-default";
-constexpr char kPersonaAudioOutputDeviceId[] = "persona-audio-output-default";
-constexpr char kPersonaInputGroupId[] = "persona-media-group-input";
-constexpr char kPersonaOutputGroupId[] = "persona-media-group-output";
-constexpr char kPersonaAudioInputLabel[] = "Default Microphone";
-constexpr char kPersonaVideoInputLabel[] = "Integrated Camera";
-constexpr char kPersonaAudioOutputLabel[] = "Default Speakers";
-
 void SendLogMessage(const std::string& message) {
@@ -102,4 +93,5 @@ void SendLogMessage(const std::string& message) {
 
-bool IsPersonaAudioOutputDeviceId(const String& device_id) {
-  return device_id == kPersonaAudioOutputDeviceId;
+bool IsPersonaAudioOutputDeviceId(ExecutionContext* context,
+                                  const String& device_id) {
+  return device_id == PersonaSpoofingHelper::MediaAudioOutputDeviceId(context);
 }
@@ -150,15 +142,18 @@ Vector<Vector<WebMediaDeviceInfo>> MakePersonaMediaDeviceEnumeration(
                   mojom::blink::MediaDeviceType::kMediaAudioInput)]
-      .push_back(WebMediaDeviceInfo(kPersonaAudioInputDeviceId,
-                                    kPersonaAudioInputLabel,
-                                    kPersonaInputGroupId));
+      .push_back(WebMediaDeviceInfo(
+          PersonaSpoofingHelper::MediaAudioInputDeviceId(context),
+          PersonaSpoofingHelper::MediaAudioInputLabel(context),
+          PersonaSpoofingHelper::MediaInputGroupId(context)));
   enumeration[static_cast<wtf_size_t>(
                   mojom::blink::MediaDeviceType::kMediaVideoInput)]
-      .push_back(WebMediaDeviceInfo(kPersonaVideoInputDeviceId,
-                                    kPersonaVideoInputLabel,
-                                    kPersonaInputGroupId));
+      .push_back(WebMediaDeviceInfo(
+          PersonaSpoofingHelper::MediaVideoInputDeviceId(context),
+          PersonaSpoofingHelper::MediaVideoInputLabel(context),
+          PersonaSpoofingHelper::MediaInputGroupId(context)));
   enumeration[static_cast<wtf_size_t>(
                   mojom::blink::MediaDeviceType::kMediaAudioOutput)]
-      .push_back(WebMediaDeviceInfo(kPersonaAudioOutputDeviceId,
-                                    kPersonaAudioOutputLabel,
-                                    kPersonaOutputGroupId));
+      .push_back(WebMediaDeviceInfo(
+          PersonaSpoofingHelper::MediaAudioOutputDeviceId(context),
+          PersonaSpoofingHelper::MediaAudioOutputLabel(context),
+          PersonaSpoofingHelper::MediaOutputGroupId(context)));
   ApplyPersonaMediaDevicePermissionVisibility(enumeration, expose_device_info);
@@ -176,10 +171,10 @@ MakePersonaVideoInputCapabilities(ExecutionContext* context) {
   Vector<media::VideoCaptureFormat> formats;
-  formats.push_back(media::VideoCaptureFormat(gfx::Size(640, 480), 30.0f,
-                                              media::PIXEL_FORMAT_I420));
-  formats.push_back(media::VideoCaptureFormat(gfx::Size(1280, 720), 30.0f,
-                                              media::PIXEL_FORMAT_I420));
-  formats.push_back(media::VideoCaptureFormat(gfx::Size(1920, 1080), 30.0f,
-                                              media::PIXEL_FORMAT_I420));
+  formats.push_back(media::VideoCaptureFormat(
+      gfx::Size(PersonaSpoofingHelper::MediaVideoWidth(context),
+                PersonaSpoofingHelper::MediaVideoHeight(context)),
+      PersonaSpoofingHelper::MediaVideoFrameRate(context),
+      media::PIXEL_FORMAT_I420));
   capabilities.push_back(mojom::blink::VideoInputDeviceCapabilities::New(
-      kPersonaVideoInputDeviceId, kPersonaInputGroupId, control_support,
+      PersonaSpoofingHelper::MediaVideoInputDeviceId(context),
+      PersonaSpoofingHelper::MediaInputGroupId(context), control_support,
       std::move(formats), mojom::blink::FacingMode::kUser,
@@ -196,8 +191,14 @@ MakePersonaAudioInputCapabilities(ExecutionContext* context) {
 
+  const int sample_rate = PersonaSpoofingHelper::MediaAudioSampleRate(context);
+  const int channels = PersonaSpoofingHelper::MediaAudioChannelCount(context);
   media::AudioParameters parameters(
       media::AudioParameters::AUDIO_PCM_LOW_LATENCY,
-      media::ChannelLayoutConfig::Stereo(), 48000, 480);
+      media::ChannelLayoutConfig::Stereo(), sample_rate, sample_rate / 100);
   capabilities.push_back(mojom::blink::AudioInputDeviceCapabilities::New(
-      kPersonaAudioInputDeviceId, kPersonaInputGroupId, parameters,
-      true, 2, 48000, base::Milliseconds(10)));
+      PersonaSpoofingHelper::MediaAudioInputDeviceId(context),
+      PersonaSpoofingHelper::MediaInputGroupId(context), parameters,
+      PersonaSpoofingHelper::MediaEchoCancellation(context), channels,
+      sample_rate,
+      base::Milliseconds(static_cast<int64_t>(
+          PersonaSpoofingHelper::MediaAudioLatency(context) * 1000))));
   return capabilities;
@@ -990,3 +991,3 @@ ScriptPromise<MediaDeviceInfo> MediaDevices::selectAudioOutput(
           options->hasDeviceId() &&
-                  !IsPersonaAudioOutputDeviceId(options->deviceId())
+                  !IsPersonaAudioOutputDeviceId(window, options->deviceId())
               ? options->deviceId()
@@ -1004,6 +1005,10 @@ void MediaDevices::OnSelectAudioOutputResult(
   if (result->status == mojom::blink::AudioOutputStatus::kSuccess) {
-    if (PersonaSpoofingHelper::UseVirtualMediaDevices(resolver->GetExecutionContext())) {
-      result->device_info.device_id = kPersonaAudioOutputDeviceId;
-      result->device_info.label = kPersonaAudioOutputLabel;
-      result->device_info.group_id = kPersonaOutputGroupId;
+    ExecutionContext* context = resolver->GetExecutionContext();
+    if (PersonaSpoofingHelper::UseVirtualMediaDevices(context)) {
+      result->device_info.device_id =
+          PersonaSpoofingHelper::MediaAudioOutputDeviceId(context).Utf8();
+      result->device_info.label =
+          PersonaSpoofingHelper::MediaAudioOutputLabel(context).Utf8();
+      result->device_info.group_id =
+          PersonaSpoofingHelper::MediaOutputGroupId(context).Utf8();
     }
@@ -1139,3 +1144,3 @@ ScriptPromise<IDLUndefined> MediaDevices::setPreferredSinkId(
   if (PersonaSpoofingHelper::UseVirtualMediaDevices(GetExecutionContext()) &&
-      IsPersonaAudioOutputDeviceId(visible_sink_id)) {
+      IsPersonaAudioOutputDeviceId(GetExecutionContext(), visible_sink_id)) {
     visible_sink_id = String();
diff --git a/third_party/blink/renderer/modules/mediastream/media_stream_track_impl.cc b/third_party/blink/renderer/modules/mediastream/media_stream_track_impl.cc
index aa9c854..6cde985 100644
--- a/third_party/blink/renderer/modules/mediastream/media_stream_track_impl.cc
+++ b/third_party/blink/renderer/modules/mediastream/media_stream_track_impl.cc
@@ -86,14 +86,2 @@ namespace {
 
-constexpr char kPersonaAudioInputDeviceId[] = "persona-audio-input-default";
-constexpr char kPersonaVideoInputDeviceId[] = "persona-video-input-default";
-constexpr char kPersonaInputGroupId[] = "persona-media-group-input";
-constexpr int kPersonaVideoSettingsWidth = 1280;
-constexpr int kPersonaVideoSettingsHeight = 720;
-constexpr double kPersonaVideoSettingsAspectRatio = 16.0 / 9.0;
-constexpr double kPersonaVideoFrameRate = 30.0;
-constexpr int kPersonaAudioSampleRate = 48000;
-constexpr int kPersonaAudioSampleSize = 16;
-constexpr int kPersonaAudioChannelCount = 2;
-constexpr double kPersonaAudioLatency = 0.01;
-
 LongRange* PersonaLongRange(int min, int max) {
@@ -118,5 +106,6 @@ Vector<bool> PersonaBoolCapability(bool value) {
 
-HeapVector<Member<V8UnionBooleanOrString>> PersonaEchoCancellationCapability() {
+HeapVector<Member<V8UnionBooleanOrString>> PersonaEchoCancellationCapability(
+    bool value) {
   HeapVector<Member<V8UnionBooleanOrString>> values;
-  values.push_back(MakeGarbageCollected<V8UnionBooleanOrString>(true));
+  values.push_back(MakeGarbageCollected<V8UnionBooleanOrString>(value));
   return values;
@@ -124,2 +113,8 @@ HeapVector<Member<V8UnionBooleanOrString>> PersonaEchoCancellationCapability() {
 
+double PersonaVideoAspectRatio(ExecutionContext* context) {
+  const int width = PersonaSpoofingHelper::MediaVideoWidth(context);
+  const int height = PersonaSpoofingHelper::MediaVideoHeight(context);
+  return height > 0 ? static_cast<double>(width) / height : 16.0 / 9.0;
+}
+
 bool IsPersonaDeviceMediaStreamTrackEnabled(
@@ -127,12 +122,13 @@ bool IsPersonaDeviceMediaStreamTrackEnabled(
     const std::optional<const MediaStreamDevice>& source_device) {
-  return PersonaSpoofingHelper::UseVirtualMediaDevices(context) && source_device &&
-         IsDeviceMediaType(source_device->type);
+  return PersonaSpoofingHelper::UseVirtualMediaDevices(context) &&
+         source_device && IsDeviceMediaType(source_device->type);
 }
 
-String PersonaTrackDeviceIdForType(MediaStreamSource::StreamType type) {
+String PersonaTrackDeviceIdForType(ExecutionContext* context,
+                                   MediaStreamSource::StreamType type) {
   switch (type) {
     case MediaStreamSource::kTypeAudio:
-      return String(kPersonaAudioInputDeviceId);
+      return PersonaSpoofingHelper::MediaAudioInputDeviceId(context);
     case MediaStreamSource::kTypeVideo:
-      return String(kPersonaVideoInputDeviceId);
+      return PersonaSpoofingHelper::MediaVideoInputDeviceId(context);
   }
@@ -141,16 +137,23 @@ String PersonaTrackDeviceIdForType(MediaStreamSource::StreamType type) {
 
-void ApplyPersonaMediaTrackSettings(MediaStreamSource::StreamType type,
+void ApplyPersonaMediaTrackSettings(ExecutionContext* context,
+                                    MediaStreamSource::StreamType type,
                                     MediaTrackSettings* settings) {
-  settings->setDeviceId(PersonaTrackDeviceIdForType(type));
-  settings->setGroupId(kPersonaInputGroupId);
+  settings->setDeviceId(PersonaTrackDeviceIdForType(context, type));
+  settings->setGroupId(PersonaSpoofingHelper::MediaInputGroupId(context));
   if (type == MediaStreamSource::kTypeAudio) {
-    settings->setEchoCancellation(
-        MakeGarbageCollected<V8UnionBooleanOrString>(true));
-    settings->setAutoGainControl(true);
-    settings->setNoiseSuppression(true);
-    settings->setVoiceIsolation(false);
-    settings->setSampleRate(kPersonaAudioSampleRate);
-    settings->setSampleSize(kPersonaAudioSampleSize);
-    settings->setChannelCount(kPersonaAudioChannelCount);
-    settings->setLatency(kPersonaAudioLatency);
+    settings->setEchoCancellation(MakeGarbageCollected<V8UnionBooleanOrString>(
+        PersonaSpoofingHelper::MediaEchoCancellation(context)));
+    settings->setAutoGainControl(
+        PersonaSpoofingHelper::MediaAutoGainControl(context));
+    settings->setNoiseSuppression(
+        PersonaSpoofingHelper::MediaNoiseSuppression(context));
+    settings->setVoiceIsolation(
+        PersonaSpoofingHelper::MediaVoiceIsolation(context));
+    settings->setSampleRate(
+        PersonaSpoofingHelper::MediaAudioSampleRate(context));
+    settings->setSampleSize(
+        PersonaSpoofingHelper::MediaAudioSampleSize(context));
+    settings->setChannelCount(
+        PersonaSpoofingHelper::MediaAudioChannelCount(context));
+    settings->setLatency(PersonaSpoofingHelper::MediaAudioLatency(context));
     return;
@@ -159,6 +162,6 @@ void ApplyPersonaMediaTrackSettings(MediaStreamSource::StreamType type,
   if (type == MediaStreamSource::kTypeVideo) {
-    settings->setWidth(kPersonaVideoSettingsWidth);
-    settings->setHeight(kPersonaVideoSettingsHeight);
-    settings->setAspectRatio(kPersonaVideoSettingsAspectRatio);
-    settings->setFrameRate(kPersonaVideoFrameRate);
+    settings->setWidth(PersonaSpoofingHelper::MediaVideoWidth(context));
+    settings->setHeight(PersonaSpoofingHelper::MediaVideoHeight(context));
+    settings->setAspectRatio(PersonaVideoAspectRatio(context));
+    settings->setFrameRate(PersonaSpoofingHelper::MediaVideoFrameRate(context));
     settings->setFacingMode("user");
@@ -168,19 +171,35 @@ void ApplyPersonaMediaTrackSettings(MediaStreamSource::StreamType type,
 
-void ApplyPersonaMediaTrackCapabilities(MediaStreamSource::StreamType type,
+void ApplyPersonaMediaTrackCapabilities(ExecutionContext* context,
+                                        MediaStreamSource::StreamType type,
                                         MediaTrackCapabilities* capabilities) {
-  capabilities->setDeviceId(PersonaTrackDeviceIdForType(type));
-  capabilities->setGroupId(kPersonaInputGroupId);
+  capabilities->setDeviceId(PersonaTrackDeviceIdForType(context, type));
+  capabilities->setGroupId(PersonaSpoofingHelper::MediaInputGroupId(context));
   if (type == MediaStreamSource::kTypeAudio) {
-    capabilities->setEchoCancellation(PersonaEchoCancellationCapability());
-    capabilities->setAutoGainControl(PersonaBoolCapability(true));
-    capabilities->setNoiseSuppression(PersonaBoolCapability(true));
-    capabilities->setVoiceIsolation(PersonaBoolCapability(false));
-    capabilities->setSampleSize(
-        PersonaLongRange(kPersonaAudioSampleSize, kPersonaAudioSampleSize));
+    const bool echo_cancellation =
+        PersonaSpoofingHelper::MediaEchoCancellation(context);
+    const bool auto_gain_control =
+        PersonaSpoofingHelper::MediaAutoGainControl(context);
+    const bool noise_suppression =
+        PersonaSpoofingHelper::MediaNoiseSuppression(context);
+    const bool voice_isolation =
+        PersonaSpoofingHelper::MediaVoiceIsolation(context);
+    const int sample_size =
+        PersonaSpoofingHelper::MediaAudioSampleSize(context);
+    const int channel_count =
+        PersonaSpoofingHelper::MediaAudioChannelCount(context);
+    const int sample_rate =
+        PersonaSpoofingHelper::MediaAudioSampleRate(context);
+    const double latency = PersonaSpoofingHelper::MediaAudioLatency(context);
+    capabilities->setEchoCancellation(
+        PersonaEchoCancellationCapability(echo_cancellation));
+    capabilities->setAutoGainControl(
+        PersonaBoolCapability(auto_gain_control));
+    capabilities->setNoiseSuppression(
+        PersonaBoolCapability(noise_suppression));
+    capabilities->setVoiceIsolation(PersonaBoolCapability(voice_isolation));
+    capabilities->setSampleSize(PersonaLongRange(sample_size, sample_size));
     capabilities->setChannelCount(
-        PersonaLongRange(kPersonaAudioChannelCount, kPersonaAudioChannelCount));
-    capabilities->setSampleRate(
-        PersonaLongRange(kPersonaAudioSampleRate, kPersonaAudioSampleRate));
-    capabilities->setLatency(
-        PersonaDoubleRange(kPersonaAudioLatency, kPersonaAudioLatency));
+        PersonaLongRange(channel_count, channel_count));
+    capabilities->setSampleRate(PersonaLongRange(sample_rate, sample_rate));
+    capabilities->setLatency(PersonaDoubleRange(latency, latency));
     return;
@@ -189,7 +208,12 @@ void ApplyPersonaMediaTrackCapabilities(MediaStreamSource::StreamType type,
   if (type == MediaStreamSource::kTypeVideo) {
-    capabilities->setWidth(PersonaLongRange(640, 1920));
-    capabilities->setHeight(PersonaLongRange(480, 1080));
-    capabilities->setAspectRatio(PersonaDoubleRange(4.0 / 3.0, 16.0 / 9.0));
-    capabilities->setFrameRate(
-        PersonaDoubleRange(kPersonaVideoFrameRate, kPersonaVideoFrameRate));
+    const int width = PersonaSpoofingHelper::MediaVideoWidth(context);
+    const int height = PersonaSpoofingHelper::MediaVideoHeight(context);
+    const double aspect_ratio = PersonaVideoAspectRatio(context);
+    const double frame_rate =
+        PersonaSpoofingHelper::MediaVideoFrameRate(context);
+    capabilities->setWidth(PersonaLongRange(width, width));
+    capabilities->setHeight(PersonaLongRange(height, height));
+    capabilities->setAspectRatio(
+        PersonaDoubleRange(aspect_ratio, aspect_ratio));
+    capabilities->setFrameRate(PersonaDoubleRange(frame_rate, frame_rate));
     Vector<String> facing_mode;
@@ -721,3 +745,4 @@ MediaTrackCapabilities* MediaStreamTrackImpl::getCapabilities() const {
   if (IsPersonaDeviceMediaStreamTrackEnabled(GetExecutionContext(), device())) {
-    ApplyPersonaMediaTrackCapabilities(component_->GetSourceType(),
+    ApplyPersonaMediaTrackCapabilities(GetExecutionContext(),
+                                       component_->GetSourceType(),
                                        capabilities);
@@ -851,3 +876,4 @@ MediaTrackSettings* MediaStreamTrackImpl::getSettings() const {
   if (IsPersonaDeviceMediaStreamTrackEnabled(GetExecutionContext(), device())) {
-    ApplyPersonaMediaTrackSettings(component_->GetSourceType(), settings);
+    ApplyPersonaMediaTrackSettings(GetExecutionContext(),
+                                   component_->GetSourceType(), settings);
   }
diff --git a/third_party/blink/renderer/modules/mediastream/user_media_processor.cc b/third_party/blink/renderer/modules/mediastream/user_media_processor.cc
index 8329ab8..056192f 100644
--- a/third_party/blink/renderer/modules/mediastream/user_media_processor.cc
+++ b/third_party/blink/renderer/modules/mediastream/user_media_processor.cc
@@ -78,5 +78,2 @@ using AudioSourceErrorCode = media::AudioCapturerSource::ErrorCode;
 namespace {
-constexpr char kPersonaAudioInputDeviceId[] = "persona-audio-input-default";
-constexpr char kPersonaVideoInputDeviceId[] = "persona-video-input-default";
-constexpr char kPersonaInputGroupId[] = "persona-media-group-input";
 
@@ -87,11 +84,16 @@ void LogCameraCaptureCapability(CameraCaptureCapability capability) {
 
-bool IsPersonaAudioInputDeviceId(const std::string& device_id) {
-  return device_id == kPersonaAudioInputDeviceId;
+bool IsPersonaAudioInputDeviceId(ExecutionContext* context,
+                                 const std::string& device_id) {
+  return String::FromUtf8(device_id) ==
+         PersonaSpoofingHelper::MediaAudioInputDeviceId(context);
 }
 
-bool IsPersonaVideoInputDeviceId(const std::string& device_id) {
-  return device_id == kPersonaVideoInputDeviceId;
+bool IsPersonaVideoInputDeviceId(ExecutionContext* context,
+                                 const std::string& device_id) {
+  return String::FromUtf8(device_id) ==
+         PersonaSpoofingHelper::MediaVideoInputDeviceId(context);
 }
 
-void NormalizePersonaDeviceIdsForCapture(TrackControls& controls,
+void NormalizePersonaDeviceIdsForCapture(ExecutionContext* context,
+                                         TrackControls& controls,
                                          bool is_audio) {
@@ -101,4 +103,4 @@ void NormalizePersonaDeviceIdsForCapture(TrackControls& controls,
   for (std::string& device_id : controls.device_ids) {
-    if ((is_audio && IsPersonaAudioInputDeviceId(device_id)) ||
-        (!is_audio && IsPersonaVideoInputDeviceId(device_id))) {
+    if ((is_audio && IsPersonaAudioInputDeviceId(context, device_id)) ||
+        (!is_audio && IsPersonaVideoInputDeviceId(context, device_id))) {
       device_id.clear();
@@ -109,10 +111,16 @@ void NormalizePersonaDeviceIdsForCapture(TrackControls& controls,
 Vector<mojom::blink::AudioInputDeviceCapabilitiesPtr>
-MakePersonaAudioInputCapabilities() {
+MakePersonaAudioInputCapabilities(ExecutionContext* context) {
+  const int sample_rate = PersonaSpoofingHelper::MediaAudioSampleRate(context);
+  const int channels = PersonaSpoofingHelper::MediaAudioChannelCount(context);
   media::AudioParameters parameters(
       media::AudioParameters::AUDIO_PCM_LOW_LATENCY,
-      media::ChannelLayoutConfig::Stereo(), 48000, 480);
+      media::ChannelLayoutConfig::Stereo(), sample_rate, sample_rate / 100);
   Vector<mojom::blink::AudioInputDeviceCapabilitiesPtr> capabilities;
   capabilities.push_back(mojom::blink::AudioInputDeviceCapabilities::New(
-      kPersonaAudioInputDeviceId, kPersonaInputGroupId, parameters, true, 2,
-      48000, base::Milliseconds(10)));
+      PersonaSpoofingHelper::MediaAudioInputDeviceId(context),
+      PersonaSpoofingHelper::MediaInputGroupId(context), parameters,
+      PersonaSpoofingHelper::MediaEchoCancellation(context), channels,
+      sample_rate,
+      base::Milliseconds(static_cast<int64_t>(
+          PersonaSpoofingHelper::MediaAudioLatency(context) * 1000))));
   return capabilities;
@@ -121,11 +129,10 @@ MakePersonaAudioInputCapabilities() {
 Vector<mojom::blink::VideoInputDeviceCapabilitiesPtr>
-MakePersonaVideoInputCapabilities() {
+MakePersonaVideoInputCapabilities(ExecutionContext* context) {
   auto control_support = media::VideoCaptureControlSupport();
   Vector<media::VideoCaptureFormat> formats;
-  formats.push_back(media::VideoCaptureFormat(gfx::Size(640, 480), 30.0f,
-                                              media::PIXEL_FORMAT_I420));
-  formats.push_back(media::VideoCaptureFormat(gfx::Size(1280, 720), 30.0f,
-                                              media::PIXEL_FORMAT_I420));
-  formats.push_back(media::VideoCaptureFormat(gfx::Size(1920, 1080), 30.0f,
-                                              media::PIXEL_FORMAT_I420));
+  formats.push_back(media::VideoCaptureFormat(
+      gfx::Size(PersonaSpoofingHelper::MediaVideoWidth(context),
+                PersonaSpoofingHelper::MediaVideoHeight(context)),
+      PersonaSpoofingHelper::MediaVideoFrameRate(context),
+      media::PIXEL_FORMAT_I420));
 
@@ -133,3 +140,4 @@ MakePersonaVideoInputCapabilities() {
   capabilities.push_back(mojom::blink::VideoInputDeviceCapabilities::New(
-      kPersonaVideoInputDeviceId, kPersonaInputGroupId, control_support,
+      PersonaSpoofingHelper::MediaVideoInputDeviceId(context),
+      PersonaSpoofingHelper::MediaInputGroupId(context), control_support,
       std::move(formats), mojom::blink::FacingMode::kUser,
@@ -846,5 +854,6 @@ void UserMediaProcessor::SetupAudioInput() {
     current_request_info_->StartTrace("GetAudioInputCapabilities");
-    if (PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
-                                                      : nullptr)) {
-      SelectAudioDeviceSettings(request, MakePersonaAudioInputCapabilities());
+    auto* context = frame_ ? frame_->DomWindow() : nullptr;
+    if (PersonaSpoofingHelper::UseVirtualMediaDevices(context)) {
+      SelectAudioDeviceSettings(request,
+                                MakePersonaAudioInputCapabilities(context));
       return;
@@ -1081,5 +1090,6 @@ void UserMediaProcessor::SetupVideoInput() {
     current_request_info_->StartTrace("GetVideoInputCapabilities");
-    if (PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
-                                                      : nullptr)) {
-      SelectVideoDeviceSettings(request, MakePersonaVideoInputCapabilities());
+    auto* context = frame_ ? frame_->DomWindow() : nullptr;
+    if (PersonaSpoofingHelper::UseVirtualMediaDevices(context)) {
+      SelectVideoDeviceSettings(request,
+                                MakePersonaVideoInputCapabilities(context));
       return;
@@ -1284,8 +1294,8 @@ void UserMediaProcessor::GenerateStreamForCurrentRequestInfo(
           .c_str()));
-  if (PersonaSpoofingHelper::UseVirtualMediaDevices(frame_ ? frame_->DomWindow()
-                                                      : nullptr)) {
+  auto* context = frame_ ? frame_->DomWindow() : nullptr;
+  if (PersonaSpoofingHelper::UseVirtualMediaDevices(context)) {
     NormalizePersonaDeviceIdsForCapture(
-        current_request_info_->stream_controls()->audio, true);
+        context, current_request_info_->stream_controls()->audio, true);
     NormalizePersonaDeviceIdsForCapture(
-        current_request_info_->stream_controls()->video, false);
+        context, current_request_info_->stream_controls()->video, false);
   }
diff --git a/third_party/blink/renderer/modules/webaudio/audio_context.cc b/third_party/blink/renderer/modules/webaudio/audio_context.cc
index a236680..2d59b26 100644
--- a/third_party/blink/renderer/modules/webaudio/audio_context.cc
+++ b/third_party/blink/renderer/modules/webaudio/audio_context.cc
@@ -89,4 +89,2 @@ constexpr double kOutputLatencyQuatizingFactor = 0.008;
 constexpr double kOutputLatencyMaxPrecisionFactor = 0.001;
-constexpr double kPersonaAudioBaseLatency = 0.01;
-constexpr double kPersonaAudioOutputLatency = 0.02;
 
@@ -1150,3 +1148,3 @@ double AudioContext::baseLatency() const {
   if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
-    return kPersonaAudioBaseLatency;
+    return PersonaSpoofingHelper::MediaAudioBaseLatency(GetExecutionContext());
   }
@@ -1168,3 +1166,3 @@ double AudioContext::outputLatency() const {
   if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
-    return kPersonaAudioOutputLatency;
+    return PersonaSpoofingHelper::MediaAudioOutputLatency(GetExecutionContext());
   }
