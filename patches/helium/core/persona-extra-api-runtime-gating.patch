diff --git a/chrome/browser/chrome_browser_interface_binders.cc b/chrome/browser/chrome_browser_interface_binders.cc
index bef2e443..4b96a44c 100644
--- a/chrome/browser/chrome_browser_interface_binders.cc
+++ b/chrome/browser/chrome_browser_interface_binders.cc
@@ -308,6 +308,18 @@ bool HeliumPersonaAllowsWebAppInstallation(
   return !snapshot.IsEnabled() || snapshot.allow_web_app_installation;
 }
 
+bool HeliumPersonaAllowsBadging(content::RenderFrameHost* render_frame_host) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshot(render_frame_host);
+  return !snapshot.IsEnabled() || snapshot.allow_badging;
+}
+
+bool HeliumPersonaAllowsWebShare(content::RenderFrameHost* render_frame_host) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshot(render_frame_host);
+  return !snapshot.IsEnabled() || snapshot.allow_web_share;
+}
+
 void BindNetworkHintsHandler(
     content::RenderFrameHost* frame_host,
     mojo::PendingReceiver<network_hints::mojom::NetworkHintsHandler> receiver) {
@@ -503,8 +515,10 @@ void PopulateChromeFrameBinders(
 #endif  // BUILDFLAG(ENABLE_UNHANDLED_TAP)
 
 #else
-  map->Add<blink::mojom::BadgeService>(
-      &badging::BadgeManager::BindFrameReceiverIfAllowed);
+  if (HeliumPersonaAllowsBadging(render_frame_host)) {
+    map->Add<blink::mojom::BadgeService>(
+        &badging::BadgeManager::BindFrameReceiverIfAllowed);
+  }
   map->Add<blink::mojom::PersistentRendererPrefsService>(
       &PersistentRendererPrefsManager::BindFrameReceiver);
   if (base::FeatureList::IsEnabled(features::kWebPayments) &&
@@ -528,11 +542,15 @@ void PopulateChromeFrameBinders(
 #endif
 
 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_MAC)
-  map->Add<blink::mojom::ShareService>(&ShareServiceImpl::Create);
+  if (HeliumPersonaAllowsWebShare(render_frame_host)) {
+    map->Add<blink::mojom::ShareService>(&ShareServiceImpl::Create);
+  }
 #endif
 #if BUILDFLAG(IS_ANDROID)
-  map->Add<blink::mojom::ShareService>(
-      &ForwardToJavaWebContents<blink::mojom::ShareService>);
+  if (HeliumPersonaAllowsWebShare(render_frame_host)) {
+    map->Add<blink::mojom::ShareService>(
+        &ForwardToJavaWebContents<blink::mojom::ShareService>);
+  }
 #endif
 
   map->Add<network_hints::mojom::NetworkHintsHandler>(&BindNetworkHintsHandler);
diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index 6e4ce8c5..94471193 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -1030,6 +1030,19 @@ bool HeliumPersonaAllowsInstalledAppQueries(
   return !snapshot.IsEnabled() || snapshot.allow_installed_apps;
 }
 
+bool HeliumPersonaAllowsFileSystemAccess(
+    content::BrowserContext* browser_context) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshotForBrowserContext(browser_context);
+  return !snapshot.IsEnabled() || snapshot.allow_file_system_access;
+}
+
+bool HeliumPersonaAllowsWebLocks(content::BrowserContext* browser_context) {
+  blink::HeliumPersonaSnapshot snapshot =
+      GetHeliumPersonaSnapshotForBrowserContext(browser_context);
+  return !snapshot.IsEnabled() || snapshot.allow_web_locks;
+}
+
 #if BUILDFLAG(IS_MAC)
 constexpr char kSecurePaymentConfirmationKeychainAccessGroup[] =
     MAC_TEAM_IDENTIFIER_STRING "." MAC_BUNDLE_IDENTIFIER_STRING
@@ -3766,6 +3779,9 @@ bool ChromeContentBrowserClient::AllowWorkerWebLocks(
     content::BrowserContext* browser_context,
     const std::vector<content::GlobalRenderFrameHostId>& render_frames,
     const blink::StorageKey& storage_key) {
+  if (!HeliumPersonaAllowsWebLocks(browser_context)) {
+    return false;
+  }
   scoped_refptr<content_settings::CookieSettings> cookie_settings =
       CookieSettingsFactory::GetForProfile(
           Profile::FromBrowserContext(browser_context));
@@ -9474,6 +9490,10 @@ bool ChromeContentBrowserClient::ShouldDisallowCredentialRequest(
 
 bool ChromeContentBrowserClient::IsFileSystemAccessApiFilePickerAllowed(
     WebContents* web_contents) {
+  if (!HeliumPersonaAllowsFileSystemAccess(
+          web_contents ? web_contents->GetBrowserContext() : nullptr)) {
+    return false;
+  }
 #if !BUILDFLAG(IS_ANDROID)
   if (base::FeatureList::IsEnabled(
           actor::kGlicBlockFileSystemAccessApiFilePicker)) {
diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
index aec909e5..b051d004 100644
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -454,6 +454,15 @@ bool HeliumPersonaAllowsWindowManagement(Profile* profile) {
   return !snapshot.IsEnabled() || snapshot.allow_window_management;
 }
 
+bool HeliumPersonaAllowsEyeDropper(Profile* profile) {
+  if (!profile) {
+    return true;
+  }
+  const blink::HeliumPersonaSnapshot snapshot =
+      content::GetContentClient()->browser()->GetHeliumPersonaSnapshot(profile);
+  return !snapshot.IsEnabled() || snapshot.allow_eye_dropper;
+}
+
 #if DCHECK_IS_ON()
 
 std::string FocusListToString(views::View* view) {
@@ -6551,6 +6560,9 @@ void BrowserView::ShowCaretBrowsingDialog() {
 std::unique_ptr<content::EyeDropper> BrowserView::OpenEyeDropper(
     content::RenderFrameHost* frame,
     content::EyeDropperListener* listener) {
+  if (!HeliumPersonaAllowsEyeDropper(browser_->profile())) {
+    return nullptr;
+  }
   return ShowEyeDropper(frame, listener);
 }
 
diff --git a/content/browser/renderer_host/render_frame_host_impl.cc b/content/browser/renderer_host/render_frame_host_impl.cc
index 98b9c464..85766e57 100644
--- a/content/browser/renderer_host/render_frame_host_impl.cc
+++ b/content/browser/renderer_host/render_frame_host_impl.cc
@@ -24,6 +24,7 @@
 #include "base/debug/crash_logging.h"
 #include "base/debug/dump_without_crashing.h"
 #include "base/feature_list.h"
+#include "base/files/file.h"
 #include "base/functional/bind.h"
 #include "base/functional/callback_helpers.h"
 #include "base/json/json_reader.h"
@@ -14901,6 +14902,12 @@ void RenderFrameHostImpl::GetFontAccessManager(
 void RenderFrameHostImpl::GetFileSystemAccessManager(
     mojo::PendingReceiver<blink::mojom::FileSystemAccessManager> receiver) {
   CHECK_CURRENTLY_ON(BrowserThread::UI);
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() &&
+      !persona_snapshot.allow_file_system_access) {
+    return;
+  }
   auto* manager = GetStoragePartition()->GetFileSystemAccessManager();
   manager->BindReceiver(
       FileSystemAccessManagerImpl::BindingContext(
@@ -14910,6 +14917,11 @@ void RenderFrameHostImpl::GetFileSystemAccessManager(
 
 void RenderFrameHostImpl::CreateLockManager(
     mojo::PendingReceiver<blink::mojom::LockManager> receiver) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_web_locks) {
+    return;
+  }
   GetStoragePartition()->BindLockManager(
       GetStorageKey(), GetFrameToken().value(), std::move(receiver));
   GetStoragePartition()->GetLockManager()->AddLockObserver(
@@ -19424,6 +19436,18 @@ void RenderFrameHostImpl::GetSandboxedFileSystemForBucket(
     const storage::BucketInfo& bucket,
     const std::vector<std::string>& directory_path_components,
     blink::mojom::BucketHost::GetDirectoryCallback callback) {
+  const blink::HeliumPersonaSnapshot& persona_snapshot =
+      GetPage().persona_snapshot();
+  if (persona_snapshot.IsEnabled() &&
+      !persona_snapshot.allow_file_system_access) {
+    std::move(callback).Run(
+        blink::mojom::FileSystemAccessError::New(
+            blink::mojom::FileSystemAccessStatus::kSecurityError,
+            base::File::FILE_ERROR_ACCESS_DENIED,
+            "File System Access is disabled by the active persona."),
+        blink::mojom::FileSystemAccessEntryPtr());
+    return;
+  }
   GetStoragePartition()->GetFileSystemAccessManager()->GetSandboxedFileSystem(
       FileSystemAccessManagerImpl::BindingContext(
           GetStorageKey(), GetLastCommittedURL(), GetGlobalId()),
diff --git a/content/browser/renderer_host/render_process_host_impl.cc b/content/browser/renderer_host/render_process_host_impl.cc
index 15e138e4..16af1499 100644
--- a/content/browser/renderer_host/render_process_host_impl.cc
+++ b/content/browser/renderer_host/render_process_host_impl.cc
@@ -2355,6 +2355,13 @@ void RenderProcessHostImpl::BindFileSystemAccessManager(
     const blink::StorageKey& storage_key,
     mojo::PendingReceiver<blink::mojom::FileSystemAccessManager> receiver) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
+  const blink::HeliumPersonaSnapshot persona_snapshot =
+      GetContentClient()->browser()->GetHeliumPersonaSnapshot(
+          GetBrowserContext());
+  if (persona_snapshot.IsEnabled() &&
+      !persona_snapshot.allow_file_system_access) {
+    return;
+  }
   // This code path is only for workers, hence always pass in
   // IPC::mojom::kRoutingIdNone as frame ID. Frames themselves go through
   // RenderFrameHostImpl instead.
@@ -2385,6 +2392,19 @@ void RenderProcessHostImpl::GetSandboxedFileSystemForBucket(
     const std::vector<std::string>& directory_path_components,
     blink::mojom::FileSystemAccessManager::GetSandboxedFileSystemCallback
         callback) {
+  const blink::HeliumPersonaSnapshot persona_snapshot =
+      GetContentClient()->browser()->GetHeliumPersonaSnapshot(
+          GetBrowserContext());
+  if (persona_snapshot.IsEnabled() &&
+      !persona_snapshot.allow_file_system_access) {
+    std::move(callback).Run(
+        blink::mojom::FileSystemAccessError::New(
+            blink::mojom::FileSystemAccessStatus::kSecurityError,
+            base::File::FILE_ERROR_ACCESS_DENIED,
+            "File System Access is disabled by the active persona."),
+        blink::mojom::FileSystemAccessEntryPtr());
+    return;
+  }
   auto* manager = storage_partition_impl_->GetFileSystemAccessManager();
   manager->GetSandboxedFileSystem(
       FileSystemAccessManagerImpl::BindingContext(
@@ -2443,6 +2463,12 @@ void RenderProcessHostImpl::CreateLockManager(
     const blink::StorageKey& storage_key,
     mojo::PendingReceiver<blink::mojom::LockManager> receiver) {
   DCHECK_CURRENTLY_ON(BrowserThread::UI);
+  const blink::HeliumPersonaSnapshot persona_snapshot =
+      GetContentClient()->browser()->GetHeliumPersonaSnapshot(
+          GetBrowserContext());
+  if (persona_snapshot.IsEnabled() && !persona_snapshot.allow_web_locks) {
+    return;
+  }
 
   base::UnguessableToken token = base::UnguessableToken::Create();
   storage_partition_impl_->BindLockManager(storage_key, token,
