--- a/components/helium_persona/persona_snapshot.h
+++ b/components/helium_persona/persona_snapshot.h
@@ -163,6 +163,19 @@ struct COMPONENT_EXPORT(HELIUM_PERSONA) PersonaSnapshot {
   bool allow_persistent_device_id = false;
   bool allow_installed_apps = false;
   bool allow_managed_device = false;
+  bool allow_notifications = false;
+  bool allow_protected_media_identifier = false;
+  bool allow_persistent_storage = false;
+  bool allow_background_sync = false;
+  bool allow_clipboard = false;
+  bool allow_payment_handler = false;
+  bool allow_idle_detection = false;
+  bool allow_wake_lock = false;
+  bool allow_storage_access = false;
+  bool allow_pointer_lock = false;
+  bool allow_fullscreen = false;
+  bool allow_web_app_installation = false;
+  bool allow_web_printing = false;
 };
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
--- a/components/helium_persona/persona_snapshot.cc
+++ b/components/helium_persona/persona_snapshot.cc
@@ -391,6 +391,32 @@ PersonaSnapshot MakePersonaSnapshot(const base::Value::Dict& persona) {
       GetNestedBool(persona, "advanced", "allowInstalledApps", false);
   snapshot.allow_managed_device =
       GetNestedBool(persona, "advanced", "allowManagedDevice", false);
+  snapshot.allow_notifications =
+      GetNestedBool(persona, "advanced", "allowNotifications", false);
+  snapshot.allow_protected_media_identifier =
+      GetNestedBool(persona, "advanced", "allowProtectedMediaIdentifier", false);
+  snapshot.allow_persistent_storage =
+      GetNestedBool(persona, "advanced", "allowPersistentStorage", false);
+  snapshot.allow_background_sync =
+      GetNestedBool(persona, "advanced", "allowBackgroundSync", false);
+  snapshot.allow_clipboard =
+      GetNestedBool(persona, "advanced", "allowClipboard", false);
+  snapshot.allow_payment_handler =
+      GetNestedBool(persona, "advanced", "allowPaymentHandler", false);
+  snapshot.allow_idle_detection =
+      GetNestedBool(persona, "advanced", "allowIdleDetection", false);
+  snapshot.allow_wake_lock =
+      GetNestedBool(persona, "advanced", "allowWakeLock", false);
+  snapshot.allow_storage_access =
+      GetNestedBool(persona, "advanced", "allowStorageAccess", false);
+  snapshot.allow_pointer_lock =
+      GetNestedBool(persona, "advanced", "allowPointerLock", false);
+  snapshot.allow_fullscreen =
+      GetNestedBool(persona, "advanced", "allowFullscreen", false);
+  snapshot.allow_web_app_installation =
+      GetNestedBool(persona, "advanced", "allowWebAppInstallation", false);
+  snapshot.allow_web_printing =
+      GetNestedBool(persona, "advanced", "allowWebPrinting", false);
   return snapshot;
 }
 
@@ -590,6 +616,21 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot) {
   advanced.Set("allowPersistentDeviceId", snapshot.allow_persistent_device_id);
   advanced.Set("allowInstalledApps", snapshot.allow_installed_apps);
   advanced.Set("allowManagedDevice", snapshot.allow_managed_device);
+  advanced.Set("allowNotifications", snapshot.allow_notifications);
+  advanced.Set("allowProtectedMediaIdentifier",
+               snapshot.allow_protected_media_identifier);
+  advanced.Set("allowPersistentStorage", snapshot.allow_persistent_storage);
+  advanced.Set("allowBackgroundSync", snapshot.allow_background_sync);
+  advanced.Set("allowClipboard", snapshot.allow_clipboard);
+  advanced.Set("allowPaymentHandler", snapshot.allow_payment_handler);
+  advanced.Set("allowIdleDetection", snapshot.allow_idle_detection);
+  advanced.Set("allowWakeLock", snapshot.allow_wake_lock);
+  advanced.Set("allowStorageAccess", snapshot.allow_storage_access);
+  advanced.Set("allowPointerLock", snapshot.allow_pointer_lock);
+  advanced.Set("allowFullscreen", snapshot.allow_fullscreen);
+  advanced.Set("allowWebAppInstallation",
+               snapshot.allow_web_app_installation);
+  advanced.Set("allowWebPrinting", snapshot.allow_web_printing);
   value.Set("advanced", std::move(advanced));
   return value;
 }
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -285,6 +285,19 @@ base::Value::Dict MakeAdvanced(bool client_hints_enabled,
   advanced.Set("allowPersistentDeviceId", false);
   advanced.Set("allowInstalledApps", false);
   advanced.Set("allowManagedDevice", false);
+  advanced.Set("allowNotifications", false);
+  advanced.Set("allowProtectedMediaIdentifier", false);
+  advanced.Set("allowPersistentStorage", false);
+  advanced.Set("allowBackgroundSync", false);
+  advanced.Set("allowClipboard", false);
+  advanced.Set("allowPaymentHandler", false);
+  advanced.Set("allowIdleDetection", false);
+  advanced.Set("allowWakeLock", false);
+  advanced.Set("allowStorageAccess", false);
+  advanced.Set("allowPointerLock", false);
+  advanced.Set("allowFullscreen", false);
+  advanced.Set("allowWebAppInstallation", false);
+  advanced.Set("allowWebPrinting", false);
   return advanced;
 }
 
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -620,6 +620,19 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   advanced->Set("allowGeolocation", true);
   advanced->Set("allowLocalFonts", true);
   advanced->Set("virtualMediaDevices", false);
+  advanced->Set("allowNotifications", true);
+  advanced->Set("allowProtectedMediaIdentifier", true);
+  advanced->Set("allowPersistentStorage", true);
+  advanced->Set("allowBackgroundSync", true);
+  advanced->Set("allowClipboard", true);
+  advanced->Set("allowPaymentHandler", true);
+  advanced->Set("allowIdleDetection", true);
+  advanced->Set("allowWakeLock", true);
+  advanced->Set("allowStorageAccess", true);
+  advanced->Set("allowPointerLock", true);
+  advanced->Set("allowFullscreen", true);
+  advanced->Set("allowWebAppInstallation", true);
+  advanced->Set("allowWebPrinting", true);
 
   EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
 
@@ -685,6 +698,15 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_FALSE(runtime.FindDict("advanced")
                    ->FindBool("speechVoiceDefault")
                    .value_or(true));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowNotifications")
+                  .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowClipboard")
+                  .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowBackgroundSync")
+                  .value_or(false));
 
   PersonaSnapshot snapshot = MakePersonaSnapshot(runtime);
   EXPECT_EQ("manual-runtime-seed", snapshot.noise_seed);
@@ -722,6 +744,19 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_TRUE(snapshot.allow_geolocation);
   EXPECT_TRUE(snapshot.allow_local_fonts);
   EXPECT_FALSE(snapshot.virtual_media_devices);
+  EXPECT_TRUE(snapshot.allow_notifications);
+  EXPECT_TRUE(snapshot.allow_protected_media_identifier);
+  EXPECT_TRUE(snapshot.allow_persistent_storage);
+  EXPECT_TRUE(snapshot.allow_background_sync);
+  EXPECT_TRUE(snapshot.allow_clipboard);
+  EXPECT_TRUE(snapshot.allow_payment_handler);
+  EXPECT_TRUE(snapshot.allow_idle_detection);
+  EXPECT_TRUE(snapshot.allow_wake_lock);
+  EXPECT_TRUE(snapshot.allow_storage_access);
+  EXPECT_TRUE(snapshot.allow_pointer_lock);
+  EXPECT_TRUE(snapshot.allow_fullscreen);
+  EXPECT_TRUE(snapshot.allow_web_app_installation);
+  EXPECT_TRUE(snapshot.allow_web_printing);
 }
 
 }  // namespace helium_persona
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -163,6 +163,19 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
   bool allow_persistent_device_id = false;
   bool allow_installed_apps = false;
   bool allow_managed_device = false;
+  bool allow_notifications = false;
+  bool allow_protected_media_identifier = false;
+  bool allow_persistent_storage = false;
+  bool allow_background_sync = false;
+  bool allow_clipboard = false;
+  bool allow_payment_handler = false;
+  bool allow_idle_detection = false;
+  bool allow_wake_lock = false;
+  bool allow_storage_access = false;
+  bool allow_pointer_lock = false;
+  bool allow_fullscreen = false;
+  bool allow_web_app_installation = false;
+  bool allow_web_printing = false;
 
   bool IsEnabled() const { return enabled && !passthrough; }
 };
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
@@ -153,4 +153,17 @@ struct HeliumPersonaSnapshot {
   bool allow_persistent_device_id;
   bool allow_installed_apps;
   bool allow_managed_device;
+  bool allow_notifications;
+  bool allow_protected_media_identifier;
+  bool allow_persistent_storage;
+  bool allow_background_sync;
+  bool allow_clipboard;
+  bool allow_payment_handler;
+  bool allow_idle_detection;
+  bool allow_wake_lock;
+  bool allow_storage_access;
+  bool allow_pointer_lock;
+  bool allow_fullscreen;
+  bool allow_web_app_installation;
+  bool allow_web_printing;
 };
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
@@ -547,6 +548,58 @@ struct BLINK_COMMON_EXPORT
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.allow_managed_device;
   }
+  static bool allow_notifications(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_notifications;
+  }
+  static bool allow_protected_media_identifier(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_protected_media_identifier;
+  }
+  static bool allow_persistent_storage(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_persistent_storage;
+  }
+  static bool allow_background_sync(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_background_sync;
+  }
+  static bool allow_clipboard(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_clipboard;
+  }
+  static bool allow_payment_handler(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_payment_handler;
+  }
+  static bool allow_idle_detection(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_idle_detection;
+  }
+  static bool allow_wake_lock(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_wake_lock;
+  }
+  static bool allow_storage_access(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_storage_access;
+  }
+  static bool allow_pointer_lock(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_pointer_lock;
+  }
+  static bool allow_fullscreen(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_fullscreen;
+  }
+  static bool allow_web_app_installation(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_web_app_installation;
+  }
+  static bool allow_web_printing(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_web_printing;
+  }
 
   static bool Read(blink::mojom::HeliumPersonaSnapshotDataView data,
                    blink::HeliumPersonaSnapshot* out);
@@ -674,6 +726,20 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
   out->allow_persistent_device_id = data.allow_persistent_device_id();
   out->allow_installed_apps = data.allow_installed_apps();
   out->allow_managed_device = data.allow_managed_device();
+  out->allow_notifications = data.allow_notifications();
+  out->allow_protected_media_identifier =
+      data.allow_protected_media_identifier();
+  out->allow_persistent_storage = data.allow_persistent_storage();
+  out->allow_background_sync = data.allow_background_sync();
+  out->allow_clipboard = data.allow_clipboard();
+  out->allow_payment_handler = data.allow_payment_handler();
+  out->allow_idle_detection = data.allow_idle_detection();
+  out->allow_wake_lock = data.allow_wake_lock();
+  out->allow_storage_access = data.allow_storage_access();
+  out->allow_pointer_lock = data.allow_pointer_lock();
+  out->allow_fullscreen = data.allow_fullscreen();
+  out->allow_web_app_installation = data.allow_web_app_installation();
+  out->allow_web_printing = data.allow_web_printing();
 
   return data.ReadId(&out->id) && data.ReadName(&out->name) &&
          data.ReadDevice(&out->device) && data.ReadOs(&out->os) &&
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -959,6 +959,20 @@ blink::HeliumPersonaSnapshot ToBlinkPersonaSnapshot(
   target.allow_persistent_device_id = source.allow_persistent_device_id;
   target.allow_installed_apps = source.allow_installed_apps;
   target.allow_managed_device = source.allow_managed_device;
+  target.allow_notifications = source.allow_notifications;
+  target.allow_protected_media_identifier =
+      source.allow_protected_media_identifier;
+  target.allow_persistent_storage = source.allow_persistent_storage;
+  target.allow_background_sync = source.allow_background_sync;
+  target.allow_clipboard = source.allow_clipboard;
+  target.allow_payment_handler = source.allow_payment_handler;
+  target.allow_idle_detection = source.allow_idle_detection;
+  target.allow_wake_lock = source.allow_wake_lock;
+  target.allow_storage_access = source.allow_storage_access;
+  target.allow_pointer_lock = source.allow_pointer_lock;
+  target.allow_fullscreen = source.allow_fullscreen;
+  target.allow_web_app_installation = source.allow_web_app_installation;
+  target.allow_web_printing = source.allow_web_printing;
   return target;
 }
 
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
@@ -91,9 +92,15 @@ std::string MakePersonaPostscriptName(const std::string& family) {
   }
   return postscript_name + "-Regular";
 }
+
+bool ShouldBlockIfNotAllowed(const ExecutionContext* context,
+                             bool HeliumPersonaSnapshot::*allow_member) {
+  const HeliumPersonaSnapshot* snapshot = PersonaSpoofingHelper::Get(context);
+  return snapshot && !(snapshot->*allow_member);
+}
 
 }  // namespace
 
 const HeliumPersonaSnapshot* PersonaSpoofingHelper::Get(
     const ExecutionContext* context) {
   if (!context) {
@@ -335,25 +341,51 @@ bool PersonaSpoofingHelper::ShouldBlockPermission(
       return ShouldBlockWebXr(context);
     case mojom::blink::PermissionName::SMART_CARD:
       return ShouldBlockDeviceApis(context);
     case mojom::blink::PermissionName::NOTIFICATIONS:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_notifications);
     case mojom::blink::PermissionName::PROTECTED_MEDIA_IDENTIFIER:
+      return ShouldBlockIfNotAllowed(
+          context,
+          &HeliumPersonaSnapshot::allow_protected_media_identifier);
     case mojom::blink::PermissionName::PERSISTENT_STORAGE:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_persistent_storage);
     case mojom::blink::PermissionName::BACKGROUND_SYNC:
-    case mojom::blink::PermissionName::CLIPBOARD_READ:
-    case mojom::blink::PermissionName::CLIPBOARD_WRITE:
-    case mojom::blink::PermissionName::PAYMENT_HANDLER:
     case mojom::blink::PermissionName::BACKGROUND_FETCH:
-    case mojom::blink::PermissionName::IDLE_DETECTION:
     case mojom::blink::PermissionName::PERIODIC_BACKGROUND_SYNC:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_background_sync);
+    case mojom::blink::PermissionName::CLIPBOARD_READ:
+    case mojom::blink::PermissionName::CLIPBOARD_WRITE:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_clipboard);
+    case mojom::blink::PermissionName::PAYMENT_HANDLER:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_payment_handler);
+    case mojom::blink::PermissionName::IDLE_DETECTION:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_idle_detection);
     case mojom::blink::PermissionName::SCREEN_WAKE_LOCK:
     case mojom::blink::PermissionName::SYSTEM_WAKE_LOCK:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_wake_lock);
     case mojom::blink::PermissionName::STORAGE_ACCESS:
     case mojom::blink::PermissionName::TOP_LEVEL_STORAGE_ACCESS:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_storage_access);
     case mojom::blink::PermissionName::POINTER_LOCK:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_pointer_lock);
     case mojom::blink::PermissionName::FULLSCREEN:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_fullscreen);
     case mojom::blink::PermissionName::WEB_APP_INSTALLATION:
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_web_app_installation);
     case mojom::blink::PermissionName::WEB_PRINTING:
-      return IsEnabled(context);
+      return ShouldBlockIfNotAllowed(
+          context, &HeliumPersonaSnapshot::allow_web_printing);
     default:
       return false;
   }
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -241,6 +241,19 @@ std::string BuildPersonaSalt(
   AppendSaltPart(salt, persona_snapshot.allow_persistent_device_id);
   AppendSaltPart(salt, persona_snapshot.allow_installed_apps);
   AppendSaltPart(salt, persona_snapshot.allow_managed_device);
+  AppendSaltPart(salt, persona_snapshot.allow_notifications);
+  AppendSaltPart(salt, persona_snapshot.allow_protected_media_identifier);
+  AppendSaltPart(salt, persona_snapshot.allow_persistent_storage);
+  AppendSaltPart(salt, persona_snapshot.allow_background_sync);
+  AppendSaltPart(salt, persona_snapshot.allow_clipboard);
+  AppendSaltPart(salt, persona_snapshot.allow_payment_handler);
+  AppendSaltPart(salt, persona_snapshot.allow_idle_detection);
+  AppendSaltPart(salt, persona_snapshot.allow_wake_lock);
+  AppendSaltPart(salt, persona_snapshot.allow_storage_access);
+  AppendSaltPart(salt, persona_snapshot.allow_pointer_lock);
+  AppendSaltPart(salt, persona_snapshot.allow_fullscreen);
+  AppendSaltPart(salt, persona_snapshot.allow_web_app_installation);
+  AppendSaltPart(salt, persona_snapshot.allow_web_printing);
   return salt;
 }
 
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -502,6 +502,19 @@ export class SettingsHeliumPersonaPageElement extends
       allowPersistentDeviceId: false,
       allowInstalledApps: false,
       allowManagedDevice: false,
+      allowNotifications: false,
+      allowProtectedMediaIdentifier: false,
+      allowPersistentStorage: false,
+      allowBackgroundSync: false,
+      allowClipboard: false,
+      allowPaymentHandler: false,
+      allowIdleDetection: false,
+      allowWakeLock: false,
+      allowStorageAccess: false,
+      allowPointerLock: false,
+      allowFullscreen: false,
+      allowWebAppInstallation: false,
+      allowWebPrinting: false,
     };
     for (const [key, value] of Object.entries(advancedDefaults)) {
       if (persona.advanced[key] === undefined) {
@@ -605,6 +618,11 @@ export class SettingsHeliumPersonaPageElement extends
       'allowPlatformCredentials', 'allowKeyboardLayout',
       'allowRealBatteryStatus', 'allowLocalFonts',
       'allowPersistentDeviceId', 'allowInstalledApps', 'allowManagedDevice',
+      'allowNotifications', 'allowProtectedMediaIdentifier',
+      'allowPersistentStorage', 'allowBackgroundSync', 'allowClipboard',
+      'allowPaymentHandler', 'allowIdleDetection', 'allowWakeLock',
+      'allowStorageAccess', 'allowPointerLock', 'allowFullscreen',
+      'allowWebAppInstallation', 'allowWebPrinting',
     ];
     for (const key of booleanAdvancedKeys) {
       persona.advanced[key] = Boolean(persona.advanced[key]);
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html
@@ -572,6 +572,45 @@
     <cr-checkbox checked="{{editablePersona_.advanced.allowManagedDevice}}">
       Allow managed device probes
     </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowNotifications}}">
+      Allow notifications permission
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowProtectedMediaIdentifier}}">
+      Allow protected media identifier
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowPersistentStorage}}">
+      Allow persistent storage
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowBackgroundSync}}">
+      Allow background sync and fetch
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowClipboard}}">
+      Allow clipboard permission probes
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowPaymentHandler}}">
+      Allow payment handler
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowIdleDetection}}">
+      Allow idle detection
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowWakeLock}}">
+      Allow wake lock
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowStorageAccess}}">
+      Allow storage access permissions
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowPointerLock}}">
+      Allow pointer lock permission
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowFullscreen}}">
+      Allow fullscreen permission
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowWebAppInstallation}}">
+      Allow web app installation
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowWebPrinting}}">
+      Allow web printing
+    </cr-checkbox>
   </div>
 
   <div class="actions-row">
