diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
@@ -61,6 +61,7 @@ class CORE_EXPORT PersonaSpoofingHelper {
   static bool ShouldBlockBadging(const ExecutionContext* context);
   static bool ShouldBlockWebShare(const ExecutionContext* context);
   static bool ShouldBlockEyeDropper(const ExecutionContext* context);
+  static bool ShouldBlockShapeDetection(const ExecutionContext* context);
   static bool ShouldBlockWebSockets(const ExecutionContext* context);
   static bool ShouldBlockWebTransport(const ExecutionContext* context);
   static bool ShouldBlockSpeechSynthesis(const ExecutionContext* context);
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
@@ -351,6 +351,12 @@ bool PersonaSpoofingHelper::ShouldBlockEyeDropper(
     const ExecutionContext* context) {
   return ShouldBlockIfNotAllowed(context,
                                  &HeliumPersonaSnapshot::allow_eye_dropper);
+}
+
+bool PersonaSpoofingHelper::ShouldBlockShapeDetection(
+    const ExecutionContext* context) {
+  return ShouldBlockIfNotAllowed(context,
+                                 &HeliumPersonaSnapshot::allow_shape_detection);
 }
 
 bool PersonaSpoofingHelper::ShouldBlockWebSockets(
diff --git a/third_party/blink/renderer/modules/shapedetection/barcode_detector.cc b/third_party/blink/renderer/modules/shapedetection/barcode_detector.cc
--- a/third_party/blink/renderer/modules/shapedetection/barcode_detector.cc
+++ b/third_party/blink/renderer/modules/shapedetection/barcode_detector.cc
@@ -14,6 +14,7 @@
 #include "third_party/blink/renderer/core/dom/dom_exception.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
 #include "third_party/blink/renderer/core/geometry/dom_rect.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/workers/worker_thread.h"
 #include "third_party/blink/renderer/modules/canvas/imagebitmap/image_bitmap_source_util.h"
 #include "third_party/blink/renderer/modules/shapedetection/barcode_detector_statics.h"
@@ -65,6 +66,12 @@ shape_detection::mojom::blink::BarcodeFormat IdlEnumToBarcodeFormat(
 BarcodeDetector* BarcodeDetector::Create(ExecutionContext* context,
                                          const BarcodeDetectorOptions* options,
                                          ExceptionState& exception_state) {
+  if (PersonaSpoofingHelper::ShouldBlockShapeDetection(context)) {
+    exception_state.ThrowDOMException(
+        DOMExceptionCode::kNotAllowedError,
+        "Shape detection is disabled by the active persona.");
+    return nullptr;
+  }
   return MakeGarbageCollected<BarcodeDetector>(context, options,
                                                exception_state);
 }
@@ -148,6 +155,18 @@ ScriptPromise<IDLSequence<DetectedBarcode>> BarcodeDetector::detect(
     ScriptState* script_state,
     const V8ImageBitmapSource* image_source,
     ExceptionState& exception_state) {
+  if (PersonaSpoofingHelper::ShouldBlockShapeDetection(
+          ExecutionContext::From(script_state))) {
+    auto* resolver =
+        MakeGarbageCollected<ScriptPromiseResolver<IDLSequence<DetectedBarcode>>>(
+            script_state, exception_state.GetContext());
+    auto promise = resolver->Promise();
+    resolver->RejectWithDOMException(
+        DOMExceptionCode::kNotAllowedError,
+        "Shape detection is disabled by the active persona.");
+    return promise;
+  }
+
   std::optional<SkBitmap> bitmap = GetBitmapFromV8ImageBitmapSource(
       script_state, image_source, exception_state);
   if (!bitmap) {
diff --git a/third_party/blink/renderer/modules/shapedetection/barcode_detector_statics.cc b/third_party/blink/renderer/modules/shapedetection/barcode_detector_statics.cc
--- a/third_party/blink/renderer/modules/shapedetection/barcode_detector_statics.cc
+++ b/third_party/blink/renderer/modules/shapedetection/barcode_detector_statics.cc
@@ -9,6 +9,7 @@
 #include "third_party/blink/public/platform/task_type.h"
 #include "third_party/blink/renderer/bindings/core/v8/script_promise_resolver.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/modules/shapedetection/barcode_detector.h"
 
 namespace blink {
@@ -48,6 +49,11 @@ BarcodeDetectorStatics::EnumerateSupportedFormats(ScriptState* script_state) {
       MakeGarbageCollected<ScriptPromiseResolver<IDLSequence<V8BarcodeFormat>>>(
           script_state);
   auto promise = resolver->Promise();
+  if (PersonaSpoofingHelper::ShouldBlockShapeDetection(
+          ExecutionContext::From(script_state))) {
+    resolver->Resolve(Vector<V8BarcodeFormat>());
+    return promise;
+  }
   get_supported_format_requests_.insert(resolver);
   EnsureServiceConnection();
   service_->EnumerateSupportedFormats(
diff --git a/third_party/blink/renderer/modules/shapedetection/face_detector.cc b/third_party/blink/renderer/modules/shapedetection/face_detector.cc
--- a/third_party/blink/renderer/modules/shapedetection/face_detector.cc
+++ b/third_party/blink/renderer/modules/shapedetection/face_detector.cc
@@ -18,6 +18,7 @@
 #include "third_party/blink/renderer/core/dom/dom_exception.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
 #include "third_party/blink/renderer/core/geometry/dom_rect.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/workers/worker_thread.h"
 #include "third_party/blink/renderer/modules/canvas/imagebitmap/image_bitmap_source_util.h"
 #include "third_party/blink/renderer/platform/heap/garbage_collected.h"
@@ -74,6 +75,18 @@ ScriptPromise<IDLSequence<DetectedFace>> FaceDetector::detect(
     ScriptState* script_state,
     const V8ImageBitmapSource* image_source,
     ExceptionState& exception_state) {
+  if (PersonaSpoofingHelper::ShouldBlockShapeDetection(
+          ExecutionContext::From(script_state))) {
+    auto* resolver =
+        MakeGarbageCollected<ScriptPromiseResolver<IDLSequence<DetectedFace>>>(
+            script_state, exception_state.GetContext());
+    auto promise = resolver->Promise();
+    resolver->RejectWithDOMException(
+        DOMExceptionCode::kNotAllowedError,
+        "Shape detection is disabled by the active persona.");
+    return promise;
+  }
+
   std::optional<SkBitmap> bitmap = GetBitmapFromV8ImageBitmapSource(
       script_state, image_source, exception_state);
   if (!bitmap) {
diff --git a/third_party/blink/renderer/modules/shapedetection/text_detector.cc b/third_party/blink/renderer/modules/shapedetection/text_detector.cc
--- a/third_party/blink/renderer/modules/shapedetection/text_detector.cc
+++ b/third_party/blink/renderer/modules/shapedetection/text_detector.cc
@@ -14,6 +14,7 @@
 #include "third_party/blink/renderer/core/dom/dom_exception.h"
 #include "third_party/blink/renderer/core/execution_context/execution_context.h"
 #include "third_party/blink/renderer/core/geometry/dom_rect.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/workers/worker_thread.h"
 #include "third_party/blink/renderer/modules/canvas/imagebitmap/image_bitmap_source_util.h"
 #include "third_party/blink/renderer/platform/heap/garbage_collected.h"
@@ -38,6 +39,18 @@ ScriptPromise<IDLSequence<DetectedText>> TextDetector::detect(
     ScriptState* script_state,
     const V8ImageBitmapSource* image_source,
     ExceptionState& exception_state) {
+  if (PersonaSpoofingHelper::ShouldBlockShapeDetection(
+          ExecutionContext::From(script_state))) {
+    auto* resolver =
+        MakeGarbageCollected<ScriptPromiseResolver<IDLSequence<DetectedText>>>(
+            script_state, exception_state.GetContext());
+    auto promise = resolver->Promise();
+    resolver->RejectWithDOMException(
+        DOMExceptionCode::kNotAllowedError,
+        "Shape detection is disabled by the active persona.");
+    return promise;
+  }
+
   std::optional<SkBitmap> bitmap = GetBitmapFromV8ImageBitmapSource(
       script_state, image_source, exception_state);
   if (!bitmap) {
