diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 164323c2..e430562c 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -239,17 +239,50 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const touchEnabled = Boolean(hardware['touchEnabled']);
     const maxTouchPoints = this.numberValue_(hardware['maxTouchPoints']);
     const storageQuota = this.numberValue_(advanced['storageQuotaGb']);
+    const allowGeolocation = Boolean(advanced['allowGeolocation']);
     const allowMediaCapture = Boolean(advanced['allowMediaCapture']);
     const allowProtectedMediaIdentifier =
         Boolean(advanced['allowProtectedMediaIdentifier']);
+    const allowDisplayCapture = Boolean(advanced['allowDisplayCapture']);
     const allowDeviceApis = Boolean(advanced['allowDeviceApis']);
+    const allowGamepads = Boolean(advanced['allowGamepads']);
+    const allowSensorApis = Boolean(advanced['allowSensorApis']);
+    const allowMidi = Boolean(advanced['allowMidi']);
+    const allowLocalNetworkApis =
+        Boolean(advanced['allowLocalNetworkApis']);
+    const allowWebNfc = Boolean(advanced['allowWebNfc']);
+    const allowWindowManagement =
+        Boolean(advanced['allowWindowManagement']);
     const allowInstalledApps = Boolean(advanced['allowInstalledApps']);
     const allowPaymentHandler = Boolean(advanced['allowPaymentHandler']);
     const allowPlatformCredentials =
         Boolean(advanced['allowPlatformCredentials']);
+    const allowWebXr = Boolean(advanced['allowWebXr']);
+    const allowKeyboardLayout = Boolean(advanced['allowKeyboardLayout']);
+    const allowRealBatteryStatus =
+        Boolean(advanced['allowRealBatteryStatus']);
+    const allowLocalFonts = Boolean(advanced['allowLocalFonts']);
+    const allowPersistentDeviceId =
+        Boolean(advanced['allowPersistentDeviceId']);
+    const allowNotifications = Boolean(advanced['allowNotifications']);
+    const allowPersistentStorage =
+        Boolean(advanced['allowPersistentStorage']);
+    const allowClipboard = Boolean(advanced['allowClipboard']);
+    const allowIdleDetection = Boolean(advanced['allowIdleDetection']);
+    const allowWakeLock = Boolean(advanced['allowWakeLock']);
+    const allowStorageAccess = Boolean(advanced['allowStorageAccess']);
+    const allowPointerLock = Boolean(advanced['allowPointerLock']);
+    const allowFullscreen = Boolean(advanced['allowFullscreen']);
     const allowWebAppInstallation =
         Boolean(advanced['allowWebAppInstallation']);
     const allowWebPrinting = Boolean(advanced['allowWebPrinting']);
+    const allowFileSystemAccess =
+        Boolean(advanced['allowFileSystemAccess']);
+    const allowWebLocks = Boolean(advanced['allowWebLocks']);
+    const allowContacts = Boolean(advanced['allowContacts']);
+    const allowBadging = Boolean(advanced['allowBadging']);
+    const allowWebShare = Boolean(advanced['allowWebShare']);
+    const allowEyeDropper = Boolean(advanced['allowEyeDropper']);
     const allowPresentationApis = Boolean(advanced['allowPresentationApis']);
     const allowRemotePlayback = Boolean(advanced['allowRemotePlayback']);
     const speechVoiceName = String(advanced['speechVoiceName'] || '');
@@ -575,16 +608,53 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'allowMediaCapture exposes real camera, microphone, screen speech recognition, and on-device speech recognition service bindings.');
     }
+    if (allowGeolocation) {
+      this.addDraftWarning_(
+          warnings,
+          'allowGeolocation exposes real or permission-backed location APIs.');
+    }
     if (allowProtectedMediaIdentifier) {
       this.addDraftWarning_(
           warnings,
           'allowProtectedMediaIdentifier exposes real protected media and hardware secure decryption capabilities.');
     }
+    if (allowDisplayCapture) {
+      this.addDraftWarning_(
+          warnings,
+          'allowDisplayCapture exposes real screen and window capture surfaces.');
+    }
     if (allowDeviceApis) {
       this.addDraftWarning_(
           warnings,
           'allowDeviceApis exposes real Bluetooth, WebUSB, Web Serial, WebHID, and Smart Card service bindings.');
     }
+    if (allowGamepads) {
+      this.addDraftWarning_(
+          warnings, 'allowGamepads exposes connected gamepad devices.');
+    }
+    if (allowSensorApis) {
+      this.addDraftWarning_(
+          warnings,
+          'allowSensorApis exposes motion, orientation, and sensor permission surfaces.');
+    }
+    if (allowMidi) {
+      this.addDraftWarning_(
+          warnings, 'allowMidi exposes connected MIDI devices and permissions.');
+    }
+    if (allowLocalNetworkApis) {
+      this.addDraftWarning_(
+          warnings,
+          'allowLocalNetworkApis exposes local network probing and address-adjacent APIs.');
+    }
+    if (allowWebNfc) {
+      this.addDraftWarning_(
+          warnings, 'allowWebNfc exposes NFC hardware capability surfaces.');
+    }
+    if (allowWindowManagement) {
+      this.addDraftWarning_(
+          warnings,
+          'allowWindowManagement exposes multi-screen and window placement APIs.');
+    }
     if (allowInstalledApps) {
       this.addDraftWarning_(
           warnings,
@@ -600,6 +670,68 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'allowPlatformCredentials exposes real Credential Management and platform passkey service bindings.');
     }
+    if (allowWebXr) {
+      this.addDraftWarning_(
+          warnings, 'allowWebXr exposes real XR device and immersive session APIs.');
+    }
+    if (allowKeyboardLayout) {
+      this.addDraftWarning_(
+          warnings,
+          'allowKeyboardLayout exposes real keyboard layout information.');
+    }
+    if (allowRealBatteryStatus) {
+      this.addDraftWarning_(
+          warnings,
+          'allowRealBatteryStatus exposes real battery and charging state.');
+    }
+    if (allowLocalFonts) {
+      this.addDraftWarning_(
+          warnings,
+          'allowLocalFonts exposes installed local font enumeration.');
+    }
+    if (allowPersistentDeviceId) {
+      this.addDraftWarning_(
+          warnings,
+          'allowPersistentDeviceId may expose stable device identifiers.');
+    }
+    if (allowNotifications) {
+      this.addDraftWarning_(
+          warnings,
+          'allowNotifications exposes notification permission and platform notification surfaces.');
+    }
+    if (allowPersistentStorage) {
+      this.addDraftWarning_(
+          warnings,
+          'allowPersistentStorage exposes persistent storage permission behavior.');
+    }
+    if (allowClipboard) {
+      this.addDraftWarning_(
+          warnings,
+          'allowClipboard exposes real clipboard read and write permission surfaces.');
+    }
+    if (allowIdleDetection) {
+      this.addDraftWarning_(
+          warnings,
+          'allowIdleDetection exposes user and screen idle state APIs.');
+    }
+    if (allowWakeLock) {
+      this.addDraftWarning_(
+          warnings, 'allowWakeLock exposes screen and system wake lock APIs.');
+    }
+    if (allowStorageAccess) {
+      this.addDraftWarning_(
+          warnings,
+          'allowStorageAccess exposes third-party storage access behavior.');
+    }
+    if (allowPointerLock) {
+      this.addDraftWarning_(
+          warnings,
+          'allowPointerLock exposes pointer lock interaction state.');
+    }
+    if (allowFullscreen) {
+      this.addDraftWarning_(
+          warnings, 'allowFullscreen exposes fullscreen permission behavior.');
+    }
     if (allowWebAppInstallation) {
       this.addDraftWarning_(
           warnings,
@@ -610,6 +742,31 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'allowWebPrinting exposes the browser print service and native print-preview integration.');
     }
+    if (allowFileSystemAccess) {
+      this.addDraftWarning_(
+          warnings,
+          'allowFileSystemAccess exposes local file system picker and handle APIs.');
+    }
+    if (allowWebLocks) {
+      this.addDraftWarning_(
+          warnings, 'allowWebLocks exposes cross-context lock coordination APIs.');
+    }
+    if (allowContacts) {
+      this.addDraftWarning_(
+          warnings, 'allowContacts exposes platform contact picker APIs.');
+    }
+    if (allowBadging) {
+      this.addDraftWarning_(
+          warnings, 'allowBadging exposes app badge service bindings.');
+    }
+    if (allowWebShare) {
+      this.addDraftWarning_(
+          warnings, 'allowWebShare exposes platform share service bindings.');
+    }
+    if (allowEyeDropper) {
+      this.addDraftWarning_(
+          warnings, 'allowEyeDropper exposes screen color sampling APIs.');
+    }
     if (!allowPresentationApis && allowRemotePlayback) {
       this.addDraftWarning_(
           warnings,
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 771bd43c..e7e5ae81 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -840,14 +840,41 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   base::Value::Dict* advanced = persona.FindDict("advanced");
   ASSERT_TRUE(advanced);
   advanced->Set("storageQuotaGb", 1.0);
+  advanced->Set("allowGeolocation", true);
   advanced->Set("allowMediaCapture", true);
   advanced->Set("allowProtectedMediaIdentifier", true);
+  advanced->Set("allowDisplayCapture", true);
   advanced->Set("allowDeviceApis", true);
+  advanced->Set("allowGamepads", true);
+  advanced->Set("allowSensorApis", true);
+  advanced->Set("allowMidi", true);
+  advanced->Set("allowLocalNetworkApis", true);
+  advanced->Set("allowWebNfc", true);
+  advanced->Set("allowWindowManagement", true);
   advanced->Set("allowInstalledApps", true);
   advanced->Set("allowPaymentHandler", true);
   advanced->Set("allowPlatformCredentials", true);
+  advanced->Set("allowWebXr", true);
+  advanced->Set("allowKeyboardLayout", true);
+  advanced->Set("allowRealBatteryStatus", true);
+  advanced->Set("allowLocalFonts", true);
+  advanced->Set("allowPersistentDeviceId", true);
+  advanced->Set("allowNotifications", true);
+  advanced->Set("allowPersistentStorage", true);
+  advanced->Set("allowClipboard", true);
+  advanced->Set("allowIdleDetection", true);
+  advanced->Set("allowWakeLock", true);
+  advanced->Set("allowStorageAccess", true);
+  advanced->Set("allowPointerLock", true);
+  advanced->Set("allowFullscreen", true);
   advanced->Set("allowWebAppInstallation", true);
   advanced->Set("allowWebPrinting", true);
+  advanced->Set("allowFileSystemAccess", true);
+  advanced->Set("allowWebLocks", true);
+  advanced->Set("allowContacts", true);
+  advanced->Set("allowBadging", true);
+  advanced->Set("allowWebShare", true);
+  advanced->Set("allowEyeDropper", true);
   advanced->Set("allowRemotePlayback", true);
   advanced->Set("speechVoiceName", "Samantha");
   advanced->Set("speechVoiceLang", "en-US");
@@ -907,14 +934,41 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "deviceMemory should use"));
   EXPECT_TRUE(WarningsContain(warnings, "Storage quota is too small"));
   EXPECT_TRUE(WarningsContain(warnings, "allowMediaCapture exposes real camera"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowGeolocation exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowProtectedMediaIdentifier exposes real"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowDisplayCapture exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowDeviceApis exposes real Bluetooth"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowGamepads exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowSensorApis exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowMidi exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowLocalNetworkApis exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowWebNfc exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowWindowManagement exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowInstalledApps exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowPaymentHandler exposes"));
   EXPECT_TRUE(WarningsContain(warnings,
                               "allowPlatformCredentials exposes real"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowWebXr exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowKeyboardLayout exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowRealBatteryStatus exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowLocalFonts exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowPersistentDeviceId"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowNotifications exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowPersistentStorage exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowClipboard exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowIdleDetection exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowWakeLock exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowStorageAccess exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowPointerLock exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowFullscreen exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowWebAppInstallation exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowWebPrinting exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowFileSystemAccess exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowWebLocks exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowContacts exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowBadging exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowWebShare exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowEyeDropper exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "Remote Playback reuses"));
   EXPECT_TRUE(WarningsContain(warnings, "maxTouchPoints should be 0"));
   EXPECT_TRUE(WarningsContain(warnings, "CoreText font rendering"));
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index c056e8ea..73398e06 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -681,22 +681,76 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedInt(persona, "hardware", "maxTouchPoints");
   const double storage_quota_gb =
       GetNestedDouble(persona, "advanced", "storageQuotaGb");
+  const bool allow_geolocation =
+      GetNestedBool(persona, "advanced", "allowGeolocation");
   const bool allow_media_capture =
       GetNestedBool(persona, "advanced", "allowMediaCapture");
   const bool allow_protected_media_identifier =
       GetNestedBool(persona, "advanced", "allowProtectedMediaIdentifier");
+  const bool allow_display_capture =
+      GetNestedBool(persona, "advanced", "allowDisplayCapture");
   const bool allow_device_apis =
       GetNestedBool(persona, "advanced", "allowDeviceApis");
+  const bool allow_gamepads =
+      GetNestedBool(persona, "advanced", "allowGamepads");
+  const bool allow_sensor_apis =
+      GetNestedBool(persona, "advanced", "allowSensorApis");
+  const bool allow_midi =
+      GetNestedBool(persona, "advanced", "allowMidi");
+  const bool allow_local_network_apis =
+      GetNestedBool(persona, "advanced", "allowLocalNetworkApis");
+  const bool allow_web_nfc =
+      GetNestedBool(persona, "advanced", "allowWebNfc");
+  const bool allow_window_management =
+      GetNestedBool(persona, "advanced", "allowWindowManagement");
   const bool allow_installed_apps =
       GetNestedBool(persona, "advanced", "allowInstalledApps");
   const bool allow_payment_handler =
       GetNestedBool(persona, "advanced", "allowPaymentHandler");
   const bool allow_platform_credentials =
       GetNestedBool(persona, "advanced", "allowPlatformCredentials");
+  const bool allow_web_xr =
+      GetNestedBool(persona, "advanced", "allowWebXr");
+  const bool allow_keyboard_layout =
+      GetNestedBool(persona, "advanced", "allowKeyboardLayout");
+  const bool allow_real_battery_status =
+      GetNestedBool(persona, "advanced", "allowRealBatteryStatus");
+  const bool allow_local_fonts =
+      GetNestedBool(persona, "advanced", "allowLocalFonts");
+  const bool allow_persistent_device_id =
+      GetNestedBool(persona, "advanced", "allowPersistentDeviceId");
+  const bool allow_notifications =
+      GetNestedBool(persona, "advanced", "allowNotifications");
+  const bool allow_persistent_storage =
+      GetNestedBool(persona, "advanced", "allowPersistentStorage");
+  const bool allow_clipboard =
+      GetNestedBool(persona, "advanced", "allowClipboard");
+  const bool allow_idle_detection =
+      GetNestedBool(persona, "advanced", "allowIdleDetection");
+  const bool allow_wake_lock =
+      GetNestedBool(persona, "advanced", "allowWakeLock");
+  const bool allow_storage_access =
+      GetNestedBool(persona, "advanced", "allowStorageAccess");
+  const bool allow_pointer_lock =
+      GetNestedBool(persona, "advanced", "allowPointerLock");
+  const bool allow_fullscreen =
+      GetNestedBool(persona, "advanced", "allowFullscreen");
   const bool allow_web_app_installation =
       GetNestedBool(persona, "advanced", "allowWebAppInstallation");
   const bool allow_web_printing =
       GetNestedBool(persona, "advanced", "allowWebPrinting");
+  const bool allow_file_system_access =
+      GetNestedBool(persona, "advanced", "allowFileSystemAccess");
+  const bool allow_web_locks =
+      GetNestedBool(persona, "advanced", "allowWebLocks");
+  const bool allow_contacts =
+      GetNestedBool(persona, "advanced", "allowContacts");
+  const bool allow_badging =
+      GetNestedBool(persona, "advanced", "allowBadging");
+  const bool allow_web_share =
+      GetNestedBool(persona, "advanced", "allowWebShare");
+  const bool allow_eye_dropper =
+      GetNestedBool(persona, "advanced", "allowEyeDropper");
   const bool allow_presentation_apis =
       GetNestedBool(persona, "advanced", "allowPresentationApis");
   const bool allow_remote_playback =
@@ -1083,14 +1137,44 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "allowMediaCapture exposes real camera, microphone, screen speech recognition, and on-device speech recognition service bindings.");
   }
+  if (allow_geolocation) {
+    AddWarning(warnings,
+               "allowGeolocation exposes real or permission-backed location APIs.");
+  }
   if (allow_protected_media_identifier) {
     AddWarning(warnings,
                "allowProtectedMediaIdentifier exposes real protected media and hardware secure decryption capabilities.");
   }
+  if (allow_display_capture) {
+    AddWarning(warnings,
+               "allowDisplayCapture exposes real screen and window capture surfaces.");
+  }
   if (allow_device_apis) {
     AddWarning(warnings,
                "allowDeviceApis exposes real Bluetooth, WebUSB, Web Serial, WebHID, and Smart Card service bindings.");
   }
+  if (allow_gamepads) {
+    AddWarning(warnings, "allowGamepads exposes connected gamepad devices.");
+  }
+  if (allow_sensor_apis) {
+    AddWarning(warnings,
+               "allowSensorApis exposes motion, orientation, and sensor permission surfaces.");
+  }
+  if (allow_midi) {
+    AddWarning(warnings,
+               "allowMidi exposes connected MIDI devices and permissions.");
+  }
+  if (allow_local_network_apis) {
+    AddWarning(warnings,
+               "allowLocalNetworkApis exposes local network probing and address-adjacent APIs.");
+  }
+  if (allow_web_nfc) {
+    AddWarning(warnings, "allowWebNfc exposes NFC hardware capability surfaces.");
+  }
+  if (allow_window_management) {
+    AddWarning(warnings,
+               "allowWindowManagement exposes multi-screen and window placement APIs.");
+  }
   if (allow_installed_apps) {
     AddWarning(warnings,
                "allowInstalledApps exposes installed app discovery service bindings.");
@@ -1103,6 +1187,58 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "allowPlatformCredentials exposes real Credential Management and platform passkey service bindings.");
   }
+  if (allow_web_xr) {
+    AddWarning(warnings,
+               "allowWebXr exposes real XR device and immersive session APIs.");
+  }
+  if (allow_keyboard_layout) {
+    AddWarning(warnings,
+               "allowKeyboardLayout exposes real keyboard layout information.");
+  }
+  if (allow_real_battery_status) {
+    AddWarning(warnings,
+               "allowRealBatteryStatus exposes real battery and charging state.");
+  }
+  if (allow_local_fonts) {
+    AddWarning(warnings,
+               "allowLocalFonts exposes installed local font enumeration.");
+  }
+  if (allow_persistent_device_id) {
+    AddWarning(warnings,
+               "allowPersistentDeviceId may expose stable device identifiers.");
+  }
+  if (allow_notifications) {
+    AddWarning(warnings,
+               "allowNotifications exposes notification permission and platform notification surfaces.");
+  }
+  if (allow_persistent_storage) {
+    AddWarning(warnings,
+               "allowPersistentStorage exposes persistent storage permission behavior.");
+  }
+  if (allow_clipboard) {
+    AddWarning(warnings,
+               "allowClipboard exposes real clipboard read and write permission surfaces.");
+  }
+  if (allow_idle_detection) {
+    AddWarning(warnings,
+               "allowIdleDetection exposes user and screen idle state APIs.");
+  }
+  if (allow_wake_lock) {
+    AddWarning(warnings,
+               "allowWakeLock exposes screen and system wake lock APIs.");
+  }
+  if (allow_storage_access) {
+    AddWarning(warnings,
+               "allowStorageAccess exposes third-party storage access behavior.");
+  }
+  if (allow_pointer_lock) {
+    AddWarning(warnings,
+               "allowPointerLock exposes pointer lock interaction state.");
+  }
+  if (allow_fullscreen) {
+    AddWarning(warnings,
+               "allowFullscreen exposes fullscreen permission behavior.");
+  }
   if (allow_web_app_installation) {
     AddWarning(warnings,
                "allowWebAppInstallation exposes web install, sub-app, and isolated web app service bindings.");
@@ -1111,6 +1247,26 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "allowWebPrinting exposes the browser print service and native print-preview integration.");
   }
+  if (allow_file_system_access) {
+    AddWarning(warnings,
+               "allowFileSystemAccess exposes local file system picker and handle APIs.");
+  }
+  if (allow_web_locks) {
+    AddWarning(warnings,
+               "allowWebLocks exposes cross-context lock coordination APIs.");
+  }
+  if (allow_contacts) {
+    AddWarning(warnings, "allowContacts exposes platform contact picker APIs.");
+  }
+  if (allow_badging) {
+    AddWarning(warnings, "allowBadging exposes app badge service bindings.");
+  }
+  if (allow_web_share) {
+    AddWarning(warnings, "allowWebShare exposes platform share service bindings.");
+  }
+  if (allow_eye_dropper) {
+    AddWarning(warnings, "allowEyeDropper exposes screen color sampling APIs.");
+  }
   if (!allow_presentation_apis && allow_remote_playback) {
     AddWarning(warnings,
                "Remote Playback reuses PresentationService; disabling Presentation API does not fully isolate that browser service while Remote Playback is allowed.");
