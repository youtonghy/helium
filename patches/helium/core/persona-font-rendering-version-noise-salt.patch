diff --git a/content/browser/helium_noise/noise_token_data.cc b/content/browser/helium_noise/noise_token_data.cc
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -240,6 +240,7 @@ std::string PersonaNoiseSalt(
   AppendSaltList(salt, persona_snapshot.font_aliases);
   AppendSaltPart(salt, persona_snapshot.font_rendering_id);
   AppendSaltPart(salt, persona_snapshot.font_rendering_name);
+  AppendSaltPart(salt, persona_snapshot.font_rendering_profile_version);
   AppendSaltPart(salt, persona_snapshot.font_rendering_engine);
   AppendSaltPart(salt, persona_snapshot.font_rendering_hinting);
   AppendSaltPart(salt, persona_snapshot.font_antialiasing);
diff --git a/content/browser/helium_noise/noise_token_data_unittest.cc b/content/browser/helium_noise/noise_token_data_unittest.cc
--- a/content/browser/helium_noise/noise_token_data_unittest.cc
+++ b/content/browser/helium_noise/noise_token_data_unittest.cc
@@ -94,6 +94,7 @@ blink::HeliumPersonaSnapshot MakeSeededPersona(std::string noise_seed) {
   snapshot.font_families = {"Arial", "Calibri", "Segoe UI"};
   snapshot.font_aliases = {"sans-serif=Arial", "system-ui=Segoe UI"};
   snapshot.font_rendering_id = "windows-cleartype";
+  snapshot.font_rendering_profile_version = 1;
   snapshot.font_rendering_engine = "DirectWrite";
   snapshot.font_antialiasing = true;
   snapshot.font_subpixel_rendering = true;
@@ -181,6 +182,9 @@ TEST_F(HeliumNoiseTokenDataTest,
   different_network.monochrome_depth = 1;
   blink::HeliumPersonaSnapshot different_seed =
       MakeSeededPersona("manual-seed-2");
+  blink::HeliumPersonaSnapshot different_font_rendering_version = persona;
+  different_font_rendering_version.font_rendering_profile_version =
+      persona.font_rendering_profile_version + 1;
   blink::HeliumPersonaSnapshot different_media_devices = persona;
   different_media_devices.media_video_input_device_id = "persona-camera-2";
   different_media_devices.media_video_input_label = "External USB Camera";
@@ -200,6 +204,9 @@ TEST_F(HeliumNoiseTokenDataTest,
   const blink::HeliumNoiseTokenMap different_network_tokens =
       HeliumNoiseTokenData::GetTokens(&browser_context_, origin,
                                       different_network);
+  const blink::HeliumNoiseTokenMap different_font_rendering_version_tokens =
+      HeliumNoiseTokenData::GetTokens(&browser_context_, origin,
+                                      different_font_rendering_version);
   const blink::HeliumNoiseTokenMap different_media_tokens =
       HeliumNoiseTokenData::GetTokens(&browser_context_, origin,
                                       different_media_devices);
@@ -218,6 +225,9 @@ TEST_F(HeliumNoiseTokenDataTest,
   EXPECT_NE(TokenValue(tokens, blink::HeliumNoiseFeature::kCanvas),
             TokenValue(different_persona_tokens,
                        blink::HeliumNoiseFeature::kCanvas));
+  EXPECT_NE(TokenValue(tokens, blink::HeliumNoiseFeature::kCanvas),
+            TokenValue(different_font_rendering_version_tokens,
+                       blink::HeliumNoiseFeature::kCanvas));
   EXPECT_NE(TokenValue(tokens, blink::HeliumNoiseFeature::kCanvas),
             TokenValue(different_network_tokens,
                        blink::HeliumNoiseFeature::kCanvas));
