--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -65,6 +65,24 @@ base::Value::Dict MakeRegion(std::string_view timezone,
   return region;
 }
 
+base::Value::Dict MakeNetwork(std::string_view type,
+                              std::string_view effective_type,
+                              int rtt,
+                              double downlink,
+                              double downlink_max,
+                              bool save_data,
+                              bool online) {
+  base::Value::Dict network;
+  network.Set("type", type);
+  network.Set("effectiveType", effective_type);
+  network.Set("rtt", rtt);
+  network.Set("downlink", downlink);
+  network.Set("downlinkMax", downlink_max);
+  network.Set("saveData", save_data);
+  network.Set("online", online);
+  return network;
+}
+
 base::Value::Dict MakeWebGLProfile(int max_texture_size,
                                   int max_samples,
                                   double max_anisotropy) {
@@ -314,6 +314,8 @@ base::Value::Dict MakePreset(std::string_view id,
   preset.Set("platform", platform);
   preset.Set("uaCh", std::move(ua_ch));
   preset.Set("region", std::move(region));
+  preset.Set("network",
+             MakeNetwork("ethernet", "4g", 50, 10.0, 0.0, false, true));
   preset.Set("gpu", std::move(gpu));
   preset.Set("screen", std::move(screen));
   preset.Set("hardware", std::move(hardware));
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -60,6 +60,16 @@ TEST(HeliumPersonaPrefsTest, BuiltinPresetsContainCompleteSystemImage) {
   EXPECT_TRUE(ProfilesContainId(presets, "preset-linux-it-it-rome"));
   EXPECT_TRUE(ProfilesContainId(presets, "preset-windows-11-en-in-kolkata"));
 
+  for (const base::Value& value : presets) {
+    const base::Value::Dict* preset = value.GetIfDict();
+    ASSERT_TRUE(preset);
+    const std::string* id = preset->FindString("id");
+    SCOPED_TRACE(id ? *id : std::string("<missing id>"));
+    EXPECT_TRUE(preset->FindDict("network"));
+    EXPECT_TRUE(PersonaHasRequiredFields(*preset));
+    EXPECT_TRUE(preset->FindString("noiseSeed"));
+  }
+
   const base::Value::Dict& windows = DictAt(presets, 1);
   EXPECT_TRUE(PersonaHasRequiredFields(windows));
   EXPECT_EQ("Windows 11", *windows.FindString("os"));
