diff --git a/third_party/blink/renderer/modules/device_orientation/DEPS b/third_party/blink/renderer/modules/device_orientation/DEPS
index 970a071..2a6f3ac 100644
--- a/third_party/blink/renderer/modules/device_orientation/DEPS
+++ b/third_party/blink/renderer/modules/device_orientation/DEPS
@@ -9,6 +9,7 @@ include_rules = [
     "+services/device/public/cpp/generic_sensor/sensor_reading_shared_buffer.h",
     "+services/device/public/cpp/generic_sensor/sensor_reading_shared_buffer_reader.h",
     "-third_party/blink/renderer/modules",
+    "+third_party/blink/renderer/core/helium_persona",
     "+third_party/blink/renderer/modules/device_orientation",
     "+third_party/blink/renderer/modules/event_modules.h",
     "+third_party/blink/renderer/modules/event_target_modules.h",
diff --git a/third_party/blink/renderer/modules/device_orientation/device_motion_controller.cc b/third_party/blink/renderer/modules/device_orientation/device_motion_controller.cc
index b16806e..5275df2 100644
--- a/third_party/blink/renderer/modules/device_orientation/device_motion_controller.cc
+++ b/third_party/blink/renderer/modules/device_orientation/device_motion_controller.cc
@@ -12,6 +12,7 @@
 #include "third_party/blink/renderer/core/dom/document.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
 #include "third_party/blink/renderer/core/frame/settings.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/modules/device_orientation/device_motion_data.h"
 #include "third_party/blink/renderer/modules/device_orientation/device_motion_event.h"
 #include "third_party/blink/renderer/modules/device_orientation/device_motion_event_pump.h"
@@ -48,6 +49,9 @@ void DeviceMotionController::DidAddEventListener(
   if (event_type != EventTypeName())
     return;
 
+  if (PersonaSpoofingHelper::IsEnabled(&GetWindow()))
+    return;
+
   // The window could be detached, e.g. if it is the `contentWindow` of an
   // <iframe> that has been removed from the DOM of its parent frame.
   if (GetWindow().IsContextDestroyed())
@@ -132,6 +136,15 @@ ScriptPromise<V8PermissionState> DeviceMotionController::RequestPermission(
 
   has_requested_permission_ = true;
 
+  if (PersonaSpoofingHelper::IsEnabled(context)) {
+    auto* resolver =
+        MakeGarbageCollected<ScriptPromiseResolver<V8PermissionState>>(
+            script_state);
+    auto promise = resolver->Promise();
+    resolver->Resolve(V8PermissionState(V8PermissionState::Enum::kDenied));
+    return promise;
+  }
+
   if (!permission_service_.is_bound()) {
     ConnectToPermissionService(context,
                                permission_service_.BindNewPipeAndPassReceiver(
diff --git a/third_party/blink/renderer/modules/device_orientation/device_orientation_absolute_controller.cc b/third_party/blink/renderer/modules/device_orientation/device_orientation_absolute_controller.cc
index 181f8c0..3a144d3 100644
--- a/third_party/blink/renderer/modules/device_orientation/device_orientation_absolute_controller.cc
+++ b/third_party/blink/renderer/modules/device_orientation/device_orientation_absolute_controller.cc
@@ -7,6 +7,7 @@
 #include "services/network/public/mojom/permissions_policy/permissions_policy_feature.mojom-blink.h"
 #include "third_party/blink/public/mojom/use_counter/metrics/web_feature.mojom-blink.h"
 #include "third_party/blink/renderer/core/frame/settings.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/modules/device_orientation/device_orientation_event_pump.h"
 
 namespace blink {
@@ -40,6 +41,9 @@ void DeviceOrientationAbsoluteController::DidAddEventListener(
   if (event_type != EventTypeName())
     return;
 
+  if (PersonaSpoofingHelper::IsEnabled(&GetWindow()))
+    return;
+
   // The window could be detached, e.g. if it is the `contentWindow` of an
   // <iframe> that has been removed from the DOM of its parent frame.
   if (GetWindow().IsContextDestroyed())
diff --git a/third_party/blink/renderer/modules/device_orientation/device_orientation_controller.cc b/third_party/blink/renderer/modules/device_orientation/device_orientation_controller.cc
index d1bdd80..8973a1a 100644
--- a/third_party/blink/renderer/modules/device_orientation/device_orientation_controller.cc
+++ b/third_party/blink/renderer/modules/device_orientation/device_orientation_controller.cc
@@ -13,6 +13,7 @@
 #include "third_party/blink/renderer/core/frame/frame_console.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
 #include "third_party/blink/renderer/core/frame/settings.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/inspector/console_message.h"
 #include "third_party/blink/renderer/modules/device_orientation/device_orientation_data.h"
 #include "third_party/blink/renderer/modules/device_orientation/device_orientation_event.h"
@@ -60,6 +61,9 @@ void DeviceOrientationController::DidAddEventListener(
   if (event_type != EventTypeName())
     return;
 
+  if (PersonaSpoofingHelper::IsEnabled(&GetWindow()))
+    return;
+
   // The window could be detached, e.g. if it is the `contentWindow` of an
   // <iframe> that has been removed from the DOM of its parent frame.
   if (GetWindow().IsContextDestroyed())
@@ -183,6 +187,15 @@ ScriptPromise<V8PermissionState> DeviceOrientationController::RequestPermission(
 
   has_requested_permission_ = true;
 
+  if (PersonaSpoofingHelper::IsEnabled(context)) {
+    auto* resolver =
+        MakeGarbageCollected<ScriptPromiseResolver<V8PermissionState>>(
+            script_state);
+    auto promise = resolver->Promise();
+    resolver->Resolve(V8PermissionState(V8PermissionState::Enum::kDenied));
+    return promise;
+  }
+
   if (!permission_service_.is_bound()) {
     ConnectToPermissionService(context,
                                permission_service_.BindNewPipeAndPassReceiver(
diff --git a/third_party/blink/renderer/modules/device_orientation/device_sensor_event_pump.cc b/third_party/blink/renderer/modules/device_orientation/device_sensor_event_pump.cc
index 0261a9e..4ebae2a 100644
--- a/third_party/blink/renderer/modules/device_orientation/device_sensor_event_pump.cc
+++ b/third_party/blink/renderer/modules/device_orientation/device_sensor_event_pump.cc
@@ -6,6 +6,7 @@
 
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 
 namespace blink {
 
@@ -14,6 +15,8 @@ void DeviceSensorEventPump::Start(LocalFrame& frame) {
 
   if (state_ != PumpState::kStopped)
     return;
+  if (PersonaSpoofingHelper::IsEnabled(frame.DomWindow()))
+    return;
 
   DCHECK(!timer_.IsActive());
 
diff --git a/third_party/blink/renderer/modules/sensor/DEPS b/third_party/blink/renderer/modules/sensor/DEPS
index ceb8fb2..636dee6 100644
--- a/third_party/blink/renderer/modules/sensor/DEPS
+++ b/third_party/blink/renderer/modules/sensor/DEPS
@@ -5,6 +5,7 @@ include_rules = [
     "+services/device/public/mojom/sensor.mojom-blink-forward.h",
     "+services/device/public/mojom/sensor_provider.mojom-blink.h",
     "-third_party/blink/renderer/modules",
+    "+third_party/blink/renderer/core/helium_persona",
     "+third_party/blink/renderer/modules/event_modules.h",
     "+third_party/blink/renderer/modules/event_target_modules.h",
     "+third_party/blink/renderer/modules/modules_export.h",
diff --git a/third_party/blink/renderer/modules/sensor/sensor.cc b/third_party/blink/renderer/modules/sensor/sensor.cc
index 2712662..245809a 100644
--- a/third_party/blink/renderer/modules/sensor/sensor.cc
+++ b/third_party/blink/renderer/modules/sensor/sensor.cc
@@ -13,6 +13,7 @@
 #include "third_party/blink/public/platform/task_type.h"
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/core/inspector/console_message.h"
 #include "third_party/blink/renderer/core/timing/dom_window_performance.h"
 #include "third_party/blink/renderer/core/timing/window_performance.h"
@@ -98,6 +99,12 @@ void Sensor::start() {
     return;
   if (state_ != SensorState::kIdle)
     return;
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+    state_ = SensorState::kActivating;
+    HandleError(DOMExceptionCode::kNotReadableError,
+                SensorProxy::kDefaultErrorDescription);
+    return;
+  }
   state_ = SensorState::kActivating;
   Activate();
 }
diff --git a/third_party/blink/renderer/modules/webmidi/DEPS b/third_party/blink/renderer/modules/webmidi/DEPS
index 35e7842..6113bec 100644
--- a/third_party/blink/renderer/modules/webmidi/DEPS
+++ b/third_party/blink/renderer/modules/webmidi/DEPS
@@ -2,6 +2,7 @@ include_rules = [
     "+media/midi/midi_service.mojom-blink.h",
     "+media/midi/midi_service.mojom-blink-forward.h",
     "-third_party/blink/renderer/modules",
+    "+third_party/blink/renderer/core/helium_persona",
     "+third_party/blink/renderer/modules/event_modules.h",
     "+third_party/blink/renderer/modules/event_target_modules.h",
     "+third_party/blink/renderer/modules/modules_export.h",
diff --git a/third_party/blink/renderer/modules/webmidi/midi_access.cc b/third_party/blink/renderer/modules/webmidi/midi_access.cc
index d126a93..aba9231 100644
--- a/third_party/blink/renderer/modules/webmidi/midi_access.cc
+++ b/third_party/blink/renderer/modules/webmidi/midi_access.cc
@@ -33,6 +33,7 @@
 #include "third_party/blink/renderer/core/dom/document.h"
 #include "third_party/blink/renderer/core/loader/document_load_timing.h"
 #include "third_party/blink/renderer/core/loader/document_loader.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/modules/webmidi/midi_access_initializer.h"
 #include "third_party/blink/renderer/modules/webmidi/midi_connection_event.h"
 #include "third_party/blink/renderer/modules/webmidi/midi_input.h"
@@ -68,7 +69,8 @@ MIDIAccess::MIDIAccess(
       dispatcher_(dispatcher),
       sysex_enabled_(sysex_enabled),
       has_pending_activity_(false) {
-  dispatcher_->SetClient(this);
+  if (dispatcher_)
+    dispatcher_->SetClient(this);
   for (const auto& port : ports) {
     if (port.type == MIDIPortType::kInput) {
       inputs_.push_back(MakeGarbageCollected<MIDIInput>(
@@ -99,6 +101,11 @@ bool MIDIAccess::HasPendingActivity() const {
 }
 
 MIDIInputMap* MIDIAccess::inputs() const {
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+    HeapVector<Member<MIDIInput>> inputs;
+    return MakeGarbageCollected<MIDIInputMap>(inputs);
+  }
+
   HeapVector<Member<MIDIInput>> inputs;
   HashSet<String> ids;
   for (MIDIInput* input : inputs_) {
@@ -115,6 +122,11 @@ MIDIInputMap* MIDIAccess::inputs() const {
 }
 
 MIDIOutputMap* MIDIAccess::outputs() const {
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
+    HeapVector<Member<MIDIOutput>> outputs;
+    return MakeGarbageCollected<MIDIOutputMap>(outputs);
+  }
+
   HeapVector<Member<MIDIOutput>> outputs;
   HashSet<String> ids;
   for (MIDIOutput* output : outputs_) {
@@ -136,6 +148,9 @@ void MIDIAccess::DidAddInputPort(const String& id,
                                  const String& version,
                                  PortState state) {
   DCHECK(IsMainThread());
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    return;
+
   auto* port = MakeGarbageCollected<MIDIInput>(this, id, manufacturer, name,
                                                version, ToDeviceState(state));
   inputs_.push_back(port);
@@ -148,6 +163,9 @@ void MIDIAccess::DidAddOutputPort(const String& id,
                                   const String& version,
                                   PortState state) {
   DCHECK(IsMainThread());
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    return;
+
   unsigned port_index = outputs_.size();
   auto* port = MakeGarbageCollected<MIDIOutput>(
       this, port_index, id, manufacturer, name, version, ToDeviceState(state));
@@ -157,6 +175,9 @@ void MIDIAccess::DidAddOutputPort(const String& id,
 
 void MIDIAccess::DidSetInputPortState(unsigned port_index, PortState state) {
   DCHECK(IsMainThread());
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    return;
+
   if (port_index >= inputs_.size())
     return;
 
@@ -167,6 +188,9 @@ void MIDIAccess::DidSetInputPortState(unsigned port_index, PortState state) {
 
 void MIDIAccess::DidSetOutputPortState(unsigned port_index, PortState state) {
   DCHECK(IsMainThread());
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    return;
+
   if (port_index >= outputs_.size())
     return;
 
@@ -179,6 +203,9 @@ void MIDIAccess::DidReceiveMIDIData(unsigned port_index,
                                     base::span<const uint8_t> data,
                                     base::TimeTicks time_stamp) {
   DCHECK(IsMainThread());
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()))
+    return;
+
   if (port_index >= inputs_.size())
     return;
 
@@ -189,6 +216,9 @@ void MIDIAccess::SendMIDIData(unsigned port_index,
                               base::span<const uint8_t> data,
                               base::TimeTicks time_stamp) {
   DCHECK(!time_stamp.is_null());
+  if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext()) || !dispatcher_)
+    return;
+
   if (!GetExecutionContext() || !data.data() || data.empty() ||
       port_index >= outputs_.size()) {
     return;
diff --git a/third_party/blink/renderer/modules/webmidi/midi_access_initializer.cc b/third_party/blink/renderer/modules/webmidi/midi_access_initializer.cc
index 8075678..6785a00 100644
--- a/third_party/blink/renderer/modules/webmidi/midi_access_initializer.cc
+++ b/third_party/blink/renderer/modules/webmidi/midi_access_initializer.cc
@@ -19,6 +19,7 @@
 #include "third_party/blink/renderer/core/frame/local_dom_window.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"
 #include "third_party/blink/renderer/core/frame/navigator.h"
+#include "third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h"
 #include "third_party/blink/renderer/modules/permissions/permission_utils.h"
 #include "third_party/blink/renderer/modules/webmidi/midi_access.h"
 #include "third_party/blink/renderer/modules/webmidi/midi_port.h"
@@ -37,6 +38,13 @@ MIDIAccessInitializer::MIDIAccessInitializer(ScriptState* script_state,
       permission_service_(ExecutionContext::From(script_state)) {}
 
 ScriptPromise<MIDIAccess> MIDIAccessInitializer::Start(LocalDOMWindow* window) {
+  if (PersonaSpoofingHelper::IsEnabled(window)) {
+    resolver_->Resolve(MakeGarbageCollected<MIDIAccess>(
+        nullptr, /*sysex_enabled=*/false, port_descriptors_,
+        resolver_->GetExecutionContext()));
+    return resolver_->Promise();
+  }
+
   // See https://bit.ly/2S0zRAS for task types.
   scoped_refptr<base::SingleThreadTaskRunner> task_runner =
       window->GetTaskRunner(TaskType::kMiscPlatformAPI);
