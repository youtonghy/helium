--- a/third_party/blink/renderer/core/frame/navigator_language.h
+++ b/third_party/blink/renderer/core/frame/navigator_language.h
@@ -36,6 +36,7 @@ class CORE_EXPORT NavigatorLanguage : public GarbageCollectedMixin {
   WeakMember<ExecutionContext> execution_context_;
   Vector<String> languages_;
   bool languages_dirty_ = true;
+  bool used_persona_languages_ = false;
 };
 
 }  // namespace blink
--- a/third_party/blink/renderer/core/frame/navigator_language.cc
+++ b/third_party/blink/renderer/core/frame/navigator_language.cc
@@ -57,7 +57,8 @@ bool NavigatorLanguage::IsLanguagesDirty() const {
   probe::ApplyAcceptLanguageOverride(execution_context_,
                                      &accept_languages_override);
 
-  return languages_dirty_ || !accept_languages_override.IsNull();
+  return languages_dirty_ || !accept_languages_override.IsNull() ||
+         PersonaSpoofingHelper::IsEnabled(execution_context_);
 }
 
 void NavigatorLanguage::SetLanguagesDirty() {
@@ -89,8 +90,13 @@ void NavigatorLanguage::EnsureUpdatedLanguage() {
   if (!persona_languages.empty()) {
     languages_ = std::move(persona_languages);
     languages_dirty_ = false;
+    used_persona_languages_ = true;
     return;
   }
+  if (used_persona_languages_) {
+    languages_dirty_ = true;
+    used_persona_languages_ = false;
+  }
 
   if (languages_dirty_) {
     languages_ = ParseAndSanitize(GetAcceptLanguages());
