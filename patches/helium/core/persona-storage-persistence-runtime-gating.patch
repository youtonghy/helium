diff --git a/third_party/blink/renderer/modules/quota/storage_manager.cc b/third_party/blink/renderer/modules/quota/storage_manager.cc
index f4db3e88..cebb47d4 100644
--- a/third_party/blink/renderer/modules/quota/storage_manager.cc
+++ b/third_party/blink/renderer/modules/quota/storage_manager.cc
@@ -91,6 +92,22 @@ void ApplyPersonaStorageEstimate(ExecutionContext* context,
   usage_breakdown->backgroundFetch = 0;
 }
 
+bool PersonaAllowsPersistentStorage(ExecutionContext* context) {
+  const HeliumPersonaSnapshot* snapshot = PersonaSpoofingHelper::Get(context);
+  return !snapshot || snapshot->allow_persistent_storage;
+}
+
+ScriptPromise<IDLBoolean> ResolveBooleanPromise(
+    ScriptState* script_state,
+    ExceptionState& exception_state,
+    bool value) {
+  auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLBoolean>>(
+      script_state, exception_state.GetContext());
+  auto promise = resolver->Promise();
+  resolver->Resolve(value);
+  return promise;
+}
+
 void QueryStorageUsageAndQuotaCallback(
     ExecutionContext* execution_context,
     ScriptPromiseResolver<StorageEstimate>* resolver,
@@ -166,6 +183,10 @@ ScriptPromise<IDLBoolean> StorageManager::persist(
     return EmptyPromise();
   }
 
+  if (!PersonaAllowsPersistentStorage(window)) {
+    return ResolveBooleanPromise(script_state, exception_state, false);
+  }
+
   auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLBoolean>>(
       script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
@@ -190,6 +211,10 @@ ScriptPromise<IDLBoolean> StorageManager::persisted(
     return EmptyPromise();
   }
 
+  if (!PersonaAllowsPersistentStorage(execution_context)) {
+    return ResolveBooleanPromise(script_state, exception_state, false);
+  }
+
   auto* resolver = MakeGarbageCollected<ScriptPromiseResolver<IDLBoolean>>(
       script_state, exception_state.GetContext());
   auto promise = resolver->Promise();
