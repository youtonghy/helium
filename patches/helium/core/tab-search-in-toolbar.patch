--- a/chrome/browser/ui/toolbar/pinned_toolbar/pinned_toolbar_actions_model.cc
+++ b/chrome/browser/ui/toolbar/pinned_toolbar/pinned_toolbar_actions_model.cc
@@ -243,11 +243,6 @@ void PinnedToolbarActionsModel::MaybeMig
     UpdatePinnedState(kActionShowChromeLabs, true);
     pref_service_->SetBoolean(prefs::kPinnedChromeLabsMigrationComplete, true);
   }
-  if (features::HasTabSearchToolbarButton() &&
-      !pref_service_->GetBoolean(prefs::kTabSearchMigrationComplete)) {
-    UpdatePinnedState(kActionTabSearch, true);
-    pref_service_->SetBoolean(prefs::kTabSearchMigrationComplete, true);
-  }
   if (!pref_service_->GetBoolean(prefs::kPinnedCastMigrationComplete)) {
     bool previously_pinned =
         pref_service_->GetBoolean(prefs::kShowCastIconInToolbar);
--- a/chrome/browser/ui/toolbar/toolbar_pref_names.cc
+++ b/chrome/browser/ui/toolbar/toolbar_pref_names.cc
@@ -24,14 +24,6 @@ void RegisterProfilePrefs(user_prefs::Pr
     default_pinned_actions.Append(chrome_labs_action.value());
   }
 
-  if (features::HasTabSearchToolbarButton()) {
-    const std::optional<std::string>& tab_search_action =
-        actions::ActionIdMap::ActionIdToString(kActionTabSearch);
-    if (tab_search_action.has_value()) {
-      default_pinned_actions.Append(tab_search_action.value());
-    }
-  }
-
   if (base::FeatureList::IsEnabled(
           features::kTabsFromOtherDevicesSidePanelPinnedByDefault)) {
     const std::optional<std::string>& tabs_from_other_devices_action =
--- a/chrome/browser/ui/tabs/features.cc
+++ b/chrome/browser/ui/tabs/features.cc
@@ -84,7 +84,7 @@ BASE_FEATURE_PARAM(int,
 
 BASE_FEATURE(kTabSelectionByPointer, base::FEATURE_ENABLED_BY_DEFAULT);
 
-BASE_FEATURE(kHorizontalTabStripComboButton, base::FEATURE_ENABLED_BY_DEFAULT);
+BASE_FEATURE(kHorizontalTabStripComboButton, base::FEATURE_DISABLED_BY_DEFAULT);
 BASE_FEATURE_PARAM(bool,
                    kHorizontalTabStripComboButtonShowStartOnly,
                    &kHorizontalTabStripComboButton,
--- a/chrome/browser/ui/tabs/tab_strip_prefs.cc
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.cc
@@ -114,16 +114,16 @@ TabSearchPosition GetTabSearchPosition(
     }
   }
 
-  if (base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
-    return TabSearchPosition::kLeadingHorizontalTabstrip;
-  }
-
+  const bool has_tab_search_toolbar_button =
+      features::HasTabSearchToolbarButton();
+  if (has_tab_search_toolbar_button) {
+    return TabSearchPosition::kToolbarButton;
+  }
+
+  if (base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
+    return TabSearchPosition::kLeadingHorizontalTabstrip;
+  }
+
-  const bool has_tab_search_toolbar_button =
-      features::HasTabSearchToolbarButton();
-  if (has_tab_search_toolbar_button) {
-    return TabSearchPosition::kToolbarButton;
-  }
-
   // If this pref has already been read, we need to return the same value.
   if (!g_tab_search_trailing_tabstrip_at_startup.has_value()) {
     g_tab_search_trailing_tabstrip_at_startup =
--- a/chrome/browser/ui/tab_search_feature.cc
+++ b/chrome/browser/ui/tab_search_feature.cc
@@ -12,6 +12,6 @@ namespace features {
 bool HasTabSearchToolbarButton() {
   // It is important that this value not change at runtime in production. Any
   // future updates to this function must maintain that property.
-  return glic::GlicEnabling::IsEnabledByGlobalCriteria();
+  return true;
 }
 }  // namespace features
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -4250,30 +4250,46 @@ void BrowserView::UpdateTabSearchBubbleHost() {
   auto* vertical_tab_strip_state_controller =
       tabs::VerticalTabStripStateController::From(browser_);
   if (vertical_tab_strip_state_controller &&
       vertical_tab_strip_state_controller->ShouldDisplayVerticalTabs()) {
-    auto* combo_button =
-        vertical_tab_strip_region_view_->GetTopContainer()->GetComboButton();
-    tab_search_bubble_host_ = std::make_unique<TabSearchBubbleHost>(
-        combo_button->end_button(), browser_.get());
-    combo_button->SetTabSearchBubbleHost(tab_search_bubble_host_.get());
-  } else if (base::FeatureList::IsEnabled(
-                 tabs::kHorizontalTabStripComboButton)) {
-    auto* combo_button = horizontal_tab_strip_region_view_->GetComboButton();
-    tab_search_bubble_host_ = std::make_unique<TabSearchBubbleHost>(
-        combo_button->end_button(), browser_.get());
-    combo_button->SetTabSearchBubbleHost(tab_search_bubble_host_.get());
-  } else if (tabs::GetTabSearchPosition(browser_) ==
-             tabs::TabSearchPosition::kToolbarButton) {
+    auto* top_container = vertical_tab_strip_region_view_
+                              ? vertical_tab_strip_region_view_->GetTopContainer()
+                              : nullptr;
+    auto* combo_button =
+        top_container ? top_container->GetComboButton() : nullptr;
+    if (combo_button && combo_button->end_button()) {
+      tab_search_bubble_host_ = std::make_unique<TabSearchBubbleHost>(
+          combo_button->end_button(), browser_.get());
+      combo_button->SetTabSearchBubbleHost(tab_search_bubble_host_.get());
+      return;
+    }
+  }
+
+  if (base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
+    auto* combo_button = horizontal_tab_strip_region_view_
+                             ? horizontal_tab_strip_region_view_->GetComboButton()
+                             : nullptr;
+    if (combo_button && combo_button->end_button()) {
+      tab_search_bubble_host_ = std::make_unique<TabSearchBubbleHost>(
+          combo_button->end_button(), browser_.get());
+      combo_button->SetTabSearchBubbleHost(tab_search_bubble_host_.get());
+      return;
+    }
+  }
+
+  if (auto* toolbar_button = toolbar_->tab_search_button()) {
     tab_search_bubble_host_ = std::make_unique<TabSearchBubbleHost>(
-        toolbar_->tab_search_button(), browser_.get());
-    auto* toolbar_button_controller =
-        TabSearchToolbarButtonController::From(browser_.get());
-    CHECK(toolbar_button_controller);
-    toolbar_button_controller->UpdateBubbleHost(tab_search_bubble_host_.get());
-  } else {
+        toolbar_button, browser_.get());
+    if (auto* toolbar_button_controller =
+            TabSearchToolbarButtonController::From(browser_.get())) {
+      toolbar_button_controller->UpdateBubbleHost(tab_search_bubble_host_.get());
+    }
+    return;
+  }
+
+  if (auto* tab_search_button =
+          BrowserElementsViews::From(browser_.get())
+              ->GetViewAs<TabSearchButton>(kTabSearchButtonElementId)) {
     tab_search_bubble_host_ = std::make_unique<TabSearchBubbleHost>(
-        BrowserElementsViews::From(browser_.get())
-            ->GetViewAs<TabSearchButton>(kTabSearchButtonElementId),
-        browser_.get());
+        tab_search_button, browser_.get());
   }
 }
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -503,8 +503,7 @@ void ToolbarView::Init() {
     pinned_toolbar_actions_ = toolbar_webview_->GetPinnedToolbarActions();
   }
 
-  if (!base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton) &&
-      features::HasTabSearchToolbarButton()) {
+  if (features::HasTabSearchToolbarButton()) {
     CHECK(!features::IsWebUIPinnedToolbarActionsEnabled())
         << "WebUIPinnedToolbarActions does not support "
            "CreatePermanentButtonFor, consider enabling "
--- a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
@@ -225,9 +225,7 @@ class TabSearchPositionMetricsLogger {
 HorizontalTabStripRegionView::HorizontalTabStripRegionView(
     BrowserView* browser_view)
     : profile_(browser_view->GetProfile()),
-      render_tab_search_before_tab_strip_(
-          !tabs::GetDefaultTabSearchRightAligned() ||
-          base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)),
+      render_tab_search_before_tab_strip_(false),
       tab_search_position_metrics_logger_(
           std::make_unique<TabSearchPositionMetricsLogger>(
               browser_view->browser())),
@@ -244,16 +242,6 @@ HorizontalTabStripRegionView::Horizontal
 
   BrowserWindowInterface* const browser = browser_view->browser();
 
-  if (browser &&
-      (browser->GetType() == BrowserWindowInterface::Type::TYPE_NORMAL) &&
-      base::FeatureList::IsEnabled(tabs::kHorizontalTabStripComboButton)) {
-    combo_button_ = AddChildView(std::make_unique<TabStripComboButton>(
-        browser, TabStripComboButton::Context::kHorizontalTabStrip));
-    combo_button_->SetProperty(views::kCrossAxisAlignmentKey,
-                               views::LayoutAlignment::kCenter);
-    combo_button_->MaybeShowIPH();
-  }
-
   if (base::FeatureList::IsEnabled(features::kTabGroupsFocusing)) {
     unfocus_button_ = AddChildView(std::make_unique<TabStripControlButton>(
         browser, views::Button::PressedCallback(), vector_icons::kArrowBackIcon,
@@ -282,26 +270,9 @@ HorizontalTabStripRegionView::Horizontal
 
       tab_strip_action_container->SetProperty(views::kCrossAxisAlignmentKey,
                                               views::LayoutAlignment::kStart);
-    } else if (!base::FeatureList::IsEnabled(
-                   tabs::kHorizontalTabStripComboButton)) {
-      tab_search_button =
-          std::make_unique<TabSearchButton>(browser, Edge::kNone, Edge::kNone);
-      tab_search_button->SetProperty(views::kCrossAxisAlignmentKey,
-                                     views::LayoutAlignment::kCenter);
     }
   }
 
-  if (tab_search_button && render_tab_search_before_tab_strip_) {
-    tab_search_button->SetPaintToLayer();
-    tab_search_button->layer()->SetFillsBoundsOpaquely(false);
-
-    tab_search_button_ = AddChildView(std::move(tab_search_button));
-
-    // Inset between the tabsearch and tabstrip should be reduced to account for
-    // extra spacing.
-    tab_search_button_->SetProperty(views::kViewIgnoredByLayoutKey, true);
-  }
-
   tab_strip_ = AddChildView(CreateTabStrip(this, browser_view));
 
   // Allow the |tab_strip_| to grow into the free space available in
@@ -342,14 +313,6 @@ HorizontalTabStripRegionView::Horizontal
           .WithOrder(3));
 
   SetProperty(views::kElementIdentifierKey, kTabStripRegionElementId);
-
-  if (browser && tab_search_button && !render_tab_search_before_tab_strip_) {
-    tab_search_button_ = AddChildView(std::move(tab_search_button));
-    tab_search_button_->SetProperty(
-        views::kMarginsKey,
-        gfx::Insets::TLBR(0, 0, 0,
-                          GetLayoutConstant(LayoutConstant::kTabStripPadding)));
-  }
   if (tab_strip_action_container) {
     tab_strip_action_container_ =
         AddChildView(std::move(tab_strip_action_container));
