diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index c9fa598..21023b5 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -1008,6 +1008,13 @@ blink::HeliumPersonaSnapshot ToBlinkPersonaSnapshot(
   target.allow_shape_detection = source.allow_shape_detection;
   target.allow_web_sockets = source.allow_web_sockets;
   target.allow_web_transport = source.allow_web_transport;
+  target.allow_ai_apis = source.allow_ai_apis;
+  target.allow_web_nn = source.allow_web_nn;
+  target.allow_privacy_sandbox_apis = source.allow_privacy_sandbox_apis;
+  target.allow_private_state_tokens = source.allow_private_state_tokens;
+  target.allow_speech_synthesis = source.allow_speech_synthesis;
+  target.allow_handwriting_recognition =
+      source.allow_handwriting_recognition;
   return target;
 }
 
@@ -9667,4 +9674,4 @@ bool ChromeContentBrowserClient::IsFullscreenAllowedForUnfocusedWebContents(
     return guest->embedder_web_contents()->ContainsOrIsFocusedWebContents();
   }
   return false;
-}
\ No newline at end of file
+}
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.html b/chrome/browser/resources/settings/privacy_page/persona_page.html
index 5ac3373..1dffe53 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html
@@ -730,6 +730,24 @@
     <cr-checkbox checked="{{editablePersona_.advanced.allowWebTransport}}">
       Allow WebTransport
     </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowAiApis}}">
+      Allow AI APIs
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowWebNn}}">
+      Allow WebNN
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowPrivacySandboxApis}}">
+      Allow Privacy Sandbox APIs
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowPrivateStateTokens}}">
+      Allow private state tokens
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowSpeechSynthesis}}">
+      Allow speech synthesis
+    </cr-checkbox>
+    <cr-checkbox checked="{{editablePersona_.advanced.allowHandwritingRecognition}}">
+      Allow handwriting recognition
+    </cr-checkbox>
     <cr-checkbox checked="{{editablePersona_.advanced.allowPointerLock}}">
       Allow pointer lock permission
     </cr-checkbox>
@@ -752,4 +770,4 @@
       $i18n{heliumPersonaSave}
     </cr-button>
   </div>
-</settings-subpage>
\ No newline at end of file
+</settings-subpage>
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 6686da0..c13e7c3 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -363,6 +363,15 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     const allowShapeDetection = Boolean(advanced['allowShapeDetection']);
     const allowWebSockets = Boolean(advanced['allowWebSockets']);
     const allowWebTransport = Boolean(advanced['allowWebTransport']);
+    const allowAiApis = Boolean(advanced['allowAiApis']);
+    const allowWebNn = Boolean(advanced['allowWebNn']);
+    const allowPrivacySandboxApis =
+        Boolean(advanced['allowPrivacySandboxApis']);
+    const allowPrivateStateTokens =
+        Boolean(advanced['allowPrivateStateTokens']);
+    const allowSpeechSynthesis = Boolean(advanced['allowSpeechSynthesis']);
+    const allowHandwritingRecognition =
+        Boolean(advanced['allowHandwritingRecognition']);
     const allowPresentationApis = Boolean(advanced['allowPresentationApis']);
     const allowRemotePlayback = Boolean(advanced['allowRemotePlayback']);
     const speechVoiceName = String(advanced['speechVoiceName'] || '');
@@ -894,6 +903,36 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'allowRemotePlayback exposes remote media playback device discovery and session APIs.');
     }
+    if (allowAiApis) {
+      this.addDraftWarning_(
+          warnings,
+          'allowAiApis exposes AI manager, translation, and language detection service bindings.');
+    }
+    if (allowWebNn) {
+      this.addDraftWarning_(
+          warnings,
+          'allowWebNn exposes WebNN GPU-backed model compilation and inference bindings.');
+    }
+    if (allowPrivacySandboxApis) {
+      this.addDraftWarning_(
+          warnings,
+          'allowPrivacySandboxApis exposes privacy sandbox browser services such as browsing topics and ad auction.');
+    }
+    if (allowPrivateStateTokens) {
+      this.addDraftWarning_(
+          warnings,
+          'allowPrivateStateTokens exposes Trust Token and related private state token service bindings.');
+    }
+    if (allowSpeechSynthesis) {
+      this.addDraftWarning_(
+          warnings,
+          'allowSpeechSynthesis exposes real speech synthesis backend capabilities.');
+    }
+    if (allowHandwritingRecognition) {
+      this.addDraftWarning_(
+          warnings,
+          'allowHandwritingRecognition exposes handwriting recognition service bindings.');
+    }
     if (!allowWebSockets) {
       this.addDraftWarning_(
           warnings,
@@ -2064,6 +2103,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       allowShapeDetection: false,
       allowWebSockets: true,
       allowWebTransport: chance(80),
+      allowAiApis: false,
+      allowWebNn: false,
+      allowPrivacySandboxApis: false,
+      allowPrivateStateTokens: false,
+      allowSpeechSynthesis: chance(1),
+      allowHandwritingRecognition: false,
     };
     const balancedProfile: Record<string, boolean> = {
       ...protectedProfile,
@@ -2093,6 +2138,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       allowShapeDetection: chance(2),
       allowWebSockets: chance(98),
       allowWebTransport: chance(90),
+      allowSpeechSynthesis: chance(4),
     };
     const compatibleProfile: Record<string, boolean> = {
       ...balancedProfile,
@@ -2125,6 +2171,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       allowShapeDetection: chance(4),
       allowWebSockets: true,
       allowWebTransport: chance(96),
+      allowAiApis: chance(1),
+      allowWebNn: chance(1),
+      allowPrivacySandboxApis: chance(1),
+      allowPrivateStateTokens: chance(2),
+      allowSpeechSynthesis: chance(10),
+      allowHandwritingRecognition: chance(2),
     };
     const roll = this.randomInt_(1, 100);
     const profile = roll <= 55 ? protectedProfile :
@@ -2692,6 +2744,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       allowShapeDetection: false,
       allowWebSockets: true,
       allowWebTransport: true,
+      allowAiApis: false,
+      allowWebNn: false,
+      allowPrivacySandboxApis: false,
+      allowPrivateStateTokens: false,
+      allowSpeechSynthesis: false,
+      allowHandwritingRecognition: false,
     };
     for (const [key, value] of Object.entries(advancedDefaults)) {
       if (persona.advanced[key] === undefined) {
@@ -2839,6 +2897,9 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       'allowBadging', 'allowWebShare', 'allowEyeDropper',
       'allowMediaSession', 'allowPictureInPicture', 'allowWebOtp',
       'allowShapeDetection', 'allowWebSockets', 'allowWebTransport',
+      'allowAiApis', 'allowWebNn', 'allowPrivacySandboxApis',
+      'allowPrivateStateTokens', 'allowSpeechSynthesis',
+      'allowHandwritingRecognition',
     ];
     for (const key of booleanAdvancedKeys) {
       persona.advanced[key] = Boolean(persona.advanced[key]);
@@ -2892,4 +2953,4 @@ declare global {
 }
 
 customElements.define(
-    SettingsHeliumPersonaPageElement.is, SettingsHeliumPersonaPageElement);
\ No newline at end of file
+    SettingsHeliumPersonaPageElement.is, SettingsHeliumPersonaPageElement);
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index a2529e3..54f9a03 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -502,6 +502,12 @@ base::Value::Dict MakeDefaultAdvancedProfile() {
   advanced.Set("allowShapeDetection", false);
   advanced.Set("allowWebSockets", true);
   advanced.Set("allowWebTransport", true);
+  advanced.Set("allowAiApis", false);
+  advanced.Set("allowWebNn", false);
+  advanced.Set("allowPrivacySandboxApis", false);
+  advanced.Set("allowPrivateStateTokens", false);
+  advanced.Set("allowSpeechSynthesis", false);
+  advanced.Set("allowHandwritingRecognition", false);
   return advanced;
 }
 
@@ -673,6 +679,12 @@ void EnsureAdvancedCompatibilityFields(base::Value::Dict& persona) {
   EnsureBool(*advanced, "allowShapeDetection", false);
   EnsureBool(*advanced, "allowWebSockets", true);
   EnsureBool(*advanced, "allowWebTransport", true);
+  EnsureBool(*advanced, "allowAiApis", false);
+  EnsureBool(*advanced, "allowWebNn", false);
+  EnsureBool(*advanced, "allowPrivacySandboxApis", false);
+  EnsureBool(*advanced, "allowPrivateStateTokens", false);
+  EnsureBool(*advanced, "allowSpeechSynthesis", false);
+  EnsureBool(*advanced, "allowHandwritingRecognition", false);
 }
 
 void EnsureHardwareCompatibilityFields(base::Value::Dict& persona) {
@@ -1794,4 +1806,4 @@ base::Value::Dict MakeRuntimeSnapshot(const PrefService& pref_service) {
   return MakeSnapshotFromPersona(GetActivePersona(pref_service));
 }
 
-}  // namespace helium_persona
\ No newline at end of file
+}  // namespace helium_persona
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 556aa3b..ae2acfb 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -154,6 +154,12 @@ void ExpectAdvancedExposedApiFields(const base::Value::Dict& advanced) {
       "allowShapeDetection",
       "allowWebSockets",
       "allowWebTransport",
+      "allowAiApis",
+      "allowWebNn",
+      "allowPrivacySandboxApis",
+      "allowPrivateStateTokens",
+      "allowSpeechSynthesis",
+      "allowHandwritingRecognition",
   };
   for (std::string_view field : kBoolFields) {
     SCOPED_TRACE(field);
@@ -658,6 +664,12 @@ TEST(HeliumPersonaPrefsTest, SavePersonaMigratesLegacyManualShape) {
   advanced->Remove("allowFileSystemAccess");
   advanced->Remove("allowMediaSession");
   advanced->Remove("allowWebSockets");
+  advanced->Remove("allowAiApis");
+  advanced->Remove("allowWebNn");
+  advanced->Remove("allowPrivacySandboxApis");
+  advanced->Remove("allowPrivateStateTokens");
+  advanced->Remove("allowSpeechSynthesis");
+  advanced->Remove("allowHandwritingRecognition");
   advanced->Remove("speechVoiceUri");
 
   EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
@@ -1237,6 +1249,12 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   advanced->Set("allowShapeDetection", true);
   advanced->Set("allowWebSockets", false);
   advanced->Set("allowWebTransport", false);
+  advanced->Set("allowAiApis", true);
+  advanced->Set("allowWebNn", true);
+  advanced->Set("allowPrivacySandboxApis", true);
+  advanced->Set("allowPrivateStateTokens", true);
+  advanced->Set("allowSpeechSynthesis", true);
+  advanced->Set("allowHandwritingRecognition", true);
   advanced->Set("allowRemotePlayback", true);
   advanced->Set("speechVoiceName", "Samantha");
   advanced->Set("speechVoiceLang", "en-US");
@@ -1340,6 +1358,13 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "allowPictureInPicture exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowWebOtp exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "allowShapeDetection exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowAiApis exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowWebNn exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowPrivacySandboxApis exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowPrivateStateTokens exposes"));
+  EXPECT_TRUE(WarningsContain(warnings, "allowSpeechSynthesis exposes"));
+  EXPECT_TRUE(WarningsContain(warnings,
+                              "allowHandwritingRecognition exposes"));
   EXPECT_TRUE(WarningsContain(warnings, "Disabling allowWebSockets"));
   EXPECT_TRUE(WarningsContain(warnings, "Disabling allowWebTransport"));
   EXPECT_TRUE(WarningsContain(warnings, "maxTouchPoints should be 0"));
@@ -1906,6 +1931,12 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   advanced->Set("allowShapeDetection", true);
   advanced->Set("allowWebSockets", false);
   advanced->Set("allowWebTransport", false);
+  advanced->Set("allowAiApis", true);
+  advanced->Set("allowWebNn", true);
+  advanced->Set("allowPrivacySandboxApis", true);
+  advanced->Set("allowPrivateStateTokens", true);
+  advanced->Set("allowSpeechSynthesis", true);
+  advanced->Set("allowHandwritingRecognition", true);
 
   EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
 
@@ -2013,6 +2044,24 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_TRUE(runtime.FindDict("advanced")
                   ->FindBool("allowBackgroundSync")
                   .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowAiApis")
+                  .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowWebNn")
+                  .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowPrivacySandboxApis")
+                  .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowPrivateStateTokens")
+                  .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowSpeechSynthesis")
+                  .value_or(false));
+  EXPECT_TRUE(runtime.FindDict("advanced")
+                  ->FindBool("allowHandwritingRecognition")
+                  .value_or(false));
 
   PersonaSnapshot snapshot = MakePersonaSnapshot(runtime);
   EXPECT_EQ("manual-runtime-seed", snapshot.noise_seed);
@@ -2101,6 +2150,12 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_TRUE(snapshot.allow_shape_detection);
   EXPECT_FALSE(snapshot.allow_web_sockets);
   EXPECT_FALSE(snapshot.allow_web_transport);
+  EXPECT_TRUE(snapshot.allow_ai_apis);
+  EXPECT_TRUE(snapshot.allow_web_nn);
+  EXPECT_TRUE(snapshot.allow_privacy_sandbox_apis);
+  EXPECT_TRUE(snapshot.allow_private_state_tokens);
+  EXPECT_TRUE(snapshot.allow_speech_synthesis);
+  EXPECT_TRUE(snapshot.allow_handwriting_recognition);
 }
 
-}  // namespace helium_persona
\ No newline at end of file
+}  // namespace helium_persona
diff --git a/components/helium_persona/persona_presets.cc b/components/helium_persona/persona_presets.cc
index 968cf04..f2f9a26 100644
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -337,6 +337,12 @@ base::Value::Dict MakeAdvanced(bool client_hints_enabled,
   advanced.Set("allowShapeDetection", false);
   advanced.Set("allowWebSockets", true);
   advanced.Set("allowWebTransport", true);
+  advanced.Set("allowAiApis", false);
+  advanced.Set("allowWebNn", false);
+  advanced.Set("allowPrivacySandboxApis", false);
+  advanced.Set("allowPrivateStateTokens", false);
+  advanced.Set("allowSpeechSynthesis", false);
+  advanced.Set("allowHandwritingRecognition", false);
   return advanced;
 }
 
@@ -1686,4 +1692,4 @@ base::Value::List GetBuiltinPersonaPresets() {
   return presets;
 }
 
-}  // namespace helium_persona
\ No newline at end of file
+}  // namespace helium_persona
diff --git a/components/helium_persona/persona_snapshot.cc b/components/helium_persona/persona_snapshot.cc
index 0634f63..66160a5 100644
--- a/components/helium_persona/persona_snapshot.cc
+++ b/components/helium_persona/persona_snapshot.cc
@@ -483,6 +483,18 @@ PersonaSnapshot MakePersonaSnapshot(const base::Value::Dict& persona) {
       GetNestedBool(persona, "advanced", "allowWebSockets", true);
   snapshot.allow_web_transport =
       GetNestedBool(persona, "advanced", "allowWebTransport", true);
+  snapshot.allow_ai_apis =
+      GetNestedBool(persona, "advanced", "allowAiApis", false);
+  snapshot.allow_web_nn =
+      GetNestedBool(persona, "advanced", "allowWebNn", false);
+  snapshot.allow_privacy_sandbox_apis =
+      GetNestedBool(persona, "advanced", "allowPrivacySandboxApis", false);
+  snapshot.allow_private_state_tokens =
+      GetNestedBool(persona, "advanced", "allowPrivateStateTokens", false);
+  snapshot.allow_speech_synthesis =
+      GetNestedBool(persona, "advanced", "allowSpeechSynthesis", false);
+  snapshot.allow_handwriting_recognition =
+      GetNestedBool(persona, "advanced", "allowHandwritingRecognition", false);
   return snapshot;
 }
 
@@ -733,8 +745,17 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot) {
   advanced.Set("allowShapeDetection", snapshot.allow_shape_detection);
   advanced.Set("allowWebSockets", snapshot.allow_web_sockets);
   advanced.Set("allowWebTransport", snapshot.allow_web_transport);
+  advanced.Set("allowAiApis", snapshot.allow_ai_apis);
+  advanced.Set("allowWebNn", snapshot.allow_web_nn);
+  advanced.Set("allowPrivacySandboxApis",
+               snapshot.allow_privacy_sandbox_apis);
+  advanced.Set("allowPrivateStateTokens",
+               snapshot.allow_private_state_tokens);
+  advanced.Set("allowSpeechSynthesis", snapshot.allow_speech_synthesis);
+  advanced.Set("allowHandwritingRecognition",
+               snapshot.allow_handwriting_recognition);
   value.Set("advanced", std::move(advanced));
   return value;
 }
 
-}  // namespace helium_persona
\ No newline at end of file
+}  // namespace helium_persona
diff --git a/components/helium_persona/persona_snapshot.h b/components/helium_persona/persona_snapshot.h
index 79bca27..c0e5864 100644
--- a/components/helium_persona/persona_snapshot.h
+++ b/components/helium_persona/persona_snapshot.h
@@ -211,6 +211,12 @@ struct COMPONENT_EXPORT(HELIUM_PERSONA) PersonaSnapshot {
   bool allow_shape_detection = false;
   bool allow_web_sockets = true;
   bool allow_web_transport = true;
+  bool allow_ai_apis = false;
+  bool allow_web_nn = false;
+  bool allow_privacy_sandbox_apis = false;
+  bool allow_private_state_tokens = false;
+  bool allow_speech_synthesis = false;
+  bool allow_handwriting_recognition = false;
 };
 
 COMPONENT_EXPORT(HELIUM_PERSONA)
@@ -224,4 +230,4 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot);
 
 }  // namespace helium_persona
 
-#endif  // COMPONENTS_HELIUM_PERSONA_PERSONA_SNAPSHOT_H_
\ No newline at end of file
+#endif  // COMPONENTS_HELIUM_PERSONA_PERSONA_SNAPSHOT_H_
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 356d8e2..ea06fa3 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -133,6 +133,15 @@ bool GetNestedBool(const base::Value::Dict& dict,
   return nested ? nested->FindBool(key).value_or(false) : false;
 }
 
+bool GetNestedBool(const base::Value::Dict& dict,
+                   std::string_view dict_key,
+                   std::string_view key,
+                   bool default_value) {
+  const base::Value::Dict* nested = dict.FindDict(dict_key);
+  return nested ? nested->FindBool(key).value_or(default_value)
+                : default_value;
+}
+
 std::vector<std::string> GetNestedStringList(const base::Value::Dict& dict,
                                              std::string_view dict_key,
                                              std::string_view key) {
@@ -579,7 +588,13 @@ bool PersonaHasRequiredFields(const base::Value::Dict& persona) {
          HasNestedBool(persona, "advanced", "allowWebOtp") &&
          HasNestedBool(persona, "advanced", "allowShapeDetection") &&
          HasNestedBool(persona, "advanced", "allowWebSockets") &&
-         HasNestedBool(persona, "advanced", "allowWebTransport");
+         HasNestedBool(persona, "advanced", "allowWebTransport") &&
+         HasNestedBool(persona, "advanced", "allowAiApis") &&
+         HasNestedBool(persona, "advanced", "allowWebNn") &&
+         HasNestedBool(persona, "advanced", "allowPrivacySandboxApis") &&
+         HasNestedBool(persona, "advanced", "allowPrivateStateTokens") &&
+         HasNestedBool(persona, "advanced", "allowSpeechSynthesis") &&
+         HasNestedBool(persona, "advanced", "allowHandwritingRecognition");
 }
 
 base::Value::List ValidatePersona(const base::Value::Dict& persona) {
@@ -781,6 +796,17 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedBool(persona, "advanced", "allowWebSockets", true);
   const bool allow_web_transport =
       GetNestedBool(persona, "advanced", "allowWebTransport", true);
+  const bool allow_ai_apis =
+      GetNestedBool(persona, "advanced", "allowAiApis");
+  const bool allow_web_nn = GetNestedBool(persona, "advanced", "allowWebNn");
+  const bool allow_privacy_sandbox_apis =
+      GetNestedBool(persona, "advanced", "allowPrivacySandboxApis");
+  const bool allow_private_state_tokens =
+      GetNestedBool(persona, "advanced", "allowPrivateStateTokens");
+  const bool allow_speech_synthesis =
+      GetNestedBool(persona, "advanced", "allowSpeechSynthesis");
+  const bool allow_handwriting_recognition =
+      GetNestedBool(persona, "advanced", "allowHandwritingRecognition");
   const std::string_view speech_voice_name =
       GetNestedString(persona, "advanced", "speechVoiceName");
   const std::string_view speech_voice_lang =
@@ -1346,6 +1372,30 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "Disabling allowWebTransport can break sites that rely on WebTransport connections.");
   }
+  if (allow_ai_apis) {
+    AddWarning(warnings,
+               "allowAiApis exposes on-device AI, translation, and language detection service bindings.");
+  }
+  if (allow_web_nn) {
+    AddWarning(warnings,
+               "allowWebNn exposes WebNN hardware acceleration and model compilation surfaces.");
+  }
+  if (allow_privacy_sandbox_apis) {
+    AddWarning(warnings,
+               "allowPrivacySandboxApis exposes Topics and Protected Audience service bindings.");
+  }
+  if (allow_private_state_tokens) {
+    AddWarning(warnings,
+               "allowPrivateStateTokens exposes private state token query surfaces.");
+  }
+  if (allow_speech_synthesis) {
+    AddWarning(warnings,
+               "allowSpeechSynthesis exposes speech synthesis service bindings and platform voice behavior.");
+  }
+  if (allow_handwriting_recognition) {
+    AddWarning(warnings,
+               "allowHandwritingRecognition exposes handwriting recognition service bindings.");
+  }
   if (!touch_enabled && max_touch_points > 0) {
     AddWarning(warnings,
                "maxTouchPoints should be 0 when touch capability is disabled.");
@@ -1444,4 +1494,4 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
   return warnings;
 }
 
-}  // namespace helium_persona
\ No newline at end of file
+}  // namespace helium_persona
diff --git a/content/browser/helium_noise/noise_token_data.cc b/content/browser/helium_noise/noise_token_data.cc
index 0d6eeb8..6ccec6f 100644
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -289,6 +289,12 @@ std::string PersonaNoiseSalt(
   AppendSaltPart(salt, persona_snapshot.allow_shape_detection);
   AppendSaltPart(salt, persona_snapshot.allow_web_sockets);
   AppendSaltPart(salt, persona_snapshot.allow_web_transport);
+  AppendSaltPart(salt, persona_snapshot.allow_ai_apis);
+  AppendSaltPart(salt, persona_snapshot.allow_web_nn);
+  AppendSaltPart(salt, persona_snapshot.allow_privacy_sandbox_apis);
+  AppendSaltPart(salt, persona_snapshot.allow_private_state_tokens);
+  AppendSaltPart(salt, persona_snapshot.allow_speech_synthesis);
+  AppendSaltPart(salt, persona_snapshot.allow_handwriting_recognition);
   return salt;
 }
 
@@ -407,4 +413,4 @@ HeliumNoiseTokenData* HeliumNoiseTokenData::RegenerateTokens(
   return result;
 }
 
-}  // namespace content
\ No newline at end of file
+}  // namespace content
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot.h b/third_party/blink/public/common/helium_persona/persona_snapshot.h
index 7510861..80bd687 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -211,10 +211,16 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
   bool allow_shape_detection = false;
   bool allow_web_sockets = true;
   bool allow_web_transport = true;
+  bool allow_ai_apis = false;
+  bool allow_web_nn = false;
+  bool allow_privacy_sandbox_apis = false;
+  bool allow_private_state_tokens = false;
+  bool allow_speech_synthesis = false;
+  bool allow_handwriting_recognition = false;
 
   bool IsEnabled() const { return enabled && !passthrough; }
 };
 
 }  // namespace blink
 
-#endif  // THIRD_PARTY_BLINK_PUBLIC_COMMON_HELIUM_PERSONA_PERSONA_SNAPSHOT_H_
\ No newline at end of file
+#endif  // THIRD_PARTY_BLINK_PUBLIC_COMMON_HELIUM_PERSONA_PERSONA_SNAPSHOT_H_
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
index a39915c..610fa2f 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
@@ -731,6 +731,30 @@ struct BLINK_COMMON_EXPORT
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.allow_web_transport;
   }
+  static bool allow_ai_apis(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_ai_apis;
+  }
+  static bool allow_web_nn(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_web_nn;
+  }
+  static bool allow_privacy_sandbox_apis(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_privacy_sandbox_apis;
+  }
+  static bool allow_private_state_tokens(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_private_state_tokens;
+  }
+  static bool allow_speech_synthesis(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_speech_synthesis;
+  }
+  static bool allow_handwriting_recognition(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.allow_handwriting_recognition;
+  }
 
   static bool Read(blink::mojom::HeliumPersonaSnapshotDataView data,
                    blink::HeliumPersonaSnapshot* out);
@@ -896,6 +920,12 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
   out->allow_shape_detection = data.allow_shape_detection();
   out->allow_web_sockets = data.allow_web_sockets();
   out->allow_web_transport = data.allow_web_transport();
+  out->allow_ai_apis = data.allow_ai_apis();
+  out->allow_web_nn = data.allow_web_nn();
+  out->allow_privacy_sandbox_apis = data.allow_privacy_sandbox_apis();
+  out->allow_private_state_tokens = data.allow_private_state_tokens();
+  out->allow_speech_synthesis = data.allow_speech_synthesis();
+  out->allow_handwriting_recognition = data.allow_handwriting_recognition();
 
   return data.ReadId(&out->id) && data.ReadName(&out->name) &&
          data.ReadDevice(&out->device) && data.ReadOs(&out->os) &&
@@ -946,4 +976,4 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
 
 }  // namespace mojo
 
-#endif  // THIRD_PARTY_BLINK_PUBLIC_COMMON_HELIUM_PERSONA_PERSONA_SNAPSHOT_MOJOM_TRAITS_H_
\ No newline at end of file
+#endif  // THIRD_PARTY_BLINK_PUBLIC_COMMON_HELIUM_PERSONA_PERSONA_SNAPSHOT_MOJOM_TRAITS_H_
diff --git a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
index 0756e6a..7ec418a 100644
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
@@ -201,4 +201,10 @@ struct HeliumPersonaSnapshot {
   bool allow_shape_detection;
   bool allow_web_sockets;
   bool allow_web_transport;
-};
\ No newline at end of file
+  bool allow_ai_apis;
+  bool allow_web_nn;
+  bool allow_privacy_sandbox_apis;
+  bool allow_private_state_tokens;
+  bool allow_speech_synthesis;
+  bool allow_handwriting_recognition;
+};
