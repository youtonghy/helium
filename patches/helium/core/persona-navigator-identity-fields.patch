diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index 39871341..acb393c6 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -813,6 +813,9 @@ blink::HeliumPersonaSnapshot ToBlinkPersonaSnapshot(
   target.os = source.os;
   target.user_agent = source.user_agent;
   target.platform = source.platform;
+  target.navigator_vendor = source.navigator_vendor;
+  target.navigator_product_sub = source.navigator_product_sub;
+  target.navigator_vendor_sub = source.navigator_vendor_sub;
   target.noise_seed = source.noise_seed;
   target.ua_ch_platform = source.ua_ch_platform;
   target.ua_ch_platform_version = source.ua_ch_platform_version;
diff --git a/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts b/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
index 7731727a..3310a4e0 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_browser_proxy.ts
@@ -12,6 +12,9 @@ export interface PersonaRecord {
   os: string;
   userAgent: string;
   platform: string;
+  navigatorVendor?: string;
+  navigatorProductSub?: string;
+  navigatorVendorSub?: string;
   uaCh: Record<string, unknown>;
   region: Record<string, unknown>;
   network: Record<string, unknown>;
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.html b/chrome/browser/resources/settings/privacy_page/persona_page.html
index bf4571f0..e18d0eaa 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html
@@ -184,6 +184,12 @@
     <cr-input label="Platform" value="{{editablePersona_.platform}}"></cr-input>
     <cr-input class="wide" label="User agent"
         value="{{editablePersona_.userAgent}}"></cr-input>
+    <cr-input label="Navigator vendor"
+        value="{{editablePersona_.navigatorVendor}}"></cr-input>
+    <cr-input label="Navigator productSub"
+        value="{{editablePersona_.navigatorProductSub}}"></cr-input>
+    <cr-input label="Navigator vendorSub"
+        value="{{editablePersona_.navigatorVendorSub}}"></cr-input>
     <cr-input label="UA-CH platform"
         value="{{editablePersona_.uaCh.platform}}"></cr-input>
     <cr-input label="UA-CH platform version"
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 417308c2..a1fc2d80 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -487,6 +487,15 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
 
   private ensureEditableShape_(persona: PersonaRecord) {
     persona.uaCh = persona.uaCh || {};
+    if (persona.navigatorVendor === undefined) {
+      persona.navigatorVendor = 'Google Inc.';
+    }
+    if (persona.navigatorProductSub === undefined) {
+      persona.navigatorProductSub = '20030107';
+    }
+    if (persona.navigatorVendorSub === undefined) {
+      persona.navigatorVendorSub = '';
+    }
     if (persona.noiseSeed === undefined || persona.noiseSeed === null ||
         !String(persona.noiseSeed).trim()) {
       persona.noiseSeed = persona.id || this.makeRandomNoiseSeed_();
@@ -685,6 +694,9 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     persona.hardware['touchEnabled'] = Boolean(persona.hardware['touchEnabled']);
     persona.uaCh['mobile'] = Boolean(persona.uaCh['mobile']);
     persona.uaCh['wow64'] = Boolean(persona.uaCh['wow64']);
+    persona.navigatorVendor = String(persona.navigatorVendor || '');
+    persona.navigatorProductSub = String(persona.navigatorProductSub || '');
+    persona.navigatorVendorSub = String(persona.navigatorVendorSub || '');
     persona.screen['width'] = toNumber(persona.screen['width']);
     persona.screen['height'] = toNumber(persona.screen['height']);
     persona.screen['availWidth'] = toNumber(persona.screen['availWidth']);
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index 032579a3..76cdc8ac 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -207,6 +207,15 @@ void EnsurePersonaCompatibilityFields(base::Value::Dict& persona) {
   if (!persona.FindDict("fontRendering")) {
     persona.Set("fontRendering", MakeDefaultFontRenderingProfile());
   }
+  if (!persona.FindString("navigatorVendor")) {
+    persona.Set("navigatorVendor", "Google Inc.");
+  }
+  if (!persona.FindString("navigatorProductSub")) {
+    persona.Set("navigatorProductSub", "20030107");
+  }
+  if (!persona.FindString("navigatorVendorSub")) {
+    persona.Set("navigatorVendorSub", "");
+  }
   EnsureNetworkCompatibilityFields(persona);
   base::Value::Dict* gpu = persona.FindDict("gpu");
   if (!gpu) {
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 1e16cf3d..7e38ba5f 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -551,6 +551,9 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   persona.Set("noiseSeed", "manual-runtime-seed");
   persona.Set("userAgent", "ManualUA/149.0");
   persona.Set("platform", "Linux x86_64");
+  persona.Set("navigatorVendor", "Manual Vendor");
+  persona.Set("navigatorProductSub", "20991231");
+  persona.Set("navigatorVendorSub", "manual-sub");
 
   base::Value::Dict* ua_ch = persona.FindDict("uaCh");
   ASSERT_TRUE(ua_ch);
@@ -728,6 +731,9 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   base::Value::Dict runtime = MakeRuntimeSnapshot(prefs);
   EXPECT_EQ("manual-runtime-seed", *runtime.FindString("noiseSeed"));
   EXPECT_EQ("ManualUA/149.0", *runtime.FindString("userAgent"));
+  EXPECT_EQ("Manual Vendor", *runtime.FindString("navigatorVendor"));
+  EXPECT_EQ("20991231", *runtime.FindString("navigatorProductSub"));
+  EXPECT_EQ("manual-sub", *runtime.FindString("navigatorVendorSub"));
   EXPECT_EQ("fr-FR", *runtime.FindDict("region")->FindString("locale"));
   EXPECT_EQ("fr-FR,fr;q=0.9,en;q=0.8",
             *runtime.FindDict("region")->FindString("acceptLanguage"));
@@ -804,6 +810,9 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
 
   PersonaSnapshot snapshot = MakePersonaSnapshot(runtime);
   EXPECT_EQ("manual-runtime-seed", snapshot.noise_seed);
+  EXPECT_EQ("Manual Vendor", snapshot.navigator_vendor);
+  EXPECT_EQ("20991231", snapshot.navigator_product_sub);
+  EXPECT_EQ("manual-sub", snapshot.navigator_vendor_sub);
   EXPECT_EQ("Europe/Paris", snapshot.timezone);
   EXPECT_EQ("fr-FR", snapshot.locale);
   EXPECT_EQ("fr-FR,fr;q=0.9,en;q=0.8", snapshot.accept_language);
diff --git a/components/helium_persona/persona_presets.cc b/components/helium_persona/persona_presets.cc
index 5954be7a..595f37ee 100644
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -333,6 +333,9 @@ base::Value::Dict MakePreset(std::string_view id,
   preset.Set("os", os);
   preset.Set("userAgent", ua);
   preset.Set("platform", platform);
+  preset.Set("navigatorVendor", "Google Inc.");
+  preset.Set("navigatorProductSub", "20030107");
+  preset.Set("navigatorVendorSub", "");
   preset.Set("uaCh", std::move(ua_ch));
   preset.Set("region", std::move(region));
   preset.Set("network", MakeNetwork("ethernet", "4g", 50, 10.0, 0.0, false));
diff --git a/components/helium_persona/persona_snapshot.cc b/components/helium_persona/persona_snapshot.cc
index 3587a30f..8ae1afbd 100644
--- a/components/helium_persona/persona_snapshot.cc
+++ b/components/helium_persona/persona_snapshot.cc
@@ -18,6 +18,13 @@ std::string GetString(const base::Value::Dict& dict, const char* key) {
   return value ? *value : std::string();
 }
 
+std::string GetStringOrDefault(const base::Value::Dict& dict,
+                               const char* key,
+                               std::string_view default_value) {
+  const std::string* value = dict.FindString(key);
+  return value ? *value : std::string(default_value);
+}
+
 std::string GetNestedString(const base::Value::Dict& dict,
                             const char* object_key,
                             const char* key) {
@@ -158,6 +165,12 @@ PersonaSnapshot MakePersonaSnapshot(const base::Value::Dict& persona) {
   snapshot.os = GetString(persona, "os");
   snapshot.user_agent = GetString(persona, "userAgent");
   snapshot.platform = GetString(persona, "platform");
+  snapshot.navigator_vendor =
+      GetStringOrDefault(persona, "navigatorVendor", "Google Inc.");
+  snapshot.navigator_product_sub =
+      GetStringOrDefault(persona, "navigatorProductSub", "20030107");
+  snapshot.navigator_vendor_sub =
+      GetStringOrDefault(persona, "navigatorVendorSub", "");
   snapshot.noise_seed = GetString(persona, "noiseSeed");
   snapshot.passthrough = persona.FindBool("passthrough").value_or(false);
 
@@ -431,6 +444,9 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot) {
   value.Set("os", snapshot.os);
   value.Set("userAgent", snapshot.user_agent);
   value.Set("platform", snapshot.platform);
+  value.Set("navigatorVendor", snapshot.navigator_vendor);
+  value.Set("navigatorProductSub", snapshot.navigator_product_sub);
+  value.Set("navigatorVendorSub", snapshot.navigator_vendor_sub);
   value.Set("noiseSeed", snapshot.noise_seed);
   value.Set("passthrough", snapshot.passthrough);
 
diff --git a/components/helium_persona/persona_snapshot.h b/components/helium_persona/persona_snapshot.h
index b90afc78..f1b1b0f6 100644
--- a/components/helium_persona/persona_snapshot.h
+++ b/components/helium_persona/persona_snapshot.h
@@ -27,6 +27,9 @@ struct COMPONENT_EXPORT(HELIUM_PERSONA) PersonaSnapshot {
   std::string os;
   std::string user_agent;
   std::string platform;
+  std::string navigator_vendor;
+  std::string navigator_product_sub;
+  std::string navigator_vendor_sub;
   std::string noise_seed;
   bool passthrough = false;
 
diff --git a/content/browser/helium_noise/noise_token_data.cc b/content/browser/helium_noise/noise_token_data.cc
index 48836f80..485001f8 100644
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -101,6 +101,9 @@ std::string PersonaNoiseSalt(
   AppendSaltPart(salt, persona_snapshot.os);
   AppendSaltPart(salt, persona_snapshot.user_agent);
   AppendSaltPart(salt, persona_snapshot.platform);
+  AppendSaltPart(salt, persona_snapshot.navigator_vendor);
+  AppendSaltPart(salt, persona_snapshot.navigator_product_sub);
+  AppendSaltPart(salt, persona_snapshot.navigator_vendor_sub);
 
   AppendSaltPart(salt, persona_snapshot.ua_ch_platform);
   AppendSaltPart(salt, persona_snapshot.ua_ch_platform_version);
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot.h b/third_party/blink/public/common/helium_persona/persona_snapshot.h
index d908323f..f13a5a8c 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -28,6 +28,9 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
   std::string os;
   std::string user_agent;
   std::string platform;
+  std::string navigator_vendor;
+  std::string navigator_product_sub;
+  std::string navigator_vendor_sub;
   std::string noise_seed;
 
   std::string ua_ch_platform;
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
index 442e6659..efb7b919 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
@@ -62,6 +62,18 @@ struct BLINK_COMMON_EXPORT
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.platform;
   }
+  static const std::string& navigator_vendor(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.navigator_vendor;
+  }
+  static const std::string& navigator_product_sub(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.navigator_product_sub;
+  }
+  static const std::string& navigator_vendor_sub(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.navigator_vendor_sub;
+  }
   static const std::string& noise_seed(
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.noise_seed;
@@ -756,6 +768,9 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
          data.ReadDevice(&out->device) && data.ReadOs(&out->os) &&
          data.ReadUserAgent(&out->user_agent) &&
          data.ReadPlatform(&out->platform) &&
+         data.ReadNavigatorVendor(&out->navigator_vendor) &&
+         data.ReadNavigatorProductSub(&out->navigator_product_sub) &&
+         data.ReadNavigatorVendorSub(&out->navigator_vendor_sub) &&
          data.ReadNoiseSeed(&out->noise_seed) &&
          data.ReadUaChPlatform(&out->ua_ch_platform) &&
          data.ReadUaChPlatformVersion(&out->ua_ch_platform_version) &&
diff --git a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
index 17441367..1b016cf3 100644
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
@@ -18,6 +18,9 @@ struct HeliumPersonaSnapshot {
   string os;
   string user_agent;
   string platform;
+  string navigator_vendor;
+  string navigator_product_sub;
+  string navigator_vendor_sub;
   string noise_seed;
 
   string ua_ch_platform;
diff --git a/third_party/blink/renderer/core/frame/navigator.cc b/third_party/blink/renderer/core/frame/navigator.cc
index 0e0176d1..950671f8 100644
--- a/third_party/blink/renderer/core/frame/navigator.cc
+++ b/third_party/blink/renderer/core/frame/navigator.cc
@@ -41,10 +41,22 @@ namespace blink {
 Navigator::Navigator(ExecutionContext* context) : NavigatorBase(context) {}
 
 String Navigator::productSub() const {
+  if (const HeliumPersonaSnapshot* snapshot =
+          PersonaSpoofingHelper::Get(GetExecutionContext())) {
+    if (!snapshot->navigator_product_sub.empty()) {
+      return String::FromUtf8(snapshot->navigator_product_sub);
+    }
+  }
   return "20030107";
 }
 
 String Navigator::vendor() const {
+  if (const HeliumPersonaSnapshot* snapshot =
+          PersonaSpoofingHelper::Get(GetExecutionContext())) {
+    if (!snapshot->navigator_vendor.empty()) {
+      return String::FromUtf8(snapshot->navigator_vendor);
+    }
+  }
   // Do not change without good cause. History:
   // https://code.google.com/p/chromium/issues/detail?id=276813
   // https://www.w3.org/Bugs/Public/show_bug.cgi?id=27786
@@ -53,6 +65,10 @@ String Navigator::vendor() const {
 }
 
 String Navigator::vendorSub() const {
+  if (const HeliumPersonaSnapshot* snapshot =
+          PersonaSpoofingHelper::Get(GetExecutionContext())) {
+    return String::FromUtf8(snapshot->navigator_vendor_sub);
+  }
   return "";
 }
 
