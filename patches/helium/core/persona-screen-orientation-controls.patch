diff --git a/chrome/browser/chrome_content_browser_client.cc b/chrome/browser/chrome_content_browser_client.cc
index be3d9aed..0ea2ca20 100644
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -911,6 +911,8 @@ blink::HeliumPersonaSnapshot ToBlinkPersonaSnapshot(
   target.color_depth = source.color_depth;
   target.pixel_depth = source.pixel_depth;
   target.refresh_rate = source.refresh_rate;
+  target.orientation_type = source.orientation_type;
+  target.orientation_angle = source.orientation_angle;
   target.hardware_concurrency = source.hardware_concurrency;
   target.device_memory = source.device_memory;
   target.touch_enabled = source.touch_enabled;
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.html b/chrome/browser/resources/settings/privacy_page/persona_page.html
index ab050e51..15281376 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.html
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.html
@@ -415,6 +415,11 @@
         value="{{editablePersona_.screen.pixelDepth}}" type="number"></cr-input>
     <cr-input label="Refresh rate"
         value="{{editablePersona_.screen.refreshRate}}" type="number"></cr-input>
+    <cr-input label="Orientation type"
+        value="{{editablePersona_.screen.orientationType}}"></cr-input>
+    <cr-input label="Orientation angle"
+        value="{{editablePersona_.screen.orientationAngle}}"
+        type="number"></cr-input>
   </div>
 
   <div class="persona-section-title">$i18n{heliumPersonaFonts}</div>
diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index 21908d4a..3219b0ba 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -572,6 +572,12 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     if (persona.screen['availTop'] === undefined) {
       persona.screen['availTop'] = 0;
     }
+    if (persona.screen['orientationType'] === undefined) {
+      persona.screen['orientationType'] = 'landscape-primary';
+    }
+    if (persona.screen['orientationAngle'] === undefined) {
+      persona.screen['orientationAngle'] = 0;
+    }
     persona.fonts = persona.fonts || {};
     persona.fontRendering = persona.fontRendering || {
       id: 'generic-grayscale',
@@ -714,6 +720,10 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
     persona.screen['colorDepth'] = toNumber(persona.screen['colorDepth']);
     persona.screen['pixelDepth'] = toNumber(persona.screen['pixelDepth']);
     persona.screen['refreshRate'] = toNumber(persona.screen['refreshRate']);
+    persona.screen['orientationType'] =
+        String(persona.screen['orientationType'] || 'landscape-primary');
+    persona.screen['orientationAngle'] =
+        toNumber(persona.screen['orientationAngle']);
     persona.fontRendering['textGamma'] =
         toNumber(persona.fontRendering['textGamma']);
     persona.fontRendering['textContrast'] =
diff --git a/components/helium_persona/persona_prefs.cc b/components/helium_persona/persona_prefs.cc
index 76cdc8ac..f930d00e 100644
--- a/components/helium_persona/persona_prefs.cc
+++ b/components/helium_persona/persona_prefs.cc
@@ -217,6 +217,15 @@ void EnsurePersonaCompatibilityFields(base::Value::Dict& persona) {
     persona.Set("navigatorVendorSub", "");
   }
   EnsureNetworkCompatibilityFields(persona);
+  base::Value::Dict* screen = persona.FindDict("screen");
+  if (screen) {
+    if (!screen->FindString("orientationType")) {
+      screen->Set("orientationType", "landscape-primary");
+    }
+    if (!screen->FindInt("orientationAngle")) {
+      screen->Set("orientationAngle", 0);
+    }
+  }
   base::Value::Dict* gpu = persona.FindDict("gpu");
   if (!gpu) {
     return;
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 9752a27e..d1326b46 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -460,6 +460,10 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   base::Value::List invalid_webgpu_features;
   invalid_webgpu_features.Append("real-hardware-only-feature");
   gpu->Set("webgpuFeatures", std::move(invalid_webgpu_features));
+  base::Value::Dict* screen = persona.FindDict("screen");
+  ASSERT_TRUE(screen);
+  screen->Set("orientationType", "portrait-primary");
+  screen->Set("orientationAngle", 45);
   base::Value::Dict* network = persona.FindDict("network");
   ASSERT_TRUE(network);
   network->Set("type", "none");
@@ -482,6 +486,8 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "WebGPU maxBufferSize"));
   EXPECT_TRUE(WarningsContain(warnings, "WebGPU maxBindGroups"));
   EXPECT_TRUE(WarningsContain(warnings, "unknown feature name"));
+  EXPECT_TRUE(WarningsContain(warnings, "Portrait screen orientation"));
+  EXPECT_TRUE(WarningsContain(warnings, "screen.orientation.angle"));
   EXPECT_TRUE(WarningsContain(warnings, "Network RTT and downlink"));
   EXPECT_TRUE(WarningsContain(warnings, "Network type none"));
   EXPECT_TRUE(SavePersona(prefs, std::move(persona)));
@@ -525,6 +531,8 @@ TEST(HeliumPersonaPrefsTest, SnapshotRoundTripsCompleteFields) {
   EXPECT_EQ(268435456, snapshot.webgpu_max_buffer_size);
   EXPECT_EQ(0, snapshot.avail_left);
   EXPECT_EQ(0, snapshot.avail_top);
+  EXPECT_EQ("landscape-primary", snapshot.orientation_type);
+  EXPECT_EQ(0, snapshot.orientation_angle);
   ASSERT_EQ(1u, snapshot.webgpu_features.size());
   EXPECT_EQ("texture-compression-bc", snapshot.webgpu_features[0]);
 
@@ -673,6 +681,8 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   screen->Set("colorDepth", 30);
   screen->Set("pixelDepth", 30);
   screen->Set("refreshRate", 120);
+  screen->Set("orientationType", "portrait-primary");
+  screen->Set("orientationAngle", 90);
 
   base::Value::Dict* hardware = persona.FindDict("hardware");
   ASSERT_TRUE(hardware);
@@ -781,6 +791,10 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_EQ(3000, runtime.FindDict("screen")->FindInt("width").value_or(0));
   EXPECT_EQ(12, runtime.FindDict("screen")->FindInt("availLeft").value_or(-1));
   EXPECT_EQ(28, runtime.FindDict("screen")->FindInt("availTop").value_or(-1));
+  EXPECT_EQ("portrait-primary",
+            *runtime.FindDict("screen")->FindString("orientationType"));
+  EXPECT_EQ(90,
+            runtime.FindDict("screen")->FindInt("orientationAngle").value_or(-1));
   EXPECT_EQ(6, runtime.FindDict("hardware")
                    ->FindInt("hardwareConcurrency")
                    .value_or(0));
@@ -844,6 +858,8 @@ TEST(HeliumPersonaPrefsTest, ManualOverridesFlowIntoRuntimeSnapshot) {
   EXPECT_TRUE(snapshot.network_save_data);
   EXPECT_EQ("Manual GPU Vendor", snapshot.gpu_vendor);
   EXPECT_EQ(3000, snapshot.screen_width);
+  EXPECT_EQ("portrait-primary", snapshot.orientation_type);
+  EXPECT_EQ(90, snapshot.orientation_angle);
   EXPECT_EQ(6, snapshot.hardware_concurrency);
   EXPECT_EQ(5, snapshot.max_touch_points);
   EXPECT_EQ(4096, snapshot.webgl_max_texture_size);
diff --git a/components/helium_persona/persona_presets.cc b/components/helium_persona/persona_presets.cc
index 10b2f722..94dc9b17 100644
--- a/components/helium_persona/persona_presets.cc
+++ b/components/helium_persona/persona_presets.cc
@@ -189,6 +189,9 @@ base::Value::Dict MakeScreen(int width,
   screen.Set("colorDepth", color_depth);
   screen.Set("pixelDepth", color_depth);
   screen.Set("refreshRate", refresh_rate);
+  screen.Set("orientationType", width >= height ? "landscape-primary"
+                                                : "portrait-primary");
+  screen.Set("orientationAngle", 0);
   return screen;
 }
 
diff --git a/components/helium_persona/persona_snapshot.cc b/components/helium_persona/persona_snapshot.cc
index c2e29d47..995841a5 100644
--- a/components/helium_persona/persona_snapshot.cc
+++ b/components/helium_persona/persona_snapshot.cc
@@ -300,6 +300,10 @@ PersonaSnapshot MakePersonaSnapshot(const base::Value::Dict& persona) {
   snapshot.color_depth = GetNestedInt(persona, "screen", "colorDepth");
   snapshot.pixel_depth = GetNestedInt(persona, "screen", "pixelDepth");
   snapshot.refresh_rate = GetNestedInt(persona, "screen", "refreshRate");
+  snapshot.orientation_type =
+      GetNestedString(persona, "screen", "orientationType");
+  snapshot.orientation_angle =
+      GetNestedInt(persona, "screen", "orientationAngle");
 
   snapshot.hardware_concurrency =
       GetNestedInt(persona, "hardware", "hardwareConcurrency");
@@ -574,6 +578,8 @@ base::Value::Dict PersonaSnapshotToValue(const PersonaSnapshot& snapshot) {
   screen.Set("colorDepth", snapshot.color_depth);
   screen.Set("pixelDepth", snapshot.pixel_depth);
   screen.Set("refreshRate", snapshot.refresh_rate);
+  screen.Set("orientationType", snapshot.orientation_type);
+  screen.Set("orientationAngle", snapshot.orientation_angle);
   value.Set("screen", std::move(screen));
 
   base::Value::Dict hardware;
diff --git a/components/helium_persona/persona_snapshot.h b/components/helium_persona/persona_snapshot.h
index 2ee5af73..3c46bc27 100644
--- a/components/helium_persona/persona_snapshot.h
+++ b/components/helium_persona/persona_snapshot.h
@@ -111,6 +111,8 @@ struct COMPONENT_EXPORT(HELIUM_PERSONA) PersonaSnapshot {
   int color_depth = 0;
   int pixel_depth = 0;
   int refresh_rate = 0;
+  std::string orientation_type;
+  int orientation_angle = 0;
 
   int hardware_concurrency = 0;
   double device_memory = 0;
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 0218d033..daa79c9b 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -251,6 +251,17 @@ bool IsKnownEffectiveConnectionType(std::string_view value) {
          EqualsInsensitive(value, "4g");
 }
 
+bool IsKnownOrientationType(std::string_view value) {
+  return EqualsInsensitive(value, "portrait-primary") ||
+         EqualsInsensitive(value, "portrait-secondary") ||
+         EqualsInsensitive(value, "landscape-primary") ||
+         EqualsInsensitive(value, "landscape-secondary");
+}
+
+bool IsCardinalOrientationAngle(int angle) {
+  return angle == 0 || angle == 90 || angle == 180 || angle == 270;
+}
+
 bool IsDeviceMemoryBucket(double value) {
   constexpr double kBuckets[] = {0.25, 0.5, 1, 2, 4, 8};
   for (double bucket : kBuckets) {
@@ -333,6 +344,8 @@ bool PersonaHasRequiredFields(const base::Value::Dict& persona) {
          HasNestedPositiveInt(persona, "screen", "colorDepth") &&
          HasNestedPositiveInt(persona, "screen", "pixelDepth") &&
          HasNestedPositiveInt(persona, "screen", "refreshRate") &&
+         HasNestedString(persona, "screen", "orientationType") &&
+         HasNestedInt(persona, "screen", "orientationAngle") &&
          HasNestedPositiveInt(persona, "hardware", "hardwareConcurrency") &&
          HasNestedPositiveDouble(persona, "hardware", "deviceMemory") &&
          HasNestedBool(persona, "hardware", "touchEnabled") &&
@@ -421,6 +434,10 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedDouble(persona, "screen", "deviceScaleFactor");
   const int color_depth = GetNestedInt(persona, "screen", "colorDepth");
   const int pixel_depth = GetNestedInt(persona, "screen", "pixelDepth");
+  const std::string_view orientation_type =
+      GetNestedString(persona, "screen", "orientationType");
+  const int orientation_angle =
+      GetNestedInt(persona, "screen", "orientationAngle");
   const int cores = GetNestedInt(persona, "hardware", "hardwareConcurrency");
   const double device_memory =
       GetNestedDouble(persona, "hardware", "deviceMemory");
@@ -681,6 +698,24 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "screen.colorDepth should usually be 24 or 30.");
   }
+  if (!IsKnownOrientationType(orientation_type)) {
+    AddWarning(warnings,
+               "screen.orientation.type should be portrait-primary, portrait-secondary, landscape-primary, or landscape-secondary.");
+  }
+  if (width > 0 && height > 0 && width >= height &&
+      Contains(orientation_type, "portrait")) {
+    AddWarning(warnings,
+               "Portrait screen orientation does not match a landscape display size.");
+  }
+  if (width > 0 && height > 0 && width < height &&
+      Contains(orientation_type, "landscape")) {
+    AddWarning(warnings,
+               "Landscape screen orientation does not match a portrait display size.");
+  }
+  if (!IsCardinalOrientationAngle(orientation_angle)) {
+    AddWarning(warnings,
+               "screen.orientation.angle should usually be 0, 90, 180, or 270.");
+  }
 
   if (cores > 0 && cores % 2 != 0) {
     AddWarning(warnings,
diff --git a/content/browser/helium_noise/noise_token_data.cc b/content/browser/helium_noise/noise_token_data.cc
index 7fc61cb5..f54e14b2 100644
--- a/content/browser/helium_noise/noise_token_data.cc
+++ b/content/browser/helium_noise/noise_token_data.cc
@@ -190,6 +190,8 @@ std::string PersonaNoiseSalt(
   AppendSaltPart(salt, persona_snapshot.color_depth);
   AppendSaltPart(salt, persona_snapshot.pixel_depth);
   AppendSaltPart(salt, persona_snapshot.refresh_rate);
+  AppendSaltPart(salt, persona_snapshot.orientation_type);
+  AppendSaltPart(salt, persona_snapshot.orientation_angle);
 
   AppendSaltPart(salt, persona_snapshot.hardware_concurrency);
   AppendSaltPart(salt, persona_snapshot.device_memory);
diff --git a/content/browser/helium_noise/noise_token_data_unittest.cc b/content/browser/helium_noise/noise_token_data_unittest.cc
index a70b4187..d9c3269b 100644
--- a/content/browser/helium_noise/noise_token_data_unittest.cc
+++ b/content/browser/helium_noise/noise_token_data_unittest.cc
@@ -69,6 +69,8 @@ blink::HeliumPersonaSnapshot MakeSeededPersona(std::string noise_seed) {
   snapshot.color_depth = 24;
   snapshot.pixel_depth = 24;
   snapshot.refresh_rate = 60;
+  snapshot.orientation_type = "landscape-primary";
+  snapshot.orientation_angle = 0;
 
   snapshot.hardware_concurrency = 8;
   snapshot.device_memory = 8;
@@ -149,6 +151,8 @@ TEST_F(HeliumNoiseTokenDataTest,
   different_network.network_save_data = true;
   different_network.avail_left = 12;
   different_network.avail_top = 28;
+  different_network.orientation_type = "portrait-primary";
+  different_network.orientation_angle = 90;
   blink::HeliumPersonaSnapshot different_seed =
       MakeSeededPersona("manual-seed-2");
 
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot.h b/third_party/blink/public/common/helium_persona/persona_snapshot.h
index 707b7db5..423bbf7a 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot.h
@@ -111,6 +111,8 @@ struct BLINK_COMMON_EXPORT HeliumPersonaSnapshot {
   int color_depth = 0;
   int pixel_depth = 0;
   int refresh_rate = 0;
+  std::string orientation_type;
+  int orientation_angle = 0;
 
   int hardware_concurrency = 0;
   double device_memory = 0;
diff --git a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
index f6d7ba33..7d15966a 100644
--- a/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
+++ b/third_party/blink/public/common/helium_persona/persona_snapshot_mojom_traits.h
@@ -362,6 +362,13 @@ struct BLINK_COMMON_EXPORT
   static int refresh_rate(const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.refresh_rate;
   }
+  static const std::string& orientation_type(
+      const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.orientation_type;
+  }
+  static int orientation_angle(const blink::HeliumPersonaSnapshot& snapshot) {
+    return snapshot.orientation_angle;
+  }
   static int hardware_concurrency(
       const blink::HeliumPersonaSnapshot& snapshot) {
     return snapshot.hardware_concurrency;
@@ -648,6 +655,7 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
   out->color_depth = data.color_depth();
   out->pixel_depth = data.pixel_depth();
   out->refresh_rate = data.refresh_rate();
+  out->orientation_angle = data.orientation_angle();
   out->network_rtt = data.network_rtt();
   out->network_downlink = data.network_downlink();
   out->network_downlink_max = data.network_downlink_max();
@@ -797,6 +805,7 @@ inline bool StructTraits<blink::mojom::HeliumPersonaSnapshotDataView,
          data.ReadGpuRenderer(&out->gpu_renderer) &&
          data.ReadWebgpuAdapter(&out->webgpu_adapter) &&
          data.ReadGpuDriver(&out->gpu_driver) &&
+         data.ReadOrientationType(&out->orientation_type) &&
          data.ReadFontPackId(&out->font_pack_id) &&
          data.ReadFontPackName(&out->font_pack_name) &&
          data.ReadFontFamilies(&out->font_families) &&
diff --git a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
index a51e5e74..6141f5c8 100644
--- a/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
+++ b/third_party/blink/public/mojom/helium_persona/persona_snapshot.mojom
@@ -101,6 +101,8 @@ struct HeliumPersonaSnapshot {
   int32 color_depth;
   int32 pixel_depth;
   int32 refresh_rate;
+  string orientation_type;
+  int32 orientation_angle;
 
   int32 hardware_concurrency;
   double device_memory;
diff --git a/third_party/blink/renderer/core/frame/local_dom_window.cc b/third_party/blink/renderer/core/frame/local_dom_window.cc
index 23d86a2d..a3a5ce7b 100644
--- a/third_party/blink/renderer/core/frame/local_dom_window.cc
+++ b/third_party/blink/renderer/core/frame/local_dom_window.cc
@@ -1140,6 +1140,13 @@ int LocalDOMWindow::orientation() const {
   if (!frame)
     return 0;
 
+  if (auto value = PersonaSpoofingHelper::ScreenOrientationAngle(this)) {
+    int orientation = static_cast<int>(*value);
+    if (orientation == 270)
+      return -90;
+    return orientation;
+  }
+
   ChromeClient& chrome_client = frame->GetChromeClient();
   int orientation = chrome_client.GetScreenInfo(*frame).orientation_angle;
   // For backward compatibility, we want to return a value in the range of
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
index 51da82ee..f29f96f7 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.cc
@@ -487,6 +487,24 @@ std::optional<float> PersonaSpoofingHelper::ScreenRefreshRate(
   return static_cast<float>(snapshot->refresh_rate);
 }
 
+std::optional<String> PersonaSpoofingHelper::ScreenOrientationType(
+    const ExecutionContext* context) {
+  const HeliumPersonaSnapshot* snapshot = Get(context);
+  if (!snapshot || snapshot->orientation_type.empty()) {
+    return std::nullopt;
+  }
+  return ToBlinkString(snapshot->orientation_type);
+}
+
+std::optional<unsigned> PersonaSpoofingHelper::ScreenOrientationAngle(
+    const ExecutionContext* context) {
+  const HeliumPersonaSnapshot* snapshot = Get(context);
+  if (!snapshot) {
+    return std::nullopt;
+  }
+  return static_cast<unsigned>(std::clamp(snapshot->orientation_angle, 0, 359));
+}
+
 std::optional<String> PersonaSpoofingHelper::NetworkType(
     const ExecutionContext* context) {
   const HeliumPersonaSnapshot* snapshot = Get(context);
diff --git a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
index 9bdf2412..526c99c3 100644
--- a/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
+++ b/third_party/blink/renderer/core/helium_persona/persona_spoofing_helper.h
@@ -76,6 +76,10 @@ class CORE_EXPORT PersonaSpoofingHelper {
       const ExecutionContext* context);
   static std::optional<float> ScreenRefreshRate(
       const ExecutionContext* context);
+  static std::optional<String> ScreenOrientationType(
+      const ExecutionContext* context);
+  static std::optional<unsigned> ScreenOrientationAngle(
+      const ExecutionContext* context);
 
   static std::optional<String> NetworkType(const ExecutionContext* context);
   static std::optional<String> NetworkEffectiveType(
diff --git a/third_party/blink/renderer/modules/screen_orientation/screen_orientation.cc b/third_party/blink/renderer/modules/screen_orientation/screen_orientation.cc
index 883d6fb0..743fc0e0 100644
--- a/third_party/blink/renderer/modules/screen_orientation/screen_orientation.cc
+++ b/third_party/blink/renderer/modules/screen_orientation/screen_orientation.cc
@@ -5,6 +5,7 @@
 #include "third_party/blink/renderer/modules/screen_orientation/screen_orientation.h"
 
 #include <memory>
+#include <optional>
 
 #include "base/memory/raw_ptr_exclusion.h"
 #include "services/network/public/mojom/web_sandbox_flags.mojom-blink.h"
@@ -19,9 +20,32 @@
 #include "third_party/blink/renderer/modules/screen_orientation/screen_orientation_controller.h"
 #include "third_party/blink/renderer/platform/heap/garbage_collected.h"
 #include "third_party/blink/renderer/platform/wtf/assertions.h"
+#include "third_party/blink/renderer/platform/wtf/text/wtf_string.h"
 
 namespace blink {
 
+namespace {
+
+V8OrientationType::Enum PersonaOrientationType(ExecutionContext* context) {
+  std::optional<String> type =
+      PersonaSpoofingHelper::ScreenOrientationType(context);
+  if (!type) {
+    return V8OrientationType::Enum::kLandscapePrimary;
+  }
+  if (DeprecatedEqualIgnoringCase(*type, "portrait-primary")) {
+    return V8OrientationType::Enum::kPortraitPrimary;
+  }
+  if (DeprecatedEqualIgnoringCase(*type, "portrait-secondary")) {
+    return V8OrientationType::Enum::kPortraitSecondary;
+  }
+  if (DeprecatedEqualIgnoringCase(*type, "landscape-secondary")) {
+    return V8OrientationType::Enum::kLandscapeSecondary;
+  }
+  return V8OrientationType::Enum::kLandscapePrimary;
+}
+
+}  // namespace
+
 V8OrientationType::Enum ScreenOrientation::OrientationTypeToV8Enum(
     display::mojom::blink::ScreenOrientation orientation) {
   switch (orientation) {
@@ -90,14 +114,16 @@ ExecutionContext* ScreenOrientation::GetExecutionContext() const {
 
 V8OrientationType ScreenOrientation::type() const {
   if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
-    return V8OrientationType(V8OrientationType::Enum::kLandscapePrimary);
+    return V8OrientationType(PersonaOrientationType(GetExecutionContext()));
   }
   return V8OrientationType(OrientationTypeToV8Enum(type_));
 }
 
 uint16_t ScreenOrientation::angle() const {
   if (PersonaSpoofingHelper::IsEnabled(GetExecutionContext())) {
-    return 0;
+    return static_cast<uint16_t>(
+        PersonaSpoofingHelper::ScreenOrientationAngle(GetExecutionContext())
+            .value_or(0));
   }
   return angle_;
 }
