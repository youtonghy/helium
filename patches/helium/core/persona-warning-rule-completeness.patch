diff --git a/chrome/browser/resources/settings/privacy_page/persona_page.ts b/chrome/browser/resources/settings/privacy_page/persona_page.ts
index d57b5719..458d092b 100644
--- a/chrome/browser/resources/settings/privacy_page/persona_page.ts
+++ b/chrome/browser/resources/settings/privacy_page/persona_page.ts
@@ -497,6 +497,7 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
       mediaDevices['audioOutputLabel'],
     ];
     const storageQuota = this.numberValue_(advanced['storageQuotaGb']);
+    const clientHintsEnabled = Boolean(advanced['clientHintsEnabled']);
     const reduceSystemInfo = Boolean(advanced['reduceSystemInfo']);
     const webdriverEnabled = Boolean(advanced['webdriverEnabled']);
     const allowGeolocation = Boolean(advanced['allowGeolocation']);
@@ -667,6 +668,13 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'Linux UA-CH platformVersion is usually blank or very coarse.');
     }
+    if (!clientHintsEnabled &&
+        (uaChBrands.length || uaChFullVersion || uaChFullVersionList?.length ||
+         uaChFormFactors.length)) {
+      this.addDraftWarning_(
+          warnings,
+          'clientHintsEnabled=false disables UA-CH request headers and high-entropy hints, so filled UA-CH metadata will not be exposed.');
+    }
     if (!uaChBrands.length) {
       this.addDraftWarning_(
           warnings,
@@ -1408,6 +1416,16 @@ export class SettingsHeliumPersonaPageElement extends PolymerElement {
           warnings,
           'Mobile UA-CH metadata does not match a desktop screen size.');
     }
+    if (this.contains_(ua, 'mobile') && width >= 1024 && height >= 720) {
+      this.addDraftWarning_(
+          warnings,
+          'Mobile UA string does not match a desktop screen size.');
+    }
+    if (this.contains_(ua, 'mobile') && !touchEnabled) {
+      this.addDraftWarning_(
+          warnings,
+          'Mobile UA string should usually pair with touch capability.');
+    }
 
     if (!this.isKnownFontHinting_(fontHinting)) {
       this.addDraftWarning_(
diff --git a/components/helium_persona/persona_prefs_unittest.cc b/components/helium_persona/persona_prefs_unittest.cc
index 29dd0aa0..62445dc8 100644
--- a/components/helium_persona/persona_prefs_unittest.cc
+++ b/components/helium_persona/persona_prefs_unittest.cc
@@ -1552,6 +1552,9 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   font_rendering->Set("engine", "DirectWrite");
   base::Value::Dict* ua_ch = persona.FindDict("uaCh");
   ASSERT_TRUE(ua_ch);
+  persona.Set("userAgent",
+              "Mozilla/5.0 (Linux; Android 15; Pixel 9) AppleWebKit/537.36 "
+              "(KHTML, like Gecko) Chrome/149.0.0.0 Mobile Safari/537.36");
   ua_ch->Set("fullVersion", "148.0.0.0");
   base::Value::Dict* webgl_profile = gpu->FindDict("webglProfile");
   ASSERT_TRUE(webgl_profile);
@@ -1600,6 +1603,7 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "voice name and URI"));
   EXPECT_TRUE(WarningsContain(warnings, "deviceMemory should use"));
   EXPECT_TRUE(WarningsContain(warnings, "reduceSystemInfo forces"));
+  EXPECT_TRUE(WarningsContain(warnings, "clientHintsEnabled=false disables"));
   EXPECT_TRUE(WarningsContain(warnings, "Storage quota is too small"));
   EXPECT_TRUE(WarningsContain(warnings, "allowMediaCapture exposes real camera"));
   EXPECT_TRUE(WarningsContain(warnings, "allowGeolocation exposes"));
@@ -1655,6 +1659,8 @@ TEST(HeliumPersonaPrefsTest, ValidationWarnsButAllowsConflicts) {
   EXPECT_TRUE(WarningsContain(warnings, "Disabling allowWebSockets"));
   EXPECT_TRUE(WarningsContain(warnings, "Disabling allowWebTransport"));
   EXPECT_TRUE(WarningsContain(warnings, "maxTouchPoints should be 0"));
+  EXPECT_TRUE(WarningsContain(warnings, "Mobile UA string does not match"));
+  EXPECT_TRUE(WarningsContain(warnings, "Mobile UA string should usually pair"));
   EXPECT_TRUE(WarningsContain(warnings, "CoreText font rendering"));
   EXPECT_FALSE(WarningsContain(warnings, "Retina macOS personas"));
   EXPECT_TRUE(WarningsContain(warnings, "UA-CH fullVersion major"));
diff --git a/components/helium_persona/persona_validation.cc b/components/helium_persona/persona_validation.cc
index 29fc6e55..d779a2b9 100644
--- a/components/helium_persona/persona_validation.cc
+++ b/components/helium_persona/persona_validation.cc
@@ -771,6 +771,8 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
       GetNestedInt(persona, "hardware", "maxTouchPoints");
   const double storage_quota_gb =
       GetNestedDouble(persona, "advanced", "storageQuotaGb");
+  const bool client_hints_enabled =
+      GetNestedBool(persona, "advanced", "clientHintsEnabled");
   const bool reduce_system_info =
       GetNestedBool(persona, "advanced", "reduceSystemInfo");
   const bool webdriver_enabled =
@@ -988,6 +990,14 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "Linux UA-CH platformVersion is usually blank or very coarse.");
   }
+  if (!client_hints_enabled &&
+      ((ua_ch_brands && !ua_ch_brands->empty()) ||
+       !ua_ch_full_version.empty() ||
+       (ua_ch_full_version_list && !ua_ch_full_version_list->empty()) ||
+       !ua_ch_form_factors.empty())) {
+    AddWarning(warnings,
+               "clientHintsEnabled=false disables UA-CH request headers and high-entropy hints, so filled UA-CH metadata will not be exposed.");
+  }
   if (!ua_ch_brands || ua_ch_brands->empty()) {
     AddWarning(warnings,
                "UA-CH brands should be set so Sec-CH-UA matches the persona.");
@@ -1521,6 +1531,14 @@ base::Value::List ValidatePersona(const base::Value::Dict& persona) {
     AddWarning(warnings,
                "Mobile UA-CH metadata does not match a desktop screen size.");
   }
+  if (Contains(ua, "mobile") && width >= 1024 && height >= 720) {
+    AddWarning(warnings,
+               "Mobile UA string does not match a desktop screen size.");
+  }
+  if (Contains(ua, "mobile") && !touch_enabled) {
+    AddWarning(warnings,
+               "Mobile UA string should usually pair with touch capability.");
+  }
 
   if (!IsKnownHintingValue(font_hinting)) {
     AddWarning(warnings,
