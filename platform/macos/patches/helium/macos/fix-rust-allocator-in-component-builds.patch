--- a/build/config/BUILD.gn
+++ b/build/config/BUILD.gn
@@ -318,6 +318,10 @@ group("shared_library_deps") {
   if (!toolchain_for_rust_host_build_tools) {
     public_deps = [ ":common_deps" ]
   }
+
+  if (is_component_build) {
+    public_deps += [ "//build/rust/allocator" ]
+  }
 }
 
 # Executable configs -----------------------------------------------------------
