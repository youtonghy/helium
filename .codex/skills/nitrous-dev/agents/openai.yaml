interface:
  display_name: "Nitrous Dev"
  short_description: "Hot-tree edit, compile loop, patch export"
  default_prompt: "Use $nitrous-dev before editing Chromium source: capture a hot-start baseline, edit and compile only declared files, then export a staged new top patch."
